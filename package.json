{
  "name": "chess-tabiya",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r --if-present build",
    "schema:check": "node --test tools/verify-scaffold.test.mjs tools/ci-local.test.mjs && node tools/verify-scaffold.mjs && node tools/verify-packaging.mjs && lefthook validate",
    "test": "vitest run",
    "test:software": "vitest run --config vitest.software.config.ts",
    "test:performance": "vitest run --config vitest.performance.config.ts",
    "test:content": "vitest run --config vitest.content.config.ts",
    "test:browser": "playwright test",
    "test:maia": "vitest run --config vitest.maia.config.ts",
    "typecheck": "pnpm -r --if-present typecheck",
    "verify": "pnpm typecheck && pnpm test && pnpm schema:check"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "@sveltejs/vite-plugin-svelte": "7.2.0",
    "esbuild": "0.28.2",
    "fast-check": "4.9.0",
    "lefthook": "2.1.10",
    "svelte": "5.56.8",
    "typescript": "5.9.2",
    "vitest": "4.1.10"
  },
  "engines": {
    "node": ">=24"
  },
  "packageManager": "pnpm@11.18.0"
}
