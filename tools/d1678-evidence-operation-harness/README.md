# D1678 run-derived evidence operation harness

Disposable research instrument for the returned `variants` RFC and D1685. It AST-censuses the
run-surface queue, selector, tablebase, corpus and local-packet request calls in the server service
and REST adapter; pins the three current request identities; and proves prediction persistence has
no distribution-subject operand.

This is a scope receipt, not the production capability gate. Sourcing/authoring commands and
provider-internal calls are outside this run-surface census and need their own explicit standard-
only disposition.

```sh
pnpm exec vitest run --config tools/d1678-evidence-operation-harness/vitest.config.ts
```
