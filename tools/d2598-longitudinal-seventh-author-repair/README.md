# D2598–D2602 longitudinal-store seventh author repair

Disposable contract model for the complete longitudinal source/row/job authority. It closes
denominator ordering, caller-writable projection admission, mutable/unparsed source identity,
hand-written mutation closure and the reduced two-state invalidation model.

It does not implement the migration, worker, consumer APIs or client features.

Run with:

```sh
make longitudinal-store-seventh-author-repair
```
