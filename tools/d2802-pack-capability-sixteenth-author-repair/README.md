# Pack capability sixteenth author repair

Disposable bounded contract repair for [[D2802]]–[[D2808]]. It joins provider payload operands to
the exact request and provider instance, requires the shared canonical JSON byte image, parses the
retry/terminal unions exactly, closes origin/consumer/provider routing, fences results at lease
expiry, and makes retained transition rows append-only in SQLite. The Make target retains the full
prior review chain.

Run:

```sh
make pack-capability-sixteenth-author-repair
```
