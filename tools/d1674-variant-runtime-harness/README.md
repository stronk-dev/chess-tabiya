# D1674 rules-aware runtime harness

Disposable research instrument for the returned `variants` RFC. It proves one legal Crazyhouse
drop and one King-of-the-Hill terminal through chessops' rules-aware authority, then asserts the
corresponding shipped runtime refusals and standard-only seams.

It is not production variant support and does not authorize implementation.

```sh
pnpm exec vitest run --config tools/d1674-variant-runtime-harness/vitest.config.ts
```
