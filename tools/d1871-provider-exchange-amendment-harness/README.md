# D1871–D1878 provider-exchange amendment harness

Disposable exploration instrument under RFC-0000's exploration gate. It makes the eight seams
from the 2026-08-27 independent buildability return executable without adding production provider
code before the RFC is accepted.

Run it through the repository-owned toolchain:

```sh
make provider-exchange-contract
```

The instrument checks stable path identity and subject-scoped satisfaction, immutable acquisition
versus delivery provenance, Maia history equality, bounded Stockfish refusal, Explorer's closed
domain result, total source-absence aggregation, the closed five-operation scheduler census, and
requested-versus-actual engine generation.
