# Held promotion collectors — fifth author repair

Disposable RFC-tier author contract for D2548–D2551. It proves the shared provider request-digest
authority is a closed operation, the promotion output retains exact move/pawn/source operands, and
the position-only tablebase result is a reading rather than an event with a fabricated edge.

Run through `make semantic-collectors-promotion-fifth-author-repair`. This is author evidence only;
it neither implements nor accepts either held projection.
