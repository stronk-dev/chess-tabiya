# D3034 collision-core author contract

Runs the bounded author checks for the owner-directed shared-resource bootstrap cut.

```sh
make shared-resource-bootstrap-collision-core-author-contract
```

The contract checks the reviewed seven-row seed against the current checker and README registers,
proves the schema mapping is extended by data, and fences the withdrawn speculative roots.
