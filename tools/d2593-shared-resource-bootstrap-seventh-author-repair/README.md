# Shared-resource bootstrap seventh author repair

Disposable author contract for [[D2593]]–[[D2597]]. It does not implement the register engine.

The maintained target retains the sixth repair and checks that:

- TypeScript graphs are compiler-constructed and completely validated, never caller supplied;
- one normalized structural-selector parser feeds admission and resolution for the seed and all
  follow-on descriptors;
- canonical `Object.freeze` wrappers resolve to the global intrinsic;
- repository nodes are retained by exact compiler-symbol reachability, not spelling; and
- projected semantic values are recursively copied/frozen under the digest they retain.

Passing is author evidence only. A new independent buildability review remains required.
