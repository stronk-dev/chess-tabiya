# Shared-resource bootstrap twelfth author repair

Bounded repair for [[D2828]]–[[D2834]]. The TypeScript graph now retains an exact compiler syntax
site for every relation, follows the selected signature to refuse aliased global `eval`, obtains
construct overloads from the compiler-selected target type, accepts only validated descriptors and
uses one canonical comparator for graph and dependency-artifact identity.

Run with `make shared-resource-bootstrap-twelfth-author-repair`. This is author evidence only and
changes no production catalogue, checker, register or product byte.
