# Provider-protocol second author repair

Disposable bounded contract repair for [[D2809]]–[[D2814]]. It revision-pins both historical
reviews, restores the human-owned checked README rule, uses a compilable mapped relation, keeps
product population validation in the product RFC, and makes the canonical-resource routing summary
match its descriptor.

Run:

```sh
make provider-protocol-second-author-repair
```
