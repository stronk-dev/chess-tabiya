# D1612 longitudinal contract harness

Disposable research instrument for the six returned longitudinal-store seams (D1612–D1617).
It supplies executable author input, not production persistence:

- the literal 67-row constructor registry and count-preserving wrong-join negative;
- generation/token/lease claims, expiry recovery and stale-publication CAS;
- exact event-prefix cuts and newer-request reopening;
- family-independent denominator aggregation with retry/rebuild equality;
- the seven-operation run-write closure;
- an import-subject gate that distinguishes asserted personal play from observed games.

Run with Node 24:

```sh
/opt/homebrew/opt/node@24/bin/node node_modules/vitest/vitest.mjs run \
  --config tools/d1612-longitudinal-contract-harness/vitest.config.ts
```
