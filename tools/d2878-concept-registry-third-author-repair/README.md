# Concept-registry third author repair

Disposable author contract for [[D2878]]–[[D2884]]. It exercises the shipped compound storage key,
runtime replay and full PackRegistry artifact boundary, plus immutable revision compilation,
restart validation, operation-level consumer closure and the portable label-collision rule.

Run with `make concept-registry-third-author-repair`.
