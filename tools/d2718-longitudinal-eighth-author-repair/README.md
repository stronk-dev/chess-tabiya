# D2718–D2723 longitudinal-store eighth author repair

Disposable author model for the storage-owned source and claim authority. It consumes real runtime
events through replay, derives exact attribution and imported-mainline boundaries, compiles
transaction calls through the TypeScript AST, binds the complete claim currentness tuple and
refuses crossed job/source subjects.

Run with:

```sh
make longitudinal-store-eighth-author-repair
```
