# Campaign two-horizon author contract

Executable authoring check for the returned `rfc/campaign-core.md` contract. It composes the
existing D1592–D1597 research model with assertions over the amended normative RFC and the shared
resource register. Passing this target does not accept the RFC or authorize implementation; it
proves the author repair contains the finite contract handed to fresh independent review.

Run through the repository entry point:

```sh
make campaign-two-horizon-author-contract
```

The target checks typed run/durable reward closure, inventory/preset separation, path-wide
consumer opportunity, non-vacuous prestige, event-owned abandonment, idempotent account-portable
awards, campaign schema lane 2 and the complete 1.0 UX journey boundary.
