# Candidate packet — twelfth author repair

Disposable executable contract for D2885–D2891. It binds every compiled receipt to
the active canonical request, gives jobs one terminal transition, retains total
collector outcomes from the single registry invocation, distinguishes non-terminal
empty populations, measures the exact cached receipt and refreshes both direct and
projection-hit LRU entries.

Run `make candidate-packet-twelfth-author-repair`.
