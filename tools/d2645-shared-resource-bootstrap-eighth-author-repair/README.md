# D2645–D2649 shared-resource bootstrap eighth author repair

Disposable positive author evidence for the bounded repository-program, compiler-symbol graph,
re-export, canonical scalar and overload-root repair. It constructs a committed temporary Git
repository and proves the projector reads the named commit rather than mutable working-tree or
caller-supplied source bytes. This is not the production register engine.
