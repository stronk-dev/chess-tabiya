# D794 bounded-reply harness

Disposable Phase-2b research instrument. It enumerates complete legal reply sets and measures exact
reply breadth, only-reply states, positive-capture threats surviving all replies, and the tactical
collector RFC's moved-piece double-attack consequence. It is not production code and does not infer
force, value, intent or a tactic name.

Run:

```sh
pnpm exec vitest run --config tools/d794-bounded-reply-harness/vitest.config.ts
```
