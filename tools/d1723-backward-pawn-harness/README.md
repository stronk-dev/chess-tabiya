# D1723 backward-pawn semantic and payload boundary

Disposable research instrument. It reconstructs the shipped file predicate as exact pawn subjects,
tests the disputed occupancy/support/controller cases, and measures how much identity the current
file-only reading loses over the fixed authored/imported populations. It does not change production
classification.

```sh
D1723_CENSUS=1 /opt/homebrew/opt/node@24/bin/node node_modules/vitest/vitest.mjs run \
  --config tools/d1723-backward-pawn-harness/vitest.config.ts --reporter=dot
```
