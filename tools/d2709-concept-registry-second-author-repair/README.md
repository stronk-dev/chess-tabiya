# Concept registry — second author repair harness

Executable requirements model for [[D2709]]–[[D2716]]. It retains the returned review target and
proves canonical immutable registry revisions/refs, storage-and-inventory-owned historical inputs,
one rollback-safe lossless population migration, and consumer closure derived from a committed
TypeScript import graph rather than a caller-supplied list.

This is contract evidence only. It does not implement the production registry, schema, migration,
content, API or UI, and a genuinely fresh independent review is still required.
