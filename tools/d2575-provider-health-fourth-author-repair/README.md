# Provider-health fourth author repair

Disposable executable contract for D2575-D2583. It retains the prior author targets and proves the
complete production-call census, provider-exchange join, monotonic recovery, tokenized group leases,
atomic exact-cache return, durable opponent recovery identity, first-class speech over sealed text,
and ordered multi-stage settlement. It does not implement production provider or client behavior.
