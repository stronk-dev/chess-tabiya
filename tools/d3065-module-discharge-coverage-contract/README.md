# D3065 module discharge coverage contract

This bounded author contract prevents the module registry from silently dropping the four
implemented RFCs that wait on production-module eligibility. It checks the exact learner homes,
the one explicit internal-predicate refusal, the regenerated requirement image, and the boundary
between withdrawn direct-call prose and the normative query contract.

Run with `make module-discharge-coverage-contract`.
