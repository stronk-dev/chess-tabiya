# Bounded-policy targets — second repeat author handoff

Repair `rfc/bounded-policy-targets.md` on [[D1962]], [[D1963]], [[D1964]], [[D1965]], [[D1966]],
[[D1967]] and [[D1968]] without reopening the local/provider/policy split or inspector-only posture.

Required work:

1. Export/version one threat-pass anchor transform and make `threats()` plus target admission use
   it. Do not duplicate the mutation.
2. Drop unknowable initial promotion provenance; retain promotion observed in the bounded line.
3. Remove impossible `target_captured`; execute fixtures for every remaining cause.
4. Replace independent booleans/nullable arrays with a discriminated bounded-result algebra and
   canonical literal move tuples.
5. Use a mapped request→result contract (or exact descriptors), including per-operation abstention
   reasons and fields.
6. Add one source-position batch/job admission owning the complete target and legal-candidate sets,
   then enforce the 512 multiplication ceiling before fan-out.
7. Specify a bounded cooperative background queue with an injected portable macrotask yield,
   cancellation checks during work and a maximum cancellation-latency criterion. A worker variant
   must first solve F1 seal/reference transport honestly.
8. Extend `make bounded-target-contract` with crossed author controls; keep
   `make bounded-target-repeat-review` as the return control until independent review.

Do not implement production, add provider policy, bind a learner surface, manufacture a target
meaning, edit protected design intent or touch concurrent D872/review work.
