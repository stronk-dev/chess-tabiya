# Bounded-policy targets third-return author repair — 2026-08-31

Author-repaired [[D2340]]–[[D2342]]; fourth fresh review remains required and production remains
unauthorised.

- Semantic validation now unions derived event roots with explicit admissible reading roots. Named
  material target and bounded return are the first two; each still needs full executable semantic
  cells plus its sole value-factory receipt.
- The public type fixture imports one generated proposed declaration module. It no longer defines a
  divergent local protocol, and crossed discriminants compile against the same bytes.
- The three factories export only from `src/internal/bounded-target-factories.ts`; they are absent
  from barrels/package subpaths/application imports, and the central value registry is their sole
  non-test importer.

`make bounded-target-third-author-repair` preserves 4/4 prior guarantees.
`make bounded-target-fourth-author-repair` passes 3/3 and compiles the imported protocol image.
No runtime, manifest, schema, content, archive or protected-design byte changed.
