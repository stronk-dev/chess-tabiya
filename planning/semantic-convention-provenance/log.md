# Semantic-convention provenance log

## 2026-08-27 — Independent buildability return

The first independent pass returned six contract gaps. The declaration graph has no value-level
ancestry with which to construct a path-specific receipt ([[D1921]]); live exact adapters seal
unregistered convention operands because they check keys only ([[D1922]]); and the RFC names 39
members without publishing the definitions, limitations, authorities and disclosures implementation
would require ([[D1923]]). The existing provider guard cannot detect a stripped limitation
([[D1924]]), a same-change edit can update both a definition and its repo-local “previous” snapshot
([[D1925]]), and persisted runs carry no convention/projection receipt despite the stated
Review/history guarantee ([[D1926]]). A six-arm disposable harness reproduces each boundary. Exact
return: `independent-buildability-review-2026-08-27.md`.

## 2026-08-27 — Value authority and persistence amendment

Extended the return harness rather than touching production. Fourteen arms now prove a
compiler-owned source/derived seal with canonical member identity, exact nested input-value
multisets, repeated-projection multiplicity and runtime anti-forgery. The manifest-derived instance
population is fourteen projections across three operand shapes; the typed extractor candidate is
set-equal to all fourteen and rejects malformed/unregistered values.

The pass found [[D1934]]: `exactObject` checks missing keys but accepts extras, allowing fixed
projections to smuggle an undeclared convention ref or arbitrary caller data. Exact key equality is
now inside the RFC boundary. The author amendment also assigns mandatory limitations to a
deterministic assembler ([[D1924]]), replaces the co-mutable prior snapshot with append-only staged
and first-parent semantic history ([[D1925]]), and claims run-schema 0.24 for durable
`evidence.attached` receipts and compiler re-sealing ([[D1926]]). D1923's source-backed 39 literal
declarations and process-register reconciliation remain before repeat review. Dossier:
`design/research/semantic-convention-value-authority.md`.

## 2026-08-27 — Initial declaration authority recovered

Published `initial-declarations.json` as the literal 39-row migration population. The disposable
D1923 source-recovery harness proves exact equality with the stable member seed, nonempty
definitions/limitations and live projection or implementation witnesses for every row. It found
and repaired two seed rows whose meaning resolved but whose literal identity did not (D1935).

The recovery also disproved the draft's original authority union: recorded-run, bounded-search and
deterministic product-composition contracts are shipped meaning without being position rules or
owner rulings. The RFC now admits a migration-only `landed_contract` authority over exact witnesses
at immutable snapshot `62a5731f`; it cannot authorize new chess truth (D1936). D1923 is author-
repaired. D1924–D1926 executable falsifiers, process-register reconciliation and repeat independent
review remain before acceptance.

## 2026-08-27 — Limitation/history/persistence cores executed; D1937 repaired

Extended the disposable product harness to 21 passing arms. Deterministic assembly preserves every
mandatory limitation when a provider returns only a summary. Append-only history refuses
same-version rewrite, deletion, reorder and skipped lineage while accepting exact next-version
append.

The persistence trace found the draft's re-sealing claim was not yet true: the run stores no
semantic input graph or signed event chain, so a caller could mutate an input digest and recompute
every unkeyed digest. The candidate now attests the canonical persisted envelope with an Ed25519
installation key and verifies origin before historical/member validation. Recomputed-digest
tampering and unknown origins fail; genuine JSON-loaded v1 survives a v2 head; legacy absence stays
empty. D1924–D1926 are author-buildable at their core. Full live schema/storage/export/deletion and
key lifecycle remain implementation acceptance work after the process predecessor and repeat
review.
