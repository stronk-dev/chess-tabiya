# Runtime corpus evidence implementation plan

- [ ] Add the server-internal corpus source, normalized explorer query, bounded cache, and fixtures.
- [ ] Add capability, permission, REST, error, and application wiring with exercising tests.
- [ ] Add the version-2 client preference, typed API, closed renderer, assistance panel, and browser coverage.
- [ ] Reconcile canonical docs, archive the lifecycle, and rerun both gates after the moves.

Implementation order follows `rfc/runtime-corpus-evidence.md`. No schema, run event,
database migration, or durable evidence is permitted in this lifecycle.
