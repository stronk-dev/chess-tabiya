# Longitudinal store — sixth author repair

- **Date:** 2026-09-04
- **Scope:** RFC contract and disposable falsifier only; no product migration, worker, reader,
  consumer, API, client or content bytes
- **Rows:** [[D2570]]–[[D2574]]
- **Gate:** another genuinely fresh independent buildability review is required

## Result

The fifth fresh return is repaired as one typed source/snapshot authority.

1. The source-mutation registry extends the seven snapshot writers with same-head collaboration
   mutations, owner/deletion suppression and startup classification. Live-session creation and a
   non-owner write-capable grant taint and invalidate atomically.
2. Structure attribution is now a durable monotone three-state value. New private runs are
   `single_player`; collaboration is `unattributable_shared`; pre-migration history is
   `unattributable_legacy`. Uncertain history never becomes learner behavior because a grant was
   revoked or a journal is absent.
3. Denominator, observation and structure rows are exact immutable camel-case types constructed by
   parsers over unknown storage/wire input. Projection-sign identity, counts, shares, provenance,
   timestamps and typed ref cardinality/subsets are validated.
4. Revision 1 imports assert only the state revision 1 owns: source-mainline decisions are
   `game`/observed-only. The future three-way subject declaration and personal-play admission test
   remain with Discharge D2.
5. One brand-sealed query parser defines omission as all and rejects present-empty, duplicate,
   unknown and contradictory filters while returning stable bytewise order.

## Executable evidence

`make longitudinal-store-sixth-author-repair` retains the prior 24 + 8 + 4 author arms, adds seven
new runtime controls, and runs strict TypeScript. The new tests cross same-head invalidation,
monotone legacy/shared truth, all three exact row families and their forges, observed-only imports,
and both accepted and refused JSON query shapes.

This is author evidence, not acceptance. Another reviewer must reconstruct the full source-mutation
set, migration default/new-run distinction, row adapter and query parser against production symbols
before any implementation becomes lawful.
