# Verifiable runtime distribution partial author repair — 2026-08-31

## Verdict

[[D2207]]–[[D2209]] are author-repaired. [[D2206]] is specified through its closed protocol but not
completed: the required first `release-manifest-schema` claim exposes register-bootstrap defect
[[D2363]]. This checkpoint is neither acceptance nor implementation, and another fresh review is
not requested until that claim can enter lawfully.

## Repaired authority

- Images embed only committed pre-image build metadata and notice bytes. The post-image release
  index is generated after image/SBOM/resource/Compose identities and mounted read-only for the
  About join. The artifact DAG is executable and rejects the old self-digest edge.
- `ReleaseManifestV1` is a closed discriminated artifact/resource protocol with one intended
  schema-derived validator/projection consumed by all six readers. It names the missing register
  resource and initial lane without modifying checker/schema production bytes early.
- FOSS eligibility uses a committed SPDX 2.3 policy: exact accepted ids/text digests, exact WITH
  pairs, explicit OR selection, conjunctive AND, custom-reference refusal and exact corrective
  override identity. Maia weight evidence remains D1 and cannot be manufactured by override.
- CPU proof depends on the reachable compiled `pawn-forward.1800@1` production route and records its
  exact digest, `maia.policy_page@1` width-20 receipt and 100 committed selections. Today's empty
  catalog can produce research data only.

## Verification

`make runtime-distribution-author-repair` passes seven executable arms plus strict TypeScript. The
SPDX expression semantics were checked against the official SPDX 2.3 Annex D. No workflow, image,
schema, checker, production, content, archive or protected-design byte changed.
