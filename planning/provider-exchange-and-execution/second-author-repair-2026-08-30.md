# Provider exchange second author repair — 2026-08-30

## Outcome

Repaired [[D2184]]–[[D2188]] at the author-contract boundary and routed [[D2189]] to the separate
process-only `provider-protocol-register` prerequisite. The product RFC remains draft: neither
provider implementation nor acceptance is authorized until that register lands and the exact
lane-1 claim replaces `none`.

## Repairs

- Public availability references now select a run prefix, node or committed edge; the authorized
  server resolves canonical FEN/move bytes and seals the full subject before a grain-declared source
  resolver runs.
- Stockfish and Maia identities now bind the launched executable or immutable container plus the
  ordered advertised/applied UCI option image from the same generation.
- Descriptors return response captures only. One operation-keyed parser binds response bytes,
  parser implementation and canonical typed payload in a sealed receipt before evidence creation.
- HTTP response identity includes admitted status and canonical allow-listed ETag; Explorer source
  metadata derives only from that sealed capture.
- Each of the five operator CLI paths terminates at its exact value-authority source factory and
  projection. Failure and Syzygy local-domain arms create no evidence.

## Verification

The maintained provider contracts pass 9/9, 7/7, 9/9, 5/5 and 8/8 after updating the two older
contracts whose section anchors and seven-domain wording were superseded by this repair. The new
second-author contract passes 6/6. Full governance and repository verification remain the next
gate for this authoring checkpoint.

No provider, engine, API, schema, content, archive or protected-intent production byte changed.
