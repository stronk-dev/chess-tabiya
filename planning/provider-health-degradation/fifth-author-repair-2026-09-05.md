# Provider health/degradation — fifth author repair

**Date:** 2026-09-05

**Scope:** bounded RFC/contract repair for [[D2753]]–[[D2760]] plus self-audit [[D2762]]–[[D2764]].
No production provider, route, web, storage, run-schema, migration, content, archive or protected-
design byte changed.

## Result

The claim-free checkpoint now has the exact named snapshot, application outcome, profile selector
and generation-bound release receipt. A separate consumer declaration population feeds the
zero-input compiler; provider exchange alone seals requests/deliveries/failures; the F1 renderer and
display operation alone seal speech text; and the registry alone owns health state.

Exact cache entries retain one same-subject sealed delivery as payload/origin under TTL,
generation invalidation and a 512-entry LRU. Shared backoff consumes a sealed snapshot-derived
generation set. Opponent failure/retry/change use SQLite transactions over the committed learner
ply and reload after restart with exact idempotency subjects.

The repair's own audit found and closed three further crossings before publication: stale or
cross-registry release snapshots ([[D2762]]), caller strings clearing backoff ([[D2763]]) and
cross-run idempotency reuse ([[D2764]]).

## Executable evidence

`make provider-health-fifth-author-repair` retains every prior author/review target, passes 8/8 new
behavioral groups and strict TypeScript. The maintained target is a `verify-governance` dependency,
so ordinary local and GitHub verification execute it.

This is author repair, not acceptance or implementation. A genuinely fresh independent review and
the provider-protocol/exchange prerequisites still gate both implementation checkpoints.
