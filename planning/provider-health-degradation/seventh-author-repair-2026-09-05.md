# Provider health/degradation — seventh author repair

**Date:** 2026-09-05

## Outcome

The composed replacement checkpoint closes [[D2846]]–[[D2851]] at contract tier. No production
provider, API, client, store, schema, migration, content, archive or protected-design byte changed.

- configuration is parsed against the closed instance and allowed-implementation declarations;
- snapshots retain family, implementation and generation and are current-registry authorities;
- release receipts and generation sets are registry-current and backoff-group scoped;
- one coordinator owns acquire, renew, settle, expiry, Retry-After and bounded shared backoff;
- exact cache identity includes application, stage, exchange, instance, implementation, generation,
  request and key, and one hit returns value, original delivery and current service receipt;
- the closed application outcome, availability selector, settlement and release surface is composed
  with the sixth repair's durable recovery authority; and
- unused dependency/condition/multi-stage grammar is deleted. `/speech` remains an independent
  operation over a sealed already-displayed text identity.

The focused gate initially failed because cache admission passed a sealed delivery through a
request-only brand assertion. The repair now checks the exact sealed exchange subject—request,
delivery or failure—without admitting structural copies. A health/cache transition also invalidates
previous availability snapshots and release receipts.

## Executable evidence

`make provider-health-seventh-author-repair` retains every predecessor and the 6/6 seventh fresh
attacks, passes 6/6 composed repair groups, and runs strict TypeScript. The target is owned by
`verify-governance`.

## Boundary

This remains author contract evidence, not acceptance or production implementation. Another
genuinely fresh review and the provider-protocol/provider-exchange prerequisites still gate both
the claim-free runtime checkpoint and lane-0.26 durable checkpoint.
