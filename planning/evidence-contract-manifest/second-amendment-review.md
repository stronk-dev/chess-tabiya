# F1 second-amendment buildability review

Date: 2026-08-21
Author/reviewer: codex
Return: `planning/evidence-contract-manifest/second-author-return.md`

Verdict: **accepted for resumed implementation**. The amendment changes no owner/product ruling.
It corrects two technical directions that made the prior contract impossible to implement
truthfully.

## Consumer direction

The census was re-derived at delivery symbols, not route or computation names. Twenty-three
operations take a distinct product/authoring action from evidence. `guidance.ts:evidencePacket`
is an aggregate producer; POST `/analysis` is a work-acquisition command. Both remain separately
anchored and are forbidden from reappearing in `ConsumerDeclaration[]`. Completed analysis is
delivered through the attached-reference renderer (`runtime.evidence_ref`). The same review
re-anchored shape firing, human/corpus panels, pivotal markers, selected-square sight, opponent
selection and Review story at their actual delivery/decision symbols rather than upstream request
parsers or computations.

## Structural payload

The amended boundary represents all of the shipped grammar:

- `authored.structural_condition.input@1` carries the complete authored AST plus its literal
  pack/shape source, document id and pointer;
- eighteen `rules.structural.predicate.<kind>@1` identities carry only direct feature-leaf results;
- `rules.structural.predicate.result@1` carries the total boolean and exact path/node/result trace.

The trace covers non-feature leaves (`pieceOnSquare`, quantified `piece`) and composition/negation;
unexpanded `plan_signature` retains the evaluator's existing refusal. A composite test uses two
feature families, a piece leaf, quantified piece and negation and cannot be truthfully wrapped as
one leaf family.

## Executed checks

- runtime TypeScript check: green;
- focused catalogue/structural-evidence tests: 8/8 green;
- `make evidence-manifest-check`: green at the reviewed checkpoint; the resumed-bind D671
  provenance correction intentionally re-runs it with 18 producers, 80 projections, 23 operations plus the
  explicit producerless arrows disposition, 197 bindings.

The review does not declare F1 implemented. Criterion 7 still requires the remaining delivery
entrypoints and negative type fixtures to consume the compiled views; that is the resumed bind
stage.
