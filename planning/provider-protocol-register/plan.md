# Provider-protocol shared-resource population plan

## State

Author-repaired 2026-09-01 after the fresh return. Provider protocol is one generic atomic absent
resource; runtime literals are separated from compile-time relations and product population is
joined to an independently accepted obligation block. No implementation is authorized before the
generic engine lands and this population survives fresh review.

## Order

1. ✅ Rebase the bespoke C11 plan onto the generic catalogue/lifecycle.
2. ✅ Define the atomic root and literal/type boundary.
3. ✅ Route count-preserving swap refusal to the prior accepted product-obligation block.
4. ✅ Inherit generic selector absence and staged/first-parent history.
5. WAIT: fresh independent review after `shared-resource-register-bootstrap` acceptance.
6. Implement the absent descriptor/register only; change no provider bytes.
7. Run normal full verification and archive with ledger/log closeout.
8. Amend/review provider exchange with the machine obligations and lane claim, then implement.

## Boundary

Process descriptor/register bytes only. Provider, engine, source, API, schema, content and protected
intent are excluded.
