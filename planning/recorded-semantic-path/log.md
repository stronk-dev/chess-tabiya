# Recorded semantic path log

## 2026-08-27 — Independent buildability return

The first independent pass returned four contract gaps plus one inherited blocker. `branchPath`
silently truncates missing-parent chains and selects a head from node-array order ([[D1927]]).
`run.record.move@1` is a loose narrative payload rather than the exact edge authority the RFC
claims ([[D1928]]). The live semantic seal accepts unrelated recorded-move values and produces the
same event id, reproducing [[D1921]] at this operation boundary. The result promises dependence on
current convention heads but digests no convention receipt ([[D1929]]), and its co-editable relative
benchmark has neither an absolute consumer budget nor deterministic CI boundary ([[D1930]]).
A five-arm disposable harness reproduces the executable boundaries and confirms the eleven-family
population itself is derivable exactly. Exact return:
`independent-buildability-review-2026-08-27.md`.

## 2026-08-27 — D1930 cost measurement preregistered

Frozen a 20/40/80-ply, twelve-path-per-arm imported population; legal replay plus all thirteen
evaluator rows; one warmup and three cold measured calls; split validation/preparation/window/total
timings; exact work counts; and the existing 500 ms synchronous envelope. Any failed arm routes
full-path work to background/incremental execution. Generic software CI receives only deterministic
work assertions; wall-clock remains in the pinned performance tier.

## 2026-08-27 — D1930 eager full-path shape refused

The frozen harness passed its mechanics and measured 36 calls per arm. Total p95 was 399.7 ms at
20 plies, 826.3 ms at 40 and 1,434.0 ms at 80, so the preregistered 500 ms synchronous decision
refuses the 40/80-ply arms. Validation remained below 0.6 ms; the thirteen windows reached only
184.9 ms at 80. Full `localSemanticEvents` preparation consumed about 88% of total p95, producing
[[D1931]]: measure canonical one-edge reuse or the exact capture/check/duty/exchange closure instead
of recomputing every local semantic family. Results:
`d1930-cost-results.{json,md}`; dossier: `design/research/recorded-semantic-path-cost.md`.

## 2026-08-27 — D1931 exact-source comparison preregistered

Froze an identity-first comparison on D1930's exact population. The candidate replaces full local
fan-out with one transition compile and one direct check probe per edge. It must produce byte-equal
event ids, receipt bytes and result digest for every path before timing matters, then pass 500 ms
p95 at all 20/40/80-ply arms. Control timings are rerun in the same process; old D1930 numbers are
not reused as a favorable baseline.

## 2026-08-27 — D1931 exact-source comparison passes

The candidate passed the identity gate on every fixed path: sorted event ids, all thirteen receipts
per start and the final digest are byte-equal to eager fan-out. Total p95 falls from
397.5/803.6/1,391.2 ms to 64.7/129.7/212.7 ms at 20/40/80 plies, passing the 500 ms envelope in
all arms. The RFC can retain a synchronous operation by compiling only one transition event set and
one checked check declaration per edge plus its exact recorded-edge/duty/exchange sources. Full
local fan-out remains refused. Results: `d1931-source-closure-results.{json,md}`.
