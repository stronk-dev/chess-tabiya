# Bot policy — second author repair

- **Date:** 2026-08-30
- **Scope:** [[D2087]]–[[D2096]]
- **Status:** author repair complete; fresh independent review required
- **Production authority:** none; this checkpoint changes RFC and disposable authoring instruments
  only

## Result

The ten fresh-review blockers were one operation-boundary failure, not ten independent patches.
The amended contract now has:

1. one `opponent.move_selected` decision+operation envelope whose canonical image names the
   pre-commit head and resulting event sequence but never its own resulting head digest;
2. twelve immutable `family.band@1` profile identities—three behavior families crossed with the
   ruled 1000/1400/1800/2200 model bands—and an exact persisted run reference;
3. the provider author's shared delivery, result, payload and cp/mate authority, imported rather
   than redefined by the bot artifact;
4. a run-root exact legal map that admits baseline Maia independently of optional Stockfish;
5. one all-legal guard derivation that selects the reference before retaining bounded Maia rows and
   abstains without fabricating centipawns on mate or mixed score domains;
6. one sealed compiler that reconstructs, truncates, guards, applies dependency-satisfied traits,
   normalizes and samples; callers supply none of its claimed outputs;
7. a parsed request id plus separate pre-provider and commit operand images, allowing replay before
   provider work while binding exact derivation/provider sources at commit;
8. a Stage-B subset set-equal to admitted bounded Maia rows while retaining packet, exact-legal and
   omitted-population identities; and
9. the same declared seeded sampler below the returned-mass floor, recorded as degraded rather than
   attributed to a nonexistent provider-selected move.

## Executable evidence

- `make bot-policy-author-contract`: **11/11 green**.
- `make provider-exchange-author-repair`: **8/8 green**, including the new shared-consumer authority
  arm that refuses an open requested/actual identity record.
- unchanged `make bot-policy-fresh-review`: **0/10**, the intended inversion of every historical
  blocker assertion.

The first author-contract run failed one idempotency arm. `preProviderOperandDigest` hashed the
whole call object, including the optional prior envelope, even though its TypeScript parameter type
listed only the five canonical operands. An identical retry therefore changed identity merely by
carrying the prior record. The implementation now constructs the canonical image explicitly from
request, root, writer lease, profile and seed; the retry, crossed-writer and crossed-seed arms all
pass. This is a real able-to-fail finding, not fixture polish.

## Boundary

No production provider, selector, run schema, migration, endpoint, storage, roster, card, content,
archive or protected-design byte changed. Implementation remains unauthorized until a new
independent buildability review accepts this RFC and its provider/packet dependencies are accepted
and implemented.
