# Bot policy — third author repair

- **Date:** 2026-08-31
- **Input:** second fresh independent return [[D2219]]–[[D2226]]
- **Additional finding:** run-schema dependency cycle [[D2364]]
- **Status:** author repair complete; fresh independent review and dependency checkpoints required
- **Production authorization:** none

## What changed

1. The twelve profile declarations now carry the exact pinned Maia3 model/source, T=0.8, top-p
   0.92, width 20, returned-mass floor 0.97 and exact ordered layers. Admission compares every
   field; the committed 837-cell captured-production artifact is read as the positive control.
2. `pawn_move@1` consumes the runtime exact legal move role, not a UCI prefix. Both colours, every
   file, captures, en passant, all promotion identities, castling and non-pawn negatives execute.
3. The bot retains each admitted shared provider delivery through decision, persistence, replay and
   export. Mutating a delivered payload after persistence invalidates its canonical delivery digest.
4. Roster availability joins provider health's exact operation state plus a generation-bound release
   receipt. Baseline remains independent of Stockfish; guarded families do not.
5. One closed route result maps all eight operation outcomes to exact HTTP/code/retry/action bytes.
6. Cross-package profile/layer/classifier/reason identity is one generated registered
   `bot-profile-catalog` resource; Stage-B feature identity derives from the shared packet union.
7. Later retries compare only pre-provider identity and replay without calls. Concurrent first
   flights compare commit identity under serialization: identical bytes replay the advanced-head
   winner, differing provider/decision bytes conflict without writing.

## Dependency correction

[[D2364]] records a real lane cycle: bot policy owns run-schema 0.18, while provider health owns
0.26. Provider health therefore gains a claim-free runtime-authority checkpoint and remains
implementing; bot policy consumes those exact symbols and may later land 0.18; provider health's
durable acquisition projection stays at 0.26. No lane is renumbered and no bot-private health model
is permitted.

The shared catalog also cannot claim lane 1 until `shared-resource-register-bootstrap.md` is
accepted and implemented with an absent `bot-profile-catalog` root. Bot-policy acceptance remains
blocked until that claim can be byte-joined to the RFC.

## Executable evidence

`make bot-policy-author-contract` passes 31/31 plus strict TypeScript. The able-to-fail arms include the committed sampler
artifact, exact model/source mismatches, six legal-board position families, provider-delivery
tampering, health generation mismatch, every route result, invalid layer grammar and both
concurrent-commit outcomes.

Full repository verification passed on 2026-08-31: 1,085 software tests, 2 performance tests and
172 real-content tests, with schema, build, browser/contract and governance gates green. No
production route, schema, migration, catalog, provider, runtime, client, content, archive or
protected-design byte is changed by this author repair.
