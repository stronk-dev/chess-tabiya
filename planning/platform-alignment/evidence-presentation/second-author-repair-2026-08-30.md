# Evidence presentation — second author repair

- **Date:** 2026-08-30
- **Scope:** [[D2135]]–[[D2140]] only
- **Verdict:** author repair complete; fresh independent review and [[D1672]] remain required
- **Production status:** unchanged; no manifest or presentation implementation is authorized

The old 117-pair family census was a useful pre-repair inventory but not a presentation authority.
It inferred UI reachability from non-machine forms and therefore counted five pairs belonging to
two backend operations. The controlling author image now classifies all 20 consumers against a
real source file and operation symbol. `opponent.selection@1` and
`runtime.repertoire_scan@1` are explicitly non-presentational.

The resulting pre-repair presentation population is 112 exact pairs: 104 adaptable, seven blocked
on retained operands and one selection-only binding to remove. Each pair carries literal forms and
one exact component target or named composition whose member/form union equals the binding. Every
member form is checked against the component capability registry.

Checkpoint P owns six atomic manifest/payload repairs: make the two backend operations machine
only; make Story rank selection-only; retain named-structure witness squares; give pack phase a
typed `{phase}` payload; and give consequence a discriminated terminal/outcome versus
nonterminal/plies/objective-state payload. Recompilation must yield 111/111 adaptable presentation
pairs before component work begins. The `rfc/README.md` shared-resource pin now names this bounded
mutation; module-registration retains sole ownership of module declarations and seats.

`FACT_STATEMENT_RENDERERS` is set-equal to every exact adapter target containing deterministic
prose. Its per-pair ids, output forms, variants, operands and templates are literal and every
changed template operand changes bytes. `PRESENTATION_ABSTENTION_ROWS` is likewise set-equal to
every non-removal adapter, with a registered learner question and terminal-reason subset. Pending
remains lifecycle state and is not a terminal reason.

Author-boundary verification:

- `make evidence-presentation-author-contract`: 5/5 plus lifecycle typecheck;
- `make evidence-presentation-second-author-repair`: 6/6, one inversion for each return row.

This is not acceptance. Fresh review must attack route classification, pair/form compatibility,
the six-operation predecessor, renderer operand closure, abstention population closure and both
consequence union arms. The owner-only protected-design mirror remains [[D1672]].
