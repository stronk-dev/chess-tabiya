# Evidence presentation — third author repair

- **Date:** 2026-08-30
- **Scope:** [[D2157]]–[[D2163]]
- **Verdict:** author repair complete; fresh independent review and [[D1672]] remain required
- **Production status:** unchanged; no presentation implementation is authorised

The return identified one repeated failure shape: an adapter row could name a desirable component
without possessing the operands, authority or real route needed to construct it. The repair closes
that shape at seven seams.

Named structures now move atomically across source payload, manifest operands and all five exact
adapters with typed `id`, registered `name`, provenance and witness squares. Citation is no longer
a sibling lookup: Checkpoint P adds `derived.citation.attribution@1`, a sealed multi-input operation
over one evidence-reference resolution and one exact source item, joined by reference, receipt and
content digests. Its attribution comes only from the deployment/endpoint registry; missing licence
or revision metadata abstains.

Every fact renderer variant now declares a formatter map set-equal to its operands. Objective
states, outcomes and PGN results use total labels; integer and SAN have notation formatters;
checkpoint ids remain metadata. All three consequence consumers cover terminal and nonterminal
arms. Abstention lifecycle values carry question, projection and producer, and each adapter retains
the original source reason through a total label/category join. The population derives from the
component's declared empty behavior, so silent claims produce neither a card nor a fake “nothing
authored” event. Finally, `count_with_denominator` has a real path in the existing Explorer
Inspector composition using `playedCount / total` from one sealed population response.

The pre-P population is now truthfully **112 rows: 100 adaptable, 11 operand repairs and one
selection-only removal**. Checkpoint P repairs those rows, removes the rank display binding and
adds the citation binding, yielding **112/112 adaptable**.

Maintained author verification:

- `make evidence-presentation-author-contract`: 5/5 plus lifecycle typecheck;
- `make evidence-presentation-second-author-repair`: 6/6;
- `make evidence-presentation-third-author-repair`: 7/7.

This is not acceptance. Fresh review must attack exact source-attribution closure, post-P
population arithmetic, formatter totality, consequence variants, source-reason preservation,
silent-row exclusion and the real denominator route. The protected design amendment [[D1672]] is
still owner/Claude work and was not written here.
