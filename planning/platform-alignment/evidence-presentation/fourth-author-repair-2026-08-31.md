# Evidence presentation — fourth author repair

**Date:** 2026-08-31

**Scope:** repair [[D2348]]–[[D2354]] in the disposable RFC contract only. No production,
protected-design, schema, content, API or UX bytes are authorised.

## Result

The return is repaired as one authority chain rather than seven prose promises:

- the post-P population is exactly 112 unique adaptable pairs after literal remove/replace/add;
- citation is nested `{content,source}` through derivation, parser and adapter;
- source attribution is a version-1 digest-sealed resource with exact literal/deployment resolvers;
- the complete source-reason vocabulary has explicit non-default learner dispositions;
- abstention questions are sealed adapter-local registry objects;
- Explorer counts construct candidate-grained numerator/denominator operands and refuse invalid
  bases; and
- all four named structures have one proposed atomic predicate/witness operation with non-empty
  exact geometry.

`make evidence-presentation-fourth-author-repair` passes seven runtime arms plus lifecycle
typecheck. The earlier second and third author contracts also remain green.

## Honest remaining blockers

This is not acceptance. A fifth fresh independent review must attack all seven seams. [[D1672]]
still requires the owner-tier component-vocabulary amendment. [[D2401]] is new and deliberate:
the source-attribution resource is now exact enough to register, but a product RFC may not create
its own shared-resource root. The process bootstrap must register the absent root first; only then
may this RFC replace its temporary `claims none` image with the unique `first lane 1` claim.

## Next executable boundary

Independent review may close [[D2348]]–[[D2354]] only if it reproduces the exact 112-row image,
attempts flat/crossed citation forges, corrupts each metadata resolver class, adds an unmapped
source reason, crosses question identities, mutates Explorer denominators and makes a matched
structure return empty/inconsistent geometry. Acceptance remains blocked even after a clean review
until [[D1672]] and [[D2401]] are discharged.
