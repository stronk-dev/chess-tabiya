# Evidence presentation — seventh fresh independent buildability review

**Date:** 2026-09-07
**Reviewed image:** sixth author repair for [[D3035]]–[[D3041]]
**Verdict:** **RETURN TO AUTHOR** on [[D3102]]–[[D3107]]. No production implementation is
authorised.

## What survived

The retained author chain stays green. Canonical JSON now refuses unsupported values; committed
Explorer UCI distinguishes absent from malformed; citation/source shapes are closed to registered
fields; and the Checkpoint-P population remains exactly eight rows. The review does not reopen
those narrower results.

## Returned seams

| row | executable finding | required repair |
|---|---|---|
| [[D3102]] | Two citations with identical evidence digest, retained field and source accept unrelated caller-authored text. | Derive text from the exact retained evidence value inside the sealed multi-input operation; parse/verify its receipt rather than accepting plain `{text,binding}`. |
| [[D3103]] | The Explorer parser accepts `{kind,reason}` without the required `detail` or `population`; the reason tuple exists only in the presentation model. | Export one production runtime discriminant tuple, derive `CorpusResult` from it and parse the complete abstention arm tied to its invocation/population. |
| [[D3104]] | The public author fixture process-seals arbitrary caller-chosen workflow/decision bytes, after which issuance accepts them. | Name and execute the real owning workflow request/decision operation; the success authority cannot originate in a caller fixture. |
| [[D3105]] | The declared production structure registry/evaluator symbols do not exist; the author model executes its own four-expression copy while production still emits empty squares. | One production registry and evaluator must decide the match, label and positive square witnesses. |
| [[D3106]] | A fake source reader returning only every declared needle passes all eight “executable” pre/postimage rows. | Resolve exact exports, invoke typed operations and assert semantic preimage→postimage behavior; string presence is only an anchor census. |
| [[D3107]] | The registry digest accepts and hashes a partial three-field object with an empty row set. | Parse the exact semantic image and issue identity only for that branded complete value. |

## Reproduction

`make evidence-presentation-seventh-fresh-review` retains the complete predecessor chain and passes
six falsifiers. The target is disposable RFC evidence and belongs to opt-in
`make verify-rfc-evidence`, not required release CI.

## Boundary

No product, schema, API, client, component, content or protected-design bytes changed. [[D1672]]
and [[D2401]] remain independent blockers. A bounded seventh author repair must close the six
seams above, retain the earlier controls, and undergo another genuinely fresh review before this
RFC can be accepted or implemented.
