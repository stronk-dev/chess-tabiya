# Theory↔drill current joins — author repair checkpoint

**Date:** 2026-08-30

**Scope:** [[D1879]]–[[D1887]] from the 2026-08-27 independent buildability return

**Verdict:** technical author repair complete; [[D1887]] owner-pending; fresh review required

## What changed

- The applicability contract now consumes the literal implemented runtime-opening payload instead
  of inventing `OpeningIdentityRef` at an impossible package boundary.
- Bare principles and anchored claim occurrences are distinct identities, and pack provenance is a
  closed shape-present/authored-claim union.
- Shared results carry closed theory actions rather than free-form routes. Factual numeric and
  cited-string operands remain; score, rank, confidence and editorial prose do not.
- `run_node` and `library` are distinct contexts. Library uses ordinary direct pack start and never
  manufactures a source derivation.
- Source-bound launch is one authenticated REST/client operation. The client supplies only a
  target id; the server authorizes the source, recomputes applicability and persists the exact
  recomputed identity and target atomically.
- Durable derivations are a discriminated union. SQL checks kind/presence while the reader validates
  canonical union bytes, target identity and provenance joins.
- Learn recommendations retain exact run/branch/node/ply firing anchors, including multiple
  branches and repeated firings.

## The remaining decision

[[D1887]] is not technical. The recommended product rule is: a completed countable attempt entered
through a theory/shape door counts exactly like the same pack entered directly; opening the door or
abandoning the run counts for nothing. Derivation origin remains visible provenance. This must be
accepted or replaced by the owner before fresh review.

## Executable author check

`make theory-drill-author-contract` checks all nine returned seams and also refuses an author status
that claims acceptance or implementation. It is a positive author check, not an independent review.
