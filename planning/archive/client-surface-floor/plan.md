# Client surface floor implementation plan

1. Remove the tablet-only document layout and retain the fitted workspace down to the compact breakpoint.
2. Make compact regions mutually exclusive, bind Evidence, remove the inert Session affordance, and size the board from its slot.
3. Measure and enforce the minimum supported run viewport with an explicit refusal below it.
4. Raise the pivotal-marker pointer target and pin both marker sizes.
5. Expand browser projection coverage, update canonical shell documentation, and run both gates.
