# Graduation clearance — second fresh independent buildability review

**Date:** 2026-09-08

**Reviewer:** Codex, independent of the bounded Claude author repair

**Verdict:** **PASSED.** The repair discharges [[D3088]]–[[D3091]] and restores the accepted
schema-0.28 implementation boundary. This verdict authorizes the production schema, evaluator and
atomic corpus migration already owned by the accepted RFC; it does not authorize authored chess
truth, claim binding, publication, or archival before the remaining criteria pass.

## What was independently checked

- `make graduation-clearance-author-repair` passes 7/7 executable controls over the real nine
  emitter shapes.
- Every entry id rejoins the closed plan registry before evaluation. Omitted, changed and extra
  persisted plan fields fail before their predicate is considered, including `subject`, `expected`,
  `instrument` and `templateId`.
- The six content-bearing templates use `repository_content_declaration`. Its digest binds the
  exact pack id, entry id, template id, registry-owned pointer/value vector and `declaredAt`; the
  contract makes no actor-authentication or trusted-clock claim.
- Planner and evaluator both hash bytes from
  `packages/schema/src/drill-pack/digest.ts:canonicalizeJson`. Lone high and low surrogates are
  refused at that shared boundary.
- The permanent sequence observes 9/9 emitted predicates false, 9/9 after the minimum truthful
  mutation, and 9/9 false after a decided input changes. The dangerous controls also prove that a
  strong-engine policy cannot satisfy the exact-tablebase plan, changing a policy without a fresh
  declaration cannot clear it, and declaring the untouched mechanical objective cannot clear it.
- Empty authored teaching collections and an empty recorded spine remain blocking even with a
  matching digest. All four closed graded outcome types compile; the emitted ungraded outcome does
  not.
- The read-only migration proposal remains total over 436 entries at 204 mechanically ready, 232
  requiring exact author-reviewed predicate fields and zero contract-blocked. Visibility of those
  232 decisions is a migration boundary, not a reason to weaken the production evaluator.

## Production obligations retained

The disposable model is not a second implementation. Its plan authority, declaration grammar,
evaluator behavior and negative controls must move into production tests with schema 0.28. The
corpus migration must remain atomic with evidence-sidecar digest restamping, and may not turn any
blocking entry into resolved merely to complete the format migration.
