# Refusal-code coverage log

Append-only.

## 2026-08-15 — opened (codex)

- Predicate wave 3 remained under concurrent final-pass editing, so the queue was blocked.
- Recounted the current tree rather than trusting the audit snapshot: the emitter inventory grew from 78 to 107 fixed codes, and later waves had already reduced the directly unpinned set from 45 to 41.
- This job is test-only standing work A. Reachable paths get constructed invalid documents; schema-shadowed exhaustive refusals get an explicit defensive classification rather than a fake runtime fixture.

## 2026-08-15 — coverage pinned (codex)

- Added direct constructed-document coverage for lint, boundary, objective-transition, stated-reasoning, trajectory, theory, timing, root, and Syzygy refusal families.
- `NEGATIVE_FEATURE_COUNT`, `STRUCTURAL_KIND_UNRECOGNISED`, and `UNSUPPORTED_FEEDBACK_POLICY` are schema-shadowed defensive backstops on ordinary `validatePackDocument` input; the test pins their continued presence and the schema gate instead of pretending a schema-valid document can reach them.
- Added an emitter-to-test-corpus drift assertion. Recount after the test: 107 fixed uppercase emitter literals, zero without a direct test disposition.

## 2026-08-15 — complete (codex)

- `ENGINES_REQUIRED=1 make verify` passed: 537 tests across 86 files; Svelte diagnostics 0/0; scaffold and packaging clean.
- `make test-browser` passed at zero retries: 24 passed, one optional Maia latency spec skipped.
- No production source, schema, migration, active RFC, or version register changed. The planning job moves directly to the archive as completed standing work.
