# Wave-C module amendment — fresh independent buildability review

**Reviewed:** 2026-09-12

**Scope:** `rfc/learner-modules.md` §4.12/A19 and its `module-registration.md` rebase

**Verdict:** pass; no technical return

## What was re-derived

The review did not treat the author harness as proof of its own premise. It joined the amendment to
the compiled `PRIMARY_EVIDENCE_MANIFEST`, the production semantic constructors and the literal
module-binding plan.

- `SEMANTIC_WAVE_EVENT_PROJECTION_IDS` contributes nine shipped event projections. Adding
  `defender_duty_set`, `overloaded_defender_response_conflict` and
  `forced_mate_after_move` produces twelve distinct compiled projections.
- The seven `derived.tactic.*_observed` projections retain exact recorded consequences and admit
  only fact-distance output. They may therefore reach Post-commit Nudge, Review Map and Full
  Inspector without revealing a candidate move or grading intent.
- The remaining five projections bind only to Full Inspector. This keeps the candidate carried by
  the overload predicate and mate proof out of proactive Support, while preserving the owner's
  requirement that every primitive remain deliberately reachable in the advanced surface.
- The literal plan contains exactly 26 Wave-C pairs: 7 × 3 observed-event bindings plus five
  Inspector-only bindings. The compiled implementation draft remains requirements-only and calls
  every pair dependency-blocked; it does not claim that the missing recorded-path operation or the
  module assembler already executes.
- Review consumes this eligibility population. It is not an authority that can grant eligibility,
  so removing the former D921→Review edge is acyclic and correct.

## Able-to-fail boundary

`make wave-c-module-amendment` passes the compiled-manifest closure, the complete 231-pair author
image and five amendment controls. The controls fail on a missing or extra Wave-C pair, proactive
admission of any of the five held rows, either transferred promotion successor, a non-shipped id,
parent/register disagreement or restoration of the false Review dependency.

The amendment is buildable and ready for owner acceptance. This review does not authorize the
still-draft `module-registration.md` implementation or claim that its 132-projection assembler is
present.
