# Module registration — eighth author repair

- **Date:** 2026-09-02
- **Rows:** [[D2530]]–[[D2535]]
- **Author gate:** `make module-registration-eighth-author-repair` — 6/6 plus retained 11/11
- **Production authorization:** none; another fresh independent review is required

## Repaired boundaries

The provider row now follows the actual `ProviderTraversalApplication`: its scheduler returns the
operation-correlated delivery and the same injected application's `sourceFactories` value seals it.
No runtime registry is inferred from the `ProviderSourceFactories` mapped type.

The catalogue adapter now fixes `run.record.position@1` as its position authority and maps each of
its three requested outputs to an exact applicability producer/projection. Its normative contract
rejects crossed, duplicate and unrelated sealed declarations and specifies the payload join for
claim, shape and opening evidence.

The shipped compare eval delta is now an edge-grained transition: two consecutive points on one
recorded branch through the Review packet's `recorded_edge` view. No cross-branch meaning was added.

Deflection now has a common input set and literal input sets for bait-capture and check-induced
arms. This exposed rather than concealed an upstream gap: the detector reads check from the first
edge's resulting FEN, but `deflectionObservedSemanticEvent` and its manifest declaration cannot
carry `rules.tactic.event.check@1`. The check arm remains dependency-blocked until that owner makes
the declaration and emitter agree.

Every binding row now points to its exact `module.<id>@1\0<projection>@1` resolution key; the gate
checks 205/205 set equality. The final resolution receipt gains a constructor-only runtime seal,
deep freezing and an assertion that the F1 emitter must call. Spread, cast and serialized copies do
not carry authority.

## Honest residual

Both upstream pool ABIs and the deflection check-evidence dependency remain unresolved. All
generated artifacts are still `requirements_only`, all final bindings remain blocked, and this
repair authorizes no implementation. A genuinely fresh reviewer must attack the amended joins.
