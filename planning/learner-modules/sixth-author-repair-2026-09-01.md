# Module registration — sixth author repair

- **Date:** 2026-09-01
- **Input:** fifth fresh return [[D2432]]–[[D2435]]
- **Status:** author-repaired; another fresh independent review required
- **Boundary:** RFC, generated requirements and disposable author contract only

## Result

The repair keeps the central refusal: modules consume sealed upstream evidence and do not become a
second detector/provider engine. Both generated artifacts still say `requirements_only`; all 117
evidence rows and 205 module/projection pairs remain dependency-blocked, with exact occurrence,
timing and presentation operations unresolved.

What changed:

- the seven observed multi-edge tactics now retain operation-owned ordered windows, horizons,
  offsets, roles, cardinalities, output anchors and identity joins;
- the candidate service exposes only root-legal-population, candidate-child-by-UCI,
  candidate-edge-by-UCI and complete-population views, explicitly refusing committed-edge and
  arbitrary current-root projection;
- eval delta and move quality retain ordered endpoint roles and their instrument/search/domain
  equality constraints;
- the five source contracts reference `CandidatePopulationService.get`,
  `compileRecordedSemanticPath`, `compileReviewEvidence`, a fully typed module-owned
  `compileCatalogueEvidencePool`, and `ProviderExchangeScheduler.get`; and
- the rejected `AUTHOR_ADDITIONAL_SUBJECT_VIEWS` table is deleted.

## Upstream finding and repair

[[D2473]] was not papered over. `derived.grade.move_quality@1` declared recorded and live eval as
one conjunction, while its accepted RFC requires a before/after pair from either lane and forbids
cross-lane grading. The production manifest now declares the two alternatives, the grade operation
enforces lane/engine/search-limit equality, and the regenerated requirements artifact records the
exact two-occurrence before/after contract as `upstream_same_lane_anyof_verified`. This clears the
specific upstream contradiction; exact consumer adapters and presentation remain separately blocked.

## Executable evidence

`make module-registration-sixth-author-repair` checks five able-to-fail boundaries: exact
multi-edge topology, candidate occurrence views/selectors, ordered delta operands, real callable
ownership with the obsolete authority removed, and sealed/non-executable artifact invariants.

No production runtime, API, schema, content, provider, bot, pack or learner-visible UX byte is
authorized or changed by this repair.
