# Module registration — tenth author repair

- **Date:** 2026-09-04
- **Rows:** [[D2584]], [[D2585]], [[D2586]]
- **Gate:** `make module-registration-tenth-author-repair` — maintained 11/11 plus 3/3
- **Status:** author repair complete; genuinely fresh independent review required

The generated requirement image now keeps the defender-duty input position-grained and copies each
exact ordered-window operand's alternative, cardinality, offsets and roles into its input binding.
The binding image no longer assigns one source to every timing: its closed `byMoment` relation uses
candidate occurrence views only before commitment and recorded identities afterward.

The same repair closes the executable splice found by the review. Observed-tactic move evidence is
joined to the exact recorded anchor (both node ids, both FENs and UCI) and offset. Duty, capture,
check and exchange inputs are joined to their exact position/window occurrence; named exchange
payloads must equal the event operand. Four controls cross move, duty, capture and exchange evidence
independently while preserving valid seals and projection ids, and all four are refused.

No learner module, route, presentation seat or final binding is implemented. Both generated files
still say `completionClaim: requirements_only`; exact source operations and the final resolution
receipt remain dependency-blocked.
