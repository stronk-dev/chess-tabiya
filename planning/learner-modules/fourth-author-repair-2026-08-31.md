# Module registration — D2343–D2347 fourth author repair

- **Date:** 2026-08-31
- **Input:** `third-fresh-independent-buildability-review-2026-08-30.md`
- **Verdict:** author repair complete; fourth fresh independent review required
- **Production status:** unchanged; requirements-only artifacts, implementation unauthorized

## Repaired boundary

1. D1865 now imports `AUTHOR_MODULE_ACCEPTS` directly. The explicit empty `guided_hint` key is
   shared, visible and still owner-blocked; the 207 non-hint pair census remains unchanged.
2. Subject grain is no longer inferred from producer family. Sealed stages provide their intrinsic
   grain and every ambiguous or derived projection has exact authority. The repaired controls are
   position `role_signature`, edge `move_quality`, branch-pair `piece_route`, and frozen-prefix
   `story.rank`.
3. Every requirement publishes grain-specific sealed views. Cross-grain use requires the typed
   `projection_between_grains@1` adapter. External inputs are keyed by projection plus grain;
   `run.record.move@1` has distinct edge and branch-pair views. Generation validates every DAG
   edge against the consumer join grain.
4. All five sealed-pool contracts declare non-empty timing and subject applicability. Binding
   timing is the complete module-policy intersection with the exact acquisition contract. Recorded
   `defender_exposure` therefore cannot enter Threat Radar before commit.
5. Module policies carry `ModuleForm` values and expand through shipped `MODULE_FORM_IMAGE`.
   Binding requirements retain the entire projection/module intersection as `requiredForms`, so a
   card cannot silently lose `list` and a later pair adapter must match exactly.

The two regenerated artifacts remain honest requirements: 117 evidence rows, 205 binding rows,
all dependency-blocked, `completionClaim: requirements_only`. No module declaration, consumer,
assembler, API route, Svelte seat, schema, migration, content or protected design byte changed.

## Executable author evidence

- `make module-registration-author-contract`: 11/11 green.
- `make module-evidence-assembly`: 13/13 green.
- `make module-registration-fourth-author-repair`: 5/5 green.

The fourth target crosses the exact five returned seams: shared blocked-hint algebra; four subject
grains; complete derivation-view compatibility; source-aware timing; and complete card-form
requirements.

## Next lawful action

A fourth fresh independent reviewer must attack the regenerated JSON rather than this summary:
remove one grain view, swap role-signature/grade grains, widen recorded-path timing to precommit,
drop `list`, or make Guided Hint absent. Acceptance and production implementation remain forbidden
until that review passes and the RFC's named upstream sealed operations are buildable.
