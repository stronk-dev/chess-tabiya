# Module registration — tenth fresh independent buildability review

- **Date:** 2026-09-04
- **Scope:** ninth author repair [[D2557]], generated requirement/binding plans, and live observed-tactic emitters
- **Gate:** `make module-registration-tenth-fresh-review` — 3/3
- **Verdict:** **RETURNED on [[D2584]]–[[D2586]]**; the bounded [[D2557]] repair survives

## What survives

The deflection row no longer calls `rules.tactic.event.check@1` unavailable. It names the live
`deflectionObservedInduction`, `checkSemanticEvent` and `deflectionObservedSemanticEvent`
authorities, while the separate recorded-path occurrence source remains honestly blocked. The
ninth repair did not falsely activate a module or mint a final F1 row.

## [[D2584]] — exact windows discard their own intrinsic grain

The exact deflection, interference and overload contracts each place
`rules.tactic.reading.defender_duty_set@1` at `positionOffsets: [0]`. The projection is a FEN state
reading. `SOURCE_INPUTS` nevertheless hard-codes it as `edge`, and generated `inputBindings`
replace the typed relation with the generic string `operation_owned_occurrences`.

The retained occurrence contract and the retained input binding therefore disagree inside the same
artifact. A set-equal/non-null successor can pass without proving that the duty set belongs to the
pre-move position. The repair must derive grain from one projection authority and retain the exact
position/edge offset relation rather than masking it.

## [[D2585]] — committed moments point at a hypothetical-candidate source

Edge-local rule facts default to `candidate_population@1/candidate_edge_by_uci`. Their Postcommit
Nudge and Review Map pair rows request `postcommit`, `review` or `analysis` while retaining a
selector whose own contract says `committedEdgeForbidden: true`.

The future resolution receipt contains free source/view/selector fields, but no closed admissibility
relation says which sources are legal for each module moment. It can become non-null and set-equal
without proving that a postcommit/review fact came from the move actually played. Precommit and
at-commit may select canonical candidates; committed moments require recorded occurrence identity.

## [[D2586]] — the runtime accepts crossed derivation evidence

The defect is not confined to generated JSON. `exactSequenceInputs` checks only the count and
projection id of `run.record.move@1`; the observed tactic emitters similarly check required
projection names. They do not join move evidence to `payload.anchors`, the duty reading to position
zero, captures/exchanges to their named edge and target, or retained identities. D2557's optional
check input is the only exact crossed-edge refusal.

The executable control constructs two distinct legal three-edge deflection lines, retains valid
brands and projection ids from the second, and passes those move/duty/capture/exchange items with
the first line's payload. `deflectionObservedSemanticEvent` accepts it and emits the first event
with the second line's provenance. An operation-owned occurrence receipt or exact per-family joins
must make each such splice fail.

## Next action

Author one bounded tenth repair covering all three layers: intrinsic input grain, typed
moment-to-source eligibility, and exact runtime derivation-input joins. Then run a new fresh review.
No module registration or presentation implementation is authorized by this review.
