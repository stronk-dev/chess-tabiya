# Bot axes and card author repair — 2026-08-26

Use `design/research/bot-policy-axes-and-card-grounding.md` and the eight-arm compiler instrument at
`tools/d1601-bot-card-contract-harness/` for the bot-roster author round.

## Required amendments

- Replace “orthogonal by measurement” with independently declared band/family axes. Family
  equality excludes model-band and presentation; no cp↔Elo comparison remains.
- Every profile stays uncalibrated until an exact-digest, time-control-scoped receipt exists.
- Replace `bio`/hand-authored behavior explanations with compiled card statements selected from
  registered layers, measurements, abstentions and absences.
- Cards always state no book, no memory, endgame scope, clock scope and calibration state. Guarded
  cards state provider/deadline/incomplete/bounded/mixed-domain abstention. Pawn cards state the
  guard dependency and measured +12.28-point result.
- Keep name/avatar/chess-neutral flavor in a closed owner-authored display registry. It never feeds
  the behavior statement compiler.

Carry positive baseline/guard/pawn fixtures, same-family cross-band equality, cross-family
inequality, wrong-digest calibration refusal, malformed family composition, absent source,
decorative separation and caller-sentence rejection into acceptance.

Persona names/assets (D1610) and the new-learner default (D1611) are still owner decisions; do not
hide either behind placeholder product bytes.
