# Feedback-delivery Stage 1 — acceptance coverage

**Audit:** 2026-08-20

**Scope:** accepted RFC criteria 1–20a. Criteria 21–23 are Stage 2/content closeout and are not
claimed here.

Aggregate `make verify` is green. This matrix asks the stronger question: does a named executable
assertion cover each accepted obligation?

| criterion | state | current proof or exact residue |
|---|---|---|
| 1 | pass | Positive and non-exhausted projections run under all four policies; the outcome-attributed early-terminal negative is exercised under all four. |
| 1a | pass | Early terminal + rewind, quiescence after rewind and resumed play, real group seeding, and no-outcome checkpoint delivery are all executable. |
| 2 | pass | `feedback-delivery.test.ts` plus the disposable walkthrough re-derive 98/196 and character counts from the shipped predicate. |
| 2a | pass, explicitly vacuous | An executable implementation↔census claim-id join proves subset/refusal accounting, lists multi-label inflation, and pins the day-zero singleton on both sides. |
| 3 | pass | Real unbound refusal, real Philidor binding, and both ledger-less registry fallbacks are asserted through delivery. |
| 4 | pass | Machine-withheld-only leaves the flag false; the same-tree walkthrough records 22/42 true before and 22/42 after—zero discrimination loss. |
| 5 | pass | Canonical identity, constructed N=2/4/8, and a same-tree 44-fork measurement record baseline/admitted volume, 7.32 entries/ply, 97.3% firing, per-fork counts and 1.017x lift. No effectiveness threshold is inferred. |
| 6 | pass; D645 repaired | The real three-way split is 19/14/17. The driver rewinds and supplies configured-policy opponent replies across attempts; 50/50 now exhaust (19/19, 14/14, 17/17). The former eight failures were repaired under `authored-consequence-lifecycle`, without anchors or a softer predicate, and a corpus validator plus disposable replay prevent recurrence. |
| 7 | pass | CR3's unfiltered N<2 projection and identical-path empty strip are pinned. D528's zero-ply column remains a separate accepted-text omission, not silently folded into CR3. |
| 8 | pass | The existing real `GET /packs/:id` test explicitly excludes `feedbackClaims`, deviations, plans and concepts. |
| 9 | pass | A same-tree oracle rebuilds pivotal timing independently and proves byte identity, including explicit castling and pawn-contact renderings. |
| 10 | pass | The only `CompareView` change is the structure renderer expression; timing, rung-0 reading and styles are unchanged. |
| 11 | not due | Ledger/RFC archive closeout belongs to Stage 2 by the accepted split. |
| 12 | pass | B4/K6 retire the stale 0/131 statement and record the measured populations separately: 98/196 evidence-admitted, 98/196 evidence-withheld; 69/98 admitted claims rendered after full-spine exhaustion and 29/98 timing-withheld without a released reveal occurrence. |
| 13 | pass | Pack/run/storage versions and RFC registers are untouched. |
| 14 | pass | A real validating binding is reordered, raises `CLAIM_POINTER_REBOUND`, loses its bound id, and is absent from the delivered claim set while an unbound companion still renders. |
| 15 | pass | Packet identity, actual `/voice` response identity, `/speech` text/byte identity, claim absence and the `authoredText` source guard are all pinned. |
| 16 | pass after D526 repair | Eight-ply constructed N=8 admits 70/450 (15.6%), below the 90% reopening threshold; the original 100% one-ply result is preserved as instrument failure. |
| 17 | pass | D97/D98 are untouched. |
| 18 | pass | The explicit derived-feature default is delivered with empty earned/spans/principles, and the corpus population/characters are re-derived in the measurement. |
| 19 | pass in inherited binding suite | The current 90.9%-versus-integer-rate hole remains explicitly pinned by `claim-binding.test.ts`. |
| 20 | pass after D644 author correction | The corpus-wide boundary assertion covers all 67 projected rows: all authored principle inputs survive byte-for-byte, 46 rows contain authored prohibited vocabulary, zero contain a template-owned prohibited token, and the injected template-owned negative fails. C8 and the claim sentence are unchanged. |
| 20a | pass | Every admitted corpus claim is checked for earned-only recorded clauses, and a bound-machine-plus-hypothesis negative proves the declaration cannot leak into that clause. |

## Recovery result

Criteria 1–20a are mapped to executable proof or an explicitly preserved out-of-scope defect.
Stage 1 may land after the scoped verification/browser rerun. Leave the RFC accepted/implementing
and its completion rows open for Stage 2.
