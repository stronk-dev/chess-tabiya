# Feedback delivery implementation log

## 2026-08-17 — Stage 1 opened

- Review verdict: accepted Stage 1 remains implementable; Stage 2 is deliberately unowned and was
  not adopted.
- Version baseline re-read from the tree: pack 0.27, run 0.17, storage 23. This RFC claims none.
- The owner ruling's no-play basis is the 2026-08-12 entry in
  `planning/exploration/log.md`; Stage 2 must re-check all later append-only entries before closeout.
- RFC status and D77/D78/D79/D167 remain open through Stage 1 by criterion 11.

## 2026-08-17 — CR1 reopening trigger fired

- The required constructed comparison measurement admitted every structure-strip entry at each
  measured width: N=2 23/23, N=4 43/43, and N=8 79/79.
- N=8's 100.0% admission is above criterion 16's 90% reopening threshold. CR1 therefore cannot
  ship as an effective filter under the accepted RFC.
- No majority, scoring, or pairwise replacement was invented. Open question 6 is live again and
  needs an RFC revision before Stage 1 can land.
- The C1 walkthrough measured conservative reach at 19/50 claim-bearing packs (38.0%), above the
  10% kill threshold; 17 opponent-choice-dependent packs received no reach credit.

## 2026-08-20 — the CR1 trigger was traced to the instrument and corrected

- Preserved the one-ply 100% result above as the false start; it is not a measurement of the
  accepted filter. At one ply the constructed candidate and common sets cannot meaningfully overlap.
- Replaced the construction with eight-ply deterministic continuations through the shipped runtime
  at N=2/4/8. The corrected N=8 result is below the 90% reopening threshold, so CR1 remains admitted.
- Reused `structure.ts`'s canonical observation identity in `compare-strips`; this removes detail-only
  pawn-safety churn instead of maintaining a second identity function.
- Stage 1 remains implementing until criteria 1-20 are mapped and the scoped verification/browser
  proof is complete. Stage 2 remains separate and no content hold was lifted.

## 2026-08-20 — criterion 20 returned the accepted RFC to its author

- Executed the literal criterion over all 67 current projected `claimBackings` rows. Forty-six rows
  across 26 packs contain a banned judgement or prescriptive word in the required authored
  principle statement/counter-case; the sole bound Philidor claim also fails.
- The renderer did not invent those words. C8 requires it to preserve the authored fields, so
  deleting or paraphrasing them would satisfy one clause by violating another and cross law 8.
- Prepared a narrow correction: constrain template-owned wording and require every prohibited word
  in the complete output to trace to unchanged authored input. Stage 1 remains uncommitted pending
  that author edit and the remaining D643 criterion matrix.

## 2026-08-20 — the remaining Stage-1 obligations were made executable

- The walkthrough now separates all three accepted populations (19 single-line, 14 learner-branch,
  17 opponent-branch), rewinds across attempts and supplies recorded opponent-policy replies instead
  of excluding the hard class. It reaches the exhaustion predicate on 42/50 packs: 19/19, 9/14 and
  14/17 respectively. The eight driver residues are named and receive no credit.
- The same-tree real-fork measurement covers 44 fork sets / 473 column plies. CR1 reduces 4,029
  candidates to 3,463 entries (86.0%), or 7.32 entries/ply; it still fires on 97.3% of plies and
  lift is only 1.017x. The RFC sets no effectiveness threshold, so this passes the criterion while
  remaining negative product evidence.
- Added executable C1 policy/exploit coverage, the expression-census join, withheld-flag before/after,
  CR3 degeneracies, pivotal timing identity, pointer-rebound withholding, route-level voice/speech
  non-contamination, derived-feature population and corpus-wide earned-label checks.
- The ledger now distinguishes fixes present in this recovery worktree from shipped closures.
  Criterion 20's author conflict and criterion 12's landing-time gate update remain; no Stage-1
  product change is committed.

## 2026-08-20 — the 42/50 reach residue was diagnosed as product state, not harness state

- Re-ran all eight failures with the real shape/plan-signature resolver. The prior missing-signature
  errors disappeared; every pack still failed under every leaf ordering with `Run is terminal at
  node` before full-spine coverage.
- C1 is therefore structurally reachable on 42/50 packs and unreachable on eight holding 38 claims,
  16 of them otherwise admitted, rather than “measured on 42 with eight unknown.” The 84% gate result remains above 10%, but the RFC's
  single/learner-branch/opponent-branch model missed terminal objective lifecycle as a fourth axis.
- D645 records the consequence. No anchor or lifecycle exception was invented in implementation.

## 2026-08-20 — recovery verification is green but is not a completion claim

- `make verify`: 762 tests / 116 files, typecheck, schema, scaffold and packaging green.
- `make test-browser`: 25 passed, one optional Maia-latency test skipped, zero retries reported.
- These gates verify the recovery worktree. They do not resolve D644, disposition D645, perform
  criterion 12's landing-time gate edit or authorise a commit.

## 2026-08-21 — D645 is a foundation repair, not a Stage-2 anchor task

- A fresh-path replay of every authored leaf found exactly eight affected packs, ten blocked leaf
  paths and a maximum six hidden plies. Every blocking transition is `achieved`.
- The repair is routed to `authored-consequence-lifecycle`: keep objective terminality intact,
  migrate intermediate success to `preserved`, remove duplicate tempo verdict declarations, and
  refuse future absorbing transitions at authored nodes that still have children.
- Feedback delivery may consume the corrected reach result, but it does not own or weaken the
  objective lifecycle invariant.

## 2026-08-21 — D645 foundation repair restored complete C1 reach

- The unchanged three-population driver now reaches 50/50 packs: 19/19 single-line, 14/14
  learner-branch and 17/17 opponent-branch.
- Claim delivery is observed after exhaustion on 32/50 packs, up from 28; the remaining withholding
  is admission/reveal behavior, not unreachable authored play.
- D645/D646 are closed by `archive/authored-consequence-lifecycle.md`. D644 and criterion 12 remain
  the independent Stage-1 landing blockers.

## 2026-08-21 — Stage 1 became criterion-complete

- D644's accepted-text conflict was corrected at the boundary the renderer owns. Across all 67
  projected provenance rows, 46 contain prohibited vocabulary copied from attributed authored
  principle fields and zero contain a template-owned prohibited token; an injected template-owned
  `best` is refused. C8 and authored chess text are unchanged.
- Criterion 12 now reports rather than conflates the delivery populations: 98/196 claims are
  evidence-admitted and 98/196 evidence-withheld; after 50/50 full-spine walkthroughs, 69/98
  admitted claims render and 29/98 remain timing-withheld without a released reveal occurrence.
- Criteria 1–20a are now mapped to executable proof or a named out-of-scope defect. Stage 2 remains
  separate content work and the RFC remains accepted/implementing.

## 2026-08-21 — criterion-complete Stage 1 verification

- Feedback delivery and criterion-20 disposable suites: 4/4 tests green; the completion lifecycle
  audit is 5/5 green.
- `make verify`: 766 tests / 117 files plus typecheck, schema, scaffold and packaging green.
- `make test-browser`: 25 passed, one optional Maia-latency probe skipped.
- This closes Stage 1 only. Criteria 21–23, the binding content wave and RFC archival remain Stage 2.

## 2026-08-21 — Stage 1 landed

Commit `a64e6c5` landed the criterion-complete recovery. D526/D527 are closed; D78 remains open on
the measured learner-value result rather than implementation correctness. The unrelated
`rfc/measurement-records.md` and D333 harness stayed outside the checkpoint.
