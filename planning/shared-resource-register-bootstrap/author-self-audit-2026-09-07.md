# Collision-core bootstrap author self-audit

- **Date:** 2026-09-07
- **Role:** Codex author self-audit; this is not the independent review owned by Claude
- **Scope:** the 282-line owner-directed cut at [[D3034]]
- **Verdict:** amended before review on [[D3082]]–[[D3087]]

## What changed

The cut remains a useful seven-resource collision catalogue, but it no longer claims to solve the
absent-source half of [[D2363]]. Catalogue admission requires a real source, so the synthetic-schema
case demonstrates adoption of already-present bytes only. [[D3082]] keeps absent-source admission as
a narrow successor rather than allowing this foundation to report false closure.

The staged-discharge condition is now executable: exactly [[D2454]], [[D2455]] and [[D2466]] join to
owners and the 2026-09-08 due date in the RFC, while work-state supplies their blocked state and
`item:D3034` blocker. This respects the existing algebra, which forbids `since` on blocked items.

Catalogue validation now asks for repository-contained regular files, not “tracked” files that
would require the Git-index reader the cut forbids. The implementation controls also refuse two
schema files carrying the same `$id` slug before tree derivation.

Finally, the five-group author gate now describes its synthetic `Map` honestly: it demonstrates the
data-row shape but cannot prove executable checker behavior before that checker exists. The later
implementation contract owns the real claim/register/tree extension and negative fixtures.

## Boundary and next event

No product bytes or checker implementation are authorized. The amended RFC is 294 lines and the
existing bounded author target remains the only canonical pre-review gate. Claude's fresh independent
review is still required before owner acceptance; it must review the amended bytes, not this receipt.
