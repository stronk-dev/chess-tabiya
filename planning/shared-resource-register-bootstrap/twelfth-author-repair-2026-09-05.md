# Shared-resource register bootstrap — twelfth author repair

- **Date:** 2026-09-05
- **Repairs:** [[D2828]]–[[D2834]]
- **Gate:** `make shared-resource-bootstrap-twelfth-author-repair`
- **Verdict:** author repair complete; another genuinely fresh independent review required

## Repair

Every compiler relation now retains its exact trivia-free child-index path inside the retained
declaration. Equal-looking repeated accesses therefore remain separate edges, and a resolved member
at one site cannot authorize an unresolved `any`/`unknown` site ([[D2828]]).

Global `eval` refusal follows both compiler value authority and the selected call signature, closing
direct, aliased and `.call` invocation while preserving a repository-local same-name method
([[D2829]]). Call-like edges obtain overloads with the exact call/construct signature kind; a
construct-signature interface retains all arms and nested class constructors cannot enter an outer
construction ([[D2830]], [[D2833]]).

The public projector consumes only the exact descriptor object issued by complete catalogue
validation; structural copies and incompatible rows fail ([[D2831]]). A single UTF-8 byte
comparator orders final edges and complete declaration-artifact paths, including case-distinct
valid inputs ([[D2832]], [[D2834]]).

## Evidence and hold

`make shared-resource-bootstrap-twelfth-author-repair` retains the complete predecessor chain,
reproduces all seven attacks against the pinned eleventh model and passes ten repair/self-audit
groups. The added controls prove direct unresolved calls remain closed, repeated legitimate
relations receive distinct sites, graph copies lose authority, and indirect global `eval` is
refused without banning a local same-name method.

This is bounded author evidence only. It changes no production catalogue, checker, register,
schema, provider, product or content byte and authorizes none before another genuinely fresh review.
