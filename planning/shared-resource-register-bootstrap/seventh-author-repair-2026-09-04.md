# Shared-resource register bootstrap — seventh author repair

- **Date:** 2026-09-04
- **Input:** seventh fresh independent return [[D2593]]–[[D2597]]
- **Status:** author-repaired; another genuinely fresh independent review is required
- **Executable contract:** `make shared-resource-bootstrap-seventh-author-repair` — retained sixth
  repair 4/4 plus seventh repair 5/5
- **Boundary:** RFC, disposable author model, maintained Make target and planning/register records;
  no catalogue engine, product authority, schema, API, storage, content, web or protected-design bytes

## Outcome

The generic register engine remains the prerequisite for semantic-convention and provider-protocol
identity. Its five projection-authority returns are now closed in the contract.

1. The TypeScript adapter accepts a descriptor/repository revision, not a caller graph. One pinned
   compiler traversal constructs the graph; a complete ABI/reachability validator rejects malformed
   program, roots, nodes, syntax trees, edges, ordering and crossings before digest construction.
2. One branded selector parser owns path and segment admission. JSON and TypeScript resolvers consume
   that parsed value. The author control parses the ten seed and three follow-on descriptor files and
   resolves every currently adopted selector through the same grammar.
3. Canonical `Object.freeze` calls resolve through the pinned checker to the global intrinsic. Local,
   parameter and import-shadowed value bindings fail.
4. Retained declarations are selected from exact compiler export symbols/declaration objects. All
   overloads remain; a nested same-spelling symbol neither enters nor renumbers the retained graph.
5. Projected identity/semantic/selector values are recursively canonical-copied and frozen before
   the digest is computed from the exact retained semantic reference. Input mutation, output
   mutation, accessors, cycles and non-canonical objects fail their intended arm.

## Remaining gate

This is positive author evidence only. A fresh reviewer must independently attack the complete
compiler graph, selector state machine, intrinsic resolution, symbol reach and deep-seal boundary.
Only after acceptance may the generic engine land; semantic-convention and provider-protocol
catalogue populations remain downstream.
