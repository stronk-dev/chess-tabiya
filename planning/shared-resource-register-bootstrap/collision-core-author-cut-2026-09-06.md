# Shared-resource bootstrap collision-core author cut

- **Date:** 2026-09-06
- **Authority:** owner ruling [[D3034]]
- **Subject:** `rfc/shared-resource-register-bootstrap.md`
- **Gate:** `make shared-resource-bootstrap-collision-core-author-contract`
- **Verdict:** author cut complete; fresh independent review still required

## What changed

The RFC was cut from 1,330 to 282 lines. It now owns one obligation: replace
`RESOURCE_NAMES` and `SCHEMA_SLUGS` with the reviewed seven-row catalogue so resource identity,
claim grammar and the three existing tree-reader configurations are data.

The previous generic TypeScript projection graph, canonical-resource projection, lifecycle engine,
staged/first-parent Git reader and three speculative roots are withdrawn. They are not deferred
inside another section or retained as implementation criteria.

The reviewed seed is
`planning/shared-resource-register-bootstrap/collision-catalogue.v1.json`. It contains exactly
the seven resources already governed at HEAD and resolves only their existing schema, migration and
literal-tuple sources.

## Executed evidence

`make shared-resource-bootstrap-collision-core-author-contract` passes five groups:

1. exact seven-row seed and README-register set equality;
2. equality with both hard-coded inventories the implementation will delete;
3. a synthetic schema becomes known through one row without appearing in checker source;
4. the 300-line and speculative-root scope fences; and
5. every configured current source resolves.

This is author evidence only. It changes no checker, product, schema, migration, vocabulary,
content or register bytes and does not authorize implementation.

## Next

A genuinely fresh review must attack strict catalogue admission, the claimed data-only extension
property, register-set equality and whether any withdrawn engine leaked back into the contract.
Only a passing review plus owner acceptance authorizes the implementation checkpoint.

The pre-cut fifteen-round harness remains historical evidence, not this cut's canonical gate. Its
dependency chain requires the withdrawn generic TypeScript projector and fails against the cut
([[D3060]]). `verify-governance` therefore retains the five-group collision-core contract and a
future fresh review must add a new bounded target rather than reusing the old chain.
