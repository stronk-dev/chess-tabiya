# Held promotion collectors — sixth author repair

- **Date:** 2026-09-04
- **Input:** sixth fresh independent return [[D2603]]–[[D2607]]
- **Scope:** the two held §3.7 promotion projections and disposable author contract only

## Result

The repair closes the five returned buildability seams without changing either held production
projection:

- the maintained target executes every earlier positive author generation before the new suite;
- `createDerivedPawnPromotionRaceTablebaseV1Evidence` is the only registered value factory;
- one module-private constructor and WeakSet-backed assertion seal the complete operation result;
- category, DTZ, precise DTZ, perspective and FEN project exhaustively from the exact recorded/live
  tablebase source; and
- exact legal-map and geometry receipts reject dropped, added, reordered, rebuilt and cross-FEN
  inputs while retaining all four underpromotions and every tied pawn by object identity.

`make semantic-collectors-promotion-sixth-author-repair` passes 19 retained runtime assertions,
four retained strict TypeScript contracts and 5/5 new behavioral controls.

## Honest boundary

This is author evidence, not acceptance or implementation. The original twelve Wave-C projections
and the separately landed deflection repair remain unchanged. A seventh genuinely fresh
independent review must attack the new total-result seal, source projection and specialized input
joins. The held pair also remains dependency-blocked on accepted/implemented provider exchange and
evidence-value authority.

## Next

Fresh review should attempt a copied result, cross two genuine request/invocation/source chains,
invent a category or perspective, alter precise DTZ zero, drop and add one underpromotion, reorder
the legal map, rebuild equal objects, cross FENs and remove any retained Make dependency. Only a
clean review plus the named dependencies authorizes production implementation.
