# Shared candidate packet — second repeat author handoff

Repair `rfc/shared-candidate-evidence-packet.md` on [[D1958]]–[[D1961]] without reopening the
neutral packet, complete legal denominator, no-F1-aggregate ruling or one-root bot table.

Required work:

1. Stop describing `semantic-evidence-check.ts` as production consumption. Bind the first landing
   to a real accepted Support/Review/bot operation, or state plainly that the lower primitive lands
   unused and obtain the necessary process ruling.
2. Publish a literal runtime receipt constructor/projector/assertion contract. A `unique symbol`
   interface brand is not a process seal. Cross a forged receipt, equal rebuild, removed reference
   and wide→narrow projection.
3. Make cancellation executable. Specify worker or cooperative-yield topology, yield granularity
   and maximum cancellation latency; cross cancellation after compilation begins.
4. Narrow move convention and compiler version to their exported literal types. Derive typed
   abstention reasons from the declared projection vocabulary and reject an undeclared pair.
5. Extend `make candidate-packet-contract` with the repaired author candidate. Keep
   `make candidate-packet-repeat-review` as the crossed return control until independent review.

Do not implement production code, add a packet F1 projection, invent a bot profile, add N child
engine searches, edit protected design intent or touch the concurrent D872/review files.
