# Semantic collectors promotion — fourteenth author repair

**Date:** 2026-09-06

## Outcome

The bounded contract repair closes [[D2892]]–[[D2896]] without implementing either held
projection. Application opening consumes one runtime-sealed installed-configuration authority,
not a path. The product model imports neither the path-taking test fixture nor a predecessor
promotion repair. Every listed generation and every referenced artifact validates before an
installed registry can be published.

Registry and generation-store construction each install their promise authority before the first
await. Concurrent callers therefore join one current lineage. A rejected promise evicts only
itself, allowing a repaired installation to retry without leaving a failed object current.

## Executable controls

`make semantic-collectors-promotion-fourteenth-author-repair` retains every predecessor return and
repair, then passes five current groups plus strict TypeScript:

1. caller strings and structurally copied configuration values fail runtime authority;
2. the product model has no test-fixture or predecessor promotion import;
3. one invalid member prevents publication of an otherwise valid multi-generation registry;
4. concurrent registry opens return the same object and failed construction can retry; and
5. concurrent first store opens return the same object and rejected construction is evicted.

## Boundary

This is author evidence, not acceptance or product implementation. The evidence spine remains
12/14. A fifteenth genuinely fresh independent review plus the existing provider-exchange and
evidence-value-authority dependencies still gate the held promotion pair.
