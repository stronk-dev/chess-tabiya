# Shared candidate packet — thirteenth author repair

- Date: 2026-09-06
- Scope: [[D2934]]–[[D2941]]
- Verdict: repaired at requirements tier; another genuinely fresh independent review is required
- Executable receipt: `make candidate-packet-thirteenth-author-repair`
- Production effect: none; no packet, cache, selector, bot, hint, Review, schema or content byte is
  authorized

## Repaired authority

- Collector dependency images derive from the planner's exported declaration, contain only exact
  predecessors and throw on every undeclared read.
- Collector invocation failures name collectors. Only validation of one returned value may name
  that value's projection.
- Joinable job identity is removed on last-waiter cancellation while a separate running set retains
  the concurrency slot until settlement; a later equal request receives a fresh generation.
- Public failures distinguish unsupported ruleset, invalid FEN, invalid scope and invalid closed
  request shape.
- Cache-entry measurement starts from packet, legal evidence and candidate-input roots. The shared
  primary manifest is measured once at service scope.
- Reader assertion revalidates declared legal evidence, exact flattened move members, unique UCI
  population, row references and recomputed child FENs.
- Child-reading ids derive from the compiled manifest intersected with the admitted tactical and
  breadth catalogues. The real child collector must be set-equal to that independent population.
- The author model extends `tsconfig.base.json`; optional timer removal uses `delete`, satisfying
  `exactOptionalPropertyTypes`.

## Evidence and next step

The maintained target retains every predecessor return and repair, reproduces all eight thirteenth
review counterexamples, then passes 22 current behavior groups and the repository TypeScript
contract. Another genuinely fresh reviewer must attack dependency access, cancellation races,
error identity, graph accounting, private receipt joins and catalogue drift before acceptance.
