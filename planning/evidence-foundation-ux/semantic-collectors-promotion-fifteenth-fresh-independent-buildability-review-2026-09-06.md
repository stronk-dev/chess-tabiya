# Semantic collectors promotion — fifteenth fresh independent buildability review

**Date:** 2026-09-06

## Verdict

Return the held promotion pair on [[D2929]]–[[D2933]]. The fourteenth repair fixes eager
generation validation and single-flight construction, but does not preserve a buildable current
collector. Its authority can be forged, its production positive path is absent, semantic documents
are not validated, and the replacement deletes the operations that would consume the store. The
evidence spine remains 12/14; no held projection is authorized.

## Findings

- [[D2929]]: `INSTALLED_CONFIGURATIONS` is exported mutable state. Any same-package importer can
  insert an arbitrary object/snapshot pair and obtain the exact authority accepted by
  `openPromotionApplication`.
- [[D2930]]: neither the product model nor its state module issues an installed configuration. The
  only constructor is explicitly test-only, so removing the raw-path issuer made the production
  positive path unreachable.
- [[D2931]]: “complete validation” checks canonical JSON, declared hashes and a narrow support
  join. It publishes over a pack rejected by `validatePackDocument` and a response rejected by
  `parseTablebasePosition`; source and legal-map payloads are likewise never semantically parsed.
- [[D2932]]: the replacement `PromotionArtifactStore` exposes only application, registry,
  generation id and manifest declarations. The current recorded lookup and exact legal-status
  operations from the previous repair are gone.
- [[D2933]]: the replacement exports no current request constructor, collector, result assertion or
  recorded-authority receipt. Retaining the earlier target as a Make prerequisite proves the
  historical models still pass; it does not compose their authority into this replacement.

## Executable evidence

`make semantic-collectors-promotion-fifteenth-fresh-review` retains every predecessor return and
repair, then passes 5/5 fresh counterexamples plus strict TypeScript. The target is enrolled in
`verify-governance`.

## Required repair

One production composition closure must own configuration loading and sealed application issuance;
no exported mutable registry may mint or replace it. Registry publication must run and retain the
complete production pack, source, evidence, provider-response and legal-map parsers. The resulting
current store must restore exact recorded/legal operations and feed one sealed
application→registry→store→request→collector→result→receipt lineage, with no predecessor or public
constructor bypass. Another genuinely fresh review and the existing provider/value dependencies
still gate the held pair.
