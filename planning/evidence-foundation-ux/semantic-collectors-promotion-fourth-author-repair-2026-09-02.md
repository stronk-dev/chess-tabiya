# Semantic collectors promotion pair — fourth author repair

- **Date:** 2026-09-02
- **Input:** fourth fresh independent return [[D2521]]–[[D2523]]
- **Status:** author-repaired; fifth fresh independent review required
- **Boundary:** RFC and disposable author contract only

## Result

The repair closes the held pair's request and authority boundaries without implementing either
projection or changing the twelve semantic collectors already in production.

- `CanonicalFullFen` is constructed only by a strict complete-six-field parser using the shipped
  chess parser and canonical renderer. Recorded lookup has a total found/absent/failed algebra;
  lookup failure never becomes absence or permission for live fallback.
- The live arm uses `ProviderExchangeScheduler.get` and the operation-keyed
  `ProviderSourceFactories["syzygy.position@1"].make` path. One constructor fixes the complete
  standard-chess request bytes and caps timeout at the smaller of the caller budget and 500 ms.
- The geometry constructor seals both its derivation receipt and the aggregate completed result.
  `collectPromotionRaceTablebase` asserts that aggregate before reading the output discriminator,
  so a forged, cloned, JSON-round-tripped or spliced no-race value cannot suppress source work.

## Executable evidence

`make semantic-collectors-promotion-fourth-author-repair` passes 3/3 plus strict TypeScript. Its
runtime model accepts the constructor-owned completion and rejects a spread clone that retains the
otherwise-valid derivation receipt, proving the aggregate seal rather than merely restating it.

The retained `make semantic-collectors-promotion-third-author-repair` contract remains 4/4 plus
strict TypeScript. This is author evidence only: a genuinely fresh fifth independent review must
attack canonicalization, failure/fallback precedence, exact provider bytes and aggregate authority
before acceptance or implementation.
