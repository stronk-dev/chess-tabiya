# Review evidence compiler — fourth fresh independent buildability review

- **Date:** 2026-09-07
- **Reviewed image:** third author repair for [[D2685]]–[[D2692]]
- **Gate:** `make review-evidence-fourth-fresh-review`
- **Verdict:** **RETURN TO AUTHOR** on [[D3109]]–[[D3115]]; no production Review compiler is
  authorised

## What survived

The third repair's local mechanics survive: copied objects are deeply frozen; missing, duplicate
and foreign slot identities fail; family folds account for every supplied node; concurrent callers
share one terminal promise; and the component/wire/parser sample rejects several crossed shapes.
[[D3108]] separately repairs the obsolete assertion that this draft model belongs in release
governance: it is correctly retained by opt-in `verify-rfc-evidence` after [[D3076]].

## Returned seams

| row | executable result | required repair |
|---|---|---|
| [[D3109]] | Prefix issuance validates and spreads caller-supplied run, branch, event, path, side and outcome fields; it invokes no recorded-path/run/import authority. | Accept the literal `ReviewPrefixAuthorizationInput` and derive every receipt field from the exact recorded authorities. |
| [[D3110]] | The complete source registry is nine generated `review.${family}@1` placeholders with no projection, operation, input, parser or absence declaration. | Execute the literal projection→adapter registry and prove equality with Review-eligible manifest inputs. |
| [[D3111]] | An available result is any nonempty digest. Compilation replaces it with `itemCount: 1` and retains neither digest nor evidence. | Parse exact projection-typed sealed evidence and retain each item in its node packet. |
| [[D3112]] | Compiled nodes contain only `nodeId` and family states. | Construct the RFC's complete node packet: ply, position key, incoming move, items, links and families. |
| [[D3113]] | Completion is called as a discarded expression; Story omits subject, manifest, progress, degradation, family, title and rank fields. | Retain the orthogonal completion result and execute the literal closed Story receipt. |
| [[D3114]] | The private canonicalizer maps `NaN` and `null` to identical bytes and accepts values refused by the shared authority. | Parse exact semantic images and use the shared fail-closed canonical serializer for every identity. |
| [[D3115]] | Attempt settlement accepts arbitrary objects and has no retry/cancel/success-release lifecycle. | Execute the closed outcome union and all bounded lifecycle transitions stated in §4.1. |

## Why this blocks the Noctie-style return loop

The issue is not missing Review copy. The current executable packet contains no evidence item to
select, explain, cite, link, or compile into a source-bound return exercise. A count-only packet can
make every coverage check green while the learner still receives opaque strings or an invented
exercise. Review and mistake-derived decks therefore remain contract-blocked at this exact seam.

## Reproduction and boundary

The gate retains 6 original controls, 5 second-author controls, 6 third-author controls and passes
7 fresh falsifiers. No product, API, schema, client, content or protected-design bytes changed. A
bounded fourth author repair and another genuinely fresh review must precede acceptance; all named
recorded-path, provider, value, presentation and module dependencies remain in force.
