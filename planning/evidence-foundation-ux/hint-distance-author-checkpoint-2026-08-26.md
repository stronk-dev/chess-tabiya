# Guided Hint author buildability checkpoint

**Reviewed:** 2026-08-26
**Reviewer:** codex (author checkpoint; this does not discharge independent D1)
**Document:** `rfc/hint-distance.md` after the D1397/D1582/D1629 rebuild

## Verdict

Returned before independent review. The measured relation/sign selector, five physically redacted
rungs, one ordinary Hint/A-little-more action, source-separated modules, deterministic fallback and
closed server-to-browser receipt remain the right product shape. Six seams are not buildable yet.

## Findings

1. **D1638 — provider identity is conflated.** External voice failure must preserve an available
   deterministic hint. Search-source failure must return an honest unavailable state while other
   modules continue. `provider_off` currently names both outcomes.
2. **D1639 — the product ceiling table is absent.** Five preset defaults, eight workflow clamps,
   timed-context narrowing and role/access behavior are owner values. Current binary module
   membership and the generic `AssistancePermission` union cannot encode them.
3. **D1640 — the horizon is forgeable.** The public disclosure constructor receives reference
   strings rather than the admitted PV, packet and occurrence authorities whose digests it claims
   to recheck. The operator object needs its own constructor membership seal.
4. **D1641 — the F1 declaration graph is unspecified and presently widening.** Search evidence is
   measured, semantic inputs mix grounding, and some source answer images do not contain
   `pattern`. Literal declaration tuples must compile under the shipped derivation rules.
5. **D1642 — `ModuleAnswerCeiling = "move"` is not a type member.** The disclosure vocabulary and
   the outer module-answer vocabulary need an explicit total mapping.
6. **D1643 — pending/reset identity is not a protocol.** Neither `decisionId` nor
   `committedMoveCount` exists; no poll/retry/cancel/stale-result join connects a queued evidence job
   to a delivery receipt.

The duplicate acceptance-criterion number 17 is editorial residue to correct in the same author
amendment, not a seventh product blocker.

## Required author order

1. Obtain the D1639 owner table; amend `intent-presets` and this document together.
2. Consume the amended/accepted shared candidate packet and define the sealed horizon constructor.
3. Publish and compile the literal F1 horizon/disclosure declarations and module-answer mapping.
4. Split source availability from optional voice provenance.
5. Specify one decision identity and idempotent enqueue/poll/stale-result state machine over existing
   run/evidence bytes, or explicitly claim the required persistent lane.
6. Re-run independent D1 review. Implementation remains forbidden before that acceptance.

No claim is made that these defects invalidate D1397's fixed-population counts. They prevent those
counts from becoming a trustworthy, configurable product path.
