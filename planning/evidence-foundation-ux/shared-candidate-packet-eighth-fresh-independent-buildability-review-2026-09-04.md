# Shared candidate packet — eighth fresh independent buildability review

- **Date:** 2026-09-04
- **Reviewed:** seventh author repair for [[D2625]]–[[D2627]]
- **Gate:** `make candidate-packet-eighth-fresh-review` — retained 3/3 + 4/4, new 6/6
- **Verdict:** **RETURNED on [[D2655]], [[D2656]], [[D2657]], [[D2658]], [[D2659]], [[D2660]]**

## What survives

The direction is still correct: the packet should consume one predecessor-owned exact legal-map
factory; request scope must choose work/retention rather than chess truth; and entry count plus the
old visible `events + 5×readings` coefficient cannot bound the retained graph. The historical sixth
and seventh author controls remain green.

## Return

The new author model does not yet exercise the operation it claims. Factory ownership is checked by
regexing sentences in the RFC; the model imports no predecessor factory, legal authority or source
graph. Scope equivalence runs one toy collector over an always-empty memo and aliases its output;
there is no dependency plan, hidden result, candidate row, registered collector or service path.

The graph measurement is incomplete in three independently executable ways. It flattens selected
fields from `candidateInputs`, so it omits the private wrapper objects and array containers retained
by the `WeakMap`; adding a 100 KB strong reference there changes neither total. It walks enumerable
string keys only, silently ignoring non-enumerable accessors and symbol-keyed values even though the
RFC promises those shapes fail. Its closed-category check builds both sides from
`RETAINED_CATEGORIES`, so a new retained root remains uncategorized and unmeasured while the check
stays green.

Finally, `measureRetainedGraph` has no cache consumer. The author model contains neither retained
limits, oversize admission nor eviction; the test only verifies that the RFC contains those words.
The prior cache and the new measurement can therefore pass independently while production has no
bounded composition.

## Required repair

Use the actual predecessor author factory/source graph; execute all three dependency-closed plans
with exact per-collector memo projections; measure from the private reference aggregate itself while
separating the process-wide manifest singleton; reject unsupported own property descriptors/keys;
derive the category/root image from one typed descriptor; and connect the resulting receipt to one
cache operation with independent entry/byte/object oversize and eviction controls. Then run another
genuinely fresh review. No production packet or consumer is authorized by this return.
