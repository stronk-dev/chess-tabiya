# Shared candidate packet — eighth author repair

- **Date:** 2026-09-04
- **Input:** eighth fresh independent return [[D2655]]–[[D2660]]
- **Status:** author-repaired; another genuinely fresh independent review is required
- **Executable contract:** `make candidate-packet-eighth-author-repair` — retained 3/3 + 4/4 +
  6/6 controls plus seven composed repair arms
- **Boundary:** RFC, disposable author model, planning/register records and maintained Make target;
  no production runtime, cache, server, API, schema, content or UX implementation

## Outcome

The repair replaces the six text-shaped checks with one executable composition.

### The predecessor remains the sole legal-value owner

The model imports the predecessor author surface instead of reconstructing it. The operation seals
an instrumented exact-map authority through that factory, calls it once per packet and retains the
same map and move object references. There is no exported request-time factory injection and no
local `compileLegalPopulation` definition.

### Scope executes a real graph

The literal registry contains ten event and three reading collectors. The event plan executes ten;
the reading plan executes five, retaining three and privately preserving the transition/tactical
dependencies; the wide plan executes thirteen. Every invocation records exactly its declared memo
keys. Separate direct and wide compilations agree on value, abstention and outcome tuples, while an
in-process wide projection keeps exact retained and execution-outcome references.

### The measured graph is the cached graph

One descriptor names the retained top-level roots and derives their semantic categories. The walker
starts from the private aggregate itself, so arrays and private candidate wrappers count. Complete
own-property inspection rejects non-enumerable, symbolic, accessor, function, bigint, non-finite and
non-plain values; a new top-level root fails until declared. Cache admission invokes that walker and
independently enforces entry, logical-byte and object-count limits, with explicit LRU eviction and
`oversize_not_cached` outcomes.

## Remaining gate

This is positive contract evidence, not implementation authorization. A new reviewer must attack
the composed operation rather than re-reading the RFC claims. Production implementation also waits
for the accepted and landed evidence-value-authority predecessor.
