# Semantic collectors promotion — fifteenth author repair

- Date: 2026-09-06
- Scope: [[D2929]]–[[D2933]]
- Verdict: repaired at requirements tier; another genuinely fresh independent review is required
- Executable receipt: `make semantic-collectors-promotion-fifteenth-author-repair`
- Production effect: none; the held promotion projections remain unauthorized and the evidence
  spine remains 12/14

## Repaired authority

- `createPromotionApplicationComposition` is the sole current configuration owner. It captures the
  configured installation, canonical-reads it and memoizes one application. No exported mutable
  authority map or test-only positive path remains; copied composition/application values fail.
- Registry publication validates every installed generation before exposing any generation id. It
  runs the shipped pack, source-manifest, evidence-ledger and Syzygy parsers, linkage and evidence
  semantic/support checks, and recomputes the exact legal map.
- Parsed pack, manifest, ledger, provider response and exact legal evidence remain in the retained
  generation/store graph; matching digests cannot authorize semantically invalid bytes.
- Each store restores typed recorded and legal-status operations over the retained values. Copied
  stores and cross-FEN reads cannot inherit authority.
- Each request is sealed against one exact store. Collection, derived reading, result assertion and
  recorded receipt reassert application, registry, generation, store and FEN identity.

## Evidence and next step

The maintained target retains every predecessor return and repair, reproduces all five fifteenth
review counterexamples, then passes 5/5 direct repair groups plus the repository TypeScript
contract. Another genuinely fresh reviewer must attack configuration/composition reachability,
semantic parser completeness, retained typed-value identity, store crossings and the complete
request/result/receipt lineage before either held projection can be accepted. Provider exchange and
central value-authority dependencies remain separate mandatory landing gates.
