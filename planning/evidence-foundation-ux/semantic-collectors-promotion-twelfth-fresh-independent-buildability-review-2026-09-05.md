# Semantic collectors promotion — twelfth fresh independent buildability review

**Date:** 2026-09-05

## Verdict

Return the held promotion pair on [[D2835]]–[[D2839]]. The eleventh author repair correctly
closes its six immediate predecessors, but its new installation/generation boundary is not yet a
closed authority. The production evidence spine remains 12/14 and no held projection is authorized.

## Executable findings

1. `openPromotionInstallationRegistry(inventoryPath)` accepts any caller-created canonical
   inventory at any real path. Two unrelated temporary roots independently mint trusted registries;
   the self-registration seam moved from generation directory to inventory file.
2. The generation root and response/legal rows do not have exact-key parsers. Undeclared root and
   nested fields survive parsing, freezing and admission.
3. Legal-map declarations are looked up with `find`. A contradictory duplicate and an orphan row
   pointing at a nonexistent file are ignored while the generation opens successfully.
4. The support-value loop has no non-vacuity condition. A tablebase record with `supports: []`
   passes strict sourcing and grounds a recorded promotion reading despite supporting no pack field.
5. The durable ledger subject is selected with `find`. Two records with the same
   FEN/source/retrieval identity and conflicting categories are admitted; array order chooses the
   record retained in the authority receipt.

`make semantic-collectors-promotion-twelfth-fresh-review` retains every predecessor target, passes
all 5/5 new behavioral falsifiers and strict TypeScript. The maintained target is enrolled in
ordinary governance verification.

## Required repair

The next author pass must consume one application-owned installed-inventory authority rather than
a product-local caller path; parse every generation/declaration object as a closed grammar; require
set equality and uniqueness across response/legal populations; require the exact singleton
`/start/fen` support with byte-equal value; and require exactly one durable record for each response
subject. It must preserve the complete predecessor chain and then undergo another genuinely fresh
review. Provider/value dependencies remain independent landing gates.
