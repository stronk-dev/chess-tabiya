# Held promotion collectors — seventh author repair

- **Date:** 2026-09-04
- **Repairs:** [[D2650]]–[[D2654]]
- **Gate:** `make semantic-collectors-promotion-seventh-author-repair`
- **Scope:** disposable authoring model only; no production collector is authorized

## Repair

The total result no longer accepts arbitrary `object` values. Its outside-domain and failure arms
retain exact sealed provider results/invocations; its no-evidence arm accepts only the exact sealed
geometry completion already carried by the RFC request. Parsed provider values are recursively
copied/frozen before they enter a result, and every public aggregate has a factory-specific runtime
assertion.

Legal moves and pawn contacts now come from the shipped FEN-derived central authorities rather than
caller arrays. Geometry derives only from the specialized contacts item. Same-FEN generic declared
items with dropped moves or invented ordering fail the specialized assertions; all four promotion
roles survive the valid exact legal map.

Recorded and live tablebase positions cross the shipped `parseTablebasePosition` parser. The model
imports the production `TABLEBASE_CATEGORIES` domain and exercises all ten members, including
Syzygy/maybe/cursed/blessed categories; invented categories and non-finite DTZ fail before source
projection.

The executable request now carries the exact RFC fields: sealed geometry result, provider scope and
abort signal. The typed Syzygy request retains rules/variant/FEN/timeout, and the invocation retains
the complete distributive provider result and exact delivery rather than a three-value label.
Cross-FEN delivery substitution fails.

Finally, the sole output is an actual `DeclaredEvidence<PromotionRaceTablebaseValue>` with literal
`derived.pawn@1 / derived.pawn.promotion_race_tablebase@1` identity. Its one value-authority receipt
names `createDerivedPawnPromotionRaceTablebaseV1Evidence` and retains the exact request, geometry,
legal-map, source and output objects; the derivation assertion crosses all of them.

## Evidence and hold

The maintained target runs the five earlier author generations and the seventh fresh falsifier,
then passes 5/5 new behavioral groups and strict TypeScript. This is author evidence, not a fresh
review. Both promotion projections remain held at 12/14 until another genuinely fresh independent
review passes and the provider/value dependencies land.
