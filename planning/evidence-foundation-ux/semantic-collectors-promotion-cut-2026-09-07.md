# Semantic collectors promotion cut

- Date: 2026-09-07
- Finding: [[D3069]]
- Scope: the two held promotion-race projections and their sixteen-round shadow-model chain

## Measured boundary

The production evidence manifest already compiles the twelve non-promotion Wave-C projections.
The two absent ids are `derived.pawn.promotion_race_geometry@1` and
`derived.pawn.promotion_race_tablebase@1`.

Exactly 31 `tools/` directories model or review those two absent ids. Exactly 64 work-state rows
were blocked on `rfc/semantic-collectors.md`; every one is a finding against those disposable
models. None names a downstream learner surface or a production consumer waiting on the twelve
compiled ids.

The real product obligation was already recorded before the review chain:

- [[D1699]] requires one exact contacts-derived geometry and same-position tablebase source join.
- [[D1700]] requires projection-effective availability and latency for the mixed local/provider
  producer.

Both rows are already blocked on `provider-exchange-and-execution.md`; the value-authority half is
also explicit in the RFC's transferred D6. Those shared foundations must land before another
promotion contract can be buildable.

## Cut

1. `semantic-collectors.md` owns twelve projections and moves to `awaiting D1/D4`; its production
   bytes do not change.
2. §3.7 and the old return chronicle remain historical quarry, not normative acceptance authority.
3. C16/C18 and promotion clauses in older criteria are withdrawn to [[D1699]]/[[D1700]].
4. The 31 old harness directories remain runnable historical evidence, but their dependency chain
   leaves canonical governance.
5. The 64 shadow-model rows are refused as obsolete-model findings. Their chess and authority
   lessons remain searchable in the ledger; refusal does not assert that the future promotion
   product capability exists.
6. [[D925]] closes on the twelve-id implementation actually shipped. [[D926]] remains open content
   debt, and D1/D4 continue to gate archival.

No seventeenth author model is commissioned. A successor may be drafted only after the shared
provider and value authorities expose the exact operations [[D1699]]/[[D1700]] require.
