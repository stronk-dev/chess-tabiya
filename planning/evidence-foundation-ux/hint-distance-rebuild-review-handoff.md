# Hint-distance rebuild: independent review handoff

**Subject:** `rfc/hint-distance.md`, rebuilt 2026-08-26.

**Disposition entering review:** draft; research gate passed for the selector; not authorised for
implementation.

**Required outcome:** independently re-derive the contract at HEAD, amend any failed claim, then
either accept it on buildability or return it with concrete blockers. Do not accept from this
handoff's summary.

## Reproduce before reading the proposed implementation shape

1. Re-run the D1397 relation-safe harness and derive the seven-family set, source role, admitted
   sign/status, relation counts and strict-direct/strict-horizon reach from the frozen JSON. Refuse
   any RFC number whose subject or denominator differs.
2. Verify every source projection and every required operand at the live catalogue/compiler symbol.
   In particular, readings/predicates must have an exact evaluated edge rather than being called
   events, and later-root occurrences must be root-side ply 3.
3. Verify the candidate-packet dependency is compatible with the exact packet/search/occurrence
   identity and does not invent cross-process packet serialization.

## Priority falsifiers

- **Selection meaning:** can any opponent action, bad-sign occurrence, merely preserved risk or
  non-persistent promotion enter the accepted family table? Can a later-line observation be worded
  as causality or recommendation?
- **Byte disclosure:** can a pattern/square/piece/distance object contain a higher-rung square,
  actor, ply, relation, UCI or SAN through optional fields, a generic record, object spread, JSON
  round-trip, double assertion, sibling rendered item or raw-PV module binding?
- **Projection truth:** is there exactly one internal horizon projection per admitted family and
  one separate learner projection per family×rung? Does each disclosure declare its literal
  source horizon and exact answer-content image? Can `module.guided_hint` accept anything else?
- **Process boundary ([[D1582]]):** does the F1-admitted view stay entirely server-local? Can the
  REST body or browser claim to possess/reconstruct a symbol/private-WeakSet seal? Is the delivery
  receipt a closed rung-discriminated wire type whose digest and family×rung projection are checked,
  without misrepresenting an unkeyed digest as authentication?
- **Provider boundary:** can the provider see the horizon, PV, packet, siblings or higher-rung
  bytes? Does timeout/refusal preserve a deterministic complete response? Can provider wording add
  recommendation, causality, judgement, move or square tokens not licensed by the one item?
- **Operation closure:** trace browser seat → store → client → REST → service → injected packet
  service → selector → disclosure compiler → F1 admission/render/voice check → delivery receipt →
  same seat. An anchor census is insufficient; each operation must consume the compiled type.
- **Policy/availability:** prove `policy_refused`, `honest_empty`, `provider_off` and `pending` remain
  distinct; engine abstention must not suppress independent theory/structure modules.
- **Rated boundary:** prove direct enqueue, hint, analysis and cached-result paths refuse while an
  open rated decision exists, before job creation or disclosure.
- **Shared resource:** reconcile `AssistanceConfig` v5 with `intent-presets`; [[D1581]] remains a
  process blocker until the required register/check exists.
- **Latency:** keep integrated latency red until the shared packet exists and the exact production
  path is measured on Node 24. Do not substitute the research harness's cold compilation time.

## Review closeout

Record every failed statement as a ledger row before amendment. The review must state which
acceptance criteria it actually executed, which remain red by dependency, and whether the RFC is
accepted for implementation. Update `rfc/README.md`, the review-return route and the exploration
log in the same commit as the decision.
