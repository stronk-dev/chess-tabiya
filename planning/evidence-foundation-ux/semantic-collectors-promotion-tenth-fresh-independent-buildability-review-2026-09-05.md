# Semantic collectors promotion — tenth fresh independent buildability review

**Date:** 2026-09-05
**Verdict:** returned on [[D2765]]–[[D2770]]; the held promotion pair remains 12/14 and no
production implementation is authorized.

## What was tested

`make semantic-collectors-promotion-tenth-fresh-review` retains the complete predecessor chain and
runs six fresh executable falsifiers:

1. the repair rejects the real `tabiya.sourcing.manifest.v1`/`entries[]` dialect while production
   rejects its invented `tabiya.source-manifest.v1`/`sources[]` dialect;
2. JSON-round-tripped caller objects mint the claimed durable sourcing authority and exact recorded
   evidence;
3. future source/retrieval times and an unresolved `/invented/claim` support pointer validate;
4. a cloned, unsealed request returns a trusted outside-domain result before request assertion;
5. callers manufacture trusted storage failure and legal unavailability without either operation;
6. a caller self-registers `attacker` as an exact Syzygy source beside its own record.

All six reproduce against the ninth author repair. No production collector, provider, storage,
schema, content, route or UI byte changed.

## Required repair boundary

The next bounded author repair must consume the production manifest schema/parser and a canonical
durable storage read rather than caller JSON. Recorded authority must retain the installed source
entry and origin, an observed current clock, the exact pack/document subject, and resolved support
pointers. The request must be asserted before domain and every other early branch. Storage failure,
legal unavailability and input abstention must be constructed only by the dependencies whose real
operations observed those outcomes. A same-file registry declaration is not independent source
authority.

The repair remains contract-tier evidence. Another genuinely fresh review plus the provider/value
dependencies still precede either held production projection.
