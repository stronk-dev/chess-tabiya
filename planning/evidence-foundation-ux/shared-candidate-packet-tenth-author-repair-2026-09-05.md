# Shared candidate packet — tenth author repair

- **Date:** 2026-09-05
- **Repairs:** [[D2678]]–[[D2684]], plus production-contact findings [[D2841]]–[[D2842]]
- **Gate:** `make candidate-packet-tenth-author-repair`
- **Verdict:** positive author evidence; fresh independent review and the draft
  `evidence-value-authority` factory still precede acceptance

## What changed

One TypeScript operation now composes the returned boundary against production evidence code:

- an exact plain-object parser admits only `beforeFen`, `ruleset` and `scope` before work starts;
- the compiler derives the seven-term packet digest from canonical FEN, legal convention,
  move-identity convention, primary-manifest digest, compiler version, standard ruleset and scope;
- direct narrow and wide-to-narrow receipts retain the same target plan's complete dependency graph;
- a module-private receipt seal binds packet, references and digest, while cache admission derives
  its key and retains aggregate entry/logical-byte/object bounds;
- recursive sealing descends through already-frozen ancestors;
- all thirteen collector names invoke the current runtime semantic functions and F1 declaration
  adapters over the complete exact legal population.

## Findings exposed by the repair

The real binding found two seams that a shape-equivalent registry could not reveal. Production
`DeclaredEvidence` and `SemanticEvidenceEvent` values each carry a private symbol brand, so the old
blanket symbol refusal made real evidence unrepresentable ([[D2841]]). The walker now admits exactly
one enumerable `true` brand slot only when the corresponding production assertion accepts the
object; arbitrary/additional symbols, hidden data and accessors still fail.

The predecessor author model's legal evidence was only a plain three-field object and fails the
production declared-evidence assertion ([[D2842]]). The repaired operation uses the real legal map
and registered declaration adapter and asserts the result. It does not falsely claim the future
one-call factory is landed: that remains a named dependency of this RFC.

## Executed result

`make candidate-packet-tenth-author-repair` retained every sixth-through-ninth predecessor gate,
then passed 9/9 new controls and strict TypeScript. The new controls cover closed input, exact
identity terms, graph equivalence, private cache authority, deep sealing, thirteen real adapter
paths, production brands, production legal evidence and inherited aggregate cache bounds.

No runtime packet, cache, route, hint, Review surface, bot policy or content byte changed. A fresh
review must attack this composed operation before the RFC can be accepted.
