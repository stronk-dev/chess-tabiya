# D1726/D1727 legacy-reading successor closure — author handoff

**Inputs:** `design/research/legacy-reading-successor-closure.md` and
`tools/d1726-evidence-successor-harness/`.

**Purpose:** stop ordinary modules from rendering the legacy census while richer exact evidence is
unbound or confined to Advanced.

## Measured facts the author must preserve

- 17 emitted legacy structural readings are live in inspector + square sight; 14 legacy transition
  leaves are live in the move inspector.
- Eight distinct richer structural projections named by the closure have zero compiled consumers.
- Every legacy transition leaf has an identity-rich successor; every successor is research-only.
- Raw structural widths are 63.89 mean / 80 median / 102 max authored and 80.80 / 84 / 106
  imported. Five inventory families contribute 85.12% / 87.90% of all facts.
- `module-registration` gives ordinary sight all legacy structural readings while exact mobility,
  king state and material-role identity are full-inspector-only.
- `named_structure@1` accepts prose-only and full id/name/prose payloads under one id.

## Required author actions

1. Amend F1/evidence-contract ownership for D1727: require a sealed pattern id, name and
   provenance. Both guidance and inspector/sight derive from it.
2. Amend collector ownership for identity-rich isolated/doubled successor projections over
   `pawn_connectivity`; route D1723/D1717/D1724 without duplicating sources.
3. Amend `module-registration`'s literal accepts table:
   - ordinary sight uses question-bound exact state sources (square control, legal/local-safe
     mobility, pawn contacts/connectivity, king state, ray/duty identities and material role as
     applicable);
   - legacy counts remain only where a named reducer/renderer proves a unique job;
   - Advanced may expose complete topology and compatibility inventory;
   - no module accepts coarse and rich forms as competing authorities.
4. Depend activation on D1710's production packet/path operation and D1711 validation.
5. Keep predicates and authored pack expressions stable until their own versioned migration.

## Able-to-fail acceptance fixtures

1. Structural mapping remains set-equal to all 18 kinds and transition mapping to all 14 leaves.
2. A learner module accepting `piece_reach_count` while exact mobility is Advanced-only fails.
3. The same inversion fails for `king_zone`/`zone_state` and `piece_count`/`role_signature`.
4. Named-structure evidence without id/name fails even with valid prose.
5. Activating a research/helper-only event fails.
6. Ordinary sight emits no unbounded census; selection is pointer/question-bound and budgeted.
7. Removing a legacy reading from ordinary UX leaves predicate truth and content digests unchanged.
8. The generated receipt joins source, projection, consumer, operation and module.

## Explicit non-goals

- no new strategic judgement or LLM-authored chess truth;
- no bulk content rewrite;
- no duplicate square, mobility, pawn, king, ray or transition detector;
- no default raw evidence dump under a renamed panel;
- no claim that a literal shade/file/count fact is itself a plan or recommendation.
