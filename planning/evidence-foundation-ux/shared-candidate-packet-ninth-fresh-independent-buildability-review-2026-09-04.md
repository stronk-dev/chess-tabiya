# Shared candidate packet — ninth fresh independent buildability review

- **Date:** 2026-09-04
- **Reviewed:** eighth author repair for [[D2655]]–[[D2660]]
- **Gate:** `make candidate-packet-ninth-fresh-review` — retained complete eighth author chain,
  new 7/7 falsifiers
- **Verdict:** **RETURNED on [[D2678]], [[D2679]], [[D2680]], [[D2681]], [[D2682]],
  [[D2683]], [[D2684]]**

## What survives

The six bounded repairs work on the graph they model. The predecessor author factory is called once;
all three plan shapes execute; the walker reaches wrapper and container objects and rejects hidden
property shapes; one descriptor closes the measured roots; and entry, logical-byte and object
bounds influence cache admission independently. Those are retained controls, not discarded work.

## Return

The composition still has multiple authorities at its public seams. The compiler accepts
caller-supplied `afterFen`, manifest and legal-move fields instead of refusing everything outside
the three-field request. Its packet id is the raw `${beforeFen}:${scope}` string, omitting manifest,
legal convention, move convention, compiler version and ruleset from the RFC's required factual
identity.

Scope invariance is also only visible-output invariance. A directly compiled readings packet and a
wide-to-readings projection have the same id and visible rows, but retain five versus thirteen
private execution outcomes and produce different graph receipts. Which bytes the cache owns can
therefore depend on request order under one purported identity.

The cache compounds this: `admit(key, compiled)` trusts an arbitrary caller key and never asserts
that the result wrapper, packet and measured private references belong to one compiled receipt. A
caller can store an events packet under a readings key, replace it with another packet, or publish a
crossed wrapper while the cache measures the untouched reference graph. The graph may also mutate
after admission because the imported predecessor model shallow-freezes its envelope and
`freezeValue` stops descending at an already frozen parent.

Finally, the claimed real collector execution remains a shape-equivalent local registry. All
thirteen rows call two local placeholder collectors and import none of the declared semantic/F1
adapters. Counts and dependency topology now execute, but the evidence operations that Support,
Review and bots actually need still do not.

## Required repair

Parse the exact closed request before any work; derive the full factual packet id internally; make
direct and projected narrow receipts retain the same dependency-closed graph for that id; admit only
a privately asserted compiled receipt through an internal key; recursively seal or independently
assert every retained predecessor value; and execute the exact registered adapters rather than a
local thirteen-row lookalike. Then run another genuinely fresh review. No production packet, cache
or consumer is authorized by this return.
