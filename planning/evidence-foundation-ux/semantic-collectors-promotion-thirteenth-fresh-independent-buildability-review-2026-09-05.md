# Semantic collectors promotion — thirteenth fresh independent buildability review

**Date:** 2026-09-05

## Verdict

Return the held promotion pair on [[D2864]]–[[D2868]]. The twelfth repair closes the five leaf
grammar/population defects it targeted, but the installation authority does not survive into the
artifact store or recorded result. The evidence spine remains 12/14; no production projection is
authorized.

## Findings

- [[D2864]]: the same public module exports a raw caller-path-to-authority function. “Test-only” is
  a comment and identifier, not an import/runtime boundary.
- [[D2865]]: the issued authority retains only a resolved path. It binds no expected inventory
  digest, bytes or installed-generation identity, so replacement after issuance changes its
  meaning.
- [[D2866]]: registry opening reads the inventory once for entries and again through the
  predecessor for the published digest/store. No equality check joins those reads; the wrapper can
  validate one inventory and return another.
- [[D2867]]: `collectPromotionRaceTablebase` and `recordedAuthorityReceipt` are the predecessor
  operations by object identity, and `openGeneration` returns the predecessor store. The resulting
  authority cannot prove that the application-issued registry admitted it.
- [[D2868]]: the parser accepts an installed generation with empty, set-equal response/legal
  populations despite C18's “non-vacuous” contract.

## Executable evidence

`make semantic-collectors-promotion-thirteenth-fresh-review` retains the complete predecessor
chain and passes 5/5 independent falsifiers. The new target is part of `verify-governance`.

## Required repair

One current checkpoint must start from a real application-composition issuer, bind an immutable
inventory snapshot/digest, read it once, and issue current store/receipt/result seals retaining that
registry by identity through collection. The production graph must not import a caller-path issuer.
An installed generation must either contain at least one exact response subject or carry an
explicitly typed empty state justified in the RFC. Another genuinely fresh review and the existing
provider/value dependencies still gate the held pair.
