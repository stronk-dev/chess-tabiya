# Semantic collectors promotion pair — D2179–D2183 second author repair

- **Date:** 2026-08-30
- **Input:** `semantic-collectors-promotion-second-fresh-review-2026-08-30.md`
- **Verdict:** author repair complete; third fresh independent review required
- **Production status:** original 12 projections unchanged; promotion projections remain held

## Repaired boundary

1. Geometry consumes the exact `createRulesPawnReadingContactsV1Evidence` value receipt through a
   specialized assertion. The global declared-evidence seal, correct ids and an equal rebuilt value
   cannot substitute for that route.
2. Recorded tablebase truth consumes exact same-record evidence from
   `createSourcingLedgerTablebaseResultV1Evidence`; category, DTZ, precise-DTZ, piece-count and
   cross-record mutations refuse. The same trace exposed and repaired the parallel recorded-engine
   route as class-level [[D2327]].
3. A successful tablebase outcome returns one sealed receipt retaining exact geometry, legal moves,
   selected whole source and output by reference. Post-construction substitution fails even for an
   equal separately sealed value.
4. `no_opposing_passed_clear_paths` is a grounded no-output state at both operation layers.
   `input_abstained` is reserved for missing, invalid or unavailable inputs.
5. `promotionWithCheck` is removed from the tablebase projection. The existing exact
   `rules.tactic.event.check@1` producer remains composable by before-FEN and triggering UCI, so the
   primitive stays available without a second check authority.

## Cross-RFC value-authority repair

The literal 191-route receipt now classifies both runtime recorded readings as derived from their
validated sourcing-ledger evidence. The reviewed 75-row partition is 37 computed / 25 derived / 9
direct source / 4 authored; total projection and route counts are unchanged. Five existing/future
promotion factory symbols are pinned across the two RFCs.

## Executable author evidence

- Previous promotion author contract: 3/3 green.
- New D2179–D2183 author contract: 5/5 plus TypeScript crossed-state fixture green.
- Measured D1699/D1700 promotion contract: 6/6 green.
- Evidence-value authority contract: 6/6 green, including exact recorded-reading route authority.
- Route receipt: 191 routes / 187 projections / 6 no-route declarations, digest
  `sha256:bfd97830399a9cd9c7639abb8fc57a0580a571d592f4701146e45be697885448`.

No production collector, provider, catalogue declaration, schema, content, consumer eligibility or
protected intent changed.

## Next lawful action

A third fresh reviewer must attack generic/equal value forges, cross-ledger recorded values,
post-construction input substitution, no-race/input precedence and any hidden check recomputation.
The held pair can implement only after that acceptance and after the value-authority/provider
dependencies it names are accepted and implemented.
