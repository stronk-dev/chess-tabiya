# Deviation classes implementation plan

1. Add the v0.21 schema and shared types for mistake sets, declared costs, timing links, and move-scoped guard overrides.
2. Carry mistake sets through Line Drill projections and render the complete canonical set.
3. Add authoring warnings, timing/guard refusals, move-scoped guard resolution, and human-only evidence pointers.
4. Pin schema, runtime, validation, projection, and rendering behavior with tests.
5. Run both verification gates, reconcile canonical docs, and archive the lifecycle.
