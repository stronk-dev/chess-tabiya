# AssistanceConfig register — D2190–D2193 third author repair

- **Date:** 2026-08-30
- **Input:** `third-fresh-independent-buildability-review-2026-08-30.md`
- **Verdict:** author repair complete; fourth fresh independent review required
- **Production status:** untouched; C9, the register and AssistanceConfig v5 remain unauthorized

## Repaired boundary

1. The two v4 `[key]: value` operations are admitted only when the TypeChecker proves an exact
   `keyof Omit<AssistanceConfig,"version">` key, correlated `AssistanceConfig[Key]` value, one
   AssistanceConfig spread and complete literal call-site edges. Widened, `version`, unknown,
   wrong-value and indirect calls fail.
2. The v5 transition is fifteen exact changed symbols. The original construction/persistence
   population is joined by `DrillScreen.hintDistance` → `GuidedHintSeat.requestHint` →
   `RunStateStore.requestHint` and the complete preset-value/context-clamp columns. A persisted but
   unused setting or nine-field preset compiler cannot complete C9.
3. Ordinary callables, imports, re-exports, helpers, components, template operations and typed
   writes are first-class traversal nodes. The current runtime barrel, converging aliases and a
   resolvable cycle are representable; unresolved dynamic dispatch fails.
4. Production roots derive from the pnpm workspace and each package's build/export graph. Every
   AssistanceConfig reach is authority, product consumer or declaration-only observer. The server
   declaration census is the real observer positive; a runtime read there or in a new package moves
   authority identity.

## Executable author evidence

- D1916 maintained contract: 7/7 green.
- D2009 maintained repeat contract: 7/7 green.
- D2037 maintained final contract: 6/6 green.
- D2113 maintained author contract: 8/8 green.
- D2190–D2193 positive author contract: 4/4 plus TypeScript crossed fixture green.
- Total mutation classes specified: 51.

No runtime, web, server, schema, content, archive, protected intent or register implementation
changed. [[D1639]] remains an owner decision: this process repair proves the columns and path must
exist but does not manufacture their product values.

## Next lawful action

A fourth fresh reviewer must attack the generic-write proof, exact fifteen-symbol delta,
run/preset non-vacuity, alias/cycle traversal and workspace observer classification. Implementation
remains forbidden until fresh acceptance; v5 additionally waits on the owner-ratified D1639 table.
