# Semantic-convention register plan

## State

Draft process RFC; independent buildability review required before implementation.

## Order

1. Land or rebase onto `assistance-config-register.md` so C9 remains single-owned.
2. Independently review `rfc/semantic-convention-register.md` and record corrections/acceptance.
3. Implement the distinct ref grammar, pre-landing state, future AST reader and fourteen fixtures.
4. Add the empty landed register and atomically transfer the exact 39-member live claim.
5. Update development docs, ledger, RFC register, roadmap receipt and append-only log.
6. Run Node-24 governance and staged process checks on exact committed bytes.

## Boundary

No runtime convention declaration, projection migration, disclosure, schema, storage, content, web
or archive byte belongs to this process RFC.
