# Semantic-convention register second return — author handoff

**State:** executed 2026-08-28; fresh independent review queued; C10 implementation remains blocked.

**Source review:** `repeat-independent-buildability-review-2026-08-28.md`

## Assigned repairs

1. ✅ **[[D2013]]/[[D2014]] — inherit the predecessor's real future state.** Semantic conventions are
   resource nine after assistance; its first product landing must consume one exact prior claimant
   through the same staged/first-parent transition authority.
2. ✅ **[[D2015]]/[[D2016]] — publish one buildable product authority.** Reconcile the literal tree
   extractor with `initial-declarations.json`, and name the append-only history path, bytes, checker,
   stable Make target and CI boundary.
3. ✅ **[[D2017]]/[[D2018]] — close numeric and lifecycle domains.** Safe-integer convention versions;
   explicit empty/pre-landing and non-empty/post-landing seed/claim rules.

Author reconciliation also found and repaired [[D2019]]: the canonical history row cannot contain
its own landing commit hash; Git's staged/first-parent transition supplies that external identity.

## Required checkpoint

- Preserve base-id serialization, identity-only membership scope and the exact 39-member seed.
- Replace—not delete or weaken—the six returned arms in
  `tools/d2013-semantic-register-repeat-review/` with transition-capable author fixtures.
- Add the [[D2019]] no-self-hash fixture found during reconciliation; the resulting author target
  has seven arms.
- `make semantic-register-contract`, `make semantic-register-repeat-review`, governance and
  `make verify` pass before fresh review.
- Do not implement C10, product declarations, semantic history or transfer the 39-member claim
  before acceptance.
