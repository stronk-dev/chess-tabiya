# Branch-set scale implementation plan

1. Extract the shared comparison cap and one-pass branch-path projection.
2. Implement unary decidedness and objective-shortfall collapse.
3. Add the sequential, disclosure-gated tablebase read route with learner-perspective conversion.
4. Add reversible local folding, stated truncation, and the rail disclosures.
5. Pin the invariants with runtime/server/client tests and run both gates.
6. Reconcile canonical docs and archive the lifecycle.
