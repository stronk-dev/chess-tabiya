# D3262 — horizon-two direct configured-Maia mass frontier

**2026-09-23 · one child layer, not a proof-class result.**
`d3262-maia-direct-mass-frontier.json`, SHA-256
`005ae03aa31417c055fc8dc12f593d0cb030503ad4bc70c337c96481edba44f6`,
joins the directly measured configured sampler to the frozen 185 named target/candidate
comparisons. `make semantic-search-maia-direct-frontier` checks the model receipt and the
exact named reply; its tests refuse crossed replies and raw policy being relabelled as
configured mass. Every one of the 196 candidate children is retained. `[V]` The checked
projection and `tools/d3262-search-calibration/maia-direct-mass-frontier.test.mjs`.

| Named comparison status | Count / 185 |
|---|---:|
| named reply has positive configured sampling mass | 52 |
| named reply is legal but excluded by top-p | 87 |
| no named positive reply exists | 46 |

The previously bounded reconstruction left two named replies unknown; direct logits show both
have positive configured mass. Of the 32 source pawn-denial controls, one named minor arrival
has positive configured mass and 31 are excluded by top-p, even though all 32 were legal and
had positive raw-model mass. That is a measured property of this exact configured Maia policy,
not evidence that those 31 arrivals are strategically useless or never played by humans.
`[V]` This projection, `d3262-maia-direct-logits.md`, and
`d3262-destination-reply-witness.md`.

The arm still needs its later traversal nodes, target-identity survival, proof-class
abstentions and cost/memory readings. It cannot supply an all-defences explanation or a
production default from this first-layer mass alone. [[D3262]] remains open. `[V]`
`d3262-preregistration.md`.
