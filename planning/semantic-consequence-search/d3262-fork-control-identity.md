# D3262 — declared fork-control identity survival

**2026-09-23 · narrow exact-reply hard control, not a general tactic detector.**
`d3262-fork-control-identity.json`, SHA-256
`687c6fef6b77aebf55dcaf630945e510abb4020244be96ddb6cb2ee6977e44f9`, joins the two
predeclared D794 fork roots to the complete exact-reply graph. `make semantic-search-fork-controls`
checks every upstream source and exact artifact bytes; four tests refuse a false attacker, false
target and vacuous empty reply population. The existing independent D794 test for the stronger
positive-exchange fork distinction passed in its own focused harness. `[V]`

| Declared control | Exact replies | Geometric attacker/rook target retained after every reply? | First refutation |
|---|---:|---|---|
| `tactical:fork-survives`, `Nc7` | 5 | yes | none |
| `tactical:fork-parried`, `Nc7` | 33 | no | `...Bxc7` (`g3c7`) |

The predicate retains the white knight's `c7` identity and the exact black rook target squares.
It checks that the same target remains geometrically attacked after a reply. The surviving
fixture has five king replies; the parried fixture has a legal bishop capture of the attacker.
The result supports an exact, named witness/refutation **for this relation and these controls**.
It does not claim a winning fork, positive exchange, a forced continuation or generalize to
overload/deflection/clearance. The D794 stronger test is corroboration, not a licence to widen
this artifact's proof class. `[V]` Source:
`tools/d3262-search-calibration/fork-control-identity.mjs`,
`tools/d794-bounded-reply-harness/bounded-reply.test.ts`.

The `h3` / `...Bh5` retained-pressure control, all other roots, forcing extensions, pruned
frontiers, source-budget agreement and end-to-end latency still await measurement. [[D3262]]
remains open, with no production search profile selected.
