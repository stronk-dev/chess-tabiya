# Research — coverage matrix and house rules

Research dossiers land here as `<topic>.md`. This README is the coverage matrix: what
has been researched, how well, and what it feeds. A dossier exists when research lands —
no empty stubs; GAP rows below are the queue.

## Coverage limits — read before trusting the matrix

Two structural limits, both proven by owner finds rather than admitted in advance:

1. **The matrix is a snapshot, not a watch.** The 28 products came from the frozen
   v2 brief's desk sweep. Nothing monitors new entrants — **ChessMotive** (owner
   find, 2026-08-12) and **taketaketake** (owner find, 2026-08-14) were both
   absent, and both were found by the owner, not the process. The failure also
   runs backwards in time: **365Chess** (owner find, 2026-08-14) is a 2007
   incumbent squarely on the gap sweep's cluster-10 shelf that the sweep still
   missed — a sweep tuned for 2024–2026 novelty does not see old incumbents
   either (`teardown-365chess-desk.md` §Method).
2. **The frame was "chess training tools", and the product outgrew the frame.**
   The sweep asked who helps you *practice*. The product now has spectating,
   live sessions, share links, and (proposed) game-story summaries — surfaces
   whose competitors are *fan and viewing* apps that the training frame never
   searched. A competitor to a surface we ship is in scope regardless of which
   category shelf it sits on.

**What a teardown answers (widened 2026-08-14, owner ruling):** three questions,
not one — (1) does it threaten E1; (2) what is its one good feature and through
which `design/05` invariant does it enter (adoption posture, `design/02`); and
(3) **why do people love it and why do people hate it** — reviews, forums,
churn complaints, with evidence labels. The third is the going-wide question:
the strategy is breadth ("the end-all-be-all"), so knowing what makes each
one-feature product beloved or abandoned is product research, not competitive
hygiene. A teardown without a love/hate section is incomplete.

Rule going forward: **when a new surface lands, ask which category of product
already does that one thing, and check the matrix covers its best example.**
Owned by whoever lands the surface, recorded here, no standing ceremony.

## House rules

- **Use one lightweight evidence label per factual research claim:**
  - `[V]` — directly checked in the current pass against the cited primary/authoritative
    source, a reproducible measurement, or a hands-on observation;
  - `[P]` — source-backed but partial, secondary, inherited desk research, or not yet
    reproduced hands-on;
  - `[M]` — model knowledge or analysis with no external evidence; unverified.
  These labels intentionally combine evidence basis and verification state for a small
  repo; they are not a complete source taxonomy.
- **Keep legal risk separate:** 🟢/🟡/🔴 may be added where relevant and means legal risk
  only, never evidentiary confidence.
- **Make every factual claim in a dossier traceable.** `[V]` and `[P]` require an inline
  URL or a precise living/archive source reference. Unsupported claims require `[M]`.
  A clearly bounded paragraph or table row may share one citation; repeated glyphs on
  every sentence are not required.
- **No RFC is drafted from a GAP row.** If the area is a gap, run a research pass and
  land the report first (composes with the exploration gate in `rfc/0000-rfc-process.md`).
- **Contradicting a design doc is a feature.** When research overturns design, flag
  `DESIGN-GAP:` in the dossier and escalate via `planning/exploration/log.md`; never
  silently resolve.
- Kill-criterion evidence (see `planning/exploration/gates.md`) is escalated, never
  buried.

## Inherited corpus

The archive's research (`archive/brief-v2/research/`) is **desk research**: product
pages, forums, papers — no standardized hands-on evaluation. Treat every inherited
claim as `[P]` unless a new dossier upgrades it. The package's own caveats:
`archive/brief-v2/research/research_limitations.md`.

- `source-index.md` — living continuation of the archive's R01–R45 (new entries R46+).
- `competitor-matrix.csv` — living copy of `archive/brief-v2/research/competitor_matrix.csv`
  (28 products, 20 columns). Extend/correct here; the archive original is frozen.
- `archive/brief-v2/research/claims_vs_verified.md` and `forum_signal_log.md` — frozen;
  cite as needed.

## Coverage matrix

| Area | Feeds | Status | Report |
|---|---|---|---|
| Competitor landscape (63 products, feature matrix) | Q1a, `design/02` | covered `[P]` — desk-first snapshot; seven 2026-08-20 additions are vendor/community evidence and need hands-on teardown | `competitor-matrix.csv`, `arch/02` |
| Capability-first competitor watch + targeted love/hate sweep | D554-D556, D623-D627, integrated 1.0 map | instrument and targeted desk/forum arm covered `[V]`/`[P]` — 22 canonical representatives, 19 capabilities and 29 evidence/routing rows cover 17/21 map families; alias and novel-capability controls pass. All 58 love/hate cells were searched or already evidenced: 38 reported/observed, 20 explicit targeted-search misses (8 love, 12 hate), zero `not_checked`. Hands-on remains distributed across R3/R7/R8/R11/R15-R17. Chessiverse's branch/theory overlap narrows uniqueness; ChessLabHQ/TryChessLab identities are split; Qchess's structured-thinking drill is now represented | `capability-watch.md`, `competitor-love-hate-sweep.md`, `capability-watch.json`, `planning/platform-alignment/capability-watch/results.json`, `tools/capability-watch-harness/` |
| ChessMotive (owner-flagged near-competitor) | Q1a, E1, Q8 | covered `[V]` desk — E1 **intact**; narrows our compare claim | `teardown-chessmotive-desk.md` |
| Competitor value props: adopt / conflict / not-relevant vs our thesis and posture | Q1a, Q2, Q8, `design/02` | covered `[P]`, 3 claims `[V]` (WhyThisMove, Noctie, Chess Endgame Training) | `competitor-value-props.md` |
| Chess Endgame Training hands-on (latency, branching; owner's "slow/poor UX" report) | Q1a, K9 | covered `[V]` first pass (desktop; mobile/records/hint pending) | `teardown-cet.md` |
| Noctie takeback/branch persistence + feedback timing | Q1a | covered `[V]`/`[P]` desk pass; residual: takeback ground truth needs hands-on | `teardown-noctie-desk.md` |
| Chessable bot-from-course-position on strategic chapters | Q1a | covered `[V]` desk pass (one-way Chess.com handoff, no tie-back) | `teardown-chessable-desk.md` |
| Chessable MoveTrainer mechanics + appeal decomposition + mining list (owner ask 2026-08-22) | D554, D549, D552, `design/02` adoption, `design/04` content strategy | covered `[V]` desk — mechanics pinned from primary docs (8-level 4h→6mo per-move ladder, full reset on lapse, whole-variation review/overstudy, difficult-moves = counted events, priority lines incl. corpus-selected; MT2.0 was a replatform, not an algorithm change); appeal weighting: the moat is the revenue-share author marketplace + Short-&-Sweet funnel, not the SRS; doctrine's memorization critique verified in Chessable's own authors' words; ranked mining list with ADR-0006/ADR-0007 collisions named — top adoptions: lapse-aware ladder, difficult-roots surface, corpus-frequency prioritization; guided first-pass ramp proposed as a design prompt. Product NOT run | `chessable-movetrainer.md` |
| Take Take Take (Carlsen social play + LLM game review; owner's game-story-slides idea) | Q1a, E1, Q1b, Q2 | covered `[V]` desk — E1 **intact**; game-story appetite validated, substance unclaimed; ADR-0005 anti-pattern shipped by a competitor | `teardown-taketaketake-desk.md` |
| Coverage-gap sweep — every shipped/specified surface vs. current market (11 clusters, 2024–2026) | Q1a, E1, `design/03` | covered `[V]` desk — 10 absent-relevant products grounded and added as matrix rows 31–40; E1 **intact** (no branch runtime found); branch groups, recovery-as-skill, and live structural naming returned nothing; teardown shortlist: Play Coach, Chessbook, ChessMind AI, Chess2Story, ChessEver | `coverage-gap-sweep.md` |
| Chess.com Practice multi-move redo + color switching | Q1a | covered `[V]` desk pass (undo destroys attempts; takeback-branch reported broken) | `teardown-chesscom-desk.md` |
| 365Chess (games-DB/explorer incumbent since 2007; missed by the sweep's cluster 10) | Q1a, E1, Q6, `design/02` adoption | covered `[V]` desk — E1 **intact** (lookup-first islands, no branches/rewind/compare; weakened-SF opponent); corpus-source verdict: additive population, unusable (no license/API), redundant via Lichess `/masters`; adoptable: per-position evidence row incl. last-played recency, via rung-4 grounded-claims invariant; love/hate: loved as OTB archive, left for no-opponents + corpus noise | `teardown-365chess-desk.md` |
| Coverage sweep 2 — by notability (old guard enumerated: platforms, databases, GUIs, courseware, publishers; Lichess + Chess.com whole-platform feature censuses) | Q1a, E1, `design/02` adoption | covered `[V]` desk — 14 absent-relevant products grounded and added as matrix CSV lines 43–56 (chessgames.com, ICC, Chess King/CT-ART, Chessity, ChessKid, Chessly, **Dr. Wolf**, Forward Chess, Chessify, ChessMonitor, OpeningTree, En Croissant, WintrChess, chessvision.ai); chess24/Magnus Trainer confirmed dead, Everyman consolidated into NIC; E1 **intact** (nearest pressure: Dr. Wolf's unlimited-undo teach-during-play; Chess.com Game Review re-entry flagged unverified); teardown shortlist: Dr. Wolf, Chessly, WintrChess, En Croissant, ChessMonitor + a scoped Chess.com whole-platform teardown (strong yes) and a scoped Lichess studies/learn-from-mistakes/practice hands-on | `coverage-sweep-2-notability.md` |
| Chess.com whole platform (Game Review re-entry; coach/practice surfaces; acquisition constellation) | Q1a, E1, Q2, `design/02` adoption | covered `[V]` desk — E1 **intact**: Game Review's only interaction is a one-ply Retry puzzle (no play-on vs an opponent); play re-entry is a manual Self-Analysis→Practice-vs-Computer chain, unlinked, attempts destroyed; coach explanations are Diamond-gated at $119.99/yr (the "$120/yr" Trustpilot complaint verified exact); M&A constellation verified (Komodo '18 integrated, Dr. Wolf '20 standalone, PMG '22 — chess24/Magnus Trainer killed, Everyman absorbed, Chessable/Aimchess standalone) — breadth bought, never integrated in-product; adoptable: the auto-offered post-game review ritual, with re-entry and preserved attempts attached | `teardown-chesscom-platform-desk.md` |
| Dr. Wolf (teach-during-play; the undo-vs-fork contrast) | Q1a, E1, `design/02` adoption | covered `[V]` desk — E1 **intact**: undo erases the attempt (mistakes harvested as isolated positions in a review queue, not attempts); "Are you certain?" blunder-guard retracts *before* the consequence (inverts commit-before-learning); band 0–1300/1500 ends where ours starts; explanation mechanism undisclosed, accuracy complaints on record, no TTT-style confabulation catch found; 4.8★/27k proves rewind-and-explain demand; adoptable: spoken post-hoc coach persona via §3a silence default + mistake-resurfacing upgraded to attempts | `teardown-drwolf-desk.md` |
| Chess2Story (game-story surface incumbent; RFC differentiator check) | Q1a, E1, game-story RFC | covered `[V]` desk — E1 **intact**; moment slides + board-jump shipped but read-only; engine-selected turning points with verified-score provenance (grounded, unlike TTT); no opponent, zero loop stages; "re-enter into play" unclaimed | `teardown-chess2story-desk.md` |
| ChessMind AI (closest stack neighbor; ADR-0005 live test case) | Q1a, E1, Q5, Q2 | covered `[V]` desk — E1 **intact**; Maia-2 in-browser ONNX verified in bundle (six bands ~1100–2000+); review prose generator undisclosed, no public confabulation catch found — ADR-0005 case unresolved, not passed; course-position→Maia sparring narrows the opening→play-out edge | `teardown-chessmindai-desk.md` |
| Chessbook (repertoire gap-finding incumbent; feeds the queued gap-finding RFC) | Q1a, E1, `design/02` adoption, audit row 48 RFC | covered `[V]` desk — E1 **intact** (opening-only, card unit, no opponent anywhere; mistakes harvested as quiz cards, not attempts); gap-finding mechanics established: position/EPD-keyed gaps = uncovered opponent replies, prioritized by expected frequency at your 200-Elo Lichess band, bounded by a user-set "1 in N games" coverage target, "go to your biggest gap" entry; SRS is FSRS move-cards (queue-flood complaint on record — evidence for our explainable ladder); §7 states the adoption contract for the RFC; love: data-driven build + speed + solo-dev community; hate: review overload, error-prone prose, no offline | `teardown-chessbook-desk.md` |
| Quick passes — WintrChess / En Croissant / ChessMonitor (sweep 2 picks #3–#5) | Q1a, E1, `design/02` adoption | covered `[V]` desk quick passes — E1 **intact** ×3; WintrChess: Stockfish + chess.com-style classification only, no prose/re-entry, public reliability complaints (trust lesson for grounding); En Croissant: workbench census — its users independently demand eval-bar hiding (validates anti-contamination default), repertoire SRS immature, our self-host constituency; ChessMonitor: honesty finding — loved numbers are mostly real-outcome records (posture-compatible) *but* the marquee FIDE-Elo estimate is a manufactured skill number with proven pull — the no-skill-numbers posture has a named, evidenced price | `quickpass-wintrChess-encroissant-chessmonitor.md` |
| Competitor play-UX — screen anatomy and presentation layer across 9 products (chess.com, Lichess, Chessable, Chessiverse, Beacon, Noctie, Aimchess, En Croissant, Nibbler); pattern language for board-first screens; the shipped screen and the play-composition canvas located in it | D717 Phases 4–5, D841, D839/D840, D718, `phase1-gap-matrix.md` §4 | covered `[V]` primary-source + `[P]` desk (no hands-on; browser unavailable) — **two field invariants**: nothing that grows sits in the board's column (lichess grid-proven from `_app-layout.scss`), and content never resizes the board (rails scroll/tab/collapse; paint goes on the board). Seven-pattern language incl. on-board paint as the at-commit consensus channel and zen/chrome-off as a shipped preset. Shipped screen = "an inspector wearing a play screen's URL" (all six D717 defects are field violations). Canvas draft: desktop rail + phone sheet match the field; five criticisms — max-load artboard puts a cue above the board AND shrinks the board 80→76 px against its own annotation, max load answered at 1440 px only, move strip clips, tablet demotes the objective, commit has no on-board echo. Three traced revisions proposed. D839 benchmark pinned `[V]` from lila source: 25+19 board themes, 41–42+11 piece sets, 5 backgrounds (device default), hue/brightness sliders, 4 animation levels, 3-state zen — ceiling, not floor; floor proposal ≥2/≥2 + None-able animation + quiet preset. Rows proposed D869–D872 (labels — renumber from head at write time; the concurrent taxonomy pass may claim the range) | `competitor-play-ux.md` |
| Chessigma (the review-only **companion** a player recommended beside chess.com — structurally our own posture; `BACKLOG:585`) | Q1a, E1, Q2, `design/02` adoption, `docs/game-import-and-story.md` | covered `[V]` desk (raw site fetch + app stores; every paid module login-gated) — **E1 intact, and narrowed at the front**: *Bot Challenge* is the closest anyone has come to re-entry-into-play from a review — *"You blew a +3 in a real game. Pick it back up at that exact move. Same clock. Bot at your level. Finish it this time."* — further than chess.com's one-ply Retry, Chess2Story's read-only slides or Dr. Wolf's erasing undo; **but it re-enters and never preserves** (no forked attempt, no comparison, no claim of one anywhere), because its unit of truth is the aggregate metric (Chess DNA: 1,000 games × 250 metrics) not the attempt — **which relocates the `AGENTS.md` §Rejected line from "does it have a rewind button" to "does the second attempt survive to be compared"**. Business finding: **the price line runs exactly between the verdict and the doing** — analysis (the thing the incumbent just paywalled) is given away unlimited, no account, on client-side Stockfish WASM, while €12–17/mo buys the weakness model and the drills, guaranteed in Elo. Band 800–1400, i.e. below ours, for the third teardown running. Law-8 case: **the sharpest live ADR-0005 example in the corpus and it is advertised** — *"usually due to weak dark squares… That's prophecy"* and *"You played Bg5 because you wanted to…"*, with true counts laundering invented causation; defence offered is a self-reported ChessQA score, not a checker. Best idea to steal: the **restraint axis** in their public detector benchmark (*"a reviewer that stamps brilliant on ordinary moves is handing out confetti, not information"*, over-calls per 1,000 ordinary moves). 8 adoption candidates with transformations; cheapest = Conversion Trainer (we already ship both halves). **Corrects `coverage-sweep-2-notability.md`**, which filed it as a commodity free-review name-drop | `teardown-chessigma-desk.md` |
| Adoption audit — every loved competitor feature vs the shipped surface (synthesis over all teardowns/sweeps; no new external claims) | `design/02` §Adoption posture, Q1a, Q2, BACKLOG | covered — 60 features audited: 34 shipped (12 in transformed form), 7 ledgered, 19 missing; refusal set empty under the transformation ruling (conflicts constrain form, not existence); top cheap adoption: auto-offered post-game story for native runs; structural RFC candidates: runtime explorer evidence, repertoire gap-finding, stated-reasoning grading, friend-link play; 5 weaker-than-incumbent findings (runtime corpus evidence, share artifacts, voice, catalog depth, mobile); 12 ledger rows proposed | `adoption-audit.md` |
| Integrated-platform alignment — fast-moving competitor capability resweep plus code audit of guidance/LLM retrieval, semantic detectors, bot personality policy, player-style metrics, post-game review and pack/content stability | Q1a, Q4c, Q8, B4, K6, `design/02` adoption, D542-D562 | covered `[V]` code + current vendor docs / `[P]` community and vendor-quality claims / recommendations `[M]` — foundation is broad but lacks the evidence compiler joining producers to bounded modules; current LLM is constrained sentence rewriting, classifier is a geometric census, bot modes are not personalities, player style has no metric contract, and scale content should wait on a proposed stability gate. Seven products added to the matrix; hands-on comparisons remain GAP | `integrated-platform-alignment.md`, `planning/platform-alignment/plan.md` |
| Semantic anchors for machine-backed prose | D1007, D1008, Feedback Delivery Stage 2, claim-backing follow-up, F1/F3 | answered `[V]` code/standards — the shipped binding proves claim identity, text location, pack scope, record provenance and value equality but never the sentence's subject-predicate-object relation. The sole validator-green candidate among 43 is semantically false. W3C selectors and PROV strengthen location/origin, not entailment; nanopublications provide the relevant separation of assertion from assertion provenance. Required repair: typed proposition first, registered deterministic clause renderer second, automatic migration only on full-clause equality; legacy prose otherwise needs explicit author review | `claim-semantic-anchors.md`, `tools/feedback-binding-wave/candidate-audit.test.ts` |
| Bot policy stack — sampling, bounded-error guard, repertoire, controlled traits, history/time capabilities and personality claims (platform R11) | R11, H5, C5, O8, F8, D590-D596, D620-D622, D649, D698, D969, D1062, D1073, D1078, D1080-D1081, D1084, D1095 | **mechanical and desk arms answered `[V]`; source boundary reached, human validation absent** — over the corrected 804-cell population, depth-8 guard and general pawn ×4 pass; forcing/quiet fail. Reweighting and Maia-window filtering/preservation fail route identity: D1078/D1080 complete 1/12. A separately identified exact-legal route source passes its eight gates: 9/12 complete vs paired guarded Maia 1/12, 31 progress + 10 preservation selections, zero regressions, 26 selections with retained Maia mass + 15 source/Stockfish tiebreaks, 55 guard refusals, max admitted loss 234 cp, no severe or repetition increase. D1095 corrected unpaired sibling randomness, a stale opportunity denominator, incomplete MultiPV scoring and the chessops/orthodox castling boundary before verdict. This earns source → guard → human-policy preference → fallback, not a route catalogue or personality claim. The validated 42-branch blind packet remains for owner use. Clock input, repertoire instances, cross-game memory and validated personality do not ship; C5 remains unmet | `bot-policy.md`, `shared-style-atoms-as-bot-traits.md`, `state-directed-bot-profile.md`, `finite-state-bot-route-controller.md`, `monotone-bot-route-controller.md`, `generated-bot-route-source.md`, `planning/platform-alignment/bot-policy/d1084-generated-route-results.json`, `planning/platform-alignment/bot-policy/blind-review/manifest.json`, `tools/r11-bot-policy-harness/`, `tools/d1062-style-bot-harness/`, `tools/d1073-state-directed-bot-harness/`, `tools/d1078-route-controller-harness/` |
| Maia production band roster — which literal Human-baseline rungs the measured result ladder licenses | D970, F8, D333-D344, D1014 | answered `[V]` from the pre-registered 16,660-game D333 run — expose exactly **1000/1400/1800/2200** for 1.0. All three adjacent score intervals are disjoint; 100-point steps are below learner-session resolution; 2000→2400 crosses parity; a denser ladder would be post-hoc interpolation. Bands are model inputs, never human Elo labels, and lose most difficulty leverage at ≤10 pieces. Use band-specific versioned model-layer identities; the compiler must reject one `id@version` carrying different declarations across profiles. Guarded/pawn-heavy families may inherit the band set only after D969 and must calibrate each exact digest | `maia-production-band-roster.md`, `maia-band-outcome-transfer.md`, `planning/platform-alignment/bot-policy/d970-roster-handoff.md`, `tools/d333-band-outcome-harness/out/summary.json` |
| Grounded coaching aggregation — recurring observations, player habits, cited sources and replay/theory/drill actions (platform R13) | R7, R12, R13, O9, F9, D699-D702 | **mechanical/code arm answered `[V]`; owner-use quality remains** — a typed deterministic card preserves occurrence/opportunity, exact `run#node`, full-vs-shown source counts and exact pack/theory applicability while five controls refuse label-only merging, missing denominators, source-less counts, ungrounded actions and diagnostic/advice vocabulary. The current production topology cannot populate it across games: imported runs are excluded from progress; F2 events do not persist there; 199 concept refs / 168 raw identities include 25 cross-pack reuses that persistence fragments into 199 pack-scoped keys; progress aggregates drop source rows/opportunity denominators; shape recommendations drop firing nodes; Review omits F2 events. The missing primitive is a personal-observation ledger feeding separate Observed habit, Recurring situation and Rehearsal result modules. Deterministic rendering is complete; an optional LLM may paraphrase a sealed card but cannot retrieve, select, diagnose, grade or prescribe | `grounded-coaching-aggregation.md`, `planning/platform-alignment/grounded-coaching/plan.md`, `planning/platform-alignment/grounded-coaching/o9-handoff.md`, `tools/r13-grounded-coaching-harness/` |
| Human-like opponents — what makes engine play non-human, how the field models human error, and the candidate-evidence gap under an evidence-to-move selector (D810/D811/D812) | D810-D820, D551, D561, D591, D544/D545, D335/D336, D344, H5/C5, the selector RFC | covered `[V]`/`[P]` desk with measured follow-ups — **non-humanness is misplaced error, not error rate**: weakened engines err off their own candidate list with bounded noise while human error is heavy-tailed, difficulty-dominated and multi-parameter under one Elo. The candidate adapter D813 and local material predicate D730 now ship; D815 refuses threat-recency weighting for 1.0; D816 admits engine-priced choice breadth; D817 refuses multi-band disagreement; time usage remains deferred. Chessiverse's curator+anchor-bot pattern is the only human-loop vendor calibration found besides Allie's live evaluation; no vendor validation of a weakened-bot Elo label was found. Explainable selection remains differentiated, but explanations may cite only the exact admitted candidate facts, never a manufactured cause | `human-like-opponents.md`, `legal-exchange-prerequisite.md`, `threat-salience-and-human-error.md` |
| Bot candidate sharpness and multi-band disagreement (D816/D817) | F8, D810-D823, opponent-selection evidence | answered `[V]` on the fixed 279-position R9/R11 snapshot — among 633 explorer-covered position-band cells, the fraction of legal moves ≥250 cp behind best tracks observed severe human mass with Spearman **.524/.514/.556** by band; near-best breadth is **−.486/−.481/−.589**, while best/second gap is weaker. This admits a typed opponent-only candidate-loss distribution, never an “only move” label or learner grade. Scope is opening/cross-phase; the 35-cell middlegame cut is weak and endgame has zero explorer cells. D817 is refused without rerunning sidecars: the prior sealed band-response measurement already reports Maia-vs-human policy movement Pearson **.021–.044**, sign **47–52%**. D815 remains behind exact tactical identity landing | `bot-candidate-sharpness.md`, `tools/d816-bot-candidate-harness/` |
| Evidence-to-move selector screens (D1162/D1271/D1297/D1312) | selector RFC, F8, H5/C5, shared evidence plane | the representation signal replicates `[V]`, but both researched selector mechanisms are refused. The diagonal head passed a non-proper gate while combined cross entropy worsened to 6.451. Conditional logit fixes that pathology but fails the severe-tail ceiling twice. Its separately preregistered 250-cp guard composition fails two different clauses: validation guarded combined CE is 2.313 vs guarded engine 2.294; confirmation's 1000–1399 human-move survival is 14/17 (82.4%) against an 85% floor. The third population remains untouched; no threshold relaxation, multi-ply, human-like/Elo/personality/production claim. Per D1320, the standing owner-funded non-Maia goal now needs an explicit RFC/owner disposition or materially different research—not a research-tier product refusal. The registered evidence plane remains shared foundation for Support, Review, drills and analysis; H5/C5 remain unmet | `evidence-to-move-head-screen.md`, `evidence-to-move-independent-population.md`, `evidence-to-move-proper-score-repair.md`, `evidence-to-move-guard-composition.md`, `planning/platform-alignment/bot-policy/d1162-*-results.json`, `planning/platform-alignment/bot-policy/d1297-*-results.json`, `planning/platform-alignment/bot-policy/d1312-*-results.json`, `tools/d1162-*-harness/`, `tools/d1297-proper-score-harness/` |
| Non-Maia selector model-family survey (D1328) | selector RFC, F8, H5/C5, D1271/D1320 | answered `[V]` primary architecture / `[P]` current chess-model results — set-dependent choice is materially different because each candidate can depend on the whole variable legal set; a larger per-candidate MLP is not. The exposed 515 decisions license falsification, not a higher-capacity human-choice model; Otter alone reports a 6.1-billion-position training population. Next bounded generation is a data-readiness census + registered compact projection contract + grouped learning curve; no model fit and no reserved-third-population read. RFC/owner still chooses fund/defer/refuse for 1.0 | `non-maia-selector-model-family.md`; sources R53–R56 |
| Non-Maia selector data readiness (D1329) | selector RFC, D1271/D1320/D1328 | source arm answered `[V]`, projection/cost arms pending — frozen June CC0 range: 827,067 games, 48.47m eligible decisions, 100% legal replay, 100% rating and 99.9481% clock/time-control coverage; all 36 roster-derived cells clear 10k (minimum 13,809). V1's undefined band set was refused, repaired and committed before the final range. Streaming output retains no identities/moves; no model fit, reserved D1297 population untouched | `non-maia-selector-data-readiness.md`, `planning/platform-alignment/bot-policy/d1329-data-readiness-*`, `tools/d1329-data-readiness-harness/`; source R57 |
| Threat recency versus severe human-error mass (D815) | F8, D811, H5/C5, opponent-selection evidence | answered `[V]` as a **1.0 refusal** on the frozen 279-position R11 snapshot. After typed exclusions, 193 positions / 570 band cells remain. Attacker-just-moved covers 29 positions but stationary-threat-created only 7 (floor 20); threat flags worsen grouped-CV RMSE by **0.477%** with permutation p **.677**; the proposed residual direction is false pooled and holds in only 1/3 bands. No salience-shaped bot weight, human-difficulty label or individual explanation is licensed. Exact threats remain valid descriptive evidence for Support/Review/drills | `threat-salience-and-human-error.md`, `tools/d815-salience-harness/` |
| Player-style metrics — transparent habits, sample floors, rating leakage and archetype stability (platform R12) | R12, O9, F9, R18, D597-D604 | **short-session arm answered `[V]`; longitudinal transfer external** — 36 accounts × 200 blitz games / 261,892 decisions; 12 of 16 literal metrics have persistent floors from 25–200 games. The 12-dimensional vector re-identifies 35/36 disjoint halves and does not predict within-band rating, but no k=4–12 clustering clears the ARI 0.70 gate (best 0.417). Continuous habit cards may proceed to decision; natural types/GM twins are refused. Style vectors are privacy-relevant identifiers | `player-style-metrics.md`, `planning/platform-alignment/player-style/results.json`, `tools/r12-player-style-harness/` |
| Interaction-state board correctness after selection (A2; exact UCI over click/drag/touch/hover/resize, all served endgames × five viewports) | A2, R3, Q3, C7, K9, D537-D541, D573 | answered `[V]`, product repair verified 2026-08-21 — baseline was 4/90 exact, 15 wrong and 71 missing; after selection-bound cache invalidation plus compact board-first layout, all **90/90** live click/drag/touch cells deliver the authored UCI with zero wrong/missing. A permanent browser gate repeats exact source→request proof over all six packs at desktop/tablet/phone | `interaction-state-correctness.md`, `tools/a2-interaction-state-harness/` |
| Drill-pack primitive stability, compatibility and migration cost (platform R6 / Gate F) | R6, O6, F3, Gate F, D499, D560, D574-D578 | answered `[V]` with a **negative Gate-F verdict** — all 92 current documents validate at 0.27, but none declares its format/capabilities; schema 0.13 named an invalid then repaired artifact; all 92 also validate under 0.24 despite 82 later principle references and 436 typed graduation entries, so syntax gives a semantic false green. History contains one breaking 20-pack evidence relocation and later 35-pack/13-principle and 92-pack/32-ledger waves, with no reusable migration ladder. Four declared primitive families have zero witnesses. Required result: immutable artifacts, capability negotiation/versioned evaluator semantics, read-only plan + separate apply, judgment refusal, and a sacrificial varied pilot; D560 remains active | `pack-primitive-stability.md`, `tools/r6-pack-stability-harness/` |
| Objective lifecycle versus authored consequence | D12b, D645-D646, feedback C1, foundation hold | answered `[V]` — 8/50 packs enter `achieved` with 1–6 moves still authored on the same path; all four tempo packs also duplicate type-owned verdict rules, making terminal copies fire one ply late. Existing D12b intent already resolves the choice: repair the documents and add executable recurrence guards, not feedback anchors or a runtime-wide redefinition of absorbing state | `objective-lifecycle-diagnosis.md`, `planning/feedback-delivery/objective-lifecycle-diagnosis.md`, `tools/feedback-delivery-harness/objective-lifecycle.test.ts` |
| Theory knowledge pipeline / Skipper reuse — can the separate Frameworks knowledge builder be reused or enhanced for chess without importing the video-consultant agent/platform? | Q4c, Q8, B4, K6, D557, D564, D579-D581 | **answered `[V]`, semantic extraction refused for 1.0** — whole Skipper required Frameworks Postgres/auth/Gateway and unrelated services. Over 55 passages / 106 chunks / 144 fixed queries, exact+FTS reached **97.7% recall@5** vs **94.7%** contextual hybrid+reranker; the semantic arm produced **8.3% ineligible top-1** and **66.7% hard-negative abstention**, failing all three retrieval gates. Source replacement passed; model identity and licence/revision/span/digest reproduction failed. Keep a separate deterministic provenance compiler with typed applicability + immutable local exact/SQLite-FTS bundle; no pgvector/contextualizer/reranker dependency for 1.0 | `theory-knowledge-pipeline.md`, `planning/platform-alignment/knowledge-retrieval/results.md`, `tools/knowledge-retrieval-harness/` |
| Practical difficulty outside the ≤7-piece tablebase (R4: fixed-depth Stockfish vs the exact classifier — agreement, per-ply cost, cross-depth stability) | **R4**, campaign design doc, `rfc/resistance-spectrum.md` §7b + open question 3, Q5 (Maia availability, brushes R5) | **answered `[V]` — "no, and cost is the second reason, not the first"**: over 171 in-range positions / **2,416 moves**, the classifier §7b actually specifies (a centipawn **window** around the best move) reaches only **κ 0.577**, but changing one word to **outcome class** at ±100 cp gives **κ 1.000 / accuracy 1.000 / set match 1.000** at depths 12 and 16, zero FP, zero FN, with Stockfish holding **no** tablebase access (`tbhits 0`) — so `DESIGN-GAP:` §7b's formulation is wrong and cheaply fixable. It repairs nothing outside the range: **88.3%** of ≤7-piece positions are decided at that threshold versus **10.2%** out of range (median |eval| **43 cp**), so the classifier silently becomes a magnitude filter, and its concession set is reproduced across depth on **99.4–100%** of in-range positions but only **29–54%** out of range, never converging. Cost out of range: median **77 ms** (d8), **938 ms** (d12), **~8.6 s** (d16), **~39 s** (d20) against the `<500 ms` budget — and ×8 candidates per selection. Also: **D35 quantified** (hash carry-over moves 83.8% of evaluations and the top move on 89/171 positions; the reset costs 6 ms), and **Maia policy mass is available at 120/120 probes** in and out of range at 1100/1500/1900 with Σ policy ≥ 0.989 over a hard **20-candidate cap**, and **Maia advertises `Elo`** — settling `resistance-spectrum` open question 2 | `practical-difficulty-outside-tablebase.md`, `tools/r4-difficulty-harness/` |
| Human win/draw/loss coverage depth at 1400/1600/1800 (R9: how deep the only oracle that exists outside the tablebase actually reaches, and whether it discriminates where engines cannot) | **R9**, R4 follow-up, campaign design doc, `rfc/resistance-spectrum.md` §1d/§7b, on-ramp lane, Q6 | **answered `[V]` — "yes it discriminates, no it does not reach the middlegame"**: over 1,381 serial explorer requests through the shipped `corpus.ts` query surface plus R4's own depth-12 Stockfish probe on 279 positions, human outcomes **separate what the engine cannot** — 124 positions at |eval| < 50 cp and ≥400 games span **0.405–0.600** in score at band 1600 (0.385–0.648 at 1800) with Pearson(cp, score) of just **−0.079/−0.043**, and **475 of 2,814 (16.9%)** engine-tied move pairs (|Δcp| < 30) are separated by ≥5 pp significantly, max **22.3 pp**. But it **ends at ply ~20**: usable (≥400 games, the stated threshold — 385 is the minimum n resolving 5 pp) coverage stops at ply 20/20/21 by band, **zero games anywhere from ply 27**, and *move-level* coverage collapses far earlier (mean **4.5** usable moves of ~35 legal at ply 12–15, **0.17** at 20–23). Instrument checked three ways: the greedy most-popular walk (corpus-independent) puts choice-level coverage at ply **19–21**; the widest possible population (all 9 buckets × 6 speeds × the whole 7.83-billion-game database) is **×23.4** and moves it only to ply **23**; and the shipped 100-game floor is validated as *exactly* the 60/40 line. Honest limits measured: band transfer r = **0.93/0.94/0.81** with 7–9% side flips, transposition inflow median **3.3%**/max **93.2%**, temporal drift **0.58 pp**, and the on-ramp gap is **worse than a mismatch** — band 1000 has 45–100× fewer games at ply 12–19 and all 26 puzzle-derived on-ramp roots return **0–5 games** at every band | `human-outcome-coverage-depth.md`, `tools/r9-explorer-depth-harness/` |
| Maia policy-scalar stability, and what the shipped `practical_resistance` does with it (R5: `resistance-spectrum` acceptance criterion 5 — the 20-repeat probe — plus the consequence for a shipped feature) | **R5**, `rfc/archive/resistance-spectrum.md` §4b + acceptance criterion 5 + open question 1, `design/BACKLOG.md:260` row framing, D35's Maia analogue, Q5 | **answered `[V]` — "yes, and that is the least interesting thing here"**: over **4,260 policy probes and 940 end-to-end selections**, the scalar is bit-stable — **105/105 keys byte-identical across 20 repeats** (35 positions × bands 1100/1500/1900; candidate set, order, rank vector, policy vector, sum and argmax all 105/105; max drift **0.0**), identical across a **second container and a round-robin order** (35/35 vs arm A) and across **MultiPV 8 vs 20** (263 shared moves, 0 mismatches), so `humanConcessionMass` is exactly reproducible and the tightest observed argmax margin (**3.1e-05**) has ~8 orders of magnitude of headroom. **What varies is the `bestmove`**: identical on only **36/105 (34.3%)** keys, median 3 distinct moves per 20 repeats, max 8 — because `bestmove` is a `torch.multinomial` sample on a process-global RNG while `policy` is the raw softmax (read out of the pinned image; Temperature 0 makes it **35/35** stable). **So the by-record determinism is load-bearing for `human_common` (which plays that sample) and belt-and-braces for `practical_resistance` — the RFC hedged in the right direction for the wrong mode.** Four defects found while measuring, all deterministic: `humanConcessionMass`'s `1+1e-9` tolerance rejects a real float32 softmax → **HTTP 500 on 30/40 (75%) of in-range roots**, invisible to fixtures whose masses sum to 0.95; the vacuity gate needs *all* candidates measured, so one terminal child lets the mode play the **alphabetically first** reply under its own name (20/20 on the one root that reached it); **the D35 analogue is real** — an Elo-less request inherits the previous request's band (**6/6**, byte-equal policy vectors) and records `eloApplied` *absent*, i.e. "not band-calibrated", latent today (35/35 packs declare `targetElo`) but reachable via `POST /runs` and the pack schema, and `ucinewgame` would not fix it; and top-p can sample a `bestmove` outside the recorded candidate list (1/700 at MultiPV 8) | `maia-policy-scalar-stability.md`, `tools/r5-maia-stability-harness/` |
| Maia's band-calibrated `Elo` range — where the requested band still reaches the model, and where it stops (R10: D60/D70's measured answer, ruled 2026-08-15 *"three samples are not a boundary"*) | **R10**, `rfc/engine-request-contract.md` §9 (`bound`), `design/BACKLOG.md:114` (D70) + `:126` (D60) + `:129` (D58), `rfc/archive/resistance-spectrum.md` consumer, Q5, the Just Play difficulty row (`BACKLOG:254`) | **answered `[V]` — "the boundary is not where the output stops changing"**: over **5,080 probes / 0 errors** (51 positions × a 68-point grid, plus a descending control, two 10-Elo edge grids and a malformed-value arm) the advertised `[0, 5000]` turns out to be an **interpolation domain, not a capability claim** — `interpolate_elo` in the pinned image clamps to `[0, 5000]` and returns a **linear blend of exactly two learned vectors** (`models.py:337-348`, `:295-296`), so **every** out-of-range request is answered **byte-identically to the endpoint** (11 values × 51/51 positions, TV **0.0** — **9000 *is* 5000**, −1000 *is* 0, silently, with no error field to catch it) while **inside** the range the distribution never stops responding (all 50 adjacent 100-Elo steps changed on 50/50 non-forced positions; at 10-Elo resolution **320/320** pairs changed at *both* endpoints, min TV 0.0032 / 0.00086). So *"distinguishable adjacent bands"* is satisfied over the whole advertised range and finds nothing; the edges come from two other legs. **High edge ≈2400-2600, ordering**: the band→policy trajectory **doubles back** — median TV from band 0 peaks at **2500** then falls to **0.409** at 5000, and bands 0 and 5000 are closer to each other than either is to 2500 on TV (0.409 vs 0.702/0.787), Spearman (**0.303** vs 0.238/0.077) *and* argmax agreement (0.33 vs 0.24/0.22); `1000 vs 3000` (ρ 0.68) is barely more distant than `1100 vs 1900` (ρ 0.88). **Low edge ≈800-1000, readability**: over the 32 truncated positions the median listed policy mass is **0.990 at 800** but **0.833 at band 0** (worst position **0.70**), and the shipped selector renormalises `concedingMass / measuredMass` over exactly that list (`opponent-selector.ts:641-643`) — the *contract* fails before the model does. **Recommended publishable bound: `bandRange = { min: 1000, max: 2400 }`** — the widest grid interval that is strictly ordered *and* readable, `configured` under §9 and intersected with `advertised`; it refuses **nothing that exists** (all **63** `targetElo` values in `content/` are 1100-1939, and the emitter clamp `[1100, 2000]` sits inside it) and refuses **50** and **9000**, D60's two named values. By-products: **816/816** byte-identical under a descending sweep (no hysteresis), and a **second reachable shape of D58** — any `Elo` the engine's `int()` rejects (`1500.5`, `abc`, ``, `2e3`, `NaN`, …) silently leaves the previous band in force (**9 of 12** value forms tested, 5/5 positions each), blocked today only by `Number.isSafeInteger` at all five boundaries. **No claim is made about play quality at any band** — that needs human-referenced data, not an engine transcript | `maia-band-calibrated-range.md`, `tools/r10-maia-band-range-harness/` |
| Does Maia's per-move WDL agree with real human outcomes? — the binding experiment registered at `capabilities.ts:108` (`per-move wdl`, `unmeasured`, *"D87 compare Maia WDL with R9 ground truth"*), runnable only after D236 pinned the divisor | **`design/06-campaign.md` §2a** (the difficulty-availability axis), `design/research/engine-layer-capability-audit.md` §never-asked-for, D236, R9 as ground truth, R4/R5/R10, `work-register` §5 + cluster B, Q5, `05` rung 4 | **answered `[V]` — "split: a real second signal where popularity fails, and not the middlegame oracle"**: 1,475 Maia probes + 279 Stockfish depth-12 probes against **R9's committed explorer readings reused unchanged** (5,557 human-decided move pairs over 279 positions × bands 1400/1600/1800, **every one at ply 0–20, median ply 7–8**). **Encoding settled from source, not sampling:** **27,330/27,330** candidate rows sum to exactly **1000** because `_probabilities_to_permille` constructs them that way (`uci.py:186-193`), and **27,330/27,330** satisfy `cp == win − loss` (`:208-210`) — so **`score cp` and `wdl` are ONE output**, expected score = **0.5 + cp/2000** exactly, the frame is the root side to move (`invert_wdl`, `:203-205`), and the read is deterministic (**799/799** byte-identical over three runs, two fresh containers). **The verdict is conditional and the condition is free:** pooled over 5,379 pairs, sign agreement is Maia WDL **72.2%**, Maia policy **70.0%**, explorer play counts **76.8%**, Stockfish d12 **84.2%**, against a 50% floor and a **94.2–99.3%** measured ceiling (the ground truth's own cross-band reproducibility) — so on the whole population WDL is the *weakest* instrument and loses to a number arriving in the same HTTP response. **But split by play-count ratio it inverts**: where the two moves are within 2× (1,116 pairs, the half popularity cannot answer) WDL holds **65.1%** while the counts collapse to **54.7%** and **Maia's own policy head goes to 34.4%** — *significantly worse than chance*, tightening to **24.9%** at 1.25× — which is `05` rung 4's *popularity is not quality* as a measured inversion and a live warning for every policy-mass object incl. `humanConcessionMass`. On R9's engine-tied pairs WDL (**64.6%**) is indistinguishable from raw counts (64.1%) and beats policy (54.6%), and it orders **R9's own flagship pair backwards** (Bg4/Nc6 after 1.e4 d5 2.exd5: 22.28 pp human, −5.05 pp Maia). **It does not reach**: pooled agreement decays monotonically **81.2 → 75.3 → 68.8 → 61.2 → 47.9%** by ply bucket (16–19 is n = 73, p = 0.82 vs chance) while **Stockfish over the identical pairs is flat, 84.8 → 87.7%** — so the decay is the instrument's, not the ground truth's, and what crosses the wall is availability only (**100%** of rows carry `wdl` at ply 40+, and the within-position spread *grows* from 10.3 pp to 35.9 pp). Two structural mismatches: **path dependence** (only **281/5,222** shared rows identical between history-conditioned and bare-FEN probes, median \|Δ\| 29 cp, max 576, against a position-keyed ground truth) and a **band dial that moves without moving toward the band** (Pearson **0.021–0.044**, sign agreement 47–52%, against a real human band movement of 1.24–2.21 pp after sampling noise). Calibration: within-position spread is **half** the human spread (4.9 vs 9.2 pp — correcting the audit's 0.191 reading, which is over the whole MultiPV-20 list) and the draw rate is **35–40% low** (r 0.54–0.59). Derived threshold (read off a curve, not chosen): WDL first beats the free incumbent at **\|Δ cp\| ≥ 71**, buying **34.1%** coverage. **Consequences: `design/06` §2a unchanged and now measured rather than inherited — the middlegame stays authored; the `per-move wdl` capability's honest destination is (B) deliberately unreached with the measurement as the reason, not (A) reached.** By-products: the shipped Maia command order is confirmed repaired (**5,466/5,466** identical to `Elo`-only after `0985fa4`), and the audit's *"explorer split fetched and discarded"* claim is **stale at HEAD** — `corpus.ts` keeps it and `corpus-sentences.ts:16-20` renders it | `maia-wdl-versus-human-outcome.md`, `tools/maia-wdl-agreement-harness/` |
| **Does Maia's band move the RESULT, or only the DISTRIBUTION?** — D333/D324, the campaign's only progression denominator (D332) and its one hard prerequisite | **D333**, **D324**, **D332** (learner-Elo denominator), **D365** (Glicko), R10 (`maia-band-calibrated-range.md`), R5 (`maia-policy-scalar-stability.md`), `maia-wdl-versus-human-outcome.md` §9.5 (D291), `design/06-campaign.md` §2b, `planning/exploration/gates.md` H5 scope note, `work-register` §5 | **answered `[V]` — "it moves the result, about a third as far as its own units claim"**: **16,660 complete games / 1,049,001 Maia forward passes** over 12 arms, band against band, on a 170-position book cut from the committed pack corpus (44 of 47 draft packs; opening 69 · endgame 51 · middlegame 44), paired openings with colours swapped inside every pair and SEs **clustered on the opening**. **Every band gap separates, down to 100 points** — 1000v2400 **−289.6 Elo** [−327.4, −256.7], 1000v2000 **−260.7** [−291.0, −233.3], 1500v1800 **−69.8** [−84.4, −55.5], 1500v1600 **−22.1** [−31.8, −12.4] (p 7.7e-06), 1900v2000 **−26.9** [−37.4, −16.5] — against a same-band control at **0.4956** [0.475, 0.517] p=0.68 and a Temperature positive control at **+468 Elo**. **The number the campaign needed is the transfer ratio**, read off D324's own ladder where all four rungs share one reference: **28.90 Elo per 100 band points, ratio 0.289 [0.269, 0.309]** over the corpus, and **39.98 per 100, ratio 0.400 [0.379, 0.421]** restricted to full-material positions (≥21 pieces) — so the band is an Elo-shaped dial **not denominated in Elo**, short by a factor of 2.5–3.5. **D324's own pre-registered ladder PASSES** (0.3069 / 0.4990 / 0.6304 / 0.7652 vs a fixed band-1400 reference, monotone, all adjacent 95% CIs disjoint) **and passing it settles the wrong question** — *monotone with disjoint CIs* is a shape test that a ratio of 0.05 would also pass. **Consequences for D332**: the stated 1000→2000 journey is worth **260.7 Elo**, the whole usable range **289.6**, and the most favourable cut (full-material ladder, band 1000→2200) **479.8 [454.9, 504.7]** — against a coverage requirement of **0.714** derived from D332's own journey over R10's own range, which the corpus-wide figure reaches 29% of and the best case 56%. **The axis is ~290–480 points wide, not 1000, on any cut of the data.** **Granularity**: against a rung threshold of ~60 Elo derived from the ±60 SE of a learner's own 30-game session, a 100-band step is **not** a rung and the smallest usable step is **≈150 band points at full material / ≈208 over the corpus** — five to nine rungs across `[1000, 2400]`, not fourteen. **Two edges where the dial stops**: 2000→2400 buys **+28.9 Elo, CI [−16.7, 74.5], p=0.21**, and **what attenuates it is MATERIAL, not the phase label** — the widest gap is worth **−468.9 Elo at ≥21 pieces, −145.5 at 11–20, −72.4 at ≤10**, and a 100-step is **−28.7/−37.2** at full material (CIs excluding parity in both arms) against **−10.1/−4.0** at ≤10 pieces (CIs straddling it in both). Declared phase tracks this only because the corpus's endgame packs are its low-material ones; **below ten pieces the band is not a difficulty lever**. **Cutting on material rather than the phase label sharpens it and moves the quotable ratio**: an 800-point gap is worth **+292.2 Elo** at ≥21 pieces and **+52.9** at ≤10, and the four-rung ladder restricted to full-material positions stays monotone and widens to **479.8 Elo ± 39.4 over 1,200 band points = 0.400** [0.367, 0.433] — so the honest ratio is **≈0.40 full-board / ≈0.29 for a mixed drill book / ≈0.07 for reduced endgames**, and even the best case reaches only 56% of the 0.714 coverage requirement. Symmetric conditioning is not the cause: the `SelfElo`/`OppoElo` sensitivity arm is **−249.7**, a *smaller* gap (Δ +39.9, p=0.10). Audited before reading, because D58/D91 is the way a band ladder goes silently wrong: across **all twelve arms including every ladder rung**, a first-ply χ² permutation test fires on every gap ≥300 and on temperature, and does **not** fire on either same-band control (p 0.65 / 0.85); it is underpowered at a 100-point gap, which the outcome test on the same records is not (p 7.7e-06, 4.1e-07). By-products: **Maia is seeded at `--seed 42`** and the shipped ENTRYPOINT passes none, so two fresh sidecars replay byte-identically and `human_common` is reproducible *by process* (D340, correcting the reading of R5 §10); **an unseeded parallel harness manufactures duplicate games** — 611/611 mirrored pairs byte-identical, 50.8% duplicates, a control returning exactly 0.500 with SE exactly 0.0 (D341); policy is bit-identical at 1–2 threads and differs from the 14-thread reduction in the 7th significant figure. 0 voids in 16,660 games (rule-of-three bound < 1 in 5,553), 0 ply-cap adjudications, no engine adjudication anywhere | `maia-band-outcome-transfer.md`, `tools/d333-band-outcome-harness/` |
| Is Maia's ENDGAME play human-shaped or arbitrary? — measured against tablebase ground truth, the one regime where every legal move's result is decidable (D366, owner 2026-08-16: *"isn't that THE place where humans play so different from agent?"*) | **D366**, D333/D332 (band→result), D57, R4's "difficulty is only measurable in endgames", R9's endgame blind spot, the 12 `human_common` endgame packs | **answered `[V]` — "human-shaped, decisively; band-calibrated, not at all"**: over **1,095 probes, 0 errors** (45 tablebase-critical positions + 15 lost positions × bands 1100/1500/1900 × 6 complete repeat rounds, at the packs' own `Temperature 0.7 / TopP 0.9`) against a **uniform-random legal move null computed exactly from the tablebase**, result preservation is **88.1 / 88.9 / 91.9%** vs the coin's **67.0%** (paired **+21 to +25 pp**, cluster-bootstrap CIs excluding zero) and **all 84 errors were `win→draw` — zero `win→loss`, zero `draw→loss`**, where the coin concedes the point on 31.3% of its errors. **Not arbitrary, on four measurements**: the 84 errors live on **6 of 45 positions and 5 distinct moves**; the modal error share inside a cell is **0.95–1.00** against **0.58–0.64** if the erring move were uniform over the same dropping moves; **argmax(policy) preserves on 39/39/40 of 45**, so the errors are the model's *belief*, not the unseeded sampler's tail; and they cluster in **3 of 11 packs — all pawn-technique — with 0 errors in the other 8** and **0/378 beyond ten plies from the zeroing horizon**. Maia plays the one drawing move of ten (`e4`, not the king move) on **13/18** probes at every band. The pre-specified *plan-conservation* operationalisation **failed to discriminate** and is reported as a negative. **The band is the fiction, not the mode**: sampled preservation at 1900 vs 1100 is better on 2 / worse on 0 / tied on **43** of 45 (sign p = 0.5), while the deterministic policy *does* respond (dropping-mass falls on 30/40, **p = 0.0022**) by a mean of only **4.3 pp**. Three further finds: **only 5.1% of corpus positions on Maia's side of a pack are decisions at all** (vs 76.4% on the learner's) — its real job is resistance, which it does well (DTZ percentile **0.72–0.75** vs 0.38; slowest-losing move **61–69%** vs 23%) and which `human_common` never declares; `perfect_tablebase` picks a capture-or-pawn move **1.57×** more often than a uniform preserving move and, in a **drawn** root, degenerates to **alphabetical UCI order**; and D56's tolerance bug is **already fixed** (one float32 ulp), so `practical_resistance` is a live candidate with three named costs | `maia-endgame-fidelity.md`, `tools/d366-endgame-fidelity-harness/` |
| Broadcast/streamer and teacher/classroom — the two B5 surfaces named in `design/03` and never researched (owner find 2026-08-15) | **B5**, `design/03` §Live + B5, `design/05` §147/§316-322, `BACKLOG:61,62,97,325`, Q1a | **answered `[V]` code audit + `[P]` desk over our own corpus — "the streamer surface ships under another name; the teacher surface does not exist and is a different consent object"**: the term census is confirmed (`teacher`/`twitch`/`streamer`/`classroom`/`cohort`/`broadcast` = **0 hits each**) and **wrong about the surfaces** — `SESSION_KINDS = ["stream","academy","match"]` is a shipped closed enum (`live-types.ts:3`, SQL CHECK `storage.ts:2309`), so Twitch mode ships as `stream` and coached sessions as `academy`, with chrome-free overlay (`/live/overlay/:runId`), 2–8-option 15–600 s advisory votes, a disjoint `chat:<adapter>:<key>` relay namespace capped at 50 000 voters, host-directed board control, possession journal, friend-link `session_join` tokens, a simul wall, and session→pack distillation (**`design/03:286`'s "does NOT exist" correction is itself stale** — `distill.ts:82` is the producer). **But `session.kind` is decorative**: exactly **two** behavioural branches in the whole server, both requiring `match` — nothing distinguishes `stream` from `academy`. Honesty: §3.8's no-per-viewer-withholding ruling is correct and shipped, and `BACKLOG:97`'s F1 "streamer overlay needs per-viewer withholding" clause is stale — but B10 later made assistance **role**-parameterised (`assistance.ts:27-29`), so spectators now get strictly *less* (safe direction, `docs/live-sessions.md:126` overstated). Four defects found: **host-seated vs participant-seated match players get asymmetric rung-3 access at a consented pause**; the overlay omits its RFC-specified adapter-attribution line; the compact Session tab is dead with an inverted role condition; the public story card's "chessboard" is `<pre>` of the FEN. `DESIGN-GAP:` `05:147` names five assistance contexts against three shipped preference keys (match/stream/on-ramp all collapse to `position`). Teacher: **`teacher` appears in no design doc, RFC or line of code** — what `03` promises is *academy* (ships) and *cohorts/events* (absent, `scheduledFor` is the one producer-less atom); a roster is a **standing** relationship, not academy depth, and must not imply a grant. All six `/progress*` routes are principal-scoped, so no coach can see a student's progress; async review of one run does work via spectator grant. Competitors (our corpus): both categories are covered by nothing with a rehearsal runtime — chat-vote entertainment (Chess vs Chat) and classroom ops (Chessido, Chess.com/ChessKid Classroom); E1 undisturbed. **Verdicts: broadcast = SCOPE (finishing pass — two defect-class fixes now, the chat bridge stays behind B5's audience condition, do NOT delete the `03` promise); teacher = DEFER, trigger = a coach asks twice for something a per-run grant cannot express, and merge with the events layer. The one overstatement to correct in `03` is the events row (pack nights/cohorts/relays), not the streamer row.** Top ledger finding: **`BACKLOG:61` and `:62` still read "blocked on F3" 3 days after both shipped** — a live RFC-completion flow-back miss, and the likely cause of the owner's impression | `broadcast-and-teacher-surfaces.md` |
| Coach/stream current conformance after the owner Teacher ruling and Live finishing passes (platform R15/R16) | R15, R16, O11, F5, F11, D80, D636, D703-D705 | **mechanical/code/desk answered `[V]`; owner use remains** — the 24-cell kind×role×disclosure matrix proves session kind changes no permission byte; after disclosure host/solo reach human/corpus evidence while participant/spectator stay locked/sight-capped, preserving D80's match-seat asymmetry until the accepted Teacher RFC lands. Stream and Match have explicit preference profiles; Academy still maps to Position. The current overlay uses shared run/session state only, contains no provider query, states withholding, attributes relayed tallies and supports 2–8 options. Twitch/YouTube/OAuth bridge and editorial delay are absent; two-second polling is transport. The accepted Teacher RFC already owns async roster/assignment/submission consent and cannot remain blocked on D649-descoped recruitment. O11 is narrowed to explicit Academy/Stream compositions, generic adapter boundary and the no-editorial-delay 1.0 posture—no new evidence mode, ambient progress mining or per-viewer truth | `professional-workflow-conformance.md`, `planning/platform-alignment/professional-workflows/o11-handoff.md`, `tools/r15-r16-professional-workflow-harness/` |
| Human play, chess-network adapters and bot-event trust boundary (platform R17) | R11, R17, O12, F11, R19, D706-D710 | **mechanical/code/desk answered `[V]`; owner-use integration remains** — native friend play preserves authorship and pause→rehearse→compare but is explicitly untimed, unrated and unenforced. Position Arena keeps local branches yet stores only an opaque URL/manual PGN, with no provider/challenge/game identity or automatic return. Official Lichess OpenAPI 2.0.165 supplies challenge/clock/FEN/seek/event/export/bulk-pairing substrate. Positive/negative prototypes require a source→provider→returned-run envelope and exact versioned bot-policy entrants; URL-only and unversioned-event controls refuse. O12 receives a costed hybrid recommendation: retain native casual play, optional Lichess-first round trip, bounded local bot events after O8/F8, and no native public pool/human tournament/federation in 1.0 | `social-play-and-event-boundary.md`, `planning/platform-alignment/social-play/o12-handoff.md`, `tools/r17-social-play-harness/` |
| Live game and tournament relay as a drill source — is a live tourney importable, from Lichess or chess.com, and is relaying someone else's broadcast licensed? (owner 2026-08-16: *"is there a way we can import easily the live game? like live tourneys or w/e? […] for streamers you want to cast a live game"*) | `BACKLOG:594`, `design/03` §Live + **B5**, `design/05` §1 + `:38`, `docs/game-import-and-story.md`, `docs/outcome-drill-grading.md`, `BACKLOG:670` (events layer) | **answered `[V]` — every endpoint exercised live, unauthenticated, one-at-a-time, 2026-08-16 14:33–14:52Z**. **Lichess: yes, comprehensively.** `/api/broadcast/top` returned **67 active broadcasts, 23 with a round ongoing** at one arbitrary Sunday sample; `/api/broadcast/round/{id}.pgn` on a live Hungarian OTB Swiss returned **63,843 B, 42 games, 42/42 `[Result "*"]`** with `[%clk]` movetext and FIDE IDs; `/api/stream/broadcast/round/{id}.pgn` held open 90 s delivered the 42-game dump **plus 12 update pushes**, each a **full-game PGN replacement keyed by `[GameURL]`**. **No credentials on any of it** — the spec declares `study:read` but public rounds return 200 without it (documented/observed discrepancy). Politeness is *"only make one request at a time […] waiting one minute"*, which `import-source.ts:21,35,68` already implements. Coverage is **explicitly third-party**: *"you can create your own broadcasts to cover any live game or chess event […] publish PGN on a public URL"*, five sync sources (`push|url|urls|ids|users`) and an organiser-set **`delay` 0–3600 s**. Separately `/api/stream/game/{id}` (NDJSON FEN+UCI+clocks, `security: []`, **3-move delay, 8 streams/IP** `[P]`) and `/api/tv/feed`. **No per-game broadcast endpoint — 404; the round is the unit.** **Chess.com: a clean no.** PubAPI TOC has no live endpoint; Monthly Archives is *"games that a player has **finished**"*; `/pub/player/{u}/games` is **Daily/correspondence only** on a *"refresh at most once every 12 hours"* cache; `/pub/tournament/live`, `/pub/events`, `/pub/broadcast`, `/pub/games/live` all **404**; terms are personal-**non**commercial with robot/AI-scraping prohibitions (only *video* relay of their UI is permitted). **Our existing chess.com refusal generalises rather than needing revision** — and their marquee events are relayed on Lichess anyway (measured: EWC 2026 round `[Site "Chess.com"]`, publicly readable). **Ownership: ecosystem at the source, platform at the aggregation** — organiser DGT boards → LiveChess/LiveChess Cloud → a public PGN URL, which Lichess polls; TWIC is finished-only and *"all rights reserved"*, Chess-Results has no moves. **Rights 🟢:** moves are facts (Moscow 25 Oct 2016: *"in the public domain and is not protected by law"*; S.D.N.Y. 22 Nov 2016 denied World Chess's injunction on **hot-news and contract, no copyright pleaded**), and Lichess ToS grants *"personal or commercial use […] in your own applications, projects, research, or products"* — revocable at their discretion, so degrade, don't break. **Product verdict: SCOPE, small, with two required substitutions.** (1) **The GM's continuation is provenance, never a grade** — `05:38`'s claim is *two attempts by the same player*; one human's one choice rendered as feedback is law 8 in a suit. (2) **The snapshot is a copy, not a live reference** — so `05` §1 survives untouched; a run that *follows* a broadcast must never be built. **Outcome grading collides and is already solved**: no authored claim and no Syzygy, so a live root inherits `imported`'s non-pack `attempt_end` status. `Result "*"` is already handled (`pgn-import.ts:57-60`). **It is NOT `BACKLOG:670`** — that row's "team relays" is a roster-with-calendar, this is an `imported` source adapter; word collision only. **Two new hazards:** the feed ships third-party move grades inline (`{ [%eval 81.15] } { Blunder. Rf8 was best. }`; `comments=false` strips all 8, 43,593→11,870 B) and needs a **test**, not a query-param default we don't control; and rung-3 assistance must be **lockable while the source game is live** — the anti-cheat delays exist for exactly that attack — which would be the first consumer of `permittedAssistance`'s declared-and-unread `sessionKind`. **Cost: a third adapter beside the two already in `import-source.ts`, and two polite one-shot fetches rather than a stream** (no long-lived socket; the server has never had one). **The strongest argument is not the drill — it is the return beat**: a live game supplies its own deadline, the one return lever `league-as-return-loop.md` found randomised evidence for. Condition: if the justification collapses to *"a streamer would love this"*, it waits behind B5's audience gate, which a relay does not relieve. E1 undisturbed | `live-relay-as-drill-source.md` |
| Campaign effect vocabulary — what a roguelike unlock/reward/modifier can actually BE under law 8, counted, and whether there is enough of it for runs to feel different (the depth question behind the campaign cluster) | **`design/06-campaign.md`** §1/§3, R7, the campaign cluster (`BACKLOG:284`, `:285`, `:286`), `05` §3/§3-forms, ADR-0007 | **answered `[V]` — "the material exists, one level below where the design is looking"**: counted against 37 packs / 25 shapes / the frozen unions. **Perceptual: 93 distinct atoms, 49 individually nameable lenses (18 structural + 6 transition + 25 shapes), 34 attested in authored content** (3/18 structural kinds, 3/6 transition kinds and 9/25 shapes are used by nothing), plus **284 authored readable items** behind the shapes — and *supersedes* `authored-transitions-and-features.md` twice: shape plans are now **96/117 (82.1%) signed** not 27%, and `STRUCTURAL_FEATURE_KINDS` is **18**, not the fifteen that dossier counts. **Economic: there is no economy** — 8 of 14 meterable operations are unconditionally free, rewind/fork have **no counter and no refusal path but `MATCH_LIVE`**, `SIMULATE_BUDGET_EXCEEDED` is **declared and never thrown**, inventory lives in **localStorage** so nothing can be earned, and the entire achievement surface mints **7 first-time-only events, ever**; the one genuinely spendable thing already ships and prices *looking* not *retrying* — the `attempt_end` reveal window that **closes on the next committed move** (`feedback.ts:27`). **Selective: 37 packs collapse to 17 distinct encounter tuples**, the middlegame is **1 pack**, 71% of Elo-bearing packs sit at 1800, and `retryVariants` (the axis that most sounds like a modifier) has **zero runtime effect**. **Combinatorics — `DESIGN-GAP:`** `06` §1's *"the deck's slot vocabulary is those nine axes"* is refuted: 2,304 nominal states → 64 content-bearing (`arrows` renders nothing; voice/spoken/ambient are form) → the honesty gate contributes exactly **2** vectors → and because all nine axes are **monotone**, the lattice has **one maximal element**, i.e. **1 distinct build**. Against that, a slot budget over the attested lens pool gives **278,256 five-slot builds** at zero authoring cost. **Variance:** a 15-node run consumes 40.5% of the catalogue and three runs exhaust it; a phase-shaped map is *impossible* (every middle act is the same pack); one non-repeating phase-balanced run = **9 middlegame packs = 9.75 agent-hours** at the measured 65.0 min/pack, Spire parity (102 packs) = **56 h, 64% of it middlegame** — the same phase R4/R9 proved has no oracle. **Synergy is both**: authored joins ship today (the shape signatures), emergent joins are architecturally supported (any conjunction costs the 29.06 µs/ply census) but **unproven — the conjunction hypothesis is `[M]` and is the natural R11**. **Highest-leverage addition: per-lens loadout below the nine axes, with a slot budget justified by D78's measured noise floor** (58 observations/position, 13 unconditional) — the opportunity cost a deck needs does not have to be invented as scarcity that prices the thesis's own selling point, because it has already been measured | `campaign-effect-vocabulary.md` |
| Engine-layer capability audit — every instrument's published capability against what the product asks for, per-mode reproducibility against the five obligations, and a definition of "100%" (owner 2026-08-15: *"engine let's get it to 100% too"*) | **`rfc/archive/engine-request-contract.md`** §3 (the five obligations, used as the audit spine), D35/D66/D67/D72, `design/BACKLOG.md:261` (Just Play difficulty), `rfc/archive/resistance-spectrum.md` §7c, R4/R5/R9/R10 as inputs, Q5, B4 | **answered `[V]` — "the layer is not short of instruments, it is short of questions — and one shipped request asks the wrong one"**: 1,164 probes through the repo's own supervisor plus a whole-repo call-site census. **Escalation first — a live regression found while measuring: `#maia` sends `Elo`, then `SelfElo`/`OppoElo` at their advertised default 1500 (`opponent-selector.ts:493-506`), and `Elo` is an alias for that pair, so every Maia request runs at band 1500 while recording the requested band as `eloApplied`.** Measured over four command orders × 12 positions × 3 bands: the shipped order changes the policy vector on **0/12** positions between band 1000 and 2400 while `elo-only`, `elo-last` and `self-oppo` all change **12/12**, and the shipped arm is byte-identical to an `Elo 1500` request on **12/12** at both extremes. It is a **state** *and* **record** violation of the contract that shipped it (`43c6c4a`, today), it makes R10's `[1000,2400]` inert and re-opens D58/D60/D70, and **both gates miss it by construction** — the unit test asserts the broken array against a fake client, the real-engine suite sends a command shape production never uses. **Never-asked-for capabilities, censused:** Maia emits `score cp` and `wdl` on **3,456/3,456 (100%)** candidate rows and `candidateLines()` parses neither — and the `wdl` is a *second* signal, disagreeing with the policy argmax on **120/200 (60%)** probes at Spearman **0.558**, spreading **0.191** median expected score across a position's listed moves (**0.190 in the middlegame**, where R4 and R9 jointly say no oracle exists), and moving with the band on **777/779 (99.7%)** pairs; the explorer's **per-move win/draw/loss split is fetched and discarded at parse time** (`corpus.ts:61-65` sums it into `playedCount`) although `resistance-spectrum` §7c names it as *"the object practical difficulty actually wants"* and R9 measured it separating 16.9% of engine-tied pairs — while the authoring parser two directories away keeps it; `UCI_ShowWDL`/the `wdl` evidence kind is **built end-to-end with zero production producers**; MultiPV **> 1 is never requested on the evidence path** (the one client hardcodes 1); `go nodes`, `searchmoves`, local `SyzygyPath`, asymmetric `SelfElo≠OppoElo`, DTZ-as-difficulty, per-move `averageRating` and the monthly `history` series (requested, parsed, reduced to one string) are all unasked. **Reproducibility is still 3 of 5 modes and now worse than R5 left it** — the machinery improved (`enumerate`, `#strongEngine` and the evidence executor moved onto the reset prologue and state their own MultiPV, closing D66) but the regression above puts a **record** violation on all three Maia modes — with **`strong_engine` the one mode whose *answer* is irreproducible, now measured after the hash fix: two byte-identical requests on a reset engine disagree on the score for 14/51 positions (27.5%)**. Every fixed bound is **51/51** reproducible; the recommended replacement is **`go nodes 50000`** — median **98.0 ms** (cheaper than the 113.7 ms bound it replaces), 0% over the 500 ms per-call budget, flat cost (1.9× median-to-max vs 7.6× at fixed depth), and **84.3%** move-preserving. **Middlegame difficulty: "authored, unless one cheap experiment succeeds"** — `humanConcessionMass` is already instrument-agnostic (it takes an *externally classified* conceding set), so `practical_resistance` is endgame-only because of its *classifier*, not its *measure*; the one live alternative is validating Maia's `wdl` against R9's own ply-≤20 explorer ground truth, which is unrun. **"100%" defined:** every published capability is either **(A) reached** by a request satisfying all five obligations and feeding a named surface, **(B) published as deliberately unreached with the reason** (the `plan_defense`/`human_external` pattern applied to capabilities, not just modes), or **(C) named as a measured impossibility** — with no capability in a fourth, silent state, which is where most findings live. Nine gaps ordered by what they unblock | `engine-layer-capability-audit.md`, `tools/engine-capability-harness/` |
| How real roguelikes bound a run, sell the next one, and what transfers (owner 2026-08-15: *"the game-games (roguelikes) vs educational/chess games… not too long… enticing people to try new builds"*) | **`design/06-campaign.md`** §0/§2a/§2b/§2c, the campaign cluster (`BACKLOG:284-286`), R6/R7/R8, R11, `campaign-effect-vocabulary.md` as its input, `05` §1/§3 | **answered `[P]` desk + `[V]` repo counts — "three of the five run-bounding devices are refused by measurements we already made, and the genre we are in is Into the Breach, not Slay the Spire"**: six games chosen for the *device each contributes* (StS map/draft, Balatro escalating requirement + capability-suppressing boss, Hades advance-visible doors + duo boons, ITB capability-set build + turn-bounded encounter, FTL pursuit clock, Monster Train covenant ladder); execution-bound action roguelikes excluded because our encounter is **deliberation**-bound. **Run bounds:** the five devices are fixed node count, escalating requirement (Balatro **300→50,000 chips over 8 antes**), external pursuit clock (FTL), in-encounter turn limit (ITB **3–5 turns**), player-elected length (ITB **2–4 islands**, StS's three keys). **B is refused by R4+R9** (no middlegame oracle ⇒ no measurable ramp ⇒ any escalating number is law 8) and **C by the thesis** (a pursuit clock is a retry price by another name, `06` §2c). **D we already have and did not know it**: new count — **36 of 37 packs declare `authoredBoundary.plyHorizon`, median 12 ply, mean 14.2, range 7–40** (openings median 11, endgames median 24, middlegame 8), inside the owner's ~5–20 guess. Minutes are **`[M]` and unmeasurable today** — a grep for `durationMs|elapsedMs|thinkTime` across `packages/`+`apps/` returns only a test stopwatch, so **no per-attempt timing telemetry exists**; the cheapest upgrade in this dossier is one owner run with a clock. **Recommended shape: 9 nodes, 3 acts × 3, 35–55 min `[M]`**, argued from three converging numbers — catalogue (9 nodes = 24.3% / **4.1 runs** to repeat vs 15 nodes = 40.5% / 2.5), the middlegame bill (**2.2 agent-hours** to make a phase-shaped run possible vs **9.75** at 15 nodes), and node-vs-minute parity (a Spire node is a 60–90 s combat; node parity would overshoot minute parity ~4×). Acts are `06` §2a's **difficulty-availability tiers**, so **stakes escalate in decidability, not in numbers** — Act I outcome-measured (`theory_strict`), Act II authored (`human_common` + plan), Act III tablebase-measured (`perfect_tablebase`, literally unbeatable, shipped). **Eight ranked mechanisms, five costing zero authoring minutes**: (1) offered-choice lens draft with a skip — **C(34,3)=5,984** offers over **C(34,5)=278,256** builds; (2) run-defining opening choice (Neow's **four** blessings incl. the disadvantage-plus-reward one); (3) **capability-suppressing boss** (Balatro boss blind) — the highest-value mechanism nobody had proposed, law-8-legal by construction because it speaks about the learner's information, and the thing that makes the monotone assistance lattice non-monotone; (4) advance-visible mutually-exclusive routes (needs `06` §1's missing human-outcome `DecidednessGround`, 2.2–9.75 h); (5) consumable with honest expiry — **the `attempt_end` window already is one**; (6) synergy discovery, **rank conditional on R11** (moves to rank 1 if it lands, and if it fails our loadouts are additive not synergistic); (7) unlock drip (earn side is **7 lifetime milestone events**); (8) persistent ladder — only two honest dials, and **the Maia band dial is measured inert today** by `engine-layer-capability-audit.md`'s `SelfElo`/`OppoElo` regression. **Escalating numeric economy refused outright rather than imitated.** **Where it breaks, four ways:** the build **does not compound** (a lens changes what you can see; a position does not care what you know); its **marginal value goes negative** past D78's measured noise floor (median 58 observations, 8.31 entries/ply at 1.01× lift) so the optimisation is *smallest sufficient set*, not strongest set — which is ITB's shape, not Spire's; **our content decays** (275 deviation notes + 145 checkpoints over 37 packs are read-once, unlike a regenerated Spire encounter) so the catalogue argument favours **shorter** runs; and **a run that cannot be lost is a playlist** — no resource refusal exists anywhere in the runtime, so the campaign has no failure state, with the proposed resolution being to **price *declaring done*, not retrying** (rewind stays free within an encounter; the submitted attempt decides the run), flagged as an owner ruling. The feeling the loop escalates is named: **legibility, not power** | `roguelike-run-design.md` |
| What an intermediate-node consequence can BE — the hole `06` §5 names (*"a run that cannot be lost is a playlist"*) reopened by the owner's *"i kinda don't like it if all the intermediate is without consequence… we can also look into other categories of games"* | **`design/06-campaign.md`** §5 (the unresolved hole) + §1/§2c/§3, the campaign cluster, R6/R7, `roguelike-run-design.md` as its input, `00-thesis.md` §Why anyone, ADR-0005/ADR-0007 | **answered `[V]` code + `[P]` desk — "the consequence already ships, one scope level down, and what is missing is a scope rather than a mechanism"**: ten constraints enumerated from the live docs first (`experimentation without cost`; consequence-before-retry; law 8; the refused escalating economy and pursuit clock; ADR-0007; legibility-not-power; rarity-is-not-value; two-gate honesty; D78 licensing a slot budget; the live-surface admission rule), leaving a **residue of exactly three legal consequence kinds** — denominated in the learner's *information*, or *the position itself*, or *self-inflicted* — which is the test every mechanism is then run against. **The find:** `ObjectiveState` has **6** values and only **3** are absorbing (`trajectory.ts:6`), so the runtime already separates *went badly, play continues* from *ended things*; `degraded` is **one-way by validator rule** (`pack-validation.ts:469`), **produced by authored deviations not by a grader** (**76 of 275** carry `offObjective`, `pack-orchestrator.ts:367-385`), and **sealed across node boundaries** — a leg transition resets to `active` *only* from `preserved`/`degraded` and stores the outgoing verdict as `sealedState` (`pack-orchestrator.ts:556-575`, `trajectory.ts:83-92`), with node-skips recorded (`skippedLegIds`). **And the intermediate/boss split is a lint rule:** `THEORY_ABSORBING_UNSUPPORTED` means **17 of 37** packs *cannot* end a run while `win`/`hold` (13) can only from an outcome condition — and **all three canonical trajectory packs are exactly 3 legs with only the last one absorbing**, i.e. `06` §5's "2 encounters + 1 act boss" is what three authors independently wrote. **The gap is scope**: every seal is path-scoped (`types.ts:102`), so a rewind to a clean line erases it — which is the thesis working. **State audit:** `AssistanceConfig` is worse than `06` §1 says — **three localStorage keys ever** (`assistance-preference.ts:4`) and the server never receives it (`api.ts` = 0 hits); above the run there is **nothing** (`campaign|run_set|curriculum|playlist` = 0 hits), the only run→run edges are `flip_sides` and a schedule carrying **a due date and a back-reference, no gameplay state**; `SIMULATE_BUDGET_EXCEEDED` confirmed declared-and-never-thrown. **Ten mechanisms across six categories** (ITB Power Grid, Balatro's cash-out and Tag, Hanabi clue tokens, XCOM/Darkest Dungeon attrition, Dishonored Chaos, Nemesis, rally service park, Invisible Inc): **survivors** = carried board state (costs content, *collapses the map* — the thing the design grows into), sealed verdict read by the boss (**recommended for v1**), converting reveals (invented cost function, R6/R7), boss-by-census (`shapeRecommendations` is the detector and gates nothing), and the skip. **Deaths, each to a named constraint:** lens fatigue dies to **ρ = −0.143** (targeting most-used is targeting noise) *and* to the hint tax; Hanabi's refill and Dishonored's drift both die to law 8 (the refill needs a chess judgement; the drift needs an accumulator, and ours is 89.0% FP at 1.01× lift). **Assembly vs new code:** ten shipped parts listed against five new ones, smallest first — a **run-level roll-up** (`attempts` is per *branch*; "did this run succeed" is computed nowhere), the commitment point, a container above the run, server-held loadout, per-lens grain (best insertion point `structure.ts:448-450`). **Owner question: which path does a node remember** — (a) the one you're standing on when you leave (no new verb; prices diligence), (b) the one you submit (**one ruling buys both this hole and `06` §5's boss hole**), or (c) the worst you produced (prices retrying; refused explicitly). **`DESIGN-GAP:`** `06` §5 conflates *no resource refusal* (true) with *no failure state* (false — the absorbing states are one), and that conflation is why every prior proposal reached for invented scarcity. By-product defect: **the compare surface bypasses the honesty gate** (`compare-strips.ts:38` calls `pivotalMarkers`, not `liveMarkers`) | `campaign-intermediate-consequence.md` |
| Fun mechanics OUTSIDE the roguelike frame — drip, experimentation, and fun without a power curve; plus "are drills the right campaign unit" (owner 2026-08-16: *"there MUST be novel gamemodes, variations, minigames, puzzles, ANYTHING… from idle games to stardew valley… allowing people FUN in the campaign runs… and EXPERIMENTATION while LEARNING"*) | **`design/06-campaign.md`** §1/§2c/§3/§5, the campaign cluster, `roguelike-run-design.md` and `campaign-intermediate-consequence.md` as its inputs (C1–C10 inherited whole), `00-thesis.md` §Why anyone, `04-content-architecture.md` §1, `02` adoption posture, ADR-0007, Q9 | **answered `[P]` desk + `[V]` repo counts — "fun without power has exactly three devices, we own runnable versions of all three, and six of the shortlist's ten objects already ship with no consumer"**: **15 genres** past the roguelike set (idle/incremental, farming and life sims, gacha *mechanically*, MMO, legacy/campaign board games, deckbuilders, management sims, puzzle games, metroidvania, Souls-like, rhythm, speedrunning, daily/party puzzles, TTRPG, ARG) plus the chess-adjacent formats. **The three devices — asked in the one place the roguelike set could not answer, because every game in it has a power curve:** (1) **score the SOLUTION, not the outcome** (Zachtronics' three-axis histogram; Barth: a leaderboard *"tells you that you suck (and not even by how much)"* and *"is a fantastic incentive for cheating"*); (2) **make KNOWLEDGE the key** (Outer Wilds' ship log — *"learning new things is the only feedback the player gets"*; Tunic's manual; La-Mulana items that *"do not grant any abilities, but open up a new area"*); (3) **buy variety with SETUP SELECTION** (Dominion's 10-of-26 kingdom ⇒ ~5.3M configurations, no unlock system; Really Bad Chess' *"one king and 15 other pieces selected at random"*). **Repo finds, `[V]` at `c55b9cf`:** the suppression path **ships end to end** — `AssistancePermission.locked_off` (`assistance.ts:20`), the named refusal `ASSISTANCE_WITHHELD` (`rest.ts:1090`/`:1107`) and an honest disabled control (`DrillScreen.svelte:717-720`) — so Balatro's boss blind is one *inner-gate* input away, **but must not be a fourth field on `permittedAssistance`**, which is the honesty gate; `RETRY_VARIANT_KINDS` is a **five-kind variety vocabulary with 9 authored instances and ZERO consumers** (Dominion's kingdom in our schema); the cross-pack collection axis is authored, stored and indexed (`attempt_concepts`) but **namespaced apart by one injectable class** (``pack:${packId}#${raw}``, `progress.ts:56-59`) — and is **156 concepts with 132 singletons**, a tag cloud not a curriculum. **`DESIGN-GAP:` `04` §1's Pack row describes an object that does not exist** — `roots` appears in **0 of 47** packs while `start` is singular in 47/47, so the shipped "pack" is `04`'s **Root** and levels 2/3/5 (`family`/`track`/`curriculum`) have **zero code**. **Unit verdict:** `RunSessionKind = "pack" \| "position" \| "imported"`, two of three carry no pack, and `projectAttempts` forces a pack-less verdict to `"open"` (`progress.ts:90`) — therefore **intermediate nodes may be pack-less, boss nodes may not**, and the content bill is bounded by act bosses (3), not nodes (9). The owner's *"packs are micro-DLC"* is right about the **role**, one level off on the **grain**; the missing container is the **Track**. Community channel is **less unexercised than briefed** — `shape-studio.ts:33-34` ships draft→register→export with four named refusals; what has no path is a third-party *Track*. **Fact refresh discharging two standing cost arguments:** 47 packs, **11 middlegame** (commit `aee7c64`), so `06` §5's *"Act II is impossible today"* and the ledger's *"Plan Drill is essentially UNAUTHORED"* are **stale**; `plyHorizon` 46/47 median 10.5; deviations/checkpoints **330/189**; shapes attested **21 of 25** (D44: 9 orphans → 4); `timingWindows` 4 and `variantOf` 2 (both were 0); and **`content/packs/` is empty** so production still serves one pack. **Ten-item shortlist, every entry at zero authoring minutes** — top three: a **daily shared position + spoiler-free branch-shape share** (Wordle's cadence; **Chessguessr already grades against what a human played, which is our thesis in miniature**), a **three-axis histogram over the learner's OWN submissions** (plies · rungs-at-submit · seal — **Chess.com's bot crowns already ship axis 2** at 3/2/1 by hints used), and **wiring `retryVariants`**. **Two hard scoring constraints contributed:** no count of rewinds/forks/attempts may ever be an axis (C1 in scoring clothes), and the population is the learner's own history, never other learners (the ChessMonitor pull). **Direct answer to "make experimentation rewarding": Hitman** — re-entry is *rewarded*, not merely permitted; computable here as a census over the learner's own graph (`attempts.countable` + first-divergent-move), anti-farmed by the shipped `plyHorizon`. **Refused, with half surviving in two of three:** the prestige multiplier (adopt the *reset* — Gloomhaven retirement), rarity tiers (adopt the *pity guarantee*), the stamina gate (refused entire — it prices *playing*). **Literal chess variants refused, the mechanic adopted**: Fog of War is the capability-suppressing boss as a rule of chess, but `UCI_Chess960` is a published capability refusal and every grounded instrument is a standard-chess instrument. **Proposed ADR-0007 twin clause — *unlocked by playing, never by WINNING*** — from four unrelated systems (Balatro's Mr. Bones unlocks on *"Lose 5 runs"*; Dungeon World's failure is the XP trigger; WoW deleted attunements). **Owner question: what is progression denominated in — cadence/completion, the learner's own history, or the catalogue.** By-product for Q9: **5D Chess ships branching timelines as the board itself at 96% of 6,575 Steam reviews**, a strictly harder surface than ours | `fun-mechanics-outside-roguelikes.md` |
| Coaching vs cheating, and the 1000→2000 career curve (owner 2026-08-16: *"balance 'cheating' to win VS 'coaching' to win"* and *"in order to complete a run to max you'd need proper 2000 ELO skills yourself? cause the journey is 1000-2000 Elo?"*) | **`design/06-campaign.md`** §0/§1/§2a/§2b/§5, **`design/05-in-run-experience.md`** §2/§3/§3a/§3b/§3-forms, D113 (hint distance), the campaign cluster, R4/R9/R10 as inputs, law 8, `00` §Target player, H5/K5 | **answered `[V]` code + content census, `[P]` inherited — "the criterion is a THIRD axis neither shipped axis can carry, and the trajectory is a claim about the encounter, never about the learner"**. **Q1:** rung is refuted as the criterion (the owner's win condition *requires* rung 5 authored counter-theory, while rung-0 all-on is measured unreadable at 1.01× lift), and **timing is refuted as *sufficient* by the shipped code** — `feedbackDeliveryOpen` re-closes the reveal window on `move.committed` (`feedback.ts:22-30`) precisely because post-commit at move *n* is pre-commit for *n+1*, and `events.ts:154-159` already indexes disclosure on the encounter's boundedness. What survives is **distance from the answer**, four syntactic values read off what an item *says* — **`kind` · `fact` · `ranking` · `move`** — giving **"cheating iff `distance === "move"` while a committing decision still depends on it; `kind`/`fact` are coaching at any time; `ranking` is the contested band"**, plus a second, separate failure mode (**coaching fails by volume**, D78). It is **orthogonal to both shipped axes**, and it is **D113 discovered from the honesty side**. **The obstacle is a shipped decision, not a gap:** `capabilities.ts` publishes **36 dispositions (14 reached / 15 refused / 6 unmeasured / 1 impossible)** whose recurring predicate is *recording vs grading*, and `bestmove / MultiPV rank / bestline` is **`refused` product-wide** on the reason *"Move verdicts are not condition measurements"* — an answer to whether it may **fire a condition**, used to refuse **display** — so the campaign cannot hand a learner the move as a reward even after the game (**0 of 764** committed records are `bestline`). By-products: `permittedAssistance` **never reads its own `sessionKind` input** and `"solo"` is unreachable in production; the nine `AssistanceConfig` axes are **not all monotone** (5 are; `boardLighting` has a collapsed top *and* a broken floor, `spoken` is not a chain, `voice` is substitutive, `arrows` is inert), correcting `06` §1; `guided` is `"free"` unconditionally with **no server gate** and is not band-shaped. **Q2:** the product measures **neither "played well" nor "run succeeded"** — 8 comparison sites, **1** per-move verdict and it is an authored lookup (`line.ts:121-165`); the only *computed* learner judgement is `TempoVerdict` against an authored budget, with **zero authored users**; prediction grading was deleted at schema 0.8→0.9 (`7d461e1`) and replaced by a threshold-free record **nothing reads** (**0** authored `prediction` interactions); **no learner number exists anywhere**, refused in three lines of shipped copy (*"That is a grade of this attempt, not a verdict on the position"*). What it *can* say is **whether an authored or tablebase-decided objective survived the submitted path** — so *"2000 Elo skills"* is a claim about the **encounter's configuration**, and the curve can be **declared but never adapted** (named price: ChessMonitor's manufactured FIDE-Elo estimate is its most-loved number). **Levers: opponent band `[1000,2400]`** — measured, enforced with `TARGET_ELO_OUT_OF_RANGE`, and **repaired at `0985fa4` after the `SelfElo`/`OppoElo` regression**, but its link to *outcome* is unmeasured (R10 measured the distribution, not the result — the one unrun experiment); **decidability tier** (measured at both ends, authored in the middle); **encounter horizon** — `plyHorizon` on **47 of 89** documents, median 10, range 2–40, and simultaneously a difficulty *and* an honesty parameter; **loadout size and suppression**, licensed by D78 but uncalibrated. **The owner's proposal is expressible, inverted: the curve is SUPPRESSION, not accumulation, and it runs down the distance axis at a flat slot count** — full suppression makes *"2000 Elo required"* true **without measuring the learner**, and the ceiling is per phase (opening `theory_strict`; middlegame ≤2400 Maia + authored plan; endgame `perfect_tablebase`, **infinite**, so the objective must be `hold`/`save`/`convert`). The on-ramp's relaxation is a **horizon** relaxation and already ships (**24 of 26** on-ramp candidates declare `immediate_guard`). **Two `DESIGN-GAP:`-class content findings:** the declared *learner* band is continuous across 1000–2000 (**42–58 packs per 100-point bin from 1400**) while the declared *opponent* band is a two-point step function — **30 packs ≤1394, 3 real packs in 1400–1799, 39 at ≥1800** (1800 alone = 36 of 45 real packs); and the `plyHorizon` act ramp runs **11 → 8 → 24**, so `06` §5's Act II is the **shortest** encounter in the corpus. Eight preregistered falsifiers, thresholds placed off every instrument's optimality boundary. **Owner question: what is the top of the hint ladder and when may it be reached** — (a) `move` stays refused permanently (status quo, chosen deliberately; closes D113), (b) `move` reachable **at a horizon boundary only** (recommended; splits the disposition, makes the criterion one enforceable clause over a predicate the runtime already computes), (c) `move` purchasable pre-commit (the natural roguelike answer; abandons the criterion — refused on the record rather than by silence) | `coaching-versus-cheating-and-the-band-curve.md` |
| Bestline collection and hint-distance adequacy — can D1061's ruled square → piece → ply-distance → move ladder be derived from the shipped producer? | D1061/D113, guided hints, campaign loadout, F5 | **provider/code arm answered `[V]`; semantic-horizon RFC required** — 256/256 PV probes are legal; two fresh 100 ms runs agree 64/64 and 100 ms↔depth12 agrees 92.2%, while depth8↔12 is only 65.6% (budget stays evidence identity). Bestline alone fails the four-rung contract: square/piece each have two meanings, origin-square equals exact-piece, destination-square is often more revealing, and ply-distance has no target/event. Current stores are distinct: 893 content-ledger records with zero typed bestlines; runtime bestline events are writable; engine-walk is read-only/non-PV. Required primitive: versioned PV + selected registered semantic event → typed actor/target/occurrence ply/first move, rendered cumulatively and honestly empty when no event qualifies | `bestline-is-not-hint-distance.md`, `planning/evidence-foundation-ux/d1061-bestline-distance-results.json`, `tools/d1061-bestline-distance-harness/` |
| Semantic-horizon reach and source safety — can the registered event layer power D1061's stages, including basic multi-edge tactics? | D1061, D1065–D1071, F2/F5/F8, Wave C | **fixed provider/code arm answered `[V]`; go to RFC with explicit residuals** — a stageability-first candidate reaches 56/64 depth-12 lines within four plies (87.5%) but only 46/64 at the 100-ms budget; among 44 jointly non-empty lines, projection agrees 84.1% and occurrence ply 88.6%. R2 cannot be reused unchanged: 115/237 compatibility selections lack a single target, and selectivity is not usefulness. Cold selection is mean 329 / p95 799 ms **per searched edge** versus 38.7 / 66.5 ms after event-cache warmup, requiring a versioned candidate packet shared with bots. Exact 3–5-edge detectors find 22 clearance witnesses in the PV sample, but production has no path compiler; their existing identities are `recorded_run` and cannot honestly label a Stockfish PV. Guided Hint's hard-coded three stages also contradict the owner-ruled four. Required split: recorded-path events for Review; separately declared provider-line horizon events for hints; module-specific precedence; theory/authored/tablebase fallback and honest empty | `semantic-horizon-coverage.md`, `planning/evidence-foundation-ux/d1067-path-compiler-audit.md`, `tools/d1066-semantic-horizon-harness/` |
| Production hint-selector table — does the RFC's exact seven-family precedence produce a grounded root-move nudge? | D1061, D1363–D1378, F5 | **current selector refuted `[V]`** — exact constructors over 64 fixed positions × two engine arms produce 150 candidates, 78 opponent-side and 72 root-side. Raw precedence selects an opponent edge in 28/72 non-empty rows; root-side filtering lowers reach to 30/64 depth-12 and 22/64 at 100 ms but still proves neither causality nor benefit. `loose_piece` is 130/150 candidates and 58/72 raw selections; mate families are honest-zero. Depth-12 selector p95 1,596 ms fails the 1,400-ms headroom gate before engine/transport/rendering | `hint-selector-production-table.md`, `planning/evidence-foundation-ux/d1363-hint-selector-results.json`, `tools/d1363-hint-selector-harness/` |
| Relation-safe engine-hint selection — what survives typed perspective and family sign? | D1364, D1397–D1399, F5 | **fixed provider population answered `[V]`; E2E/owner-use open** — only 35/150 occurrences survive: 78 opponent events, 16 self-exposure events, 20 preserved self-risk events and one non-persistent promotion are refused. Strict direct reach is 10/64 in both arms; admitting later root-side searched-line events raises depth-12 to 16/64 and 100-ms stays 10/64. Immediate family/status/relation agrees 10/10 across arms; all six later additions are depth-only. The engine ladder is a sparse module source, composed with independent theory/authored/tablebase paths and honest empty; no causal/quality language is licensed | `hint-relation-safe-selection.md`, `planning/evidence-foundation-ux/d1397-hint-relation-results.json`, `tools/d1397-hint-relation-harness/` |
| Mechanics × modes — every user-meaningful capability against Drills / Just Play / coaching-streaming-teaching / Campaign, and the half-integrations ranked (owner 2026-08-16: *"making sure we have all the right breadth in mechanics and they're not implemented half-assed, but fully integrated"*) | **B1–B11**, `design/03` §Play/§Review/§Learn/§Live/§Adaptive guidance, `design/05` §3/§3-forms/§3a/§3b/§4, `design/06` §1/§2b/§5, `BACKLOG:477` (Just Play difficulty), `rfc/archive/board-annotation.md`, `rfc/teacher-surface.md`, Q4c | **answered `[V]` code census + hands-on (live server, Playwright against the real bundle) — "there are not four modes; there is one run surface, a Live wrapper, and a Campaign with no code — so the half-integrations are at the entries, in the content, and in a handful of controls that exist on one surface and not the other"**: measured hands-on, the **Drills and Just Play run screens are the same 18/19 controls**, differing only by an inert ambient glyph; `DrillScreen` is mounted at exactly one place (`App.svelte:574`); **Live never creates a run** and neither live surface renders any authored vocabulary, `session.kind` has 4 server comparisons of which 2 are creation-time guards, and **`academy` — the owner's *coaching/teaching* mode — has zero behavioural consumers anywhere**; campaign is **0 hits** outside disposable harnesses. **Owner check 1, *"configurable as to what is exposed"*: the config is mode-aware in its ADDRESSING and not in its CONTENT** — `permittedAssistance` takes `sessionKind` and never reads it (`assistance.ts:27-30`), so pack/position/imported get byte-identical permissions and no lever exists to make Just Play more permissive than a drill; and `loadAssistance` returns `SILENT_ASSISTANCE` for **all six** profiles, so the per-context defaults `05` §3-forms requires do not exist. Six profiles is itself a correction — the *"three localStorage keys"* claim is **refuted**. **2 of 9 axes drive nothing** (`arrows` — typed, migrated, validated, *permissioned*, zero readers; `ambient` — a button with no `onclick`), **no single surface configures all nine** (6 in `/settings`, 6 in-run, 3 overlap), and the config is read once at `onMount`. The hard consequence: **`api.reveal` has no call site in the run screen**, so under the triple-fenced `attempt_end` **rungs 3, 4 and 6 are structurally unreachable during a Just Play game** — the owner's *"FULL toolkit"* is rung 0 plus a locked panel until the game ends — and the `onramp` profile is type-unreachable from Just Play. **Owner check 2, *"it steers you by classifying"*: aspirational.** All 4 non-test consumers of `classifyPhase`/`structuralReading`/`endgameReading`/`shapeFirings`/`pivotalMarkers` are renderers; **nothing selects an opponent, band, position, objective, pack, node or rung from a classification**; `shapeRecommendations` computes a `packIds` list and the client throws it away (`navigate("/play")`); `PackList` has **0** filters and `/learn` names the three phases **0** times. **The one real steer is repertoire gap finding** — and only its top-ranked gap is enterable and its write-back verb has 0 client callers. **Guided mode is inverted**: the shape library renders live and ungated in every mode while the `guided` switch gates a smaller duplicate needing `markers` on, so §3a's silence default is violated in code and §3b's band-shaping does not exist. **13 ranked half-integrations**, the top three costing an afternoon each: a reveal control (`DrillScreen.svelte:712-726`), per-context defaults (`assistance-preference.ts:30-33`), and a `tablebase:` branch in `evidence-sentences.ts:143` — the **actual** residual behind B4's Syzygy row, since the other Syzygy path already ships. **Largest promise/behaviour gap: pack-free theory recognition** — `design/03:35-39` promises it, every export in `line.ts` takes a pack, and **no design doc anywhere states a reason**. Absent-by-design set is real and cited (objectives, rung 5, engine conditions/`immediate_guard`, branch race, opponent-intent prompts, the simul wall). **11 prior figures corrected**, incl. `design/03:95` overstating the `MATCH_LIVE` list by one verb (compare is *not* refused), `03:299` stale (narrative mode ships), `variantOf` now used by 2 packs, `0 of 20` opening timing windows **still** exactly 0 (the four new windows are all middlegame), and one **withdrawn finding of this dossier's own** (a stale server process, not 10 unserved packs) | `mechanics-by-mode.md` |
| Time as a difficulty lever — is a game clock the refused pursuit clock, and does time pressure sort assistance by distance to the answer? (owner 2026-08-16: *"what if we want to simulate the time pressure of a GREAT move during 10+0 chess and then give actual time?"*, D330/D331) | **`design/06-campaign.md`** §2c/§5 (the refused list and the power/legibility dichotomy), **`design/05-in-run-experience.md`** §1/§2/§3/§3a, `00-thesis.md` §76 *"experimentation without cost"*, D78, D113, the coaching/cheating distance axis, the campaign cluster | **answered `[V]` harness + code read — "the clock clears doctrine and fails measurement: it is admissible and useless for the job it was proposed for"**. **The obstacle clears, mechanically:** the object `06` §5 refuses is the **run-pooled** clock — a rewind budget with a real-valued counter, since every second on attempt 1 is unavailable to attempt 2 — while an **attempt-scoped** clock that resets at the fork prices *deliberation*, not retrying, and §2c's own decomposition rules that only *how often* conflicts with the thesis. **The shipped placeholder already picked the safe altitude:** `clockState` is declared on **`Node`** (`types.ts:111`, schema `:265`), written only from `CommitMoveOptions` (`runtime.ts:57,341`), so a clock on the reserved field **rewinds with the board automatically** and the refused form **needs a new run-level field** — the ruling is checkable in a diff. **The hypothesis is refuted, and not narrowly.** Over **43,272 rendered items** from the shipped renderers (47 packs / 721 transitions / 609 positions, 25 shape entries, **43 real cached explorer responses**, 105 recorded Maia candidate counts), variance in log(words) explained by **distance class** is **η² = 0.201** (four classes) and **0.038** (the three the product may ship), against **η² = 0.984 for which renderer printed it**; ρ(distance rank, words) = **−0.046** all items, **+0.125** excluding `move` — the order of the ρ = −0.143 this repo already treats as no relationship. **Reading cost is renderer verbosity, not distance from the answer**: `kind` spans **6 words** (phase reading) to **298** (shape panel, 50×), `ranking` spans **12** to **230** (corpus page, 19×). **And the ordering inverts where it matters: `move` is the cheapest class the product could ever print** — median **1 word / 0.3 s** against `ranking`'s **63 / 15.9 s** — so a clock is a gradient **toward** the answer, sharply so under the coaching dossier's option (c). **The 10+0 arithmetic** (600 s / **40-move reference game — Lichess's own `initial + 40 × increment` convention**, at **238 wpm**, Brysbaert 2019): **15.0 s = 60 readable words** per learner move, against an all-on rung-0 reading of **978 words / 247 s at ONE node — 16.4× the whole budget**; two nodes = **82% of the entire 600-second game**; corpus page **3.9×**, full shape panel **5.0×**, human split **1.06×**, `bestmove` **0.02×**. **The durable residue is a derived slot number D78 licensed but never supplied: ≈6.6 `fact` items or 0.94 `ranking` items** — the campaign's five-slot loadout costs 45 words / 11.3 s, so two independent routes land on 5–7 — **and no clock is required to collect it**. **The owner's sentence literally asks for a DEPICTED clock** (*"and then give actual time"*), which touches no invariant and is **one retained field away**: `parsePgnMainline` keeps headers whole (`pgn-import.ts:63`, so `TimeControl` survives) but drops the per-move `[%clk]` comments at `:54`; and *"a GREAT move"* is not expressible — the nearest honest object is `storyMoments`' **≥150 cp recorded swing** (`story.ts:30,86`), a change and not a verdict. **Three clock-named objects, none a clock:** `clockState` (6 passthrough refs, zero readers, no client sends it, `additionalProperties: true` so it will persist anything), **`clock_zeroed` emitted 0 times in 721 transitions** while shipping a renderer sentence (dead code), and `timingWindows`' move-counted `luxuryMoveBudget`. **`DESIGN-GAP:` `06` §5 twice** — the refused list is one word too broad, and *"what escalates is LEGIBILITY, not power"* has no slot for a device that shrinks the **learner's** capacity. **By-product correction: D78's "median 58 observations per position" is 78** on the 47-pack corpus while the mean moved only 57.90 → 62.46 — a bimodal-by-phase distribution whose median sits in the gap (endgame 256 words / 65 s, middlegame 1,199 / 302 s), so the per-phase figures should replace it wherever it is cited. **Owner question: is time nothing, a decoration, or a rule** — (a) close the cluster and delete the reserved field, banking the ≈6-item loadout; (b) the depicted clock (recommended, cheapest); (c) the enforced attempt-scoped clock — admissible, but it amends `06` §5 twice and **a run-pooled budget must be refused in the same ruling**, because the difference is one field's altitude and nothing today would catch it | `time-as-a-difficulty-lever.md`, `tools/d355-reading-cost-harness/` |
| Two accusations from the wild, turned on our own surfaces — **band-tuned flattery** (*"the lower your elo, the more generous it gets with marking your moves as brilliant"*) and **the valuable tool is not the prominent button** (owner-supplied r/chess thread, 2026-08-15; introspective audit of the shipped tree, not a teardown) | **law 8 / ADR-0005**, `design/00-thesis.md` §Target player (the on-ramp lane's three knobs), `design/05-in-run-experience.md` §3/§3a/§3a-i/§3-forms, `design/03-product-breadth.md` §70 (phase filters), **D332/D365** (the learner-Elo denominator), B4, B9, B10, Q8, Q9, `mechanics-by-mode.md` as prior art | **answered `[V]` code read + 3 censuses over 92 pack documents at `d0bc12f` — "flattery is structurally unreachable and the reason is about to expire; the buried-tool failure is real, in three places, and only one of them is §3a"**. **FM1 — no flattery, and no host surface for it:** the `learners` table and the client `Learner` interface carry **no rating column of any kind**, so the conditioning variable does not exist; every band-shaped number (`targetElo`, `difficulty.minOnlineRapid`) is a property of the **pack**, authored. **Twelve knobs audited, seven band-varying: six difficulty, one measurement, zero praise** — because the guard has **six conditions and all six are regressions** (eval swing against the learner, mate appearing, tablebase category/DTZ regression, material `<= -3`, undefended piece) and its output is a `feedback.generated` event carrying **`{nodeId, evidenceRefs}` — a pointer at evidence, never a word**. `guard.evalSwingCp` **does not track band**: 24 of 31 on-ramp packs declare no guard at all (inheriting the fixed 200 cp) and the seven that do run 150/200/250 *against* band (`opponent-intent-early-queen` at Elo 1150 → **150**; `opening-principles` at Elo 1200 → **250**) — it tracks the drill's subject, and lower means **stricter**. The rules tier is **unauthorable** (`rulesTier ?? true`, 0 of 92 packs declare it; `-3` and `MATERIAL_VALUES` hardcoded). **The one genuinely band-varying measurement is the model case**: `corpusPopulation(targetElo)` picks the explorer rating bucket and `renderCorpusPage` prints the buckets first and `CORPUS_GUARD` second — *"These counts say what this population played, not what is good."* And `resistanceSentences` makes the antidote structural: the outcome panel **cannot print a grade without printing the band it was achieved against**, honoured or not, closing on *"Not perfect play."* — while `/learn` refuses the skill number in shipped copy twice (*"never a mastery score"*). **Band separation is perfect and by design**: all **31** `immediate_guard` packs sit at `targetElo` **1100–1394**, all **61** others at **1500–1939**. **Six latent holes, ranked, none exercised by a shipped pack:** (1) **`BANNED_JUDGEMENTS` (18 words, duplicated as `KEY_POINT_JUDGEMENTS`) covers the entire criticism register and NONE of the praise register** — `voiceCheck` returns `valid: true` for *"a brilliant practical choice"*, the exact word from the accusation, wherever `providers.llm === "external"`; (2) **24 packs tell the learner they face *"an opponent near your rating"*** when no learner rating exists and the number used is the **Lichess puzzle's difficulty rating** — the emitter's own `graduationBlockers` already call it a mechanical placeholder and block publication, but draft packs are listed and playable; (3) `feedbackClaims[].text`, `objective.summary` and `PlanClass.description` have **no vocabulary gate at all** (only a numeric-rate check); (4) `variantOf: same_root_other_objective` + a lower `targetElo` + softer prose is a band-conditioned praise variant **nothing compares**; (5) `KEY_POINT_PHRASE_IS_JUDGEMENT` fires only when a phrase is *entirely* judgement words, as a warning; (6) **the standing risk — D332/D365's learner Elo creates the conditioning variable**, so the invariant to pin *before* it lands is *a learner rating may select **what** a learner is shown and may never be an input to **what is said about a move they played***. **One undocumented band effect** (`DESIGN-GAP:`-adjacent): `feedbackDisclosed` returns `true` unconditionally for `immediate_guard`, so `feedbackDeliveryOpen` → `mayRequestSplit` → **the 1100–1394 band gets `humanSplit` and `corpus` unlocked pre-commit** while all 61 core packs lock them until a checkpoint — more scaffolding for the lower band, honest in kind, unstated in `05` §3a-i. **`DESIGN-GAP:` the thesis's third on-ramp knob has no encoding** — none of the 12 `OBJECTIVE_TYPES` is principle- or threat-shaped, on-ramp packs collapse onto the most generic (`play_until_checkpoint` **28 of 31**) and carry **fewer** opponent-intent checkpoints than core (**6 of 31** vs **43** of 61); the lane is specified on three knobs and ships on two. **FM2 — split, and the split is exactly §3a's own line.** The good case is decisive: **Compare is ONE click or `Tab`** (`selectedCompareIds()` falls back to `[active, firstOther]`, no selection needed) in a persistent action bar beside Fork/Replay/Export with keyboard hints, and all 8 destinations are one click from each other. **Three failures, three different causes.** (a) **The comparison narrative — the `explanation-grounds` output, the thing B4 is gated on — is the 7th of 8 sections and the only substantive one that starts COLLAPSED**, below an *expanded* row of centipawn scores and an *expanded* `●●●●` eval sparkline: 2 clicks + a scroll past five sections, and the prominence order is the ADR-0005 anti-pattern as a layout default. §3a gives no cover — compare is post-commitment by construction. (b) **The recorded human-model split takes 5 clicks and has an unsignposted prerequisite**: `projectedPivotal` is `[]` unless `assistance.markers === "live"`, and the only *"Show recorded human-model split"* button in the codebase lives inside the pivotal-marker modal — so ticking the split checkbox alone yields no button, no error and no explanation (extends `mechanics-by-mode.md`'s `guided`/`markers` find: unlike `guided`, the split has **no** rendering path at all). (c) **`/settings` omits 3 of 9 axes and the omitted set is exactly `{humanSplit, corpus, voice}`** — both on-request evidence rungs. **The reference implementation ships in the same panel**: corpus counts are 3 clicks, every step visible, with a rendered reason when locked — *"Available only after this run opens feedback, and never to participants or spectators."* **Two smaller finds:** **`HonestControl` renders its reason `clip: rect(0,0,0,0)` — visually hidden at all 7 call sites**, so a sighted learner sees a greyed *Compare* and is never told *"Create at least two branches"*, while the sibling `class="honest"` idiom (28 sites) is visible; and `/learn` prints the repertoire-scan population as raw **`JSON.stringify(...)`**. **`design/03` §70's *"first-class navigation and filters"* is unshipped** — 92 pack documents in a flat grid, phase rendered as an inert chip. **The line the whole dossier turns on: §3a's silence is a rule about DEFAULTS and has been read as a rule about LAYOUT** — default-off with a visible, labelled, reason-carrying control is §3a working; a collapsed narrative ranked below a dashboard, and a ticked control inert until an unmentioned second control is also ticked, are not silence. **12 ledger rows proposed** | `band-flattery-and-buried-value.md` |
| **The league as a return-loop shape** — the mechanism `design/01` §Return loop has never examined, arriving as owner-supplied evidence (2026-08-15, `BACKLOG:585`: an r/chess commenter on the Lichess4545 leagues, *"a very positive impact on my chess learning"*). Zero prior coverage — `4545` had exactly one repo hit, the ledger row itself | **`design/01-training-model.md`** §Return loop, `design/03` §Live and community (`:87-88` events row) + B5, `design/BACKLOG.md:650` (events layer, parked) + `:585` + `:711`, **`rfc/learner-rating.md` R10** (leaderboards) + Open question 6 (the human anchor), ADR-0007 (`BACKLOG:738`), **`design/00-thesis.md:76-79`** (experimentation without cost), `design/05` §1 invariants, `broadcast-and-teacher-surfaces.md` §4.4, `fun-mechanics-outside-roguelikes.md` §10, **`docs/return-and-progression.md` (the shipped ladder, validated)**, `BACKLOG:580`, Q1b, Q1c | **answered `[V]` primary-source rules + the league's own open-source software + computed season data + `[V]` code census + `[V]` randomised literature — "the pull is obligation, not content; it grips at 92–95% and converts nobody; it is the parked events row plus a standings layer; and the lever with the best randomised support is the one we already shipped"**: eleven league documents fetched, **`Lichess4545/heltour` read as the authoritative encoding**, and **attrition computed by parsing every published pairings page** (cross-checked exactly against the league's own S49 stats). **The obligation mechanism, enumerated:** a published seven-day round with the next round's start as the hard edge; **pairing is done to you** by moderators + the @chesster bot; **scheduling is itself an obligation with its own clock and a specified minimum effort** — *"a minimum of 3 distinct times across at least 2 separate days"* within *"the first 24 hours"*, and *"Players shall not abandon the scheduling discussion"*, with **no default or fallback time**, only three moderator-mediated exits; **the enforcement point is hour 25 and it is automated** — the bot starts an alternate search *"by setting the unresponsive player unavailable"* and *"It is not sufficient to start messaging the opponent now"*, with captains watching a green-check/red-cross compliance dashboard; **no-show = a 20-minute grace (bot fires at 21:30, heltour `timedelta(minutes=22)`)**, screenshot claim *"automatically approved"*, appeal offered, and if the accused proves presence *"their OPPONENT shall lose by forfeit"*; **failure is cumulative and crosses seasons** — yellow → *"A second yellow card offence will automatically result in a red card"* → withdrawal + a yellow carried forward + possible permanent ban, implemented as `if self.games_missed >= 2 … _set_unavailable_for_season()`; **your absence becomes a four-tier staffing problem** (alternate queue that *"rotates as alternates are used"*, contacted from 48 h out at 8–12 h intervals → a teammate plays up and plays **two games that week** → playing down with the opponent team's permission → a ruled draw or forfeit); byes **do not exist in the team league** (the word appears nowhere) and are **unlimited half-point** in LoneWolf with a 4 h deadline and a moderator power to award a **zero**-point bye *"to maintain your approximate position in the standings"*. **The transferable design result, and it is free:** the registration form's commit question was **deleted** — heltour's source carries the reason verbatim, *"it was decided that it is a useless question"* — and replaced by **`weeks_unavailable`**, a concrete per-round toggle. **Intention is not a signal; a per-occasion declaration is.** Second free steal: declaring unavailable **before** round start yields a ruled **draw**, **after** it yields a **forfeit loss** (changelog dated 2018) — **the league prices lateness, not absence**. **Scale:** founded Oct 2015 (11 teams × 4 boards, Google Docs), now **S49 = 44 teams × 8 boards = 352 players + a 121-deep alternate bench**, LoneWolf #41 = 325, 1,752 Lichess team members, **4,614 unique players / 47,452 games / 46 seasons**, and **no prizes of any kind** — *"free to enter and offers no cash prizes"*, no monetary field anywhere in heltour's prize model. **Status replaces money**, and the mechanisms are all non-numeric records of events: **permanent gold/silver/bronze name shading in every future season's table**, a Most Active Players board (**314 games / 45 seasons** at the top), a 100 Games Club, an awards stream. **The mechanism contains no chess** — the founders built it because other leagues *"felt very opaque… there wasn't much of a community"*, and the FAQ's closing word is *"The camaraderie… There really isn't anything quite like cheering on your team-mate as he plays the deciding match… **It's also** a great excuse to play some longer time control games and improve your chess."* Even the 8-board change was an engagement intervention (*"bigger teams lead to more active teams"*). **Lever verdict: the league's is stronger, structurally** — our loop's failure is visible to nobody, a league's to an opponent, a captain, a table and moderators. **It is a fourth progression denomination** beside `fun-mechanics` §10's cadence / own-history / catalogue — *another person's expectation* — and **the only one that cannot be shipped for a single learner**, which is why a single-player-games sweep missed it; it is also **not** D301's daily (same-for-everyone-directed-at-no-one vs different-for-everyone-directed-at-one-person). **HOW WELL IT WORKS, MEASURED:** over seven parsed seasons, **forfeits fell 9.6% (S10) → 3.9% (S48)** with scheduling draws ~1–4%, i.e. **92–95% of scheduled obligations become a played game** — a decade of rule iteration doing visible work — but **the conversion is bought by a volunteer replacement machine**: **46 of 352 boards (13.1%) needed a substitute in a single S49 round**, on a **34% bench-to-roster ratio**, and the founders' own account is *"Flakes were a huge problem at first"* / *"It was a nightmare finding alts at the very beginning."* Within a season, **flakiness converts into negotiation** (forfeits 13→7 per round while scheduling draws rise 1→12). **SELECTION vs CAUSATION — selection PROVEN, and causation is not merely unestablished but TESTED AND NULL-TO-NEGATIVE in every large randomised analogue (above):** two documented entry filters (an *"established classical rating on Lichess"* only exists if you already return to long chess voluntarily, and must be **re-established** if lapsed, with manual review; plus the 3-times-across-2-days + three-hour-appointment burden), a documented **exit** filter (two cards ejects you mid-season — *"Any player who misses two games in the season will be permanently replaced"*), so **every within-season statistic including the 92–95% is definitionally conditioned on having returned**; and the population number points away from the anecdote — **≈20.6 game-appearances per player as a mean `[M]` against a documented maximum of 314**, forcing a median near one season, with the organisers confirming *"many folks joining but also many folks stepping away."* **The league retains a hard core, not a population**, and the commenter is likelier in the tail than at the median. **Named causal design nobody has run: alternates vs rostered players within one season** — same entry filters paid, obligation assigned by **queue position rather than motivation**, and heltour stores everything needed. **Compose: more ships than expected, and the gaps are not chess-shaped.** Present: `match` sessions with two seats, `MATCH_LIVE`, **single-use, expiring, handle-locked, colour-slotted friend links**, arbitrary-FEN position runs, **Position Arena's external-challenge-URL + PGN-import path with a real `'1-0'/'0-1'/'1/2-1/2'/'*'` result**, a persisted directed invitation, `terminalOutcome`, and `live_sessions.scheduled_for` already the session list's sort key. Absent — **term census 0 hits each: `roster`, `standings`, `leaderboard`, `pairing`, `season`, `league`, `tournament`, `forfeit`, `bye`, `captain`** — plus six verified holes: **nothing can reach a learner who is not on the site** (`notify`/`notification`/`email`/`webpush`/`reminder` = **0**, Live polls rather than pushes) against **Chesster's twelve distinct notification behaviours** — the largest missing piece and a precondition for *any* cadence mechanism incl. D301; **no way to discover another learner** (no directory route; `/sessions` is grant-scoped) against a 473-person pool; **no organiser, by owner ruling** (`02`: *"No operator account exists… never a privileged user"*) — the TD role was refused on purpose, and 4545's moderators rule on reasonable effort, approve registrations by hand and hear forfeit appeals; **`session_invitations.state` is written `'open'` and never transitions** (`accepted`/`revoked` = declared `CHECK` values, zero producers — the obligation state machine is schema-only); **a result-without-a-game is inexpressible** (only `importLeg` writes `result` and it demands a parseable PGN, so forfeit `1X/0F`, scheduling draw `½Z`, and both bye kinds have no representation — **4–5% of all league games**); and **two-human play and the return loop are disjoint** — `service.ts:1763` forces `countable:false` on a match run's primary branch, so winning your Tuesday game puts nothing on `/learn` (D314's disjunction, second instance). **Is it the parked events row? Substantially yes** — `broadcast-and-teacher-surfaces.md` already merged that row with the teacher aggregate as *"a roster with a calendar"*; **a league is a roster with a calendar AND A TABLE**, the table being the new half because it is also the colliding half. **Do not open a third row.** **COLLISIONS:** ⚠️ **R10 is hit head-on and needs an owner ruling** — no league exists without a ranked table, and R10's cheating rationale is now **observed rather than predicted** (4545's own history: cheating investigations altered final standings in **Seasons 7, 8 and 17**; closing your account mid-season converts *all* your games to forfeit losses), against our accepted limitation that we *"do not pretend to prevent a host from cheating on themselves"* — **a product that declines to police self-cheating should be slow to publish a table that rewards it**; but the distinction worth ruling on is that **R10 refuses a manufactured skill number compared across learners while a standing records what happened**, the same object class the owner admitted as `corpus_observed` (D332: *"what happened, never what was good"*), and **the league supplies the transformation that survives either ruling — permanent shading, an honour roll with no number and no ordering**. ⚠️ **`00-thesis` mechanism 1 is the sharpest collision, not R10**: *"Every other context charges you a lost game for that curiosity"* — a league **is** that context; and **§1.6 is the proof the transformation is already practised**, since 4545 bans *every* in-game assistance including talking to your captain (*"DO NOT DO IT"*) and displaces all learning to strictly before (OpeningTree prep, *"cannot be overstated"*) or strictly after the game. **ADR-0007 is not merely un-violated but CORROBORATED** — the largest working instance of the mechanism pays nothing; D334's convenience/content line already covers alternate-queue priority. The mutually-accepted pause is right for a coached game and **wrong for a competitive one** (`session.kind` is the cheap home for that, and prior work found the enum decorative). **C5** transforms to per-league delegated organiser capability (run-grant-shaped), never a platform operator. **C7, the gift:** the league's entry gate *requires* an established classical Lichess rating, assistance is banned by rule, rewind does not exist, and boards pair *"usually within 100 points"* — **which is a better-controlled version of the human-anchor experiment `learner-rating` Open question 6 calls *"the single highest-value unrun experiment this RFC creates"*** and which R7 is permanent without; importing games from a league would supply it. **TWO EXTERNAL VALIDATIONS worth more than the verdict:** the community invented **whispering** — in-game thoughts sealed from both players until the game ends, *"to aid their postgame analysis"* — which is `design/05` §1's *"You commit before you learn anything"* as a folk custom with no product behind it; and the Slack-only **Book Club league** pairs *"a chapter of a book each week"* with games *"intended to cement their grasp of the material"* — our premise assembled by hand out of a book and a pairing bot. **Verdict: DEFER the hosted league — now on EVIDENCE as well as feasibility, since building the obligation layer would be building the intervention that failed every large randomised test of it while the spacing layer that passed them is already shipped (trigger = a population that fills one section WITH A BENCH — `BACKLOG:711`'s *"I don't expect much usage"* and *"this deployment has one learner"* are the ceiling); SCOPE the import path** — be the rehearsal surface a league's players use between rounds, which is what 4545 built by hand and cannot sustain (**the Ledger newsletter has been dead since #157, May 2023; the community stats site now 404s**), and which needs no organiser, no operator account, no notifications and no population of our own. **⚠️ THE FINDING THAT REVERSED THIS DOSSIER'S OWN FIRST DRAFT, and it is the most important row here (law 6):** the draft concluded *"no controlled evidence exists in either direction"* — **wrong**. Scheduled social accountability has been randomised repeatedly and comes back null or negative. **Kizilcec et al. 2020 *PNAS*, preregistered, ~250,000 students / 247 courses / Harvard-MIT-Stanford**, with one arm prompting learners *"to make a plan to ask people to regularly check in about their course progress"* — **β = 0.89 pp on completion, 95% CI −0.22 to 1.99, P = 0.115**, while it *did* move week-1 activity (P = 0.006) and sustained activity (P < 0.001) and **still never converted**; single-course precursors had reported +29% and 17%→41%, and at scale effectiveness fell *"by an order-of-magnitude"*. **Baker, Evans & Dee 2016 *AERA Open*, n = 18,043**: a scheduling nudge produced **TOT −4.8 pp on certificates against a 9.3% control rate — a 52% relative reduction**, authors confident it was causal and *"surprising"*. **Russell et al. 2009 *JRTE*, the only true cohort-vs-solo RCT of one course (n = 231, stratified random)**: dropout **53% fully-supported scheduled cohort / 41% facilitated-peer / 45% instructor-only / 44% self-paced** — n.s., cohort arm **directionally worst**, learning gains comparable. **parkrun, 223,224 participants over three years** — free, weekly, scheduled, intensely social: **76.4% "Few-Timers" (~4 attendances in 3 years) and 4.3% high maintainers**. **And the "cohorts complete at 85–96%" story is not evidence**: altMBA's 96% traces to its own co-founder and is absent from the current site, EdSurge's *"5% to 85%"* resolves to a vendor impact report, a student-publication quote and a figure written by an author employed by the company cited, and a widely repeated *"72% per a Research.com meta-analysis"* is **unverifiable — the page has no completion statistics at all**; where cohorts ARE randomised (MDRC, ~7,000 students, six colleges) the effect is **+0.5 credits and no measurable persistence effect**, best site +4.6 pp on six-year degree completion inside a bundle MDRC says it cannot decompose. Even the MOOC baseline is partly artefact — Reich & Ruipérez-Valiente 2019 *Science*: **52% of registrants never enter the courseware**, intenders complete at **15.42%** and the paying track at **46–56%**. **✅ AND THE COROLLARY IS THE BEST NEWS IN THE DOSSIER: the intervention with the cleanest randomised support is the one already shipped.** Ariely & Wertenbroch 2002: externally **imposed, evenly spaced** deadlines beat self-set (M 88.76 vs 85.67, p = .003; project 86 vs 77, p < .001), and **restricting the self-imposed group to those who happened to space evenly cut effect sizes 55–79% and killed significance — so the mechanism is SPACING, not commitment and not sociality; the study has no social component at all.** That is a description of `return-and-progression`'s **1/3/7/16/35-day ladder**. Where commitment devices do endure the binding agent is **money** (Royer 2015: half the gain retained with a self-funded contract, 12% take-up; Giné 2010: +3 pp at 11% take-up; Patterson 2018: +11 pp from an 18% take-up slice the author says *"is not representative"*). **The one upgrade the evidence licenses: make the spacing IMPOSED rather than freely dismissible** (`dismiss` is currently free, silent and unrecorded) — **not add a person**; with Ariely's own caveat that imposed structure made people work more and **enjoy it less**, which is `BACKLOG:580`'s gamification-backlash row arriving from a second direction. **Heterogeneity warning aimed straight at us:** study-together RCTs help low-achieving and less-motivated learners and **mildly hurt time management for highly motivated ones**, and in the exercise meta-analysis a standard class beats home-based-with-periodic-phone-contact by only **d = .09, n.s.** (deliberate group *process* is the ingredient, d = .74, not co-presence) — **our users self-select as motivated, the group sociality helps least.** **THE OBLIGATION-FREE CONTROL, and it sharpens the verdict:** Chess.com's Leagues — the other thing the word names — is a weekly 50-player division table scored off *"regular game play, open seeks, arenas, and tournaments"*, resetting Sundays, with **no pairing, no deadline, no counterparty and no downside** (*"**You can never go back down once you advance**"*). **So a ranked table is NOT what makes a league a return loop** — the largest table in chess has no obligation attached — which **lowers the cost of refusing C1's table**. The two sit at opposite corners: 4545 = named counterparty + negotiation deadline + replacement bot + cards + ejection + no prizes + ~473 people; Chess.com = a table + a weekly reset + nothing else + tens of millions. Caveat: Chess.com Leagues **scores behaviour already happening**, so it tests whether a table alone adds play, not whether obligation causes return — **the genuinely comparable object is 4545's own perpetual join-and-pause leagues (Infinite Quest, Correspondence) against its 8-round obligation league, side by side in one community. That is the sharpest test available anywhere and nobody has published it.** **13 ledger rows proposed** incl. three defects, the R10 ruling, and the free per-occasion-availability adoption. **Not established:** the perpetual-vs-obligation comparison above (the largest remaining gap), Lichess team battles/Swiss and daily team matches, controlled selection-vs-causation evidence, commitment-device literature, rating-gain data, and **any league-published attrition figure at all** (§5.1 is computed from raw pairings; the league's own stats site now 404s and its newsletter died in May 2023); the per-player mean is `[M]` arithmetic; the originating Reddit comment is unverifiable here (`[P]`, owner-supplied) | `league-as-return-loop.md` |
| Target learner/coach problem interviews + concept preference test | Q1b, E2 | **GAP** | — |
| Learning effect versus simpler formats | Q1c, H1–H4, C2–C4 | **GAP** — requires slice | — |
| Maia-3 capabilities (models, conditioning, sampling) | Q5 | covered `[P]` | `arch/research/source_index.md` R04 |
| Maia opponent quality (coherence over 10–20 plies) | Q5 | **answered `[V]`**: 80-game smoke, history-conditioned 5M plays plan-coherent 20-ply continuations with level-appropriate errors; weakened-SF control shows exactly the plan-less noodling Maia avoids | `tools/maia-harness/`, exploration log |
| Maia-3 variants vs ChessMimic (if runnable) vs corpus policy vs weakened-Stockfish control, same positions/seeds | Q5, H5 | **GAP** (queue 6) | — |
| Drill-pack authoring cost — the Q7/K10 verdict (nine waves, 33 instrumented packs) | Q7, K10, C6, K7, `rfc/authoring-frictions.md` | **answered `[P]`** (self-reported clocks, arithmetic `[V]`): **K10 not firing** — 35 packs exist, 33 instrumented at **43.5 min/pack**, tooling friction **11.6%** corpus-wide (9.2% excl. pack A), below the ~25% build-tooling threshold; the 43% single-pack figure in `BACKLOG:292` is superseded. Cost tracks the *grounding* bar, not the format: openings 28.8 min/pack with **zero** engine validation, Syzygy-grounded endgames 40.6, trajectories 97.5. Two qualifications carried, not buried: friction fell partly because **no wave has played a run since 2026-08-12**, and the cheap opening tier has an unpaid §3b grounding bill of unknown size. `review`/`owner-review` was 0 *before* it was retired, so none of the decline is an artefact of the retirement | `pack-authoring-cost.md` |
| Author-declared transitions/timing windows (35 authored packs, 23 shape entries, 9 waves) | Q4a, Q7, **E3**, K7 | **answered `[V]`, split verdict**: boundaries **yes** — 32/35 packs declare `authoredBoundary`, **17 of them voluntarily** (no validator compulsion), `plyHorizon` = deepest spine path in 19/32, and all **6/6** cross-phase trajectory leg boundaries fire at the claimed ply under the shipped evaluator. Timing windows **no** — **0 of 135 checkpoints**, 0 of the 3 races `04` §7 names, two independent authors recorded *why* (single trigger where the claim needs a move set), and `windowOpens`/`luxuryMoveBudget` have no evaluator. **E3 partially met**; K7 splits — structure encodable, timing not. `DESIGN-GAP:` `04` §2d requires a window per opening root; 18/18 opening-phase packs have none | `authored-transitions-and-features.md` |
| Deterministic feature set as authoring assistance — what fires and what cannot | Q4b, Q8, B4, next predicate wave | **answered `[V]`**: reliable for **censuses**, absent for **judgments**. For: 22/25 shape references and 14/14 in-spine structural triggers fire with 0 evaluator errors; the gap→predicate→adoption loop closed inside a day for 5 wave-2 predicates. Against: **73% of shape plans (75/103) ship `signature: null`** with the author's own "no census distinguishes this"; 7 of 15 feature kinds unused in any pack; `piece_reach_count atLeast 0` is faking existence 39 times; D32 (schema-valid condition crashes the validator) and D34 (no king geometry — `reach_structure`, a pawn word, doing duty for a king target). §6 is the predicate roadmap, ordered by attestation | `authored-transitions-and-features.md` |
| Move primitives across a transition — computability, per-ply cost, and the routing hypothesis | R1, R2 (`planning/campaign-research-queue.md`), `rfc/predicate-wave-3.md` §7 F4, `design/BACKLOG.md:224` | **answered `[V]`, one hypothesis refuted**: **R1** — 9 of 10 candidate primitives are censuses; the whole transition census costs **29.06 µs/ply** over 593 spine transitions from the 35 committed packs (33.25 dense, 7.47 sparse), and ~70 of the 290 implementation lines are verbatim copies of code already in the tree but **private** (`pivotal.ts:32-57`, `structure.ts:401-410`). Only **tempo-as-forcing** is not mechanical (needs an opponent model — F9's routing). The cost outlier is the shipped-and-dead `structuralDelta` at **1721 µs/ply**, **59× the whole census**, ~43% of it re-parsing FENs inside `evictionChanges`. Selectivity ordering measured: **overload 6.7%** is the sharpest instrument, `structuralDelta` at 93.3% the bluntest. **R2 — the routing hypothesis fails as stated**: exact given an author-named target (**9/9** recall, 0.20 µs/ply) but **98.7% false positives** with any computed target set (fires on 49.6% of quiet piece moves; endgame king walks dominate), and even with the target supplied **52.8%** of the piece's own legal alternatives also satisfy it. The target set is the judgment; routing is a renderer, not a detector. Routing explains only **9 of 17** author-labeled repositions — vacation, attack-set and safety cover the rest. F4 **promotion trigger not met** (0 transition claims among 78 `signature: null` plans; no RFC ships the discovered-threat surface), but F4's routing row and `predicate-wave-3` §4b's delta claim need amending before inheritance | `move-primitive-computability.md`, `tools/r1r2-primitives-harness/` |
| False-positive rate of a hint built only from transition census (R3: is a true census hint worth reading, and does selectivity predict that) | **R3**, `rfc/transition-primitives.md` §5.4 (the live Just Play tier) + §2.4 (the unmeasured corrected rates), `design/05-in-run-experience.md` §3/§3a/§5a, Q4b, B9 | **answered `[V]` — "mostly not, and the RFC's proxy is invalid"**: over **634 spine transitions / 37 packs**, a census-only reading prints **6.18 observations per ply of which 0.68 clear the necessary conditions — an 89.0% false-positive rate at the observation level**. Per leaf the FP rate of a firing is defences **95.8%**, attacks **94.1%**, irreversibility **89.1%**, escape squares **86.1%**, duties **53.2%**, lines **41.7%**. *"Useless"* is defined as failing any of three mechanical necessary conditions — **T** (the fact is a side effect, not a restatement of the move; grounded on `05` §5's *"what is the moved piece no longer doing"*), **C** (it names something contested; grounded on the queue row's own *"attacks a defended pawn"* exemplar and `05` §3b), **D** (it is not equally true of the moves not played; R2's method) — so the signal rates are **upper bounds** and the FP rates **lower bounds**. **Selectivity is not a proxy**: Spearman ρ(firing rate, FP rate) = **−0.143**; the cleanest leaf (lines, 41.7% FP) is the second-commonest, and its apparent quality dies on axis D (**1.05× lift** over a random quiet move, 32.5% of alternatives also signal — R2's renderer-not-detector shape). **The RFC's one-live-primitive bet fails its own bar**: of 44 `defended_duty_acquired` witnesses only **13 (29.5%)** clear T∧C, so a *"clear majority informative"* is unreachable by arithmetic without a reader study, and the marker signals on **2.1%** of played moves against **3.4%** of random quiet moves (**0.61×**, the wrong direction). Fallback endorsed, with a narrower live variant measured as the alternative (2.1% firing, 0% FP by construction, ~0.8 markers/20-ply branch on the learner-proxy population, **zero firings in 259 endgame transitions**). By-product: corrected firing rates measured (attacks **51.3→37.5%**, defences **76.5→34.1%** like-for-like), two of §2.3's six published figures are per-colour-per-direction rather than leaf rates (escape is **94.0%** not 61.2%; irreversibility **24.6%** not 13.2%), and **§5.4's 37-point empty band collapses to a 5.5-point band** | `census-hint-false-positives.md`, `tools/r3-census-hint-harness/` |
| The conjunction hypothesis (R11: does a conjunction of two independent primitives beat either alone on R3's T/C/D gate — the load-bearing `[M]` claim under the campaign's whole synergy/deck argument) | **R11**, `design/research/campaign-effect-vocabulary.md` §4/§7 (the claim under test), `roguelike-run-design.md` §3 rank 6 + §7, `design/06-campaign.md` §1/§3, R3 as parent, Q4b, B4, K6 | **answered `[V]` — "refuted, and the premise fails before the conclusion does"**: over **721 spine transitions / 47 packs** and **19,099 legal alternatives** on the unmodified R3 harness. The best single leaf:direction reaches **69.4%** precision and **12.64×** axis-D lift; the best of **55 conjunctions** reaches **35.7%** and **2.73×**, only **7 of 55** produce enough co-signalling witnesses to measure discrimination at all, their **median lift is 0.66×** (worse than a random quiet move, 5 of 7 below 1.0×), and **0 of 7 beat even their own two components on both axes** — 0 clear the global bars. **The arithmetic is the finding:** §4 assumed the conjunction multiplies rarity while preserving informativeness; given both fired, the two *signals* are near-independent (observed precision a median **1.18×** the independence prediction, 8/46 within ±25%), so **the false positives multiply at the same rate the specificity does**. Firing is not independent either — median lift **1.136**, 20/55 coupled at ≥1.2×, max **5.26×** (φ 0.456), because six leaves off one attack map are six views of one object. **Triples run the mechanism to its end:** only **38** ever co-signal anywhere, the most frequent **6 times in 721**, and **none reaches 10** — a build of 3+ transition primitives is arithmetically empty, not underpowered. **`DESIGN-GAP:` and it is analytic, not statistical — a lens read BEFORE the move is discrimination-inert by construction** (same value for the played move and every alternative), so all three of §4's worked triples, the shape library's 96 signed signatures and the boss-by-census mechanism are **selectors, not synergies**: across 161 pre-move combinations precision moves a median **+0.1 pp** and the big gains (`slider_lines_changed:closed ∧ phase:endgame` +42.3 pp) are population effects with the within-position also-signalling share **unchanged** (51.7%). The one positive arm is unproposed and is about **when** the lens is read: post-move lenses reach **8.77×** max (`defended_duties_changed:released ∧ after:isolated_pawn`), 25/72 beating their own leaf, median still 0.79×. **Population, stated:** the authored spine is the *generous* population and the hypothesis still loses — swapping to the 17,906 quiet alternatives removes its single precision winner (1/51 → 0/55); a Maia-weighted population would shrink every lift here, not grow it. **R3's headline holds: 88.7%** observation-level FP (6.43 obs/ply, 0.73 T∧C) against 89.0%, ρ still **−0.143**, and **R3's limit 3 is discharged** — the middlegame is now 105 transitions from 11 packs and is the corpus's *least* false-positive phase (86.0%), i.e. the census works best exactly where R4/R9 proved no oracle exists. **By-product: `move_irreversibility`'s `last_of_role` is the most discriminating primitive in the set at 12.64× against all alternatives** — R3 could only record "no quiet firings"; it is a *single* rare primitive and it beats every conjunction by 5× | `conjunction-hypothesis.md`, `tools/r11-conjunction-harness/` |
| Can feedback beat "Stockfish labels + prose"? — the shipped feedback surface measured against the named anti-pattern (Q8: R3's T/C/D gate turned on our own surfaces, plus a delivery census of the authored tier) | **Q8**, K6, B4, B10, `design/05-in-run-experience.md` §3/§5b, `docs/explanation-grounds.md` | **answered `[V]` — "yes on honesty, timing and re-entry; not yet on specificity; forfeit on the claim layer"**: over the same 37-pack / **634-transition / 515-position** corpus R3 used, the three structural wins are shipped and real — disclosure is gated on commitment and `attempt_end` **re-closes on the next committed move** (`feedback.ts:22-30`), rewind appends and deletes nothing (`runtime.ts:385-414`), and **235 deliverable deviation notes** are each bound to a *named non-spine move* and revealed whether or not the learner played it, which is the one object R3's axis D says a census cannot produce. The two failures are ours, not structural. **The shipped compare strip fails axis D worse than the leaf R3 condemned**: it fires on **99.8%** of transitions at **8.31 entries/ply**, and of 14,463 quiet alternatives **99.3%** also fire and **90.4%** fire the *same kind* — lift **≈1.01×** against `slider_lines_changed`'s condemned 1.05×; at the 44 authored fork pairs two branches' readings overlap at **Jaccard 65.7%**, leaving a **median 36 unranked differences** as the answer to "why did these two attempts differ". The pulled rung-0 reading prints a **median 58 observations per position** (max 97), **13 of them unconditional** (12 piece counts + king distance), while `outpost` — the feature the whole rung-0 argument rests on — fires on **2 of 515**. And **0 of 131 authored feedback claims are deliverable** (the only consumer is a `stated_reasoning` ground; **0 of 145 checkpoints** use one), while **66 of 66** machine-checkable evidence-type labels on the 32 ledger-bearing packs raise `EVIDENCE_TYPE_UNBACKED` — **22.1% of the 202,479-character authored prose corpus cannot reach a learner**. By-products: R3's 79.9% FP figure for the *shipped* irreversibility marker **independently reproduced** (13.4% firing: pawn_break 7.6 / castled 3.2 / last_of_role 2.7); `phase_change` fires on **1/634**; and B10's "endgame steering by named technique" reaches **6.0%** of positions and renders *"No technique entry is available yet."* **K6: partial evidence FOR firing** — generic on what is delivered, not generic on what is authored | `feedback-versus-the-dashboard.md`, `tools/q8-feedback-surface-harness/` |
| Is the authoring vocabulary COMPLETE, and does it leverage the engine? (the content-scale gate: 37 packs authored, `design/04` maps ≥3× that) | **content scale-up**, Q4a, Q4b, Q7, B4, `design/04` §2d/§7, `design/BACKLOG.md` friction batches | **answered `[V]` — "complete enough for structure; the two things that would cost most to redo are shipped, executable, and unexercised"**: measured at `fa1e689` over 37 authored packs / 25 shape entries / 32 evidence ledgers / 188 schema tokens, with `pack-check` and `sourcing-check` run over the whole corpus. **`pack-check` passes 37/37 with zero warnings** and the census found zero unsatisfiable expressions, and **no vocabulary is in flight** (all three active RFCs claim nothing versioned) — so the format is not the bottleneck. **Shape entries improved sharply**: `signature: null` is **21 of 117 (17.9%)**, not 73%, and `piece_reach_count`'s 143 existence-encoding leaves are now **0**; only **3 of 18** structural feature kinds are unused anywhere (was 7 of 15). **The re-author exposure is elsewhere and it is inverted** — the top two risks are not missing vocabulary but unexercised vocabulary: (1) `design/04` §2d **requires** a timing window per opening root and **0 of 20** opening packs, **0 of 145** checkpoints, **0** success conditions and all **7** tempo verdicts have zero users, although `tempo.ts:140-274` now consumes every field (`DESIGN-GAP:` widened from 18/18 to 20/20); (2) **131 authored feedback claims are undeliverable by construction** — their only consumer is a `stated_reasoning` checkpoint and **0 of 145** checkpoints are one, while authors wrote `intent_capture` at **38 of 38** interaction sites; (3) **275 deviations carry no `mistake` and no `cost`** one wave after `deviation-classes` shipped both. **The risk is not hypothetical**: `930b367` deleted the corpus's only `plan_consequence` condition and replaced it with `transition_feature` — one authored condition re-authored one wave later because a better primitive landed, with no rule for choosing between the two grammars (41 of 105 plan classes carry a `shapePlan` binding and **0** are graded). **Engine leverage: one capability of six is referenceable as a live condition.** `guard.evalSwingCp`/`fireOnMate` is the **only** author-declarable condition evaluated against a live engine number at play time (`guard.ts:199-229`, 6 of 37 packs); `assessedBy` syzygy/engine are authored constants checked once, for `/start/fen` only; and **DTZ/DTM, Maia policy, explorer W/D/L, Stockfish WDL/bestline/top-N have no condition surface at all** — `dtz` appears **0 times** in all three schemas, and the one Maia-shaped token (`human_model_predicted`) is **unbackable by construction** (no Maia evidence kind exists in `EVIDENCE_KINDS`). Sharpest form: **235 machine-validated records (135 Stockfish + 100 syzygy) are anchored to `/deviations/{i}/moveUci`** — the engine has already answered, at the exact pointer — and **0 of 275** deviations declare a `cost`, because the binding row was declined by two RFCs (`BACKLOG:293`). By-products: **66 `EVIDENCE_TYPE_UNBACKED` + 11 `EVIDENCE_DIGEST_STALE` + 5 ledgerless packs** measured, all **43** documents are `reviewStatus: draft` and publishing promotes the 66 warnings to errors; **`variantOf` has never appeared in `content/`** in the entire git history despite two closed friction rows citing it as shipped; **D44 re-derived unchanged at 9 of 25**. Not-expressible-and-unclaimed (14 rows; top 5): `deviation.planClassId`, per-leg `shapes`/`opponentPolicy` on trajectories, `cost`-to-engine binding, validated intent answers, D28 | `authoring-vocabulary-completeness.md` |
| Detection landscape / automatic phase, structure, tactic and event semantics for Just Play | Q4c, B2, B4, D558 | **covered `[V]` for producer taxonomy and four external-theme probes; R2 selection is now covered separately and default UX remains R3** — six evidence planes separated; every requested family routed to an exact atom, convention, search/model, corpus, theory or authored claim; 50,000 Lichess puzzles plus legal alternatives show cheap geometry is not a semantic motif (fork 32.3% precision vs tag, discovered attack 19.7%, hanging 7.9%); Lichess is disagreement evidence, not ground truth | `detection-landscape.md`, `tools/detection-landscape-harness/` |
| Current 1.0 evidence-foundation closure / which requested primitives really ship and where does each chain stop? | D558, D717, D904, D921, D925, D1011, D1017, D1023, Gate F | **covered `[V]` at 2026-08-24 HEAD — broad producer layer, incomplete product closure**: manifest is 37 producers / 193 projections / 25 core consumers / 210 bindings plus 67 semantic events. The original 17-absence list is mostly closed by Waves A/B and 12/14 Wave C ids; exact legal mobility and runtime opening identity now land as inspector-only foundations while the Review packet remains a draft; promotion race is 2 ids short; module reduction/presets/storage/bot profiles are blocked; and non-mate 2–3-ply prevention/prophylaxis is the sole newly isolated collector-level research residue. Matrix separates shipped mechanics from learner/module/bot/Review/theory/longitudinal reach so “detected” cannot be mistaken for “usable” | `foundation-capability-closure.md`, `planning/evidence-foundation-ux/phase1-gap-matrix.md` |
| Named-target removal/return under exact, Stockfish and Maia policies | D1023, D558, Wave-C collector RFC, Support/Review/bots/drills | **answered `[V]` — split by authority**: exact material-target removal is 4.10× authored / 2.85× imported but 69/120 and 130/188 played removals reappear within three plies; all-defence survival is 2/120 and 0/188. Stockfish 18 passes the frozen depth-8/depth-10 stability gate at 88/96 = 91.67%. Maia's 90%-mass gate admits 52/66/77/85 rows at bands 1000/1400/1800/2200; refused rows remain unavailable and bands are never aggregated. Exact removal/return and policy-labelled bounds may proceed to RFC; “prophylaxis,” intent, plan and quality remain theory/authored joins, not collector output | `bounded-policy-targets.md`, `tools/d1023-bounded-policy-harness/` |
| Middlegame breadth + player-style taxonomy / is the first collector wave exhaustive, and how may move evidence become a habit or personality? | Q4c, R12, O9, F5/F8/F9, D723-D762 | **taxonomy + twenty one-edge probes + first sequence census covered `[V]`/`[P]`; local exchange answered, bounded search and longer identity-preserving sequences remain GAP** — Phase 2 is an exchange-first build wave, not a complete ontology. Pawn harassment, locked pairs, defence-edge loss, exchange-exposing defender loss and material-role asymmetry discriminate in both populations. Pawn contact, future-square contest, generic/targeted slider coordination and connected rooks do not earn global presentation; blockade and king composites are population-sparse/unstable. Across 692/6,883 consecutive pairs, only 3/6 pawn-harass→minor-retreat sequences preserve the same valuable-target line. Style has three proof tiers: per-game fact, opportunity-normalized stable habit, separately validated type; current evidence permits the first two only | `middlegame-evidence-and-style-taxonomy.md`, `wave-b-breadth-probe.md`, `planning/evidence-foundation-ux/phase2-collector-audit.md`, `tools/d723-breadth-harness/`, `tools/d754-wave-b-harness/` |
| Player analysis and skills / how do game review, longitudinal habits and a skills/progression surface stay grounded (D549/D552)? | D549/D552/D553/D562/D729, O9, F6/F9, R12/R13 | **design basis answered `[V]`/`[P]` desk + synthesis** — competitor credit/score mechanisms (chess.com Skills per-move counters, Aimchess aspect scores, style-to-the-greats) publish no denominator, floor or re-derivable rule and may not be adopted as-is; our differentiator is the denominator: opportunity-normalized, phase-banded, attempt-structured, re-derivable aggregates over declared evidence. Five-category skills map sorted into today / post-2c / post-2d / Phase-3 / refused; credit rule = opportunity-normalized rate + per-metric floor + milestone tiers, with the D345/D603 anti-gaming properties; one feature vocabulary for bot personas and style axes confirmed; the D552 tip decomposes into baselined phase-split aggregates under the F1 §6.1 sealed packet; longitudinal store is the blocking dependency, R15 byte-identity extended | `player-analysis-and-skills.md` |
| Grounded skills/progression taxonomy — which current evidence events can honestly become learner credit rather than habits or Review facts? | D549/D842/D562, R20, R12, D345/D603 | **desk arm answered `[V]`; measurement blocked** — executable set-equality classified all **67** current semantic-event projections: **47 habit-only, 11 Review-only, 4 refused-as-skill, 5 exact credit candidates, 0 production-ready**. The candidates have complete legal-alternative opportunity denominators (loose-piece avoidance, double-attack conversion, mate conversion, discovered execution, promotion completion), but no longitudinal floor or versioned reference population. Neutral occurrence is never promoted to mastery; openings and strategy remain honestly empty as skill-credit categories. The accepted module join is **181 declared / 179 intended compiled / 2 awaiting**, correcting the queue's stale 175. Production requires the preregistered 25/50/100/200-game stability, phase/time-control transfer and anti-farming arms after the store lands | `grounded-skills-taxonomy.md`, `tools/r20-skills-taxonomy/` |
| Longitudinal style-feedback aggregation contract — can R12's twelve retained habits actually be produced by the accepted evidence/store foundation, and what may the LLM say? | D552/D562/D843, R21, R12/R13 | **desk contract answered `[V]`; production/transfer measurement blocked** — executable set-equality closes all **12** retained metrics and inherits their distinct 25–200-game floors, but finds **0 production-ready**: **2** need runtime opening identity/reference artifacts, **2** have a castling denominator incompatible with the generic per-decision store projection, and **8** need literal collector/store extensions (two fianchetto configurations, three clock-phase rows, three move-role/destination residuals). One feature vocabulary/two gates is explicit: learner habits pass R12 stability/privacy, bot traits independently pass controlled-policy measurement and never read learner state. Deterministic cards show value, interval, floor, population/version and exact contributors; optional LLM authority is paraphrase-only. Archetypes/GM twins/adjective diagnoses remain refused by the failed ARI gate | `longitudinal-style-feedback-contract.md`, `tools/r21-style-feedback-contract/` |
| Longitudinal projection cost / can whole-run or one-decision complete-alternative projection run on a mutation request? | D1405, longitudinal-store B6/amendment criterion 12, 500 ms post-move envelope | **answered `[V]` — no, in both shapes.** Whole-run clean-extraction p95 is 11.44/23.43/42.56 seconds at 20/40/80 plies, 23×/47×/85× over budget; 25 games take 738.8 s. The preregistered Node-24 single-decision arm over 46 edge + 13 avoidance families also fails: overall p50/p95 589.8/959.9 ms, opening p95 786.7 ms, middlegame 1,027.0 ms; SQLite publish is only 0.242 ms p95. Revision 1 therefore writes a job watermark on request and projects all semantic rows in bounded background work; incrementality removes quadratic growth but does not make the exact population interactive | `longitudinal-projection-cost.md`, `planning/longitudinal-store/d1405-longitudinal-cost-results.json`, `planning/longitudinal-store/d1405b-single-decision-results.json`, `tools/d1405-longitudinal-cost-harness/`, `tools/d1405b-single-decision-harness/` |
| Legal local exchange prerequisite / can a cheap tactical predicate reject geometric noise without becoming engine judgement? | Q4c, F1/F2/F5/F8, D730/D733/D734/D741/D749-D753/D759 | **answered `[V]` for the prerequisite, not the ontology** — legal recapture-only minimax excludes illegal pinned recaptures and king captures, admits legal along-ray capture of the pinner, preserves X-rays and runs at 0.038–0.043 ms/edge across focused runs of the disposable probe. `moved_piece_en_prise` is robustly negative-primary (0.36× authored, 0.57× imported; both upper intervals <1). Exchange-filtered `double_attack` rises to 1.72×/1.96× versus geometry 0.72×/1.00×, but the authored 95% interval crosses 1, so the exact event gets no universal positive-primary disposition. Threat/trapped abstentions and capture-vs-quiet escape semantics are explicit; Wave-B breadth and bounded-search semantics remain open | `legal-exchange-prerequisite.md`, `tools/d730-see-harness/`, `rfc/tactical-collectors.md` |
| Wave-B second breadth probe / which square, pawn, coordination, imbalance, king and defender joins deserve admission? | Q4c, F5/F8/F9, D724-D729, D754-D762 | **answered `[V]` for nine candidate events; Phase 2b remains open** — defender loss joined to a positive local exchange (4.50×/6.52× on per-probe eligible denominators) and increased material-role asymmetry (2.47×/4.35×) survive both populations. Material-signature change is even higher but too generic to spend a hint slot. Future-square pawn contest is background (0.96×/0.95×), connected rooks near 1, blockade and targeted coordination reverse, and the tested king composite is absent authored. Exact operands remain useful for hover, theory, bots and opportunity-normalized habits; global learner ranking is refused | `wave-b-breadth-probe.md`, `tools/d754-wave-b-harness/` |
| Pawn-controlled minor destination / does a pawn actually prevent a bishop or knight from using a square? | Q4c, F5/F8/F9, D724/D755/D771 | **answered `[V]`: legal reach and local safety are different** — the named minor retains the same legal destination; the exact event is a transition from locally non-losing to positive `legal-exchange@1` capture by the moved pawn. The refinement measures 1.00× authored and 1.02× imported, so it is on-demand square evidence, not a default hint or intention claim. Occupied-minor harassment remains the sharper separate event | `legal-square-denial.md`, `tools/d771-legal-denial-harness/` |
| Three-edge defender consequences / can exact identities survive reply and ground tactical sequences? | Q4c, F5/F8/F9, D728/D748/D757/D772 | **answered `[V]` for observed sequence vocabulary, not causal tactic names** — 29/6,775 imported windows lose a defender edge then positively capture the retained target; 26 capture the exact defender first. Another 13 newly expose and relocate the exact defender before taking its former target. Authored paths contain 0/622, so canonical fixtures are a prerequisite. Force, removal, deflection, overload, intent and whole-position value remain unproved | `identity-retaining-three-edge-consequences.md`, `tools/d772-three-edge-harness/` |
| Pawn conversion events / which passed-pawn transitions are stable and which are phase-bound? | Q4c, F5/F8/F9, D725/D742/D774-D777 | **answered `[V]` for five exact transitions** — moved-pawn passage is 12.46×/13.45×/7.72× over imported ply bands 1–20/21–40/41+; capture-created passage is 21.18×/14.45×/11.58×. Existing-passer advances are distinctive only late (3.17×), while protected/connected gains become reliable late. The authored 18.81× passer-advance headline is endgame-pack composition, not a universal prior | `pawn-conversion-events.md`, `tools/d774-pawn-conversion-harness/` |
| Decomposed king state / which operands survive without manufacturing “king unsafe”? | Q4c, F5/F8/F9, D727/D762/D778-D782 | **answered `[V]` for twelve exact transitions** — king-zone defender loss is 6.07×/5.12×/3.94× across imported horizon bands but reverses to 0/.07×/.38× without captures: use capture identity + defender role. Relocation-to-shelter is chiefly castling (10.08×/8.19×/5.31×); do not duplicate it. Escape reduction, slider check and zone-attacker gain become discriminating middle/later; shelter loss and mover escape gain stay mixed/background. No global king-safety composite is admitted | `decomposed-king-state.md`, `tools/d778-king-state-harness/` |
| Identity-retaining piece mobility / do exact legal and locally non-losing square-set changes ground restriction or activity? | Q4c, F5/F8/F9, D724/D726/D739/D783-D787 | **answered `[V]` for six exact transitions** — generic legal restriction is background (0.71×/1.03×); safe reduction is mild (1.14×/1.35×) and becomes background without captures (0.93×/1.05×). Moved-piece safe gain is stable but modest (1.20×/1.29×). Zero-safe is 2.58× imported but uncertain authored and cannot be called trapped without attack/reply predicates. Preserve exact square sets and piece identity | `identity-retaining-mobility.md`, `tools/d783-piece-mobility-harness/` |
| Pawn lever and candidate-passer timing / which exact pawn events survive without manufacturing a plan? | Q4c, F5/F8/F9, D725/D742/D774/D788-D793 | **answered `[V]` for four one-edge transitions + three retained sequences** — existing pawn-contact execution is 9.82×/15.07× but remains capture identity; lever creation is background (1.03×/.90×). Disclosed `candidate-majority@1` gain is 2.80×/3.30× and strongest early; advancement fades with horizon. Create→reply→execute occurs 1/622 authored and 45/6,775 imported windows; immediate candidate→passer conversion 0/1. No break/timing/plan claim admitted | `pawn-lever-and-candidate-timing.md`, `tools/d788-pawn-timing-harness/` |
| Bounded reply semantics / what does complete one-reply enumeration actually prove? | Q4c, F5/F8/F9, D741/D749/D785/D794-D801 | **answered `[V]` for exact reply breadth and two all-reply consequences** — authored/imported mean breadth 27.08/34.45; only-reply is 5.16%/.52% and corpus-shaped. Generic positive-capture threat is background (.91×/1.04×) and survives all replies 1/0 played times. Meaningful double attacks occur 10/29 times; only 0/2 retain a positive original-target capture after every reply. Expose replies/refutations/horizon; refuse forcing/winning/deeper inevitability | `bounded-reply-semantics.md`, `tools/d794-bounded-reply-harness/` |
| Basic semantic tactics / can overload, defender manipulation, clearance, interference, zwischenzug and king/promotion consequences be exact foundation events? | Q4c, F5/F6/F7/F8/F9, D832/D872/D902/D908-D909/D916-D921/D927-D931 | **Stage 0–11 partial `[V]` — basic, not optional enrichment.** Exact split tactics, strict overload, mate-through-four, promotion/Syzygy authority, runtime opening identity and typed Review operands are measured. C5 compiles 20 candidate contracts with Support 16 / Review 20 / bot 13 / inspector 20 / theory 3 reach and zero premature habit rows; pre-commit rows are rules-only and contain no move/line/eval answer. A whole-game 658-transition C4 run finds raw WDL perspective corrupted and non-engine factual overlap at 19/24 engine top-three moments. Official Stockfish 17.1→18 cross-version top-eight Jaccard is .600 for both cp and normalized WDL, so version remains an operand. Review needs typed source-local admission plus family quotas, not one cross-source scalar. Learner usefulness remains | `basic-semantic-tactics-stage-0.md`, `tools/d872-semantic-tactics-harness/` |
| Wave-A returned-contract closure / are the last ten tactical projections implementable without invented chess meaning? | Q4c, F5/F8/F9, D829–D835/D931 | **answered `[V]` for the mechanical boundary; independent RFC review remains.** Seven executable boundary tests separate adjacency from pawn support, preserve multi-base chains, remove undefined rook `cutOff`, bind discovered execution to its latency state, restrict trade to an immediate capture/recapture pair, keep loose-piece comparisons mover-relative, and make promotion absence typed. These are basic exact/convention collectors, not deep inference | `wave-a-contract-closure.md`, `tools/d829-wave-a-contract-harness/` |
| Runtime opening identity / what can the pinned catalogue truthfully say at a live node versus about a played game? | C3, F4/F7/F8/F9, D552/D694/D894/D1534 | **answered `[V]` for catalogue, imported reach, and the runtime adapter** — 3,810 source rows are 3,810 unique named endpoints; all-prefix indexing yields 7,854 keys but up to 2,023 descendant identities at one prefix. In 108 fixed imported games, named endpoints cover 401/6,991 nodes (5.7%), all catalogue paths 527 (7.5%), deepest named match median/p90 ply 4/8, and carrying the last name becomes stale in 108/108 games. The compiled local artifact, four inspector-only manifest projections, typed unavailable states, production HTTP boundary, provenance renderers, and package boundary now execute these meanings. Implementation caught and corrected one accepted-fixture error: `1.e4` is itself `B00 King's Pawn Game` while also having 2,023 descendants; the true unnamed fan-out witness after `1.d4 Nf6 2.c4 e6` has 304. Current endpoint, catalogue membership and retrospective deepest-reached remain separate; absence ≠ out of book | `runtime-opening-identity.md`, `tools/d894-opening-runtime-harness/`, `docs/runtime-opening-identity.md` |
| Shipped detector semantic conformance / does each declared matcher, reader and consumer mean the same thing? | O1-O2, F1-F3, Gate F, D629-D633 | **answered `[V]` for the current 18 structural + six transition families** — 11/18 structural families round-trip; three are reader subsets, three lossy and `pawn_count` matcher-only. All six transition families are lossy and 0/3,371 observations retain affected squares. All 18 structural matchers have positive/hard-negative fixtures; all 14 transition leaves have corpus witnesses. Five generic sinks accept whole readings, so zero whole families are universally learner-eligible without consumer-specific eligibility/selection. `outpost` indirectly exposes D566 to 23 expressions in three documents | `detector-semantic-conformance.md`, `tools/detector-conformance-harness/` |
| Transition event hand-off / do the six lossy count readings still require producer rewrites for board relations? | F1-F3, D630, D921, D1564, D1568, D1577-D1578 | **answered `[V]`: no broad rewrite.** Across 754 edges, 3,373 legacy observations still retain 0 squares, while 5,314 newer semantic facts retain 5,314 subject/move identities. Five geometry families reconstruct legacy counts edge-by-edge; independent rule events reconstruct all four legacy irreversibility leaves through an explicit compatibility priority. Live gaps are manifest forms, module admission and typed relation adapters; en-passant's victim square already exists in the admitted capture-class derivation, not raw capture | `transition-event-handoff.md`, `tools/d1577-transition-event-handoff/`, `planning/evidence-foundation-ux/d1577-transition-event-handoff.json` |
| Shipped evidence-contract topology / is there one pool from producer through selector, module and workflow? | O1-O4, F1-F6, Gate F, B4, D634 | **answered `[V]` with a negative baseline** — 14 producer paths terminate in five delivery states; only four are renderer-visible. Runtime-event and sourcing vocabularies have 0 name joins; 39 dispositions' eight free-text surfaces join 0/7 canonical IDs; only 2/7 sidecar kinds become readings; production contains 0/9 research module IDs and 0/6 workflow IDs. Transition/Maia/Explorer/opening identity never enter `EvidencePacket`; observations/plans are typed but outside its normative sentence source. F1 must declare projections and consumer/timing/form dispositions, not flatten payloads or expose more source toggles | `evidence-contract-topology.md`, `tools/evidence-topology-harness/` |
| Shipped workflow/default conformance / are primitives configurable behind opinionated presets and real ceilings? | O4/O11, R3/R15, F5/F11, B5/B10, D635-D636 | **answered `[V]` for internal architecture, participant validation still external** — six technical profiles × nine axes = 54 Settings controls but only one unnamed unset default. Only 2/6 intended workflows bind directly; Learn is mixed, Review/Analyze inherit source context, Campaign is absent. Session-kind permissions are byte-identical; Academy has no profile; Story voice ignores the imported preference. Settings now covers all nine axes, correcting the old 36-control/three-overlap claim. Separate workflow/preset state from raw primitive preferences; compose preset ∩ workflow ceiling ∩ honesty/access ∩ availability | `workflow-default-conformance.md`, `tools/workflow-default-harness/` |
| Selection, sign and significance / can a bounded evidence packet replace the raw reading? | Q4c, Q8, B4, K6, D542-D545, platform-alignment R2 | **answered `[V]`, split verdict** — local legal-alternative admission plus cap-two cuts raw volume from 8.70→0.79 entries/decision authored and 11.42→1.03 imported while raising counterfactual specificity from 18.65→93.26% and 18.23→93.84%, retaining 108/108 rare rules events. **Refusal:** the most-selected families include `piece_count`, `bishop_on_shade` and generic changed-count rows, proving distinctive ≠ significant; 1,554 `avoided` relations carry no good/bad valence. Evidence compilation therefore needs semantic eligibility before local selection/budget; the LLM performs neither | `selection-sign-and-significance.md`, `tools/r2-selection-harness/` |
| Evidence presentation / can selected evidence become quiet, comprehensible modules instead of source dumps? | Q4c, Q8, B4, K6, D582-D585/D628/D686, platform-alignment R3 | **mechanical/desk/prototype/real-packet arms answered `[V]`; OWNER USE to complete** — settings expose 54 source/mechanism controls; board lighting and the old LLM path bypass learner selection. The nine-module/five-preset/six-workflow disposable contract now passes 19 checks. Three sealed F2 fixtures evaluate 55/55 alternatives and preserve five exact selected facts, but also prove local distinctiveness is not learner significance: `occupied_defence` and `direct_attack_count` fill spare cap beside critical castling/promotion. F5 needs module-specific eligibility before selection and must leave unused budget empty. Competitor patterns support progressive intent and explanation-bound visuals, not pre-commit grading defaults | `evidence-presentation.md`, `planning/platform-alignment/evidence-presentation/participant-plan.md`, `tools/r3-presentation-harness/` |
| Sealed projection-to-component adapter feasibility | D1664, D1588, `evidence-presentation`, `module-registration` | answered `[V]` for one real admitted claim projection: 5/5 positive/negative arms prove a process-local seal + evidence-identity owner + closed digest-bound wire receipt + exact client parser can preserve admission; literal/spread/JSON, cross-owner, tamper, widening, unparsed and unregistered paths refuse. General relation/chart/citation adapters, route/seat delivery and performance remain GAP; a registered adapter is still trusted code | `sealed-component-adapter.md`, `tools/d1664-component-adapter-harness/` |
| Rules-aware Tier-2 runtime boundary | D1674, D1678, D1679, `variants` | answered `[V]` for the foundational split: chessops accepts/serializes a legal Crazyhouse drop that the shipped run refuses, and a King-of-the-Hill win is a standard draw. AST census: 159 production calls across 32 files (146 position, 11 legal-move, 2 terminal); 9 play/input/export/opponent authorities must become rules-aware while 23 standard-pack/evidence readers require an exhaustive standard-only/suppressed disposition. Does not clear sidecar packaging, all-family terminals, campaign or UX | `variant-runtime-boundary.md`, `tools/d1674-variant-runtime-harness/` |
| Variant rules + setup-family identity | D1675, D1676, D1678, D1680, `variants` | answered `[V]` for the minimum identity/import boundary: chessops collapses 20 Standard/from-position/960 aliases plus missing Variant to `chess` and ignores `SetUp`; production false-accepts From Position without FEN and false-refuses Chess960 with FEN. Five harness arms prove `rules + setupFamily` distinguishes provider capability even for identical FEN bytes. Workflow origin remains a separate admission axis; schema spelling and malformed-PGN repair policy remain RFC choices | `variant-setup-identity.md`, `tools/d1675-setup-identity-harness/` |
| Variant evidence-operation boundary | D1678, D1679, D1685, evidence foundation | answered `[V]` for the run surface: 15 producer-request calls (8 service, 7 REST) cover queue, selector/enumeration, direct tablebase, corpus and local evidence packets; queue-only suppression leaves seven direct calls. `EvidenceJobInput`, `SelectMoveRequest` and `RunStart` have no rules/setup family. Also proves prediction persists a client-chosen distribution with no subject operand. Provider internals and offline sourcing remain a separately declared population | `variant-evidence-operation-boundary.md`, `tools/d1678-evidence-operation-harness/` |
| Variant producer capability / what does honest availability mean per rules subject? | D1679, D1687, D1688, evidence foundation | answered `[V]` for the 37-producer boundary: a 37 × 3 executable matrix covers Standard, Chess960 and Tier-2 evidence-dark subjects and separates computation, learner admission and absence semantics. Provider outage is honest-empty; wrong-domain is suppressed; 12 derived producers inherit exact inputs; Fairy opponent computability does not authorize learner evidence. Producer defaults remain subordinate to 193 projection dispositions and 210 consumer bindings | `variant-producer-capability.md`, `tools/d1679-rules-capability-harness/` |
| Grounded Review Map / can a few post-game moments become preserved action instead of engine labels plus prose? | O7, F6, Q8, K4/K6, D687-D691, platform-alignment R7 | **mechanical + targeted desk arms answered `[V]`; owner use/ruling remain** — Tabiya's rewind→fork re-entry preserves both attempts, but `review.story` admits 0 F2 semantic/avoidance events, private/public top-eight policies disagree and the PNG falsely stamps every source as engine evidence. Same-mainline comparison proves local cap-one selection still yields 13/16 moments on 52/60-ply trajectories, requiring a second whole-game selector; recorded consecutive engine evals reach 20/20 openings and 0/29 middlegame/endgame mainlines, so engine pivot cannot be a general prerequisite at HEAD. Chess.com contributes ritual/explanation-bound visuals, Lichess verdict-hidden retry, Beacon reply continuity, Quackmate a one-moment/share ritual; their engine grades and causal prose are refused | `review-map-and-reentry.md`, `tools/r7-review-map-harness/`, `planning/platform-alignment/review-map/plan.md` |
| Theory↔drill current joins / can reusable theory identity launch exact rehearsal and return? | R8, O5/O6, F7, D692-D696 | **mechanical + targeted desk arms answered `[V]`; owner use/ruling remain** — 38/50 draft packs carry 44 shape refs across 21/25 shapes and 82 principle refs span 12 identities. The server reverse-resolves encountered shapes to pack IDs, but UI drops the ID and prospective refs are wrongly eligible; ShapePanel/Library/Review lack exact actions. Opening identity is 52 records/9 candidates but 0/50 draft packs. The prototype preserves source run/node, excludes prospective refs, returns theory-without-pack and candidate-only as honest empty, and shows 52 opening records map to 49 transposition keys with three multi-record identities. Architecture: exact applicability set + launch/back links + abstention, not semantic similarity as authority | `theory-drill-current-joins.md`, `tools/r8-theory-drill-harness/`, `planning/platform-alignment/theory-drill/plan.md` |
| LLM renderer contract / can a model safely word an already-selected module packet? | platform-alignment R5, Q4c, B4, F5, D146/D226/D266, D586-D589 | **answered `[V]` for 1.0 architecture** — over 16 fixed synthetic cases, deterministic rendering retained 100%; one hosted sentence arm passed the pinned run, the other falsely asserted absence and was accepted by `voiceCheck`; both hosted typed arms parsed and cited admitted IDs 16/16 yet dropped required theory citations, proving schema does not bind prose semantics. A 360M local model failed 15/16 typed cases and 3 sentence cases; the guard accepted 17/18 local hard failures. Keep deterministic rendering as the self-host/normative fallback and any LLM as a conformance-gated optional style layer after selection; reliability and learner comprehension remain downstream validation | `llm-renderer-contract.md`, `planning/platform-alignment/renderer-evaluation/results.json`, `tools/r5-renderer-harness/` |
| ~~Strong reviewer recruitment for packs A/B/C~~ | Q7 | ⛔ **closed 2026-08-13 — not a prerequisite.** Owner ruled no review workflow; packs carry a publication channel (official/community) instead, and C1 is withdrawn. Reopen only as a content-sourcing partnership wanted for its own sake | — |
| Self-host, privacy, accessibility and rights release floor (platform R18) 🟡 | O5, O12, O13, F12, D604-D615 | **mechanical/code/desk arm answered `[V]`; participant accessibility external** — a clean provider-off deployment completes and persists a rehearsal without a cloud secret, but 1.0 fails independently on account export/deletion, backup/update recovery, optional-engine health honesty, keyboard/assistive move entry, Tab focus traversal and shipped rights/notices. The 5.11 GB Maia CPU-tested image contains 18 CUDA/NVIDIA packages, 15 proprietary/licence-ref. O13 is ready; F12 is not cleared | `release-platform-audit.md`, `planning/platform-alignment/release-platform/results.md`, `planning/platform-alignment/release-platform/browser-results.json`, `tools/r18-release-platform-harness/` |
| Paid-competitor pricing/positioning | Q2 | **GAP** | — |
| Server/client stack selection (Go vs Node/TS; Svelte vs React vs vanilla; chessground/WASM interop) | branch-runtime RFC, deferred-decisions register | covered `[V]`/`[P]` — recommends TS-everywhere + Svelte 5; flags Maia-3 Python-sidecar question | `stack-selection.md` |
| Mobile — scope, tolerate or non-goal, answered against the shipped client (Q3) | **Q3**, B8, `design/02` §Platform, `design/03` §Stable application shell, Q9's phone half | **answered `[V]`/`[P]` desk + geometry harness — "tolerate, responsive-only, with a two-tier floor"**: everything that is *one board* survives a phone (374 px board at 390×844, chessground touch input, every loop verb a visible control); everything that is *N boards* does not. Measured: the compare band is **2010 px wide at 8 columns on every viewport** (240 px column floor, no viewport awareness) — **5.88 screens of horizontal pan at 390 px, 2.79 at iPad portrait, and 1.85 even at 1280×720**, so eight-way compare already overflows the desktop projection the suite tests; branch groups 1214 px at 8 members. Three shipped defects found: the two advertised compact tabs **Evidence and Session bind to nothing** (`DrillScreen.svelte:741-744` vs `:814,847`; Session is dead by construction and its role condition is inverted); the **720–992 px band** (iPad portrait) loses the fitted grid without gaining the compact tabs — the rejected document-shaped drill, untested by any projection; and the **phone viewport assertion is vacuous** (measured: global `overflow:hidden` + `#app{position:fixed}` make `scrollHeight <= clientHeight` unfailable, so "the board remains visible" is unasserted). Market: the incumbent with the best branching-study tool shipped its phone client **without** the study surfaces and took the complaints (`coverage-sweep-2-notability.md:158-164`); CET proves the PWA/chessground route on our exact stack. Verdict: phone floor = the run loop + read surfaces + 2-column compare; full-surface floor = 768 px, **currently unmet**; 4–8-way compare, branch groups ≥4, live session detail and the studio are named not-phone-shaped; mobile-native stays a non-goal. The compare-scale fix (`BACKLOG:269,313` semantic zoom, already shipped in `GroupPanel`) is the real work and it is not mobile work. Residual: no hands-on — one owner run on a real phone and tablet | `mobile-scope.md` |
| Branch/rewind/compare comprehension at 2/4/8 branches | Q9, E5 | partial `[V]`, owner n=1 real-engine walkthrough: fork/rewind quick and promising; compare selection cumbersome; scrolling shell odd; comparison-learning value not testable without the deferred instructional layer. Four/eight branches and phone remain GAP for *comprehension* — the phone **geometry** half is now answered in `mobile-scope.md` (8-way compare = 2010 px / 5.88 screens of pan at 390 px, and 1.85 screens at 1280×720) | `planning/drill-client/log.md`, `mobile-scope.md` |
| Theory/pattern sourcing incl. provenance & rights | Q6, Q7, content phase | covered `[V]` — every pack need (skeleton/evidence/prose/positions/grading) has a CC0-or-ours source; do-not-use list included | `theory-sourcing.md` |
| Deliberate-practice evidence base | thesis | covered `[P]` (observational; population skew caveat) | `arch/research/source_index.md` R01 |
| Lichess corpus scale/licensing | Q6 | covered `[P]` | `arch/research/source_index.md` R02 |
| Famous-game sources and licensing — is the masters-DB refusal at `capabilities.ts:159` supported? | [[D1043]] owner ruling 2026-08-23, [[D329]], D410/D959, Q6 | answered `[V]`/`[P]` — **refusal over-broad on the licence limb**. Lichess asserts no rights over the masters data, requires no attribution, and serves per-game master PGN publicly as a **bare score with no annotations** (live probe); `/masters` and `/lichess` have identical terms and access posture (both 401 unauthenticated). Game scores are facts not works — settled enough in the US (Feist, 499 U.S. 340), *"cannot be answered conclusively"* in Europe per the Swiss IPI. Real residual risks are **annotations** (copyrightable; already stripped at the record boundary per D410, hole at D959) and **bulk index extraction** (EU Directive 96/9/EC Art. 7(5)), neither of which is a licence bar. Recommends splitting the row into a lift + two narrow refusals, with rate-limit and no-bulk-walk obligations. Best licensed alternatives: Lichess broadcasts (CC BY-SA 4.0, 1.19M games, 2019+) and Project Gutenberg (pre-1929 **annotated** classics); TWIC and chessgames.com are 🔴 on their own terms | `famous-game-sources-licensing.md` |
| Engine stack (Stockfish UCI, Syzygy sizes) | Q5, arch | covered `[P]` | `arch/research/source_index.md` R03–R06 |
| Forum demand signals | Q1b | covered `[P]` — anecdotes, problem-shape only | `arch/research/forum_signal_log.md` |
| Titled-player training tradition (Botvinnik / Dvoretsky / Yusupov / Aagaard, named drill methods, modern GM prep) — what transfers into pack features | owner ask 2026-08-20, D549, D553, [[D717]] "connected to a workflow", `design/01` | covered `[P]` desk + `[V]` code census — the thesis IS theme-position sparring productized; **three famous methods ship dead at HEAD**: guess-the-move = `prediction` (0 packs, pack-gated so imported games can never reach it, grading deleted 0.8→0.9), Botvinnik write-before-checking = `stated_reasoning` (1 browser fixture, 0 authored packs), Dvoretsky endgame-blitz/Woodpecker clock = `TempoVerdict` (0 authored users); Yusupov pass-marks map onto consumerless `attempt_concepts`; scheduler is expanding-only so the Woodpecker (shrinking) direction is inexpressible; named non-transfers: adjournment-scale analysis, novelty hunting, peer critique, de la Maza. Rows proposed D851–D854 | `titled-player-training.md` |
| Assistance-surface taxonomy / are we making full use of the evidence primitives, and how are they smartly exposed and filtered (owner ask 2026-08-22)? | Phase-3/Phase-5 RFCs (`planning/evidence-foundation-ux/plan.md`), D841 module composition, D717/D745/D842/D860/D869/D870, `design/05` §3/§3-forms | **covered `[V]`/`[P]` desk + `[V]` code derivation — "seven axes are enough; the gap is named cells, not primitives"**: timing × initiative × subject × form × valence × disclosure-cost × grounding holds all 85 freshly-fetched field rows (chess.com 27 / Lichess 20 / eight specialists 38) with Lichess classification constants pinned from source (win%-drop ≥ .10/.20/.30, accuracy fit constants, the 4×-stricter practice ladder) and chess.com's expected-points bands; our coverage map reads 14 served / 13 plausible / 7 needs-collector-or-store / 8 refused-with-ruled-transformation. **Move classification by engine delta is verified absent at HEAD (the words exist only in `voice.ts` `BANNED_JUDGEMENTS`) and is FREE**: rung-2 reading + published convention, word+number+threshold co-rendered, post-commit/review only, praise classes refused, never rating-conditioned. Six cell families are novel to us (avoided-reading with denominator, opportunity-normalized habits, attempt-unit metrics, explainable bot miss, prediction-vs-human-distribution, prophylaxis reading). Exposure model: one rule per axis onto `preset ∩ ceiling ∩ role ∩ availability`; initiative is the only axis a preset raises. DESIGN-GAP: no sound form in §3-forms; no at-commit timing slot. Rows proposed D873–D877 (D872 landed in flight) | `assistance-surface-taxonomy.md` |
| Shared candidate-event population for bots, semantic hints and Review | D1071, D1072, D1066, D813, D816, D1570-D1580 | **contract shape and buildability answered `[V]`; Node-24 cache unit returned `[V]` 2026-08-26**: the original six falsifiers still reproduce. Across 64 roots, a complete packet is 3,779 / 5,482 / 5,803 events and 5.13 / 7.44 / 7.88 MB structural JSON; only 41/67 declared event projections appear, so code—not the sample—must own closure. The amended F1 tuple, literal engine inputs, one-process service, full-FEN/scope identity, runtime reading migration and node-free evaluation source close D1570–D1576. The release receipt then falsifies equal retained-item weight: eight event packets add 52.28 MB heap while eight mixed packets add 91.78 MB; added readings cost 4.31× an event per heap item. `events + 5×readings` retains six mixed roots / 67.17 MB under 52,975/56,000 weight, but no numeric core/cpu memory ceiling exists to call that a release pass. Lower primitive remains correct; weighted-unit/F12 threshold cross-review remains before acceptance | `shared-candidate-evidence-packet.md`, `shared-candidate-packet-buildability.md`, `planning/evidence-foundation-ux/d1573-candidate-packet-envelope.json`, `planning/evidence-foundation-ux/d1579-candidate-packet-node24-envelope.json`, `tools/d1071-candidate-packet-harness/` |
| Training-mode variant catalogue — engagement formats across the field (Lucas Chess, chess.com/Lichess/ChessKid/Aimchess/Magnus Trainer/Listudy/Chess Hero, the teaching tradition) mapped to the `design/06` §5 encounter vocabulary, plus didactic reductions and material progression (owner asks 2026-08-22) | D869/D870 (variant family), D860/D862 (dead mechanisms), D549/D297 (progression), `design/06` §5, [[D327]]/[[D328]] boundary | **covered `[V]` source-level + `[P]` desk — "formats are cheap; verdict producers are the scarce thing"**: Lucas Chess enumerated `[V]` from its own repo (~46 named modes reducing to six verdict mechanics; "Boxing" is the Resistance Test's internal codename, no "maze" mode exists); commercial field ~20 formats in six clusters, none grading avoidance, threat detection, or prediction-vs-banded-distribution — the three things our evidence uniquely grounds. Of 30 mapped formats, 21 seal under the two shipped producers + D869's third shape; 4 need the one flagged fourth shape (score-threshold over an unbounded run — owner-tier); 5 shouldn't be encounters. Didactic reductions priced: reduced-material standard-board starts are fully instrumented legal chess (tablebase-exact ≤7); smaller boards and fairy pieces exit the evidence plane (Fairy-Stockfish the lone instrument); balance rule proposed owner-tier. Novel set ranked: avoid-the-blunder, band-split solitaire, threat-radar, hold-under-shrinking-clock, play-the-structure, defender-chain hunt, brain-with-banded-hand. Rows proposed D883–D891 (head moved twice in flight) | `training-mode-variants.md` |
| Non-Maia bot composition — is `HumanPolicyModel` a slot, and can Fairy-Stockfish fill it? (owner ruling [[D1153]], 2026-08-23) | D1153, `rfc/variants.md` OQ1/OQ3 (blocking), `rfc/bot-policy.md` §2.1/§7/§9, D1034, D819, D843, D810 | **answered `[V]` at the symbol + `[V]` external — "the slot wants a policy head, not an engine"**: the shipped base-layer contract is a normalized mass over every legal move (`BotPolicyCandidateInput.rawMass`, completeness = Σ rawMass, `p^(1/T)` re-tempering), and `BotPolicyInput` names Maia in the type. Fairy-Stockfish is alpha-beta + NNUE *evaluation* — no policy output in its full registered option table — so the sampler has nothing to reconstruct and the 0.97 completeness gate would pass vacuously on a cp-derived distribution. GPL-3.0 ⟂ AGPL-3.0 permitted by `LICENSE:552-558`; sidecar-able, but **shipped Stockfish 18 + `UCI_Chess960` already covers Tier 1**, so Fairy-Stockfish is a Tier-2 dependency. Reframe: Maia-1 *is* an Lc0 net, and lc0 emits per-move policy priors and supports 960 — the 960 blocker is absent human-trained 960 weights, not an absent instrument. New defect found: the pinned maia3 sidecar builds `chess.Board(fen)` with no `chess960=True`, so it cannot *parse* 960 (silent castling failure), which is stronger than "Maia is dark". Human-likeness of an engine composition is the documented candidate-roulette family and remains an open measurement; §5 designs it (move-match on committed R11 captures first, free; then D333 ladder under D341 seeding, 500–800 games/arm, Regan–Haworth (s,c) + Chabris blunder-tail acceptance). Label rule binds: any non-Maia profile ships `uncalibrated` and 960 results stay unrated. Variants OQ1 recommended answer: **960 ships an engine-composed opponent, disclosed and labelled uncalibrated**, measurement in parallel | `non-maia-bot-composition.md` |
| Engine-composed rating-band discriminator (D1163) | D1162-D1163, D1166, bot roster Gate 0 | **answered `[V]` with an abstaining formal verdict and adverse direction** — the positive control was not informative (Maia 1400/1600/1800 peaked on human 1600/1800/1800), so the preregistered verdict abstains. All five engine-derived profiles nevertheless peaked on human 1800; temperature lowered match magnitude without producing lower-band identity. Do not fund the game ladder from this screen; do not label the family human-like/persona/Elo. Raw inputs remain a reproducibility debt | `engine-composed-band-discriminator.md`, `planning/platform-alignment/bot-policy/d1163-engine-composed-results.json`, `tools/d1163-engine-composed-bot-harness/` |
| UX — arrival and getting into a session (first run, home, choosing a drill/opponent/preset, into the board) — owner ask 2026-08-24 *"from a user perspective per feature… PROPER UX"* | [[D484]], [[D494]], [[D1427]] Q6, [[D1451]], [[D1452]], `rfc/intent-presets.md` §7/§8.1, `rfc/bot-roster.md`, `rfc/theming.md`, B1/B7 | covered `[V]` for our own surfaces (source read at `8a65a34`, `make graduation-report`, `content/` census) + `[P]` desk for every competitor claim — **no competitor was used hands-on except CET**; four owner rulings requested (first-run legality, guest-then-claim, unreviewed-content disclosure, pre-run preset/opponent surface ownership); DESIGN-GAP on `03` §Learn and return's unbuilt phase navigation | `ux-arrival-and-start.md` |
| In-run UX from the learner's side — asking for help, the hint ladder, marks/highlights/arrows, surfacing evidence without a census, honest empty, and the moment after a committed move (owner diagnosis: *"a LOT of evidence collectors, and piss poor UX for a user to actually experience them"*) | `rfc/module-registration.md`, `rfc/evidence-presentation.md`, `rfc/intent-presets.md` §7.1, `rfc/hint-distance.md`, `rfc/play-composition.md` Discharge D1, `planning/evidence-foundation-ux/plan.md` Phase 4, [[D1430]]/[[D1431]]/[[D1434]]/[[D1447]] | covered `[V]` shipped-state (derived at `2b69ee8`, re-verified unchanged at `a76769e7`) + `[P]` competitor, re-labelled per claim from landed dossiers; **no product driven hands-on this pass**. Finds **the product has no learner-facing verbs** — all nine assistance controls in `DrillScreen.svelte` name a *producer* rather than a question, so asking for help requires knowing our architecture; measured census load is up to 11 captions / 19 marks / 9 squares per square selection and 58 observations per position at the median (max 97); quiet states read as breakage rather than as answers | `ux-in-run.md` |
| Core-loop UX from the user's side — commit, consequence, rewind, branch, compare, replay-at-resistance; the expectation-versus-doctrine tension; the compare-visualisation answer and its selection gate | owner commission 2026-08-24, B1/B3 residuals, `rfc/evidence-presentation.md`, `rfc/module-registration.md`, [[D945]], [[D78]]/[[D542]], [[D687]]–[[D689]] | covered `[V]` shipped-state (read at `f2ddba55`) + `[P]` competitor (desk, inherited; no hands-on this pass); **no participant evidence**; surfaces two unreconciled corpus contradictions (Chessiverse, chessfeed.ai) | `ux-core-loop.md` |
| Post-run UX per feature — run end, two-tier review and its selector, progression/act close, earned rewinds, the return offer (owner ask: *"from a user perspective per feature… PROPER UX"*) | [[D1421]]–[[D1424]], [[D945]], [[D1151]], [[D1273]]; `rfc/review-map.md` §§3–7, `rfc/return-scheduling.md`, `rfc/campaign-core.md` D6, `rfc/move-quality-grades.md` `grade-convention@2` | **shipped-baseline arm answered `[V]`; competitor arm inherited `[P]`/`[V]`-by-citation; owner-use arm open.** Every post-run string re-read at HEAD (three stale audit claims corrected, four live defects found); the D1421 arithmetic recomputed independently; two standing dossiers erratum'd in place. **No new competitor pass was run and nothing was reproduced hands-on.** Seven owner decisions named; six blockers recorded, of which [[D1445]] and [[D1409]] gate the whole grade surface | `ux-after-the-run.md` |
| Live, casting, following and social play from the user's side — discovering casting exists, cast setup and the audience relationship, the viewer's side, following a broadcast, streamer mode, inviting a human under native-first (owner ask: *"streamer/casting/etc it's just a form you fill in with ZERO info on what it is… no screens, no explanation"*) | `rfc/live-following.md` criterion 12, `rfc/social-play.md` §5 (returned by [[D1414]]), [[D1272]], [[D1451]], `design/02:101-103`, `design/03:83,90` | code arms `[V]` at `f2ddba55`; competitor arms `[P]`/`[V]`-by-citation inherited from eight landed dossiers, **none of them hands-on with a live product UI**; the recommended UX is `[M]` design argument over those two. **DESIGN-GAP: there is no viewer** — `PublicTokenRecord` admits only `story_read` and `session_join` (`storage.ts:168-169`), so nobody without a Tabiya account can watch a live board, while `design/03:83,90` promise spectator-safe read-only views. Also finds [[D1414]] falsifies `social-play` §5's fixed *"no clock · no rating"* header, and that `live-following` criterion 12's **grep** assertion makes the explained casting screen fail its own suite unless diff-scoped. Three owner decisions named | `ux-live-and-social.md` |
| Settings, appearance, theming, account and the visual identity — the theme picker and preview, mode and silent substitution, the 72 settings controls, what stays a setting versus what becomes a flow (owner ask: *"which app doesn't even have a effing preview for the themes? why are then 10 million options in settings as opposed to proper flows for presets"*) | `rfc/theming.md` (accepted, D1/D5 open), `rfc/intent-presets.md` §7.1 (amended 2026-08-24), `design/03` shell table + B1/B8, `design/05` §3-forms O4 + §3a, [[D484]] [[D839]] [[D875]] [[D976]] [[D982]] [[D1425]] [[D1426]] [[D1433]] | covered `[V]` shipped-state — every count measured this pass with commands inline — plus `[V]` primary-source fetches of lila's `Theme.scala`, `PieceSet.scala` and `ui/dasher`; **no competitor product was driven hands-on this pass**. Finds **the named layer exists twice in this codebase and ships zero times**: five assistance presets carry authored labels and promise sentences (`presets.ts:31-37`) that **no `.svelte` file imports**, and the settings page ships the raw nine-field enum matrix eight times over instead; appearance ships **three** of a ten-scheme catalog as a native `<select>` with no preview | `ux-settings-and-identity.md` |
| The opponent — choosing one, the bot card, knowing who you are playing, what a strength number may claim, personality inside law 8, losing to a bot, and the cross-session relationship; answers the owner's two bot asks verbatim (*"how do we even define our bots… a set of knobs"*, *"nice bots that play human / with personalities"*) | `rfc/bot-roster.md` §4/§6, `rfc/bot-policy.md` O8.2/§2.1/§7/§9, `rfc/player-style.md` §6, [[D1084]], [[D1181]], [[D1184]], [[D1473]], `design/05:536-538` | covered `[V]` shipped-state plus `[V]` arithmetic over shipped tables; competitor arms `[P]` desk, none hands-on. Finds the product **ships two opponents and describes neither** (zero occurrences of `avatar` anywhere in `apps/web/src`); **Just Play silently plays Maia at band 1500** — Maia's UCI spin default, not one of the four measured rungs, chosen by nobody and never disclosed; `BOT_POLICY_PROFILES = compileBotPolicyCatalog([])` is **empty** (8/12 specified profiles blocked because `searchBound` cannot express depth, 4 because nothing populates `candidate.traits`); and the persona filter is an eight-adjective word list that a law-8 violation needs no banned word to pass. Names **observed traits** (already licensed at `bot-policy.md:161`) as what turns a rung into a someone | `ux-opponents.md` |
| The coach's side — the classroom itself, assigning work, receiving it back, mid-session coaching, the review rail and the cohort standing — plus its mirror, the learner who is in a classroom | `rfc/archive/teacher-surface.md` (accepted and implemented 2026-08-22, `24e3379`), `rfc/learner-rating.md` §10a, [[D1458]], [[D1130]] | code arms `[V]` at `f95aed8b`, read at source; competitor arms `[P]` and **structurally thin** — the two nearest competitors (Chess.com Classroom, Lichess Classes) have **no teardown at all**, and per [[D1458]] no product in this corpus has ever been driven hands-on; the recommended UX is `[M]` over those two. Finds **a complete permissions architecture with almost no user experience on top of it**, and that the one question a coach actually asks — *who needs me?* — is the one the surface cannot answer, in either tense. Four owner decisions named; two intent-tier changes named and not written (law 5) | `ux-teacher-and-classroom.md` |
| Bringing games in and owning your data — importing a game or study, the account and sign-in, what is stored about a learner, export, deletion, and privacy as a product surface | `rfc/archive/portable-account-data.md`, `rfc/archive/learner-identity-and-authorization.md`, `rfc/archive/game-import-and-story.md`, `docs/account-data-lifecycle.md`, `docs/identity-and-authorization.md`, `docs/game-import-and-story.md`, `design/03` B6/B8, `design/02` §Deployment | covered `[V]` shipped-state, measured this pass. Finds **the product's only complete, honest statement of what it knows about a learner is the thing it shows them on their way out the door** — the deletion preview enumerates four categorised record classes with live counts under `aria-live="polite"`, over a storage inventory of **forty tables in twelve data classes** (`account-data.ts:38-87`), while the Account section above it names five nouns and the word *privacy* appears **zero** times in the entire web client. The paired defect: **import is correctly a capability and built like an afterthought** (four unrelated forms on four screens, none reachable from anything called "import"), while **the account is incorrectly a gate and built like a vault** (an unrecoverable password wall in front of every route, including the only anonymous page). Boundary with `ux-arrival-and-start.md` (guest question) and `ux-settings-and-identity.md` (seating) stated | `ux-import-and-account.md` |
| Authoring and library UX from the user's side — Pack Studio, the shape editor, validation/provenance/graduation while authoring, and whether a browse surface over shapes, principles and theory has a learner reason to exist | owner commission 2026-08-24, B6/B7/B11, `design/04` §0/§8, [[D1434]], [[D695]], [[D700]], [[D1394]]/[[D1396]], [[D103]], [[D692]]/[[D693]] | covered `[V]` shipped-state (read at `f95aed8b`) + `[V]` corpus measurement (56 drafts, 36 candidates, 25 shapes, 13 principles, 293 graduation conditions) + `[P]` competitor economics/policy; **screen anatomy for authoring is `[M]` — no competitor authoring editor has ever been seen at any label, which is narrower than [[D1458]]**; no participant evidence. Finds the authoring capability shipped server-side and unwired client-side, and `/library` a strictly worse duplicate of two routes while 284 authored knowledge units are reachable only mid-run | `ux-authoring-and-library.md` |
| The campaign from the user's side — acts, floors and layers, the encounter vocabulary, the deck, earned rewinds, difficulty, the failure state, and being part-way through a run | `design/06-campaign.md`, `rfc/campaign-core.md` §5.2 / criterion 15 / Deviations 4–6, ADR-0007, [[D945]], [[D1300]], [[D277]], `roguelike-run-design.md` §4d | covered `[V]` shipped-state and arithmetic over shipped tables; prior-art/competitor arms `[P]`. **DESIGN-GAP (major): the campaign has no door and the product's own map does not contain it** — a case-insensitive grep for `campaign` over `design/00`–`05` returns **zero** hits, including the document `CLAUDE.md` calls the complete surface map; `router.ts:22-32` holds nine routes and none is `/campaign`; `PLANNED_SURFACES` is an empty array. **The mechanism is built, correct, and has zero consumers** — five files ship it, a repo-wide grep for its symbols outside them returns zero, and `content/campaigns/` does not exist, so zero campaigns are authored. The reward pool is **eight usable, not ten**: `threat_radar` is unlockable, grantable and can never turn on, and switching preset to use an earned module silently removes four others. `CAMPAIGN_PATH_WIDTH` returns zero hits repo-wide, so a fully linear campaign validates silently. [[D1300]] — whether a campaign can be lost — is put from the user's side and deliberately not answered | `ux-campaign.md` |
| Accessibility and small screens — the cross-cutting pass: keyboard, screen reader, contrast, colour vision, motion, touch and phones, **across every surface** rather than one | [[D1447]], [[D1130]], `design/05` five regions, B8, `mobile-scope.md` (Q3) | covered `[V]` shipped-state at `acb17fa6` (every cited file re-read; `board-input.ts` excluded as concurrently modified) + `[P]` device-geometry inference. **The product is not usable end-to-end by keyboard**: `DrillScreen.svelte:497-510` classifies every `<button>` and `<a>` in the composed path as an interactive target and suppresses the entire drill shortcut set, so the twelve shortcuts `KeyboardHelp.svelte` advertises are live **only** while focus rests on one of two `tabindex="-1"` container divs — one Tab kills them all with no announcement and no documented way back; ten further breaks in §3.2. On phones the three-tab drawer is `position: fixed` at `min(68dvh, 38rem)` over a board that provably does not move, occluding ~42% of it `[P]`, and the run **refuses to mount below 360×680**, excluding iPhone SE, every phone in landscape and the iPhone 12–15 Pro class at reported iOS Safari `innerHeight` `[P]`. Exists because the one accessibility defect on record was found **by accident** — a defect class with no instrument pointed at it | `ux-accessibility-and-mobile.md` |
| AssistanceConfig shared-resource identity / can hint and preset work move one persisted closed contract safely? | D1581, `hint-distance`, `intent-presets`, RFC-0000 rule 7 | **answered `[V]` for the live shape/history/persistence boundary and `[M]` for the narrow checker design; pre-review correction [[D1627]].** The runtime exports head 4 plus nine closed axes; web owns complete v1/v2/v3→v4 migrations under one stable localStorage key. The existing declaration census sees 32 axis/member subjects but omits numeric `version=4` and cannot reserve a head. Register a TypeChecker-normalized semantic domain digest and a single head+1 writer; local/imported/readonly-tuple aliases must resolve because Guided Hint's v5 is `"off" | HintRung`. Presets consume v5 without a second claim. Registration alone does not bind the hand-written browser parser; [[D1629]] routes that product conformance into the v5 landing | `assistance-config-shared-resource.md` |
| Campaign failure resource / consequence without taxing the failed move (R12) | [[D1515]], [[D1496]], [[D1565]], campaign-core author return | **mechanism search answered `[V]` structurally / felt experience still open.** Primary SRDs separate consequence from a player-elected resistance spend and use a second carried state after a short buffer; Hades is the opposite control, adding resilience after death. A Node-24 finite-state harness runs all 512 outcomes of the accepted 3×3 campaign: global HP completes 46 and carries Act-I failure into Act III; act HP completes 64 and retains automatic failure attribution; shared rewind charges complete all but retain cross-act pricing; act-reset acquired-tool availability completes all, has learner-choice debit authority and adds no counter. Recommended research family: rest an owned affordance, never ownership/theory/Review, until the act boundary. R12 remains owner-blocked on the no-tool second stage and has no licensed tuning | `campaign-failure-resource.md`, `tools/d1515-failure-resource-harness/` |
| Engine↔tablebase corroboration on the committed endgame population | D143, B4, D916/D917/D928 | **answered `[V]` at one fixed engine/version/bound; production normalization refused.** All 288 distinct committed Syzygy FENs were evaluated twice through Stockfish 18 at the depth-22 authoring profile; 288/288 observations repeated byte-for-byte. Non-terminal decisive sign agrees 210/210; terminal values remain five typed `mate 0` losses and eleven `cp 0` draws. Exact draws have max \|cp\| 21 and the nearest decisive cp is 135, so the preregistered ±25 screen separates this population 288/288. This qualifies an independent validation population, not a universal scalar: Syzygy W/D/L/DTZ/DTM and bounded/versioned cp/mate remain source-local operands | `engine-tablebase-corroboration.md`, `planning/d143-engine-tablebase-corroboration/results*.json`, `tools/d143-engine-tablebase-corroboration-harness/` |
