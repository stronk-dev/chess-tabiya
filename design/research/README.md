# Research — coverage matrix and house rules

Research dossiers land here as `<topic>.md`. This README is the coverage matrix: what
has been researched, how well, and what it feeds. A dossier exists when research lands —
no empty stubs; GAP rows below are the queue.

## Coverage limits — read before trusting the matrix

Two structural limits, both proven by owner finds rather than admitted in advance:

1. **The matrix is a snapshot, not a watch.** The 28 products came from the frozen
   v2 brief's desk sweep. Nothing monitors new entrants — **ChessMotive** (owner
   find, 2026-08-12) and **taketaketake** (owner find, 2026-08-14) were both
   absent, and both were found by the owner, not the process. The failure also
   runs backwards in time: **365Chess** (owner find, 2026-08-14) is a 2007
   incumbent squarely on the gap sweep's cluster-10 shelf that the sweep still
   missed — a sweep tuned for 2024–2026 novelty does not see old incumbents
   either (`teardown-365chess-desk.md` §Method).
2. **The frame was "chess training tools", and the product outgrew the frame.**
   The sweep asked who helps you *practice*. The product now has spectating,
   live sessions, share links, and (proposed) game-story summaries — surfaces
   whose competitors are *fan and viewing* apps that the training frame never
   searched. A competitor to a surface we ship is in scope regardless of which
   category shelf it sits on.

**What a teardown answers (widened 2026-08-14, owner ruling):** three questions,
not one — (1) does it threaten E1; (2) what is its one good feature and through
which `design/05` invariant does it enter (adoption posture, `design/02`); and
(3) **why do people love it and why do people hate it** — reviews, forums,
churn complaints, with evidence labels. The third is the going-wide question:
the strategy is breadth ("the end-all-be-all"), so knowing what makes each
one-feature product beloved or abandoned is product research, not competitive
hygiene. A teardown without a love/hate section is incomplete.

Rule going forward: **when a new surface lands, ask which category of product
already does that one thing, and check the matrix covers its best example.**
Owned by whoever lands the surface, recorded here, no standing ceremony.

## House rules

- **Use one lightweight evidence label per factual research claim:**
  - `[V]` — directly checked in the current pass against the cited primary/authoritative
    source, a reproducible measurement, or a hands-on observation;
  - `[P]` — source-backed but partial, secondary, inherited desk research, or not yet
    reproduced hands-on;
  - `[M]` — model knowledge or analysis with no external evidence; unverified.
  These labels intentionally combine evidence basis and verification state for a small
  repo; they are not a complete source taxonomy.
- **Keep legal risk separate:** 🟢/🟡/🔴 may be added where relevant and means legal risk
  only, never evidentiary confidence.
- **Make every factual claim in a dossier traceable.** `[V]` and `[P]` require an inline
  URL or a precise living/archive source reference. Unsupported claims require `[M]`.
  A clearly bounded paragraph or table row may share one citation; repeated glyphs on
  every sentence are not required.
- **No RFC is drafted from a GAP row.** If the area is a gap, run a research pass and
  land the report first (composes with the exploration gate in `rfc/0000-rfc-process.md`).
- **Contradicting a design doc is a feature.** When research overturns design, flag
  `DESIGN-GAP:` in the dossier and escalate via `planning/exploration/log.md`; never
  silently resolve.
- Kill-criterion evidence (see `planning/exploration/gates.md`) is escalated, never
  buried.

## Inherited corpus

The archive's research (`archive/brief-v2/research/`) is **desk research**: product
pages, forums, papers — no standardized hands-on evaluation. Treat every inherited
claim as `[P]` unless a new dossier upgrades it. The package's own caveats:
`archive/brief-v2/research/research_limitations.md`.

- `source-index.md` — living continuation of the archive's R01–R45 (new entries R46+).
- `competitor-matrix.csv` — living copy of `archive/brief-v2/research/competitor_matrix.csv`
  (28 products, 20 columns). Extend/correct here; the archive original is frozen.
- `archive/brief-v2/research/claims_vs_verified.md` and `forum_signal_log.md` — frozen;
  cite as needed.

## Coverage matrix

| Area | Feeds | Status | Report |
|---|---|---|---|
| Competitor landscape (28 products, feature matrix) | Q1a, `design/02` | covered `[P]` — desk only | `competitor-matrix.csv`, `arch/02` |
| ChessMotive (owner-flagged near-competitor) | Q1a, E1, Q8 | covered `[V]` desk — E1 **intact**; narrows our compare claim | `teardown-chessmotive-desk.md` |
| Competitor value props: adopt / conflict / not-relevant vs our thesis and posture | Q1a, Q2, Q8, `design/02` | covered `[P]`, 3 claims `[V]` (WhyThisMove, Noctie, Chess Endgame Training) | `competitor-value-props.md` |
| Chess Endgame Training hands-on (latency, branching; owner's "slow/poor UX" report) | Q1a, K9 | covered `[V]` first pass (desktop; mobile/records/hint pending) | `teardown-cet.md` |
| Noctie takeback/branch persistence + feedback timing | Q1a | covered `[V]`/`[P]` desk pass; residual: takeback ground truth needs hands-on | `teardown-noctie-desk.md` |
| Chessable bot-from-course-position on strategic chapters | Q1a | covered `[V]` desk pass (one-way Chess.com handoff, no tie-back) | `teardown-chessable-desk.md` |
| Take Take Take (Carlsen social play + LLM game review; owner's game-story-slides idea) | Q1a, E1, Q1b, Q2 | covered `[V]` desk — E1 **intact**; game-story appetite validated, substance unclaimed; ADR-0005 anti-pattern shipped by a competitor | `teardown-taketaketake-desk.md` |
| Coverage-gap sweep — every shipped/specified surface vs. current market (11 clusters, 2024–2026) | Q1a, E1, `design/03` | covered `[V]` desk — 10 absent-relevant products grounded and added as matrix rows 31–40; E1 **intact** (no branch runtime found); branch groups, recovery-as-skill, and live structural naming returned nothing; teardown shortlist: Play Coach, Chessbook, ChessMind AI, Chess2Story, ChessEver | `coverage-gap-sweep.md` |
| Chess.com Practice multi-move redo + color switching | Q1a | covered `[V]` desk pass (undo destroys attempts; takeback-branch reported broken) | `teardown-chesscom-desk.md` |
| 365Chess (games-DB/explorer incumbent since 2007; missed by the sweep's cluster 10) | Q1a, E1, Q6, `design/02` adoption | covered `[V]` desk — E1 **intact** (lookup-first islands, no branches/rewind/compare; weakened-SF opponent); corpus-source verdict: additive population, unusable (no license/API), redundant via Lichess `/masters`; adoptable: per-position evidence row incl. last-played recency, via rung-4 grounded-claims invariant; love/hate: loved as OTB archive, left for no-opponents + corpus noise | `teardown-365chess-desk.md` |
| Coverage sweep 2 — by notability (old guard enumerated: platforms, databases, GUIs, courseware, publishers; Lichess + Chess.com whole-platform feature censuses) | Q1a, E1, `design/02` adoption | covered `[V]` desk — 14 absent-relevant products grounded and added as matrix CSV lines 43–56 (chessgames.com, ICC, Chess King/CT-ART, Chessity, ChessKid, Chessly, **Dr. Wolf**, Forward Chess, Chessify, ChessMonitor, OpeningTree, En Croissant, WintrChess, chessvision.ai); chess24/Magnus Trainer confirmed dead, Everyman consolidated into NIC; E1 **intact** (nearest pressure: Dr. Wolf's unlimited-undo teach-during-play; Chess.com Game Review re-entry flagged unverified); teardown shortlist: Dr. Wolf, Chessly, WintrChess, En Croissant, ChessMonitor + a scoped Chess.com whole-platform teardown (strong yes) and a scoped Lichess studies/learn-from-mistakes/practice hands-on | `coverage-sweep-2-notability.md` |
| Chess.com whole platform (Game Review re-entry; coach/practice surfaces; acquisition constellation) | Q1a, E1, Q2, `design/02` adoption | covered `[V]` desk — E1 **intact**: Game Review's only interaction is a one-ply Retry puzzle (no play-on vs an opponent); play re-entry is a manual Self-Analysis→Practice-vs-Computer chain, unlinked, attempts destroyed; coach explanations are Diamond-gated at $119.99/yr (the "$120/yr" Trustpilot complaint verified exact); M&A constellation verified (Komodo '18 integrated, Dr. Wolf '20 standalone, PMG '22 — chess24/Magnus Trainer killed, Everyman absorbed, Chessable/Aimchess standalone) — breadth bought, never integrated in-product; adoptable: the auto-offered post-game review ritual, with re-entry and preserved attempts attached | `teardown-chesscom-platform-desk.md` |
| Dr. Wolf (teach-during-play; the undo-vs-fork contrast) | Q1a, E1, `design/02` adoption | covered `[V]` desk — E1 **intact**: undo erases the attempt (mistakes harvested as isolated positions in a review queue, not attempts); "Are you certain?" blunder-guard retracts *before* the consequence (inverts commit-before-learning); band 0–1300/1500 ends where ours starts; explanation mechanism undisclosed, accuracy complaints on record, no TTT-style confabulation catch found; 4.8★/27k proves rewind-and-explain demand; adoptable: spoken post-hoc coach persona via §3a silence default + mistake-resurfacing upgraded to attempts | `teardown-drwolf-desk.md` |
| Chess2Story (game-story surface incumbent; RFC differentiator check) | Q1a, E1, game-story RFC | covered `[V]` desk — E1 **intact**; moment slides + board-jump shipped but read-only; engine-selected turning points with verified-score provenance (grounded, unlike TTT); no opponent, zero loop stages; "re-enter into play" unclaimed | `teardown-chess2story-desk.md` |
| ChessMind AI (closest stack neighbor; ADR-0005 live test case) | Q1a, E1, Q5, Q2 | covered `[V]` desk — E1 **intact**; Maia-2 in-browser ONNX verified in bundle (six bands ~1100–2000+); review prose generator undisclosed, no public confabulation catch found — ADR-0005 case unresolved, not passed; course-position→Maia sparring narrows the opening→play-out edge | `teardown-chessmindai-desk.md` |
| Chessbook (repertoire gap-finding incumbent; feeds the queued gap-finding RFC) | Q1a, E1, `design/02` adoption, audit row 48 RFC | covered `[V]` desk — E1 **intact** (opening-only, card unit, no opponent anywhere; mistakes harvested as quiz cards, not attempts); gap-finding mechanics established: position/EPD-keyed gaps = uncovered opponent replies, prioritized by expected frequency at your 200-Elo Lichess band, bounded by a user-set "1 in N games" coverage target, "go to your biggest gap" entry; SRS is FSRS move-cards (queue-flood complaint on record — evidence for our explainable ladder); §7 states the adoption contract for the RFC; love: data-driven build + speed + solo-dev community; hate: review overload, error-prone prose, no offline | `teardown-chessbook-desk.md` |
| Quick passes — WintrChess / En Croissant / ChessMonitor (sweep 2 picks #3–#5) | Q1a, E1, `design/02` adoption | covered `[V]` desk quick passes — E1 **intact** ×3; WintrChess: Stockfish + chess.com-style classification only, no prose/re-entry, public reliability complaints (trust lesson for grounding); En Croissant: workbench census — its users independently demand eval-bar hiding (validates anti-contamination default), repertoire SRS immature, our self-host constituency; ChessMonitor: honesty finding — loved numbers are mostly real-outcome records (posture-compatible) *but* the marquee FIDE-Elo estimate is a manufactured skill number with proven pull — the no-skill-numbers posture has a named, evidenced price | `quickpass-wintrChess-encroissant-chessmonitor.md` |
| Chessigma (the review-only **companion** a player recommended beside chess.com — structurally our own posture; `BACKLOG:585`) | Q1a, E1, Q2, `design/02` adoption, `docs/game-import-and-story.md` | covered `[V]` desk (raw site fetch + app stores; every paid module login-gated) — **E1 intact, and narrowed at the front**: *Bot Challenge* is the closest anyone has come to re-entry-into-play from a review — *"You blew a +3 in a real game. Pick it back up at that exact move. Same clock. Bot at your level. Finish it this time."* — further than chess.com's one-ply Retry, Chess2Story's read-only slides or Dr. Wolf's erasing undo; **but it re-enters and never preserves** (no forked attempt, no comparison, no claim of one anywhere), because its unit of truth is the aggregate metric (Chess DNA: 1,000 games × 250 metrics) not the attempt — **which relocates the `AGENTS.md` §Rejected line from "does it have a rewind button" to "does the second attempt survive to be compared"**. Business finding: **the price line runs exactly between the verdict and the doing** — analysis (the thing the incumbent just paywalled) is given away unlimited, no account, on client-side Stockfish WASM, while €12–17/mo buys the weakness model and the drills, guaranteed in Elo. Band 800–1400, i.e. below ours, for the third teardown running. Law-8 case: **the sharpest live ADR-0005 example in the corpus and it is advertised** — *"usually due to weak dark squares… That's prophecy"* and *"You played Bg5 because you wanted to…"*, with true counts laundering invented causation; defence offered is a self-reported ChessQA score, not a checker. Best idea to steal: the **restraint axis** in their public detector benchmark (*"a reviewer that stamps brilliant on ordinary moves is handing out confetti, not information"*, over-calls per 1,000 ordinary moves). 8 adoption candidates with transformations; cheapest = Conversion Trainer (we already ship both halves). **Corrects `coverage-sweep-2-notability.md`**, which filed it as a commodity free-review name-drop | `teardown-chessigma-desk.md` |
| Adoption audit — every loved competitor feature vs the shipped surface (synthesis over all teardowns/sweeps; no new external claims) | `design/02` §Adoption posture, Q1a, Q2, BACKLOG | covered — 60 features audited: 34 shipped (12 in transformed form), 7 ledgered, 19 missing; refusal set empty under the transformation ruling (conflicts constrain form, not existence); top cheap adoption: auto-offered post-game story for native runs; structural RFC candidates: runtime explorer evidence, repertoire gap-finding, stated-reasoning grading, friend-link play; 5 weaker-than-incumbent findings (runtime corpus evidence, share artifacts, voice, catalog depth, mobile); 12 ledger rows proposed | `adoption-audit.md` |
| Practical difficulty outside the ≤7-piece tablebase (R4: fixed-depth Stockfish vs the exact classifier — agreement, per-ply cost, cross-depth stability) | **R4**, campaign design doc, `rfc/resistance-spectrum.md` §7b + open question 3, Q5 (Maia availability, brushes R5) | **answered `[V]` — "no, and cost is the second reason, not the first"**: over 171 in-range positions / **2,416 moves**, the classifier §7b actually specifies (a centipawn **window** around the best move) reaches only **κ 0.577**, but changing one word to **outcome class** at ±100 cp gives **κ 1.000 / accuracy 1.000 / set match 1.000** at depths 12 and 16, zero FP, zero FN, with Stockfish holding **no** tablebase access (`tbhits 0`) — so `DESIGN-GAP:` §7b's formulation is wrong and cheaply fixable. It repairs nothing outside the range: **88.3%** of ≤7-piece positions are decided at that threshold versus **10.2%** out of range (median |eval| **43 cp**), so the classifier silently becomes a magnitude filter, and its concession set is reproduced across depth on **99.4–100%** of in-range positions but only **29–54%** out of range, never converging. Cost out of range: median **77 ms** (d8), **938 ms** (d12), **~8.6 s** (d16), **~39 s** (d20) against the `<500 ms` budget — and ×8 candidates per selection. Also: **D35 quantified** (hash carry-over moves 83.8% of evaluations and the top move on 89/171 positions; the reset costs 6 ms), and **Maia policy mass is available at 120/120 probes** in and out of range at 1100/1500/1900 with Σ policy ≥ 0.989 over a hard **20-candidate cap**, and **Maia advertises `Elo`** — settling `resistance-spectrum` open question 2 | `practical-difficulty-outside-tablebase.md`, `tools/r4-difficulty-harness/` |
| Human win/draw/loss coverage depth at 1400/1600/1800 (R9: how deep the only oracle that exists outside the tablebase actually reaches, and whether it discriminates where engines cannot) | **R9**, R4 follow-up, campaign design doc, `rfc/resistance-spectrum.md` §1d/§7b, on-ramp lane, Q6 | **answered `[V]` — "yes it discriminates, no it does not reach the middlegame"**: over 1,381 serial explorer requests through the shipped `corpus.ts` query surface plus R4's own depth-12 Stockfish probe on 279 positions, human outcomes **separate what the engine cannot** — 124 positions at |eval| < 50 cp and ≥400 games span **0.405–0.600** in score at band 1600 (0.385–0.648 at 1800) with Pearson(cp, score) of just **−0.079/−0.043**, and **475 of 2,814 (16.9%)** engine-tied move pairs (|Δcp| < 30) are separated by ≥5 pp significantly, max **22.3 pp**. But it **ends at ply ~20**: usable (≥400 games, the stated threshold — 385 is the minimum n resolving 5 pp) coverage stops at ply 20/20/21 by band, **zero games anywhere from ply 27**, and *move-level* coverage collapses far earlier (mean **4.5** usable moves of ~35 legal at ply 12–15, **0.17** at 20–23). Instrument checked three ways: the greedy most-popular walk (corpus-independent) puts choice-level coverage at ply **19–21**; the widest possible population (all 9 buckets × 6 speeds × the whole 7.83-billion-game database) is **×23.4** and moves it only to ply **23**; and the shipped 100-game floor is validated as *exactly* the 60/40 line. Honest limits measured: band transfer r = **0.93/0.94/0.81** with 7–9% side flips, transposition inflow median **3.3%**/max **93.2%**, temporal drift **0.58 pp**, and the on-ramp gap is **worse than a mismatch** — band 1000 has 45–100× fewer games at ply 12–19 and all 26 puzzle-derived on-ramp roots return **0–5 games** at every band | `human-outcome-coverage-depth.md`, `tools/r9-explorer-depth-harness/` |
| Maia policy-scalar stability, and what the shipped `practical_resistance` does with it (R5: `resistance-spectrum` acceptance criterion 5 — the 20-repeat probe — plus the consequence for a shipped feature) | **R5**, `rfc/archive/resistance-spectrum.md` §4b + acceptance criterion 5 + open question 1, `design/BACKLOG.md:260` row framing, D35's Maia analogue, Q5 | **answered `[V]` — "yes, and that is the least interesting thing here"**: over **4,260 policy probes and 940 end-to-end selections**, the scalar is bit-stable — **105/105 keys byte-identical across 20 repeats** (35 positions × bands 1100/1500/1900; candidate set, order, rank vector, policy vector, sum and argmax all 105/105; max drift **0.0**), identical across a **second container and a round-robin order** (35/35 vs arm A) and across **MultiPV 8 vs 20** (263 shared moves, 0 mismatches), so `humanConcessionMass` is exactly reproducible and the tightest observed argmax margin (**3.1e-05**) has ~8 orders of magnitude of headroom. **What varies is the `bestmove`**: identical on only **36/105 (34.3%)** keys, median 3 distinct moves per 20 repeats, max 8 — because `bestmove` is a `torch.multinomial` sample on a process-global RNG while `policy` is the raw softmax (read out of the pinned image; Temperature 0 makes it **35/35** stable). **So the by-record determinism is load-bearing for `human_common` (which plays that sample) and belt-and-braces for `practical_resistance` — the RFC hedged in the right direction for the wrong mode.** Four defects found while measuring, all deterministic: `humanConcessionMass`'s `1+1e-9` tolerance rejects a real float32 softmax → **HTTP 500 on 30/40 (75%) of in-range roots**, invisible to fixtures whose masses sum to 0.95; the vacuity gate needs *all* candidates measured, so one terminal child lets the mode play the **alphabetically first** reply under its own name (20/20 on the one root that reached it); **the D35 analogue is real** — an Elo-less request inherits the previous request's band (**6/6**, byte-equal policy vectors) and records `eloApplied` *absent*, i.e. "not band-calibrated", latent today (35/35 packs declare `targetElo`) but reachable via `POST /runs` and the pack schema, and `ucinewgame` would not fix it; and top-p can sample a `bestmove` outside the recorded candidate list (1/700 at MultiPV 8) | `maia-policy-scalar-stability.md`, `tools/r5-maia-stability-harness/` |
| Maia's band-calibrated `Elo` range — where the requested band still reaches the model, and where it stops (R10: D60/D70's measured answer, ruled 2026-08-15 *"three samples are not a boundary"*) | **R10**, `rfc/engine-request-contract.md` §9 (`bound`), `design/BACKLOG.md:114` (D70) + `:126` (D60) + `:129` (D58), `rfc/archive/resistance-spectrum.md` consumer, Q5, the Just Play difficulty row (`BACKLOG:254`) | **answered `[V]` — "the boundary is not where the output stops changing"**: over **5,080 probes / 0 errors** (51 positions × a 68-point grid, plus a descending control, two 10-Elo edge grids and a malformed-value arm) the advertised `[0, 5000]` turns out to be an **interpolation domain, not a capability claim** — `interpolate_elo` in the pinned image clamps to `[0, 5000]` and returns a **linear blend of exactly two learned vectors** (`models.py:337-348`, `:295-296`), so **every** out-of-range request is answered **byte-identically to the endpoint** (11 values × 51/51 positions, TV **0.0** — **9000 *is* 5000**, −1000 *is* 0, silently, with no error field to catch it) while **inside** the range the distribution never stops responding (all 50 adjacent 100-Elo steps changed on 50/50 non-forced positions; at 10-Elo resolution **320/320** pairs changed at *both* endpoints, min TV 0.0032 / 0.00086). So *"distinguishable adjacent bands"* is satisfied over the whole advertised range and finds nothing; the edges come from two other legs. **High edge ≈2400-2600, ordering**: the band→policy trajectory **doubles back** — median TV from band 0 peaks at **2500** then falls to **0.409** at 5000, and bands 0 and 5000 are closer to each other than either is to 2500 on TV (0.409 vs 0.702/0.787), Spearman (**0.303** vs 0.238/0.077) *and* argmax agreement (0.33 vs 0.24/0.22); `1000 vs 3000` (ρ 0.68) is barely more distant than `1100 vs 1900` (ρ 0.88). **Low edge ≈800-1000, readability**: over the 32 truncated positions the median listed policy mass is **0.990 at 800** but **0.833 at band 0** (worst position **0.70**), and the shipped selector renormalises `concedingMass / measuredMass` over exactly that list (`opponent-selector.ts:641-643`) — the *contract* fails before the model does. **Recommended publishable bound: `bandRange = { min: 1000, max: 2400 }`** — the widest grid interval that is strictly ordered *and* readable, `configured` under §9 and intersected with `advertised`; it refuses **nothing that exists** (all **63** `targetElo` values in `content/` are 1100-1939, and the emitter clamp `[1100, 2000]` sits inside it) and refuses **50** and **9000**, D60's two named values. By-products: **816/816** byte-identical under a descending sweep (no hysteresis), and a **second reachable shape of D58** — any `Elo` the engine's `int()` rejects (`1500.5`, `abc`, ``, `2e3`, `NaN`, …) silently leaves the previous band in force (**9 of 12** value forms tested, 5/5 positions each), blocked today only by `Number.isSafeInteger` at all five boundaries. **No claim is made about play quality at any band** — that needs human-referenced data, not an engine transcript | `maia-band-calibrated-range.md`, `tools/r10-maia-band-range-harness/` |
| Does Maia's per-move WDL agree with real human outcomes? — the binding experiment registered at `capabilities.ts:108` (`per-move wdl`, `unmeasured`, *"D87 compare Maia WDL with R9 ground truth"*), runnable only after D236 pinned the divisor | **`design/06-campaign.md` §2a** (the difficulty-availability axis), `design/research/engine-layer-capability-audit.md` §never-asked-for, D236, R9 as ground truth, R4/R5/R10, `work-register` §5 + cluster B, Q5, `05` rung 4 | **answered `[V]` — "split: a real second signal where popularity fails, and not the middlegame oracle"**: 1,475 Maia probes + 279 Stockfish depth-12 probes against **R9's committed explorer readings reused unchanged** (5,557 human-decided move pairs over 279 positions × bands 1400/1600/1800, **every one at ply 0–20, median ply 7–8**). **Encoding settled from source, not sampling:** **27,330/27,330** candidate rows sum to exactly **1000** because `_probabilities_to_permille` constructs them that way (`uci.py:186-193`), and **27,330/27,330** satisfy `cp == win − loss` (`:208-210`) — so **`score cp` and `wdl` are ONE output**, expected score = **0.5 + cp/2000** exactly, the frame is the root side to move (`invert_wdl`, `:203-205`), and the read is deterministic (**799/799** byte-identical over three runs, two fresh containers). **The verdict is conditional and the condition is free:** pooled over 5,379 pairs, sign agreement is Maia WDL **72.2%**, Maia policy **70.0%**, explorer play counts **76.8%**, Stockfish d12 **84.2%**, against a 50% floor and a **94.2–99.3%** measured ceiling (the ground truth's own cross-band reproducibility) — so on the whole population WDL is the *weakest* instrument and loses to a number arriving in the same HTTP response. **But split by play-count ratio it inverts**: where the two moves are within 2× (1,116 pairs, the half popularity cannot answer) WDL holds **65.1%** while the counts collapse to **54.7%** and **Maia's own policy head goes to 34.4%** — *significantly worse than chance*, tightening to **24.9%** at 1.25× — which is `05` rung 4's *popularity is not quality* as a measured inversion and a live warning for every policy-mass object incl. `humanConcessionMass`. On R9's engine-tied pairs WDL (**64.6%**) is indistinguishable from raw counts (64.1%) and beats policy (54.6%), and it orders **R9's own flagship pair backwards** (Bg4/Nc6 after 1.e4 d5 2.exd5: 22.28 pp human, −5.05 pp Maia). **It does not reach**: pooled agreement decays monotonically **81.2 → 75.3 → 68.8 → 61.2 → 47.9%** by ply bucket (16–19 is n = 73, p = 0.82 vs chance) while **Stockfish over the identical pairs is flat, 84.8 → 87.7%** — so the decay is the instrument's, not the ground truth's, and what crosses the wall is availability only (**100%** of rows carry `wdl` at ply 40+, and the within-position spread *grows* from 10.3 pp to 35.9 pp). Two structural mismatches: **path dependence** (only **281/5,222** shared rows identical between history-conditioned and bare-FEN probes, median \|Δ\| 29 cp, max 576, against a position-keyed ground truth) and a **band dial that moves without moving toward the band** (Pearson **0.021–0.044**, sign agreement 47–52%, against a real human band movement of 1.24–2.21 pp after sampling noise). Calibration: within-position spread is **half** the human spread (4.9 vs 9.2 pp — correcting the audit's 0.191 reading, which is over the whole MultiPV-20 list) and the draw rate is **35–40% low** (r 0.54–0.59). Derived threshold (read off a curve, not chosen): WDL first beats the free incumbent at **\|Δ cp\| ≥ 71**, buying **34.1%** coverage. **Consequences: `design/06` §2a unchanged and now measured rather than inherited — the middlegame stays authored; the `per-move wdl` capability's honest destination is (B) deliberately unreached with the measurement as the reason, not (A) reached.** By-products: the shipped Maia command order is confirmed repaired (**5,466/5,466** identical to `Elo`-only after `0985fa4`), and the audit's *"explorer split fetched and discarded"* claim is **stale at HEAD** — `corpus.ts` keeps it and `corpus-sentences.ts:16-20` renders it | `maia-wdl-versus-human-outcome.md`, `tools/maia-wdl-agreement-harness/` |
| **Does Maia's band move the RESULT, or only the DISTRIBUTION?** — D333/D324, the campaign's only progression denominator (D332) and its one hard prerequisite | **D333**, **D324**, **D332** (learner-Elo denominator), **D365** (Glicko), R10 (`maia-band-calibrated-range.md`), R5 (`maia-policy-scalar-stability.md`), `maia-wdl-versus-human-outcome.md` §9.5 (D291), `design/06-campaign.md` §2b, `planning/exploration/gates.md` H5 scope note, `work-register` §5 | **answered `[V]` — "it moves the result, about a third as far as its own units claim"**: **16,660 complete games / 1,049,001 Maia forward passes** over 12 arms, band against band, on a 170-position book cut from the committed pack corpus (44 of 47 draft packs; opening 69 · endgame 51 · middlegame 44), paired openings with colours swapped inside every pair and SEs **clustered on the opening**. **Every band gap separates, down to 100 points** — 1000v2400 **−289.6 Elo** [−327.4, −256.7], 1000v2000 **−260.7** [−291.0, −233.3], 1500v1800 **−69.8** [−84.4, −55.5], 1500v1600 **−22.1** [−31.8, −12.4] (p 7.7e-06), 1900v2000 **−26.9** [−37.4, −16.5] — against a same-band control at **0.4956** [0.475, 0.517] p=0.68 and a Temperature positive control at **+468 Elo**. **The number the campaign needed is the transfer ratio**, read off D324's own ladder where all four rungs share one reference: **28.90 Elo per 100 band points, ratio 0.289 [0.269, 0.309]** over the corpus, and **39.98 per 100, ratio 0.400 [0.379, 0.421]** restricted to full-material positions (≥21 pieces) — so the band is an Elo-shaped dial **not denominated in Elo**, short by a factor of 2.5–3.5. **D324's own pre-registered ladder PASSES** (0.3069 / 0.4990 / 0.6304 / 0.7652 vs a fixed band-1400 reference, monotone, all adjacent 95% CIs disjoint) **and passing it settles the wrong question** — *monotone with disjoint CIs* is a shape test that a ratio of 0.05 would also pass. **Consequences for D332**: the stated 1000→2000 journey is worth **260.7 Elo**, the whole usable range **289.6**, and the most favourable cut (full-material ladder, band 1000→2200) **479.8 [454.9, 504.7]** — against a coverage requirement of **0.714** derived from D332's own journey over R10's own range, which the corpus-wide figure reaches 29% of and the best case 56%. **The axis is ~290–480 points wide, not 1000, on any cut of the data.** **Granularity**: against a rung threshold of ~60 Elo derived from the ±60 SE of a learner's own 30-game session, a 100-band step is **not** a rung and the smallest usable step is **≈150 band points at full material / ≈208 over the corpus** — five to nine rungs across `[1000, 2400]`, not fourteen. **Two edges where the dial stops**: 2000→2400 buys **+28.9 Elo, CI [−16.7, 74.5], p=0.21**, and **what attenuates it is MATERIAL, not the phase label** — the widest gap is worth **−468.9 Elo at ≥21 pieces, −145.5 at 11–20, −72.4 at ≤10**, and a 100-step is **−28.7/−37.2** at full material (CIs excluding parity in both arms) against **−10.1/−4.0** at ≤10 pieces (CIs straddling it in both). Declared phase tracks this only because the corpus's endgame packs are its low-material ones; **below ten pieces the band is not a difficulty lever**. **Cutting on material rather than the phase label sharpens it and moves the quotable ratio**: an 800-point gap is worth **+292.2 Elo** at ≥21 pieces and **+52.9** at ≤10, and the four-rung ladder restricted to full-material positions stays monotone and widens to **479.8 Elo ± 39.4 over 1,200 band points = 0.400** [0.367, 0.433] — so the honest ratio is **≈0.40 full-board / ≈0.29 for a mixed drill book / ≈0.07 for reduced endgames**, and even the best case reaches only 56% of the 0.714 coverage requirement. Symmetric conditioning is not the cause: the `SelfElo`/`OppoElo` sensitivity arm is **−249.7**, a *smaller* gap (Δ +39.9, p=0.10). Audited before reading, because D58/D91 is the way a band ladder goes silently wrong: across **all twelve arms including every ladder rung**, a first-ply χ² permutation test fires on every gap ≥300 and on temperature, and does **not** fire on either same-band control (p 0.65 / 0.85); it is underpowered at a 100-point gap, which the outcome test on the same records is not (p 7.7e-06, 4.1e-07). By-products: **Maia is seeded at `--seed 42`** and the shipped ENTRYPOINT passes none, so two fresh sidecars replay byte-identically and `human_common` is reproducible *by process* (D340, correcting the reading of R5 §10); **an unseeded parallel harness manufactures duplicate games** — 611/611 mirrored pairs byte-identical, 50.8% duplicates, a control returning exactly 0.500 with SE exactly 0.0 (D341); policy is bit-identical at 1–2 threads and differs from the 14-thread reduction in the 7th significant figure. 0 voids in 16,660 games (rule-of-three bound < 1 in 5,553), 0 ply-cap adjudications, no engine adjudication anywhere | `maia-band-outcome-transfer.md`, `tools/d333-band-outcome-harness/` |
| Is Maia's ENDGAME play human-shaped or arbitrary? — measured against tablebase ground truth, the one regime where every legal move's result is decidable (D366, owner 2026-08-16: *"isn't that THE place where humans play so different from agent?"*) | **D366**, D333/D332 (band→result), D57, R4's "difficulty is only measurable in endgames", R9's endgame blind spot, the 12 `human_common` endgame packs | **answered `[V]` — "human-shaped, decisively; band-calibrated, not at all"**: over **1,095 probes, 0 errors** (45 tablebase-critical positions + 15 lost positions × bands 1100/1500/1900 × 6 complete repeat rounds, at the packs' own `Temperature 0.7 / TopP 0.9`) against a **uniform-random legal move null computed exactly from the tablebase**, result preservation is **88.1 / 88.9 / 91.9%** vs the coin's **67.0%** (paired **+21 to +25 pp**, cluster-bootstrap CIs excluding zero) and **all 84 errors were `win→draw` — zero `win→loss`, zero `draw→loss`**, where the coin concedes the point on 31.3% of its errors. **Not arbitrary, on four measurements**: the 84 errors live on **6 of 45 positions and 5 distinct moves**; the modal error share inside a cell is **0.95–1.00** against **0.58–0.64** if the erring move were uniform over the same dropping moves; **argmax(policy) preserves on 39/39/40 of 45**, so the errors are the model's *belief*, not the unseeded sampler's tail; and they cluster in **3 of 11 packs — all pawn-technique — with 0 errors in the other 8** and **0/378 beyond ten plies from the zeroing horizon**. Maia plays the one drawing move of ten (`e4`, not the king move) on **13/18** probes at every band. The pre-specified *plan-conservation* operationalisation **failed to discriminate** and is reported as a negative. **The band is the fiction, not the mode**: sampled preservation at 1900 vs 1100 is better on 2 / worse on 0 / tied on **43** of 45 (sign p = 0.5), while the deterministic policy *does* respond (dropping-mass falls on 30/40, **p = 0.0022**) by a mean of only **4.3 pp**. Three further finds: **only 5.1% of corpus positions on Maia's side of a pack are decisions at all** (vs 76.4% on the learner's) — its real job is resistance, which it does well (DTZ percentile **0.72–0.75** vs 0.38; slowest-losing move **61–69%** vs 23%) and which `human_common` never declares; `perfect_tablebase` picks a capture-or-pawn move **1.57×** more often than a uniform preserving move and, in a **drawn** root, degenerates to **alphabetical UCI order**; and D56's tolerance bug is **already fixed** (one float32 ulp), so `practical_resistance` is a live candidate with three named costs | `maia-endgame-fidelity.md`, `tools/d366-endgame-fidelity-harness/` |
| Broadcast/streamer and teacher/classroom — the two B5 surfaces named in `design/03` and never researched (owner find 2026-08-15) | **B5**, `design/03` §Live + B5, `design/05` §147/§316-322, `BACKLOG:61,62,97,325`, Q1a | **answered `[V]` code audit + `[P]` desk over our own corpus — "the streamer surface ships under another name; the teacher surface does not exist and is a different consent object"**: the term census is confirmed (`teacher`/`twitch`/`streamer`/`classroom`/`cohort`/`broadcast` = **0 hits each**) and **wrong about the surfaces** — `SESSION_KINDS = ["stream","academy","match"]` is a shipped closed enum (`live-types.ts:3`, SQL CHECK `storage.ts:2309`), so Twitch mode ships as `stream` and coached sessions as `academy`, with chrome-free overlay (`/live/overlay/:runId`), 2–8-option 15–600 s advisory votes, a disjoint `chat:<adapter>:<key>` relay namespace capped at 50 000 voters, host-directed board control, possession journal, friend-link `session_join` tokens, a simul wall, and session→pack distillation (**`design/03:286`'s "does NOT exist" correction is itself stale** — `distill.ts:82` is the producer). **But `session.kind` is decorative**: exactly **two** behavioural branches in the whole server, both requiring `match` — nothing distinguishes `stream` from `academy`. Honesty: §3.8's no-per-viewer-withholding ruling is correct and shipped, and `BACKLOG:97`'s F1 "streamer overlay needs per-viewer withholding" clause is stale — but B10 later made assistance **role**-parameterised (`assistance.ts:27-29`), so spectators now get strictly *less* (safe direction, `docs/live-sessions.md:126` overstated). Four defects found: **host-seated vs participant-seated match players get asymmetric rung-3 access at a consented pause**; the overlay omits its RFC-specified adapter-attribution line; the compact Session tab is dead with an inverted role condition; the public story card's "chessboard" is `<pre>` of the FEN. `DESIGN-GAP:` `05:147` names five assistance contexts against three shipped preference keys (match/stream/on-ramp all collapse to `position`). Teacher: **`teacher` appears in no design doc, RFC or line of code** — what `03` promises is *academy* (ships) and *cohorts/events* (absent, `scheduledFor` is the one producer-less atom); a roster is a **standing** relationship, not academy depth, and must not imply a grant. All six `/progress*` routes are principal-scoped, so no coach can see a student's progress; async review of one run does work via spectator grant. Competitors (our corpus): both categories are covered by nothing with a rehearsal runtime — chat-vote entertainment (Chess vs Chat) and classroom ops (Chessido, Chess.com/ChessKid Classroom); E1 undisturbed. **Verdicts: broadcast = SCOPE (finishing pass — two defect-class fixes now, the chat bridge stays behind B5's audience condition, do NOT delete the `03` promise); teacher = DEFER, trigger = a coach asks twice for something a per-run grant cannot express, and merge with the events layer. The one overstatement to correct in `03` is the events row (pack nights/cohorts/relays), not the streamer row.** Top ledger finding: **`BACKLOG:61` and `:62` still read "blocked on F3" 3 days after both shipped** — a live RFC-completion flow-back miss, and the likely cause of the owner's impression | `broadcast-and-teacher-surfaces.md` |
| Campaign effect vocabulary — what a roguelike unlock/reward/modifier can actually BE under law 8, counted, and whether there is enough of it for runs to feel different (the depth question behind the campaign cluster) | **`design/06-campaign.md`** §1/§3, R7, the campaign cluster (`BACKLOG:284`, `:285`, `:286`), `05` §3/§3-forms, ADR-0007 | **answered `[V]` — "the material exists, one level below where the design is looking"**: counted against 37 packs / 25 shapes / the frozen unions. **Perceptual: 93 distinct atoms, 49 individually nameable lenses (18 structural + 6 transition + 25 shapes), 34 attested in authored content** (3/18 structural kinds, 3/6 transition kinds and 9/25 shapes are used by nothing), plus **284 authored readable items** behind the shapes — and *supersedes* `authored-transitions-and-features.md` twice: shape plans are now **96/117 (82.1%) signed** not 27%, and `STRUCTURAL_FEATURE_KINDS` is **18**, not the fifteen that dossier counts. **Economic: there is no economy** — 8 of 14 meterable operations are unconditionally free, rewind/fork have **no counter and no refusal path but `MATCH_LIVE`**, `SIMULATE_BUDGET_EXCEEDED` is **declared and never thrown**, inventory lives in **localStorage** so nothing can be earned, and the entire achievement surface mints **7 first-time-only events, ever**; the one genuinely spendable thing already ships and prices *looking* not *retrying* — the `attempt_end` reveal window that **closes on the next committed move** (`feedback.ts:27`). **Selective: 37 packs collapse to 17 distinct encounter tuples**, the middlegame is **1 pack**, 71% of Elo-bearing packs sit at 1800, and `retryVariants` (the axis that most sounds like a modifier) has **zero runtime effect**. **Combinatorics — `DESIGN-GAP:`** `06` §1's *"the deck's slot vocabulary is those nine axes"* is refuted: 2,304 nominal states → 64 content-bearing (`arrows` renders nothing; voice/spoken/ambient are form) → the honesty gate contributes exactly **2** vectors → and because all nine axes are **monotone**, the lattice has **one maximal element**, i.e. **1 distinct build**. Against that, a slot budget over the attested lens pool gives **278,256 five-slot builds** at zero authoring cost. **Variance:** a 15-node run consumes 40.5% of the catalogue and three runs exhaust it; a phase-shaped map is *impossible* (every middle act is the same pack); one non-repeating phase-balanced run = **9 middlegame packs = 9.75 agent-hours** at the measured 65.0 min/pack, Spire parity (102 packs) = **56 h, 64% of it middlegame** — the same phase R4/R9 proved has no oracle. **Synergy is both**: authored joins ship today (the shape signatures), emergent joins are architecturally supported (any conjunction costs the 29.06 µs/ply census) but **unproven — the conjunction hypothesis is `[M]` and is the natural R11**. **Highest-leverage addition: per-lens loadout below the nine axes, with a slot budget justified by D78's measured noise floor** (58 observations/position, 13 unconditional) — the opportunity cost a deck needs does not have to be invented as scarcity that prices the thesis's own selling point, because it has already been measured | `campaign-effect-vocabulary.md` |
| Engine-layer capability audit — every instrument's published capability against what the product asks for, per-mode reproducibility against the five obligations, and a definition of "100%" (owner 2026-08-15: *"engine let's get it to 100% too"*) | **`rfc/archive/engine-request-contract.md`** §3 (the five obligations, used as the audit spine), D35/D66/D67/D72, `design/BACKLOG.md:261` (Just Play difficulty), `rfc/archive/resistance-spectrum.md` §7c, R4/R5/R9/R10 as inputs, Q5, B4 | **answered `[V]` — "the layer is not short of instruments, it is short of questions — and one shipped request asks the wrong one"**: 1,164 probes through the repo's own supervisor plus a whole-repo call-site census. **Escalation first — a live regression found while measuring: `#maia` sends `Elo`, then `SelfElo`/`OppoElo` at their advertised default 1500 (`opponent-selector.ts:493-506`), and `Elo` is an alias for that pair, so every Maia request runs at band 1500 while recording the requested band as `eloApplied`.** Measured over four command orders × 12 positions × 3 bands: the shipped order changes the policy vector on **0/12** positions between band 1000 and 2400 while `elo-only`, `elo-last` and `self-oppo` all change **12/12**, and the shipped arm is byte-identical to an `Elo 1500` request on **12/12** at both extremes. It is a **state** *and* **record** violation of the contract that shipped it (`43c6c4a`, today), it makes R10's `[1000,2400]` inert and re-opens D58/D60/D70, and **both gates miss it by construction** — the unit test asserts the broken array against a fake client, the real-engine suite sends a command shape production never uses. **Never-asked-for capabilities, censused:** Maia emits `score cp` and `wdl` on **3,456/3,456 (100%)** candidate rows and `candidateLines()` parses neither — and the `wdl` is a *second* signal, disagreeing with the policy argmax on **120/200 (60%)** probes at Spearman **0.558**, spreading **0.191** median expected score across a position's listed moves (**0.190 in the middlegame**, where R4 and R9 jointly say no oracle exists), and moving with the band on **777/779 (99.7%)** pairs; the explorer's **per-move win/draw/loss split is fetched and discarded at parse time** (`corpus.ts:61-65` sums it into `playedCount`) although `resistance-spectrum` §7c names it as *"the object practical difficulty actually wants"* and R9 measured it separating 16.9% of engine-tied pairs — while the authoring parser two directories away keeps it; `UCI_ShowWDL`/the `wdl` evidence kind is **built end-to-end with zero production producers**; MultiPV **> 1 is never requested on the evidence path** (the one client hardcodes 1); `go nodes`, `searchmoves`, local `SyzygyPath`, asymmetric `SelfElo≠OppoElo`, DTZ-as-difficulty, per-move `averageRating` and the monthly `history` series (requested, parsed, reduced to one string) are all unasked. **Reproducibility is still 3 of 5 modes and now worse than R5 left it** — the machinery improved (`enumerate`, `#strongEngine` and the evidence executor moved onto the reset prologue and state their own MultiPV, closing D66) but the regression above puts a **record** violation on all three Maia modes — with **`strong_engine` the one mode whose *answer* is irreproducible, now measured after the hash fix: two byte-identical requests on a reset engine disagree on the score for 14/51 positions (27.5%)**. Every fixed bound is **51/51** reproducible; the recommended replacement is **`go nodes 50000`** — median **98.0 ms** (cheaper than the 113.7 ms bound it replaces), 0% over the 500 ms per-call budget, flat cost (1.9× median-to-max vs 7.6× at fixed depth), and **84.3%** move-preserving. **Middlegame difficulty: "authored, unless one cheap experiment succeeds"** — `humanConcessionMass` is already instrument-agnostic (it takes an *externally classified* conceding set), so `practical_resistance` is endgame-only because of its *classifier*, not its *measure*; the one live alternative is validating Maia's `wdl` against R9's own ply-≤20 explorer ground truth, which is unrun. **"100%" defined:** every published capability is either **(A) reached** by a request satisfying all five obligations and feeding a named surface, **(B) published as deliberately unreached with the reason** (the `plan_defense`/`human_external` pattern applied to capabilities, not just modes), or **(C) named as a measured impossibility** — with no capability in a fourth, silent state, which is where most findings live. Nine gaps ordered by what they unblock | `engine-layer-capability-audit.md`, `tools/engine-capability-harness/` |
| How real roguelikes bound a run, sell the next one, and what transfers (owner 2026-08-15: *"the game-games (roguelikes) vs educational/chess games… not too long… enticing people to try new builds"*) | **`design/06-campaign.md`** §0/§2a/§2b/§2c, the campaign cluster (`BACKLOG:284-286`), R6/R7/R8, R11, `campaign-effect-vocabulary.md` as its input, `05` §1/§3 | **answered `[P]` desk + `[V]` repo counts — "three of the five run-bounding devices are refused by measurements we already made, and the genre we are in is Into the Breach, not Slay the Spire"**: six games chosen for the *device each contributes* (StS map/draft, Balatro escalating requirement + capability-suppressing boss, Hades advance-visible doors + duo boons, ITB capability-set build + turn-bounded encounter, FTL pursuit clock, Monster Train covenant ladder); execution-bound action roguelikes excluded because our encounter is **deliberation**-bound. **Run bounds:** the five devices are fixed node count, escalating requirement (Balatro **300→50,000 chips over 8 antes**), external pursuit clock (FTL), in-encounter turn limit (ITB **3–5 turns**), player-elected length (ITB **2–4 islands**, StS's three keys). **B is refused by R4+R9** (no middlegame oracle ⇒ no measurable ramp ⇒ any escalating number is law 8) and **C by the thesis** (a pursuit clock is a retry price by another name, `06` §2c). **D we already have and did not know it**: new count — **36 of 37 packs declare `authoredBoundary.plyHorizon`, median 12 ply, mean 14.2, range 7–40** (openings median 11, endgames median 24, middlegame 8), inside the owner's ~5–20 guess. Minutes are **`[M]` and unmeasurable today** — a grep for `durationMs|elapsedMs|thinkTime` across `packages/`+`apps/` returns only a test stopwatch, so **no per-attempt timing telemetry exists**; the cheapest upgrade in this dossier is one owner run with a clock. **Recommended shape: 9 nodes, 3 acts × 3, 35–55 min `[M]`**, argued from three converging numbers — catalogue (9 nodes = 24.3% / **4.1 runs** to repeat vs 15 nodes = 40.5% / 2.5), the middlegame bill (**2.2 agent-hours** to make a phase-shaped run possible vs **9.75** at 15 nodes), and node-vs-minute parity (a Spire node is a 60–90 s combat; node parity would overshoot minute parity ~4×). Acts are `06` §2a's **difficulty-availability tiers**, so **stakes escalate in decidability, not in numbers** — Act I outcome-measured (`theory_strict`), Act II authored (`human_common` + plan), Act III tablebase-measured (`perfect_tablebase`, literally unbeatable, shipped). **Eight ranked mechanisms, five costing zero authoring minutes**: (1) offered-choice lens draft with a skip — **C(34,3)=5,984** offers over **C(34,5)=278,256** builds; (2) run-defining opening choice (Neow's **four** blessings incl. the disadvantage-plus-reward one); (3) **capability-suppressing boss** (Balatro boss blind) — the highest-value mechanism nobody had proposed, law-8-legal by construction because it speaks about the learner's information, and the thing that makes the monotone assistance lattice non-monotone; (4) advance-visible mutually-exclusive routes (needs `06` §1's missing human-outcome `DecidednessGround`, 2.2–9.75 h); (5) consumable with honest expiry — **the `attempt_end` window already is one**; (6) synergy discovery, **rank conditional on R11** (moves to rank 1 if it lands, and if it fails our loadouts are additive not synergistic); (7) unlock drip (earn side is **7 lifetime milestone events**); (8) persistent ladder — only two honest dials, and **the Maia band dial is measured inert today** by `engine-layer-capability-audit.md`'s `SelfElo`/`OppoElo` regression. **Escalating numeric economy refused outright rather than imitated.** **Where it breaks, four ways:** the build **does not compound** (a lens changes what you can see; a position does not care what you know); its **marginal value goes negative** past D78's measured noise floor (median 58 observations, 8.31 entries/ply at 1.01× lift) so the optimisation is *smallest sufficient set*, not strongest set — which is ITB's shape, not Spire's; **our content decays** (275 deviation notes + 145 checkpoints over 37 packs are read-once, unlike a regenerated Spire encounter) so the catalogue argument favours **shorter** runs; and **a run that cannot be lost is a playlist** — no resource refusal exists anywhere in the runtime, so the campaign has no failure state, with the proposed resolution being to **price *declaring done*, not retrying** (rewind stays free within an encounter; the submitted attempt decides the run), flagged as an owner ruling. The feeling the loop escalates is named: **legibility, not power** | `roguelike-run-design.md` |
| What an intermediate-node consequence can BE — the hole `06` §5 names (*"a run that cannot be lost is a playlist"*) reopened by the owner's *"i kinda don't like it if all the intermediate is without consequence… we can also look into other categories of games"* | **`design/06-campaign.md`** §5 (the unresolved hole) + §1/§2c/§3, the campaign cluster, R6/R7, `roguelike-run-design.md` as its input, `00-thesis.md` §Why anyone, ADR-0005/ADR-0007 | **answered `[V]` code + `[P]` desk — "the consequence already ships, one scope level down, and what is missing is a scope rather than a mechanism"**: ten constraints enumerated from the live docs first (`experimentation without cost`; consequence-before-retry; law 8; the refused escalating economy and pursuit clock; ADR-0007; legibility-not-power; rarity-is-not-value; two-gate honesty; D78 licensing a slot budget; the live-surface admission rule), leaving a **residue of exactly three legal consequence kinds** — denominated in the learner's *information*, or *the position itself*, or *self-inflicted* — which is the test every mechanism is then run against. **The find:** `ObjectiveState` has **6** values and only **3** are absorbing (`trajectory.ts:6`), so the runtime already separates *went badly, play continues* from *ended things*; `degraded` is **one-way by validator rule** (`pack-validation.ts:469`), **produced by authored deviations not by a grader** (**76 of 275** carry `offObjective`, `pack-orchestrator.ts:367-385`), and **sealed across node boundaries** — a leg transition resets to `active` *only* from `preserved`/`degraded` and stores the outgoing verdict as `sealedState` (`pack-orchestrator.ts:556-575`, `trajectory.ts:83-92`), with node-skips recorded (`skippedLegIds`). **And the intermediate/boss split is a lint rule:** `THEORY_ABSORBING_UNSUPPORTED` means **17 of 37** packs *cannot* end a run while `win`/`hold` (13) can only from an outcome condition — and **all three canonical trajectory packs are exactly 3 legs with only the last one absorbing**, i.e. `06` §5's "2 encounters + 1 act boss" is what three authors independently wrote. **The gap is scope**: every seal is path-scoped (`types.ts:102`), so a rewind to a clean line erases it — which is the thesis working. **State audit:** `AssistanceConfig` is worse than `06` §1 says — **three localStorage keys ever** (`assistance-preference.ts:4`) and the server never receives it (`api.ts` = 0 hits); above the run there is **nothing** (`campaign|run_set|curriculum|playlist` = 0 hits), the only run→run edges are `flip_sides` and a schedule carrying **a due date and a back-reference, no gameplay state**; `SIMULATE_BUDGET_EXCEEDED` confirmed declared-and-never-thrown. **Ten mechanisms across six categories** (ITB Power Grid, Balatro's cash-out and Tag, Hanabi clue tokens, XCOM/Darkest Dungeon attrition, Dishonored Chaos, Nemesis, rally service park, Invisible Inc): **survivors** = carried board state (costs content, *collapses the map* — the thing the design grows into), sealed verdict read by the boss (**recommended for v1**), converting reveals (invented cost function, R6/R7), boss-by-census (`shapeRecommendations` is the detector and gates nothing), and the skip. **Deaths, each to a named constraint:** lens fatigue dies to **ρ = −0.143** (targeting most-used is targeting noise) *and* to the hint tax; Hanabi's refill and Dishonored's drift both die to law 8 (the refill needs a chess judgement; the drift needs an accumulator, and ours is 89.0% FP at 1.01× lift). **Assembly vs new code:** ten shipped parts listed against five new ones, smallest first — a **run-level roll-up** (`attempts` is per *branch*; "did this run succeed" is computed nowhere), the commitment point, a container above the run, server-held loadout, per-lens grain (best insertion point `structure.ts:448-450`). **Owner question: which path does a node remember** — (a) the one you're standing on when you leave (no new verb; prices diligence), (b) the one you submit (**one ruling buys both this hole and `06` §5's boss hole**), or (c) the worst you produced (prices retrying; refused explicitly). **`DESIGN-GAP:`** `06` §5 conflates *no resource refusal* (true) with *no failure state* (false — the absorbing states are one), and that conflation is why every prior proposal reached for invented scarcity. By-product defect: **the compare surface bypasses the honesty gate** (`compare-strips.ts:38` calls `pivotalMarkers`, not `liveMarkers`) | `campaign-intermediate-consequence.md` |
| Fun mechanics OUTSIDE the roguelike frame — drip, experimentation, and fun without a power curve; plus "are drills the right campaign unit" (owner 2026-08-16: *"there MUST be novel gamemodes, variations, minigames, puzzles, ANYTHING… from idle games to stardew valley… allowing people FUN in the campaign runs… and EXPERIMENTATION while LEARNING"*) | **`design/06-campaign.md`** §1/§2c/§3/§5, the campaign cluster, `roguelike-run-design.md` and `campaign-intermediate-consequence.md` as its inputs (C1–C10 inherited whole), `00-thesis.md` §Why anyone, `04-content-architecture.md` §1, `02` adoption posture, ADR-0007, Q9 | **answered `[P]` desk + `[V]` repo counts — "fun without power has exactly three devices, we own runnable versions of all three, and six of the shortlist's ten objects already ship with no consumer"**: **15 genres** past the roguelike set (idle/incremental, farming and life sims, gacha *mechanically*, MMO, legacy/campaign board games, deckbuilders, management sims, puzzle games, metroidvania, Souls-like, rhythm, speedrunning, daily/party puzzles, TTRPG, ARG) plus the chess-adjacent formats. **The three devices — asked in the one place the roguelike set could not answer, because every game in it has a power curve:** (1) **score the SOLUTION, not the outcome** (Zachtronics' three-axis histogram; Barth: a leaderboard *"tells you that you suck (and not even by how much)"* and *"is a fantastic incentive for cheating"*); (2) **make KNOWLEDGE the key** (Outer Wilds' ship log — *"learning new things is the only feedback the player gets"*; Tunic's manual; La-Mulana items that *"do not grant any abilities, but open up a new area"*); (3) **buy variety with SETUP SELECTION** (Dominion's 10-of-26 kingdom ⇒ ~5.3M configurations, no unlock system; Really Bad Chess' *"one king and 15 other pieces selected at random"*). **Repo finds, `[V]` at `c55b9cf`:** the suppression path **ships end to end** — `AssistancePermission.locked_off` (`assistance.ts:20`), the named refusal `ASSISTANCE_WITHHELD` (`rest.ts:1090`/`:1107`) and an honest disabled control (`DrillScreen.svelte:717-720`) — so Balatro's boss blind is one *inner-gate* input away, **but must not be a fourth field on `permittedAssistance`**, which is the honesty gate; `RETRY_VARIANT_KINDS` is a **five-kind variety vocabulary with 9 authored instances and ZERO consumers** (Dominion's kingdom in our schema); the cross-pack collection axis is authored, stored and indexed (`attempt_concepts`) but **namespaced apart by one injectable class** (``pack:${packId}#${raw}``, `progress.ts:56-59`) — and is **156 concepts with 132 singletons**, a tag cloud not a curriculum. **`DESIGN-GAP:` `04` §1's Pack row describes an object that does not exist** — `roots` appears in **0 of 47** packs while `start` is singular in 47/47, so the shipped "pack" is `04`'s **Root** and levels 2/3/5 (`family`/`track`/`curriculum`) have **zero code**. **Unit verdict:** `RunSessionKind = "pack" \| "position" \| "imported"`, two of three carry no pack, and `projectAttempts` forces a pack-less verdict to `"open"` (`progress.ts:90`) — therefore **intermediate nodes may be pack-less, boss nodes may not**, and the content bill is bounded by act bosses (3), not nodes (9). The owner's *"packs are micro-DLC"* is right about the **role**, one level off on the **grain**; the missing container is the **Track**. Community channel is **less unexercised than briefed** — `shape-studio.ts:33-34` ships draft→register→export with four named refusals; what has no path is a third-party *Track*. **Fact refresh discharging two standing cost arguments:** 47 packs, **11 middlegame** (commit `aee7c64`), so `06` §5's *"Act II is impossible today"* and the ledger's *"Plan Drill is essentially UNAUTHORED"* are **stale**; `plyHorizon` 46/47 median 10.5; deviations/checkpoints **330/189**; shapes attested **21 of 25** (D44: 9 orphans → 4); `timingWindows` 4 and `variantOf` 2 (both were 0); and **`content/packs/` is empty** so production still serves one pack. **Ten-item shortlist, every entry at zero authoring minutes** — top three: a **daily shared position + spoiler-free branch-shape share** (Wordle's cadence; **Chessguessr already grades against what a human played, which is our thesis in miniature**), a **three-axis histogram over the learner's OWN submissions** (plies · rungs-at-submit · seal — **Chess.com's bot crowns already ship axis 2** at 3/2/1 by hints used), and **wiring `retryVariants`**. **Two hard scoring constraints contributed:** no count of rewinds/forks/attempts may ever be an axis (C1 in scoring clothes), and the population is the learner's own history, never other learners (the ChessMonitor pull). **Direct answer to "make experimentation rewarding": Hitman** — re-entry is *rewarded*, not merely permitted; computable here as a census over the learner's own graph (`attempts.countable` + first-divergent-move), anti-farmed by the shipped `plyHorizon`. **Refused, with half surviving in two of three:** the prestige multiplier (adopt the *reset* — Gloomhaven retirement), rarity tiers (adopt the *pity guarantee*), the stamina gate (refused entire — it prices *playing*). **Literal chess variants refused, the mechanic adopted**: Fog of War is the capability-suppressing boss as a rule of chess, but `UCI_Chess960` is a published capability refusal and every grounded instrument is a standard-chess instrument. **Proposed ADR-0007 twin clause — *unlocked by playing, never by WINNING*** — from four unrelated systems (Balatro's Mr. Bones unlocks on *"Lose 5 runs"*; Dungeon World's failure is the XP trigger; WoW deleted attunements). **Owner question: what is progression denominated in — cadence/completion, the learner's own history, or the catalogue.** By-product for Q9: **5D Chess ships branching timelines as the board itself at 96% of 6,575 Steam reviews**, a strictly harder surface than ours | `fun-mechanics-outside-roguelikes.md` |
| Coaching vs cheating, and the 1000→2000 career curve (owner 2026-08-16: *"balance 'cheating' to win VS 'coaching' to win"* and *"in order to complete a run to max you'd need proper 2000 ELO skills yourself? cause the journey is 1000-2000 Elo?"*) | **`design/06-campaign.md`** §0/§1/§2a/§2b/§5, **`design/05-in-run-experience.md`** §2/§3/§3a/§3b/§3-forms, D113 (hint distance), the campaign cluster, R4/R9/R10 as inputs, law 8, `00` §Target player, H5/K5 | **answered `[V]` code + content census, `[P]` inherited — "the criterion is a THIRD axis neither shipped axis can carry, and the trajectory is a claim about the encounter, never about the learner"**. **Q1:** rung is refuted as the criterion (the owner's win condition *requires* rung 5 authored counter-theory, while rung-0 all-on is measured unreadable at 1.01× lift), and **timing is refuted as *sufficient* by the shipped code** — `feedbackDeliveryOpen` re-closes the reveal window on `move.committed` (`feedback.ts:22-30`) precisely because post-commit at move *n* is pre-commit for *n+1*, and `events.ts:154-159` already indexes disclosure on the encounter's boundedness. What survives is **distance from the answer**, four syntactic values read off what an item *says* — **`kind` · `fact` · `ranking` · `move`** — giving **"cheating iff `distance === "move"` while a committing decision still depends on it; `kind`/`fact` are coaching at any time; `ranking` is the contested band"**, plus a second, separate failure mode (**coaching fails by volume**, D78). It is **orthogonal to both shipped axes**, and it is **D113 discovered from the honesty side**. **The obstacle is a shipped decision, not a gap:** `capabilities.ts` publishes **36 dispositions (14 reached / 15 refused / 6 unmeasured / 1 impossible)** whose recurring predicate is *recording vs grading*, and `bestmove / MultiPV rank / bestline` is **`refused` product-wide** on the reason *"Move verdicts are not condition measurements"* — an answer to whether it may **fire a condition**, used to refuse **display** — so the campaign cannot hand a learner the move as a reward even after the game (**0 of 764** committed records are `bestline`). By-products: `permittedAssistance` **never reads its own `sessionKind` input** and `"solo"` is unreachable in production; the nine `AssistanceConfig` axes are **not all monotone** (5 are; `boardLighting` has a collapsed top *and* a broken floor, `spoken` is not a chain, `voice` is substitutive, `arrows` is inert), correcting `06` §1; `guided` is `"free"` unconditionally with **no server gate** and is not band-shaped. **Q2:** the product measures **neither "played well" nor "run succeeded"** — 8 comparison sites, **1** per-move verdict and it is an authored lookup (`line.ts:121-165`); the only *computed* learner judgement is `TempoVerdict` against an authored budget, with **zero authored users**; prediction grading was deleted at schema 0.8→0.9 (`7d461e1`) and replaced by a threshold-free record **nothing reads** (**0** authored `prediction` interactions); **no learner number exists anywhere**, refused in three lines of shipped copy (*"That is a grade of this attempt, not a verdict on the position"*). What it *can* say is **whether an authored or tablebase-decided objective survived the submitted path** — so *"2000 Elo skills"* is a claim about the **encounter's configuration**, and the curve can be **declared but never adapted** (named price: ChessMonitor's manufactured FIDE-Elo estimate is its most-loved number). **Levers: opponent band `[1000,2400]`** — measured, enforced with `TARGET_ELO_OUT_OF_RANGE`, and **repaired at `0985fa4` after the `SelfElo`/`OppoElo` regression**, but its link to *outcome* is unmeasured (R10 measured the distribution, not the result — the one unrun experiment); **decidability tier** (measured at both ends, authored in the middle); **encounter horizon** — `plyHorizon` on **47 of 89** documents, median 10, range 2–40, and simultaneously a difficulty *and* an honesty parameter; **loadout size and suppression**, licensed by D78 but uncalibrated. **The owner's proposal is expressible, inverted: the curve is SUPPRESSION, not accumulation, and it runs down the distance axis at a flat slot count** — full suppression makes *"2000 Elo required"* true **without measuring the learner**, and the ceiling is per phase (opening `theory_strict`; middlegame ≤2400 Maia + authored plan; endgame `perfect_tablebase`, **infinite**, so the objective must be `hold`/`save`/`convert`). The on-ramp's relaxation is a **horizon** relaxation and already ships (**24 of 26** on-ramp candidates declare `immediate_guard`). **Two `DESIGN-GAP:`-class content findings:** the declared *learner* band is continuous across 1000–2000 (**42–58 packs per 100-point bin from 1400**) while the declared *opponent* band is a two-point step function — **30 packs ≤1394, 3 real packs in 1400–1799, 39 at ≥1800** (1800 alone = 36 of 45 real packs); and the `plyHorizon` act ramp runs **11 → 8 → 24**, so `06` §5's Act II is the **shortest** encounter in the corpus. Eight preregistered falsifiers, thresholds placed off every instrument's optimality boundary. **Owner question: what is the top of the hint ladder and when may it be reached** — (a) `move` stays refused permanently (status quo, chosen deliberately; closes D113), (b) `move` reachable **at a horizon boundary only** (recommended; splits the disposition, makes the criterion one enforceable clause over a predicate the runtime already computes), (c) `move` purchasable pre-commit (the natural roguelike answer; abandons the criterion — refused on the record rather than by silence) | `coaching-versus-cheating-and-the-band-curve.md` |
| Mechanics × modes — every user-meaningful capability against Drills / Just Play / coaching-streaming-teaching / Campaign, and the half-integrations ranked (owner 2026-08-16: *"making sure we have all the right breadth in mechanics and they're not implemented half-assed, but fully integrated"*) | **B1–B11**, `design/03` §Play/§Review/§Learn/§Live/§Adaptive guidance, `design/05` §3/§3-forms/§3a/§3b/§4, `design/06` §1/§2b/§5, `BACKLOG:477` (Just Play difficulty), `rfc/archive/board-annotation.md`, `rfc/teacher-surface.md`, Q4c | **answered `[V]` code census + hands-on (live server, Playwright against the real bundle) — "there are not four modes; there is one run surface, a Live wrapper, and a Campaign with no code — so the half-integrations are at the entries, in the content, and in a handful of controls that exist on one surface and not the other"**: measured hands-on, the **Drills and Just Play run screens are the same 18/19 controls**, differing only by an inert ambient glyph; `DrillScreen` is mounted at exactly one place (`App.svelte:574`); **Live never creates a run** and neither live surface renders any authored vocabulary, `session.kind` has 4 server comparisons of which 2 are creation-time guards, and **`academy` — the owner's *coaching/teaching* mode — has zero behavioural consumers anywhere**; campaign is **0 hits** outside disposable harnesses. **Owner check 1, *"configurable as to what is exposed"*: the config is mode-aware in its ADDRESSING and not in its CONTENT** — `permittedAssistance` takes `sessionKind` and never reads it (`assistance.ts:27-30`), so pack/position/imported get byte-identical permissions and no lever exists to make Just Play more permissive than a drill; and `loadAssistance` returns `SILENT_ASSISTANCE` for **all six** profiles, so the per-context defaults `05` §3-forms requires do not exist. Six profiles is itself a correction — the *"three localStorage keys"* claim is **refuted**. **2 of 9 axes drive nothing** (`arrows` — typed, migrated, validated, *permissioned*, zero readers; `ambient` — a button with no `onclick`), **no single surface configures all nine** (6 in `/settings`, 6 in-run, 3 overlap), and the config is read once at `onMount`. The hard consequence: **`api.reveal` has no call site in the run screen**, so under the triple-fenced `attempt_end` **rungs 3, 4 and 6 are structurally unreachable during a Just Play game** — the owner's *"FULL toolkit"* is rung 0 plus a locked panel until the game ends — and the `onramp` profile is type-unreachable from Just Play. **Owner check 2, *"it steers you by classifying"*: aspirational.** All 4 non-test consumers of `classifyPhase`/`structuralReading`/`endgameReading`/`shapeFirings`/`pivotalMarkers` are renderers; **nothing selects an opponent, band, position, objective, pack, node or rung from a classification**; `shapeRecommendations` computes a `packIds` list and the client throws it away (`navigate("/play")`); `PackList` has **0** filters and `/learn` names the three phases **0** times. **The one real steer is repertoire gap finding** — and only its top-ranked gap is enterable and its write-back verb has 0 client callers. **Guided mode is inverted**: the shape library renders live and ungated in every mode while the `guided` switch gates a smaller duplicate needing `markers` on, so §3a's silence default is violated in code and §3b's band-shaping does not exist. **13 ranked half-integrations**, the top three costing an afternoon each: a reveal control (`DrillScreen.svelte:712-726`), per-context defaults (`assistance-preference.ts:30-33`), and a `tablebase:` branch in `evidence-sentences.ts:143` — the **actual** residual behind B4's Syzygy row, since the other Syzygy path already ships. **Largest promise/behaviour gap: pack-free theory recognition** — `design/03:35-39` promises it, every export in `line.ts` takes a pack, and **no design doc anywhere states a reason**. Absent-by-design set is real and cited (objectives, rung 5, engine conditions/`immediate_guard`, branch race, opponent-intent prompts, the simul wall). **11 prior figures corrected**, incl. `design/03:95` overstating the `MATCH_LIVE` list by one verb (compare is *not* refused), `03:299` stale (narrative mode ships), `variantOf` now used by 2 packs, `0 of 20` opening timing windows **still** exactly 0 (the four new windows are all middlegame), and one **withdrawn finding of this dossier's own** (a stale server process, not 10 unserved packs) | `mechanics-by-mode.md` |
| Time as a difficulty lever — is a game clock the refused pursuit clock, and does time pressure sort assistance by distance to the answer? (owner 2026-08-16: *"what if we want to simulate the time pressure of a GREAT move during 10+0 chess and then give actual time?"*, D330/D331) | **`design/06-campaign.md`** §2c/§5 (the refused list and the power/legibility dichotomy), **`design/05-in-run-experience.md`** §1/§2/§3/§3a, `00-thesis.md` §76 *"experimentation without cost"*, D78, D113, the coaching/cheating distance axis, the campaign cluster | **answered `[V]` harness + code read — "the clock clears doctrine and fails measurement: it is admissible and useless for the job it was proposed for"**. **The obstacle clears, mechanically:** the object `06` §5 refuses is the **run-pooled** clock — a rewind budget with a real-valued counter, since every second on attempt 1 is unavailable to attempt 2 — while an **attempt-scoped** clock that resets at the fork prices *deliberation*, not retrying, and §2c's own decomposition rules that only *how often* conflicts with the thesis. **The shipped placeholder already picked the safe altitude:** `clockState` is declared on **`Node`** (`types.ts:111`, schema `:265`), written only from `CommitMoveOptions` (`runtime.ts:57,341`), so a clock on the reserved field **rewinds with the board automatically** and the refused form **needs a new run-level field** — the ruling is checkable in a diff. **The hypothesis is refuted, and not narrowly.** Over **43,272 rendered items** from the shipped renderers (47 packs / 721 transitions / 609 positions, 25 shape entries, **43 real cached explorer responses**, 105 recorded Maia candidate counts), variance in log(words) explained by **distance class** is **η² = 0.201** (four classes) and **0.038** (the three the product may ship), against **η² = 0.984 for which renderer printed it**; ρ(distance rank, words) = **−0.046** all items, **+0.125** excluding `move` — the order of the ρ = −0.143 this repo already treats as no relationship. **Reading cost is renderer verbosity, not distance from the answer**: `kind` spans **6 words** (phase reading) to **298** (shape panel, 50×), `ranking` spans **12** to **230** (corpus page, 19×). **And the ordering inverts where it matters: `move` is the cheapest class the product could ever print** — median **1 word / 0.3 s** against `ranking`'s **63 / 15.9 s** — so a clock is a gradient **toward** the answer, sharply so under the coaching dossier's option (c). **The 10+0 arithmetic** (600 s / **40-move reference game — Lichess's own `initial + 40 × increment` convention**, at **238 wpm**, Brysbaert 2019): **15.0 s = 60 readable words** per learner move, against an all-on rung-0 reading of **978 words / 247 s at ONE node — 16.4× the whole budget**; two nodes = **82% of the entire 600-second game**; corpus page **3.9×**, full shape panel **5.0×**, human split **1.06×**, `bestmove` **0.02×**. **The durable residue is a derived slot number D78 licensed but never supplied: ≈6.6 `fact` items or 0.94 `ranking` items** — the campaign's five-slot loadout costs 45 words / 11.3 s, so two independent routes land on 5–7 — **and no clock is required to collect it**. **The owner's sentence literally asks for a DEPICTED clock** (*"and then give actual time"*), which touches no invariant and is **one retained field away**: `parsePgnMainline` keeps headers whole (`pgn-import.ts:63`, so `TimeControl` survives) but drops the per-move `[%clk]` comments at `:54`; and *"a GREAT move"* is not expressible — the nearest honest object is `storyMoments`' **≥150 cp recorded swing** (`story.ts:30,86`), a change and not a verdict. **Three clock-named objects, none a clock:** `clockState` (6 passthrough refs, zero readers, no client sends it, `additionalProperties: true` so it will persist anything), **`clock_zeroed` emitted 0 times in 721 transitions** while shipping a renderer sentence (dead code), and `timingWindows`' move-counted `luxuryMoveBudget`. **`DESIGN-GAP:` `06` §5 twice** — the refused list is one word too broad, and *"what escalates is LEGIBILITY, not power"* has no slot for a device that shrinks the **learner's** capacity. **By-product correction: D78's "median 58 observations per position" is 78** on the 47-pack corpus while the mean moved only 57.90 → 62.46 — a bimodal-by-phase distribution whose median sits in the gap (endgame 256 words / 65 s, middlegame 1,199 / 302 s), so the per-phase figures should replace it wherever it is cited. **Owner question: is time nothing, a decoration, or a rule** — (a) close the cluster and delete the reserved field, banking the ≈6-item loadout; (b) the depicted clock (recommended, cheapest); (c) the enforced attempt-scoped clock — admissible, but it amends `06` §5 twice and **a run-pooled budget must be refused in the same ruling**, because the difference is one field's altitude and nothing today would catch it | `time-as-a-difficulty-lever.md`, `tools/d355-reading-cost-harness/` |
| Target learner/coach problem interviews + concept preference test | Q1b, E2 | **GAP** | — |
| Learning effect versus simpler formats | Q1c, H1–H4, C2–C4 | **GAP** — requires slice | — |
| Maia-3 capabilities (models, conditioning, sampling) | Q5 | covered `[P]` | `arch/research/source_index.md` R04 |
| Maia opponent quality (coherence over 10–20 plies) | Q5 | **answered `[V]`**: 80-game smoke, history-conditioned 5M plays plan-coherent 20-ply continuations with level-appropriate errors; weakened-SF control shows exactly the plan-less noodling Maia avoids | `tools/maia-harness/`, exploration log |
| Maia-3 variants vs ChessMimic (if runnable) vs corpus policy vs weakened-Stockfish control, same positions/seeds | Q5, H5 | **GAP** (queue 6) | — |
| Drill-pack authoring cost — the Q7/K10 verdict (nine waves, 33 instrumented packs) | Q7, K10, C6, K7, `rfc/authoring-frictions.md` | **answered `[P]`** (self-reported clocks, arithmetic `[V]`): **K10 not firing** — 35 packs exist, 33 instrumented at **43.5 min/pack**, tooling friction **11.6%** corpus-wide (9.2% excl. pack A), below the ~25% build-tooling threshold; the 43% single-pack figure in `BACKLOG:292` is superseded. Cost tracks the *grounding* bar, not the format: openings 28.8 min/pack with **zero** engine validation, Syzygy-grounded endgames 40.6, trajectories 97.5. Two qualifications carried, not buried: friction fell partly because **no wave has played a run since 2026-08-12**, and the cheap opening tier has an unpaid §3b grounding bill of unknown size. `review`/`owner-review` was 0 *before* it was retired, so none of the decline is an artefact of the retirement | `pack-authoring-cost.md` |
| Author-declared transitions/timing windows (35 authored packs, 23 shape entries, 9 waves) | Q4a, Q7, **E3**, K7 | **answered `[V]`, split verdict**: boundaries **yes** — 32/35 packs declare `authoredBoundary`, **17 of them voluntarily** (no validator compulsion), `plyHorizon` = deepest spine path in 19/32, and all **6/6** cross-phase trajectory leg boundaries fire at the claimed ply under the shipped evaluator. Timing windows **no** — **0 of 135 checkpoints**, 0 of the 3 races `04` §7 names, two independent authors recorded *why* (single trigger where the claim needs a move set), and `windowOpens`/`luxuryMoveBudget` have no evaluator. **E3 partially met**; K7 splits — structure encodable, timing not. `DESIGN-GAP:` `04` §2d requires a window per opening root; 18/18 opening-phase packs have none | `authored-transitions-and-features.md` |
| Deterministic feature set as authoring assistance — what fires and what cannot | Q4b, Q8, B4, next predicate wave | **answered `[V]`**: reliable for **censuses**, absent for **judgments**. For: 22/25 shape references and 14/14 in-spine structural triggers fire with 0 evaluator errors; the gap→predicate→adoption loop closed inside a day for 5 wave-2 predicates. Against: **73% of shape plans (75/103) ship `signature: null`** with the author's own "no census distinguishes this"; 7 of 15 feature kinds unused in any pack; `piece_reach_count atLeast 0` is faking existence 39 times; D32 (schema-valid condition crashes the validator) and D34 (no king geometry — `reach_structure`, a pawn word, doing duty for a king target). §6 is the predicate roadmap, ordered by attestation | `authored-transitions-and-features.md` |
| Move primitives across a transition — computability, per-ply cost, and the routing hypothesis | R1, R2 (`planning/campaign-research-queue.md`), `rfc/predicate-wave-3.md` §7 F4, `design/BACKLOG.md:224` | **answered `[V]`, one hypothesis refuted**: **R1** — 9 of 10 candidate primitives are censuses; the whole transition census costs **29.06 µs/ply** over 593 spine transitions from the 35 committed packs (33.25 dense, 7.47 sparse), and ~70 of the 290 implementation lines are verbatim copies of code already in the tree but **private** (`pivotal.ts:32-57`, `structure.ts:401-410`). Only **tempo-as-forcing** is not mechanical (needs an opponent model — F9's routing). The cost outlier is the shipped-and-dead `structuralDelta` at **1721 µs/ply**, **59× the whole census**, ~43% of it re-parsing FENs inside `evictionChanges`. Selectivity ordering measured: **overload 6.7%** is the sharpest instrument, `structuralDelta` at 93.3% the bluntest. **R2 — the routing hypothesis fails as stated**: exact given an author-named target (**9/9** recall, 0.20 µs/ply) but **98.7% false positives** with any computed target set (fires on 49.6% of quiet piece moves; endgame king walks dominate), and even with the target supplied **52.8%** of the piece's own legal alternatives also satisfy it. The target set is the judgment; routing is a renderer, not a detector. Routing explains only **9 of 17** author-labeled repositions — vacation, attack-set and safety cover the rest. F4 **promotion trigger not met** (0 transition claims among 78 `signature: null` plans; no RFC ships the discovered-threat surface), but F4's routing row and `predicate-wave-3` §4b's delta claim need amending before inheritance | `move-primitive-computability.md`, `tools/r1r2-primitives-harness/` |
| False-positive rate of a hint built only from transition census (R3: is a true census hint worth reading, and does selectivity predict that) | **R3**, `rfc/transition-primitives.md` §5.4 (the live Just Play tier) + §2.4 (the unmeasured corrected rates), `design/05-in-run-experience.md` §3/§3a/§5a, Q4b, B9 | **answered `[V]` — "mostly not, and the RFC's proxy is invalid"**: over **634 spine transitions / 37 packs**, a census-only reading prints **6.18 observations per ply of which 0.68 clear the necessary conditions — an 89.0% false-positive rate at the observation level**. Per leaf the FP rate of a firing is defences **95.8%**, attacks **94.1%**, irreversibility **89.1%**, escape squares **86.1%**, duties **53.2%**, lines **41.7%**. *"Useless"* is defined as failing any of three mechanical necessary conditions — **T** (the fact is a side effect, not a restatement of the move; grounded on `05` §5's *"what is the moved piece no longer doing"*), **C** (it names something contested; grounded on the queue row's own *"attacks a defended pawn"* exemplar and `05` §3b), **D** (it is not equally true of the moves not played; R2's method) — so the signal rates are **upper bounds** and the FP rates **lower bounds**. **Selectivity is not a proxy**: Spearman ρ(firing rate, FP rate) = **−0.143**; the cleanest leaf (lines, 41.7% FP) is the second-commonest, and its apparent quality dies on axis D (**1.05× lift** over a random quiet move, 32.5% of alternatives also signal — R2's renderer-not-detector shape). **The RFC's one-live-primitive bet fails its own bar**: of 44 `defended_duty_acquired` witnesses only **13 (29.5%)** clear T∧C, so a *"clear majority informative"* is unreachable by arithmetic without a reader study, and the marker signals on **2.1%** of played moves against **3.4%** of random quiet moves (**0.61×**, the wrong direction). Fallback endorsed, with a narrower live variant measured as the alternative (2.1% firing, 0% FP by construction, ~0.8 markers/20-ply branch on the learner-proxy population, **zero firings in 259 endgame transitions**). By-product: corrected firing rates measured (attacks **51.3→37.5%**, defences **76.5→34.1%** like-for-like), two of §2.3's six published figures are per-colour-per-direction rather than leaf rates (escape is **94.0%** not 61.2%; irreversibility **24.6%** not 13.2%), and **§5.4's 37-point empty band collapses to a 5.5-point band** | `census-hint-false-positives.md`, `tools/r3-census-hint-harness/` |
| The conjunction hypothesis (R11: does a conjunction of two independent primitives beat either alone on R3's T/C/D gate — the load-bearing `[M]` claim under the campaign's whole synergy/deck argument) | **R11**, `design/research/campaign-effect-vocabulary.md` §4/§7 (the claim under test), `roguelike-run-design.md` §3 rank 6 + §7, `design/06-campaign.md` §1/§3, R3 as parent, Q4b, B4, K6 | **answered `[V]` — "refuted, and the premise fails before the conclusion does"**: over **721 spine transitions / 47 packs** and **19,099 legal alternatives** on the unmodified R3 harness. The best single leaf:direction reaches **69.4%** precision and **12.64×** axis-D lift; the best of **55 conjunctions** reaches **35.7%** and **2.73×**, only **7 of 55** produce enough co-signalling witnesses to measure discrimination at all, their **median lift is 0.66×** (worse than a random quiet move, 5 of 7 below 1.0×), and **0 of 7 beat even their own two components on both axes** — 0 clear the global bars. **The arithmetic is the finding:** §4 assumed the conjunction multiplies rarity while preserving informativeness; given both fired, the two *signals* are near-independent (observed precision a median **1.18×** the independence prediction, 8/46 within ±25%), so **the false positives multiply at the same rate the specificity does**. Firing is not independent either — median lift **1.136**, 20/55 coupled at ≥1.2×, max **5.26×** (φ 0.456), because six leaves off one attack map are six views of one object. **Triples run the mechanism to its end:** only **38** ever co-signal anywhere, the most frequent **6 times in 721**, and **none reaches 10** — a build of 3+ transition primitives is arithmetically empty, not underpowered. **`DESIGN-GAP:` and it is analytic, not statistical — a lens read BEFORE the move is discrimination-inert by construction** (same value for the played move and every alternative), so all three of §4's worked triples, the shape library's 96 signed signatures and the boss-by-census mechanism are **selectors, not synergies**: across 161 pre-move combinations precision moves a median **+0.1 pp** and the big gains (`slider_lines_changed:closed ∧ phase:endgame` +42.3 pp) are population effects with the within-position also-signalling share **unchanged** (51.7%). The one positive arm is unproposed and is about **when** the lens is read: post-move lenses reach **8.77×** max (`defended_duties_changed:released ∧ after:isolated_pawn`), 25/72 beating their own leaf, median still 0.79×. **Population, stated:** the authored spine is the *generous* population and the hypothesis still loses — swapping to the 17,906 quiet alternatives removes its single precision winner (1/51 → 0/55); a Maia-weighted population would shrink every lift here, not grow it. **R3's headline holds: 88.7%** observation-level FP (6.43 obs/ply, 0.73 T∧C) against 89.0%, ρ still **−0.143**, and **R3's limit 3 is discharged** — the middlegame is now 105 transitions from 11 packs and is the corpus's *least* false-positive phase (86.0%), i.e. the census works best exactly where R4/R9 proved no oracle exists. **By-product: `move_irreversibility`'s `last_of_role` is the most discriminating primitive in the set at 12.64× against all alternatives** — R3 could only record "no quiet firings"; it is a *single* rare primitive and it beats every conjunction by 5× | `conjunction-hypothesis.md`, `tools/r11-conjunction-harness/` |
| Can feedback beat "Stockfish labels + prose"? — the shipped feedback surface measured against the named anti-pattern (Q8: R3's T/C/D gate turned on our own surfaces, plus a delivery census of the authored tier) | **Q8**, K6, B4, B10, `design/05-in-run-experience.md` §3/§5b, `docs/explanation-grounds.md` | **answered `[V]` — "yes on honesty, timing and re-entry; not yet on specificity; forfeit on the claim layer"**: over the same 37-pack / **634-transition / 515-position** corpus R3 used, the three structural wins are shipped and real — disclosure is gated on commitment and `attempt_end` **re-closes on the next committed move** (`feedback.ts:22-30`), rewind appends and deletes nothing (`runtime.ts:385-414`), and **235 deliverable deviation notes** are each bound to a *named non-spine move* and revealed whether or not the learner played it, which is the one object R3's axis D says a census cannot produce. The two failures are ours, not structural. **The shipped compare strip fails axis D worse than the leaf R3 condemned**: it fires on **99.8%** of transitions at **8.31 entries/ply**, and of 14,463 quiet alternatives **99.3%** also fire and **90.4%** fire the *same kind* — lift **≈1.01×** against `slider_lines_changed`'s condemned 1.05×; at the 44 authored fork pairs two branches' readings overlap at **Jaccard 65.7%**, leaving a **median 36 unranked differences** as the answer to "why did these two attempts differ". The pulled rung-0 reading prints a **median 58 observations per position** (max 97), **13 of them unconditional** (12 piece counts + king distance), while `outpost` — the feature the whole rung-0 argument rests on — fires on **2 of 515**. And **0 of 131 authored feedback claims are deliverable** (the only consumer is a `stated_reasoning` ground; **0 of 145 checkpoints** use one), while **66 of 66** machine-checkable evidence-type labels on the 32 ledger-bearing packs raise `EVIDENCE_TYPE_UNBACKED` — **22.1% of the 202,479-character authored prose corpus cannot reach a learner**. By-products: R3's 79.9% FP figure for the *shipped* irreversibility marker **independently reproduced** (13.4% firing: pawn_break 7.6 / castled 3.2 / last_of_role 2.7); `phase_change` fires on **1/634**; and B10's "endgame steering by named technique" reaches **6.0%** of positions and renders *"No technique entry is available yet."* **K6: partial evidence FOR firing** — generic on what is delivered, not generic on what is authored | `feedback-versus-the-dashboard.md`, `tools/q8-feedback-surface-harness/` |
| Is the authoring vocabulary COMPLETE, and does it leverage the engine? (the content-scale gate: 37 packs authored, `design/04` maps ≥3× that) | **content scale-up**, Q4a, Q4b, Q7, B4, `design/04` §2d/§7, `design/BACKLOG.md` friction batches | **answered `[V]` — "complete enough for structure; the two things that would cost most to redo are shipped, executable, and unexercised"**: measured at `fa1e689` over 37 authored packs / 25 shape entries / 32 evidence ledgers / 188 schema tokens, with `pack-check` and `sourcing-check` run over the whole corpus. **`pack-check` passes 37/37 with zero warnings** and the census found zero unsatisfiable expressions, and **no vocabulary is in flight** (all three active RFCs claim nothing versioned) — so the format is not the bottleneck. **Shape entries improved sharply**: `signature: null` is **21 of 117 (17.9%)**, not 73%, and `piece_reach_count`'s 143 existence-encoding leaves are now **0**; only **3 of 18** structural feature kinds are unused anywhere (was 7 of 15). **The re-author exposure is elsewhere and it is inverted** — the top two risks are not missing vocabulary but unexercised vocabulary: (1) `design/04` §2d **requires** a timing window per opening root and **0 of 20** opening packs, **0 of 145** checkpoints, **0** success conditions and all **7** tempo verdicts have zero users, although `tempo.ts:140-274` now consumes every field (`DESIGN-GAP:` widened from 18/18 to 20/20); (2) **131 authored feedback claims are undeliverable by construction** — their only consumer is a `stated_reasoning` checkpoint and **0 of 145** checkpoints are one, while authors wrote `intent_capture` at **38 of 38** interaction sites; (3) **275 deviations carry no `mistake` and no `cost`** one wave after `deviation-classes` shipped both. **The risk is not hypothetical**: `930b367` deleted the corpus's only `plan_consequence` condition and replaced it with `transition_feature` — one authored condition re-authored one wave later because a better primitive landed, with no rule for choosing between the two grammars (41 of 105 plan classes carry a `shapePlan` binding and **0** are graded). **Engine leverage: one capability of six is referenceable as a live condition.** `guard.evalSwingCp`/`fireOnMate` is the **only** author-declarable condition evaluated against a live engine number at play time (`guard.ts:199-229`, 6 of 37 packs); `assessedBy` syzygy/engine are authored constants checked once, for `/start/fen` only; and **DTZ/DTM, Maia policy, explorer W/D/L, Stockfish WDL/bestline/top-N have no condition surface at all** — `dtz` appears **0 times** in all three schemas, and the one Maia-shaped token (`human_model_predicted`) is **unbackable by construction** (no Maia evidence kind exists in `EVIDENCE_KINDS`). Sharpest form: **235 machine-validated records (135 Stockfish + 100 syzygy) are anchored to `/deviations/{i}/moveUci`** — the engine has already answered, at the exact pointer — and **0 of 275** deviations declare a `cost`, because the binding row was declined by two RFCs (`BACKLOG:293`). By-products: **66 `EVIDENCE_TYPE_UNBACKED` + 11 `EVIDENCE_DIGEST_STALE` + 5 ledgerless packs** measured, all **43** documents are `reviewStatus: draft` and publishing promotes the 66 warnings to errors; **`variantOf` has never appeared in `content/`** in the entire git history despite two closed friction rows citing it as shipped; **D44 re-derived unchanged at 9 of 25**. Not-expressible-and-unclaimed (14 rows; top 5): `deviation.planClassId`, per-leg `shapes`/`opponentPolicy` on trajectories, `cost`-to-engine binding, validated intent answers, D28 | `authoring-vocabulary-completeness.md` |
| Automatic phase/structure recognition ground truth for Just Play | Q4c, B2, B4 | **GAP** — owner breadth ruling makes honest detection/retrieval/abstention load-bearing for Just Play, while curated packs retain authored boundaries | — |
| ~~Strong reviewer recruitment for packs A/B/C~~ | Q7 | ⛔ **closed 2026-08-13 — not a prerequisite.** Owner ruled no review workflow; packs carry a publication channel (official/community) instead, and C1 is withdrawn. Reopen only as a content-sourcing partnership wanted for its own sake | — |
| Dependency/model/content licensing across source, deployment, monetization, and rights axes 🟡 | Q2, Q6 | **GAP** (queue 10) | — |
| Paid-competitor pricing/positioning | Q2 | **GAP** | — |
| Server/client stack selection (Go vs Node/TS; Svelte vs React vs vanilla; chessground/WASM interop) | branch-runtime RFC, deferred-decisions register | covered `[V]`/`[P]` — recommends TS-everywhere + Svelte 5; flags Maia-3 Python-sidecar question | `stack-selection.md` |
| Mobile — scope, tolerate or non-goal, answered against the shipped client (Q3) | **Q3**, B8, `design/02` §Platform, `design/03` §Stable application shell, Q9's phone half | **answered `[V]`/`[P]` desk + geometry harness — "tolerate, responsive-only, with a two-tier floor"**: everything that is *one board* survives a phone (374 px board at 390×844, chessground touch input, every loop verb a visible control); everything that is *N boards* does not. Measured: the compare band is **2010 px wide at 8 columns on every viewport** (240 px column floor, no viewport awareness) — **5.88 screens of horizontal pan at 390 px, 2.79 at iPad portrait, and 1.85 even at 1280×720**, so eight-way compare already overflows the desktop projection the suite tests; branch groups 1214 px at 8 members. Three shipped defects found: the two advertised compact tabs **Evidence and Session bind to nothing** (`DrillScreen.svelte:741-744` vs `:814,847`; Session is dead by construction and its role condition is inverted); the **720–992 px band** (iPad portrait) loses the fitted grid without gaining the compact tabs — the rejected document-shaped drill, untested by any projection; and the **phone viewport assertion is vacuous** (measured: global `overflow:hidden` + `#app{position:fixed}` make `scrollHeight <= clientHeight` unfailable, so "the board remains visible" is unasserted). Market: the incumbent with the best branching-study tool shipped its phone client **without** the study surfaces and took the complaints (`coverage-sweep-2-notability.md:158-164`); CET proves the PWA/chessground route on our exact stack. Verdict: phone floor = the run loop + read surfaces + 2-column compare; full-surface floor = 768 px, **currently unmet**; 4–8-way compare, branch groups ≥4, live session detail and the studio are named not-phone-shaped; mobile-native stays a non-goal. The compare-scale fix (`BACKLOG:269,313` semantic zoom, already shipped in `GroupPanel`) is the real work and it is not mobile work. Residual: no hands-on — one owner run on a real phone and tablet | `mobile-scope.md` |
| Branch/rewind/compare comprehension at 2/4/8 branches | Q9, E5 | partial `[V]`, owner n=1 real-engine walkthrough: fork/rewind quick and promising; compare selection cumbersome; scrolling shell odd; comparison-learning value not testable without the deferred instructional layer. Four/eight branches and phone remain GAP for *comprehension* — the phone **geometry** half is now answered in `mobile-scope.md` (8-way compare = 2010 px / 5.88 screens of pan at 390 px, and 1.85 screens at 1280×720) | `planning/drill-client/log.md`, `mobile-scope.md` |
| Theory/pattern sourcing incl. provenance & rights | Q6, Q7, content phase | covered `[V]` — every pack need (skeleton/evidence/prose/positions/grading) has a CC0-or-ours source; do-not-use list included | `theory-sourcing.md` |
| Deliberate-practice evidence base | thesis | covered `[P]` (observational; population skew caveat) | `arch/research/source_index.md` R01 |
| Lichess corpus scale/licensing | Q6 | covered `[P]` | `arch/research/source_index.md` R02 |
| Engine stack (Stockfish UCI, Syzygy sizes) | Q5, arch | covered `[P]` | `arch/research/source_index.md` R03–R06 |
| Forum demand signals | Q1b | covered `[P]` — anecdotes, problem-shape only | `arch/research/forum_signal_log.md` |
