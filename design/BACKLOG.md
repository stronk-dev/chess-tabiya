# Design Backlog — the Topic Ledger

Every idea gets a row the moment it's uttered. Statuses:
`💡 candidate` (mentioned, not designed) → `📐 designed` (has a home in a design doc or
archive section) → `🔬 researched` (coverage-matrix row ✅ in `research/README.md`) →
`📜 RFC` (spec'd/implementing) → `⛔ rejected` (with reason, kept for the record).

Rules: **an idea missing from this ledger is a process bug** · **do NOT RFC from a
candidate row** (and per `rfc/0000-rfc-process.md`, not from any row until the
exploration gate opens). Honesty note: everything marked 🔬 below rests on the archive's
**desk research** — provenance effectively `[P]` until Phase-0 hands-on work upgrades it
(`archive/brief-v2/research/research_limitations.md` is the package's own accounting).

Archive homes are cited as `arch/NN §…` = `archive/brief-v2/NN_….md`.

## Core systems (designed in the brief, awaiting exploration → RFCs)

| Topic | Status | Home |
|---|---|---|
| Drill pack format (modes, objectives, checkpoints, provenance, evidence types) | 📐🔬 | `arch/rfcs/RFC-0001` sketch + `arch/schemas/drill_pack.schema.json` |
| Branch runtime (immutable graph, fork/rewind/compare, event-sourced runs) | 📐🔬 | `arch/rfcs/RFC-0002` sketch, `arch/05`, `arch/schemas/drill_run.schema.json` |
| Line Drill — opening recall → ideas → cross the book boundary → first-plan fork | 📐🔬 | `arch/04`, `01-training-model.md` |
| Plan Drill — 8–20-ply segments, rewind, branch comparison | 📐🔬 | `arch/05`, `01-training-model.md` |
| Outcome Drill — win/hold/save/resist, WDL-preserving grading | 📐🔬 | `arch/06`, `01-training-model.md` |
| Trajectory Drill — causal opening→middlegame→endgame spines, causal-integrity rule | 📐 | `arch/07` |
| Opponent policy broker (theory_strict…strong_engine modes; policy mixer for coherence) | 📐 · feasibility = exploration **Q5** · implemented 2026-08-12 (run schema v0.4 typed selection payload; minimal maia3 policy-exposure patch in the sidecar) | `docs/engine-workers.md`; rationale in `rfc/archive/engine-workers.md`; `arch/rfcs/RFC-0003` sketch, `arch/08` |
| Objectives & grading (objective state machine: active→preserved/degraded/failed/achieved/transitioned) | 📐 | `arch/rfcs/RFC-0004` sketch, `arch/schemas/drill_run.schema.json` |
| Author-declared phase transitions and timing windows | 📐 · feasibility = exploration **Q4a** | `arch/rfcs/RFC-0005` sketch, `arch/04` |
| Deterministic phase-feature assistance for authors | 📐 · feasibility = exploration **Q4b** | `arch/rfcs/RFC-0005` sketch |
| Automatic phase/structure recognition and re-anchoring | 💡 · required for Just Play B2/B4, not authoritative over curated pack boundaries · feasibility = exploration **Q4c** | `03-product-breadth.md`, `arch/rfcs/RFC-0005` sketch |
| Tempo contract / timing windows (window opens/closes, luxury-move budget) | 📐 · validation = exploration **Q4a/Q4b** | `arch/04`, `arch/09` |
| Evidence-backed feedback (claims + evidence refs + uncertainty; timing events over eval deltas) | 📐 · quality = exploration **Q8** | `arch/rfcs/RFC-0006` sketch, `arch/09`, `arch/schemas/feedback_packet.schema.json` |
| Engine integration — Stockfish judge, Maia workers, Syzygy adapter, responsibility table | 📐🔬 | `arch/08`, `arch/12` |
| Corpus position/transition index (staged Lichess ingestion, Parquet + DuckDB) | 📐 · Stage-0 coverage/provenance/rights = exploration **Q6** | `arch/rfcs/RFC-0008` sketch, `arch/08`, `arch/13` |
| System architecture — local-first modular monolith + UCI workers | 📐 (ADR-0004) | `arch/12` |
| UX: branching/rewind surface, compare mode, keyboard-first, latency budgets | 📐 · comprehension/growth = exploration **Q9** | `arch/10`, `02-product-shape.md` |
| Anti-contamination defaults (hide eval/labels until checkpoint) | 📐 (ADR-0006) | `arch/10`, `arch/09` |
| Content authoring workflow + pack production cost | 📐 · cost = exploration **Q7** | `arch/product/content_pack_authoring.md` |
| v0 content scope — packs A (Sicilian timing) / B (Carlsbad or IQP) / C (rook endings) | 📐 | `arch/implementation/v0_content_inventory.md` |
| Vertical slice (deterministic mock opponent, acceptance scenario) | 📐 · drafting gated by E1–E5 | `arch/implementation/vertical_slice_spec.md` |
| Benchmark plan (latency + coherence measurement) | 📐 | `arch/implementation/benchmark_plan.md` |
| Validation protocol — H1–H5, kill criteria, continuation gates | 📐 → **lifted into living tier** | `planning/exploration/gates.md` |

## Breadth-first product surfaces (owner-ratified 2026-08-11)

These are one product, not optional bolt-ons behind a one-pack drill screen.
Each must reach a minimal real end-to-end workflow before content depth and
branch-scoring polish become the main work. Canonical matrix:
`03-product-breadth.md`.

Every row below is **scheduled**, not optional. The "Program" column names the
RFC-program item in `03-product-breadth.md` that owns it and the current
verified state from the 2026-08-12 alignment pass (`planning/breadth/`).
"Foundation" means it is blocked only by one of the three primitives F1–F3 in
`planning/breadth/synthesis.md` §2, not by design.

| Surface | Breadth requirement | Program | Home |
|---|---|---|---|
| Just Play — normal/from-position play with branch-and-learn as the game develops | Pack-optional runs, phase/structure recognition, honest dynamic evidence, rewind/fork/compare/replay | #3 · blocked on **F2** (pack is mandatory at six layers) | `03-product-breadth.md`, `planning/breadth/session-contexts.md` |
| Phase-oriented product discovery | Opening/early game, middlegame, endgame, and trajectory navigation exists independently of catalog size | #3 · blocked on **D6** — `phase` is never projected to the client | `03-product-breadth.md`, `01-training-model.md` |
| Streamer/Twitch mode | Real host board + chat voting + overlay + rewind/branch/compare scenario | #8 · **RULED 2026-08-12: the streamer may cheat on themselves, and that is documented rather than engineered against.** The overlay is not fixable by per-viewer reveal (corrected 2026-08-12 by the F3 draft): under any account model a player can hold a second account, be granted spectator on their own run, and read what they are playing blind. So "audience sees eval while streamer does not" needs a channel the player's own client is provably never authorized on — or an accepted decision that a streamer may cheat on themselves. Chat voting and host rewind/branch/compare are unaffected and blocked only on F3 roles | `03-product-breadth.md`, `planning/breadth/live-and-platform.md` |
| Academy/coached sessions | Host/participant/spectator roles, voting/proposals, replay and session-to-pack distillation | #8 · roles blocked on **F3**; the lease cannot express a second writer role | `03-product-breadth.md`, `arch/11` |
| Position Arena | Real external challenge/invite + two-leg/PGN return workflow before native matchmaking depth | #8 · needs PGN import (`parsePgn` has no production caller) and both legs imported as root-forked branches of **one** run | `03-product-breadth.md`, `arch/11`, `arch/rfcs/RFC-0007` |
| Learn/return system | History/resume, progress, episode/concept SRS, related retries, optional recommendations | #7 · history/resume ship; everything else blocked on **F3** | `03-product-breadth.md`, `planning/breadth/create-and-return.md` |
| Episode/concept SRS | Schedule whole rehearsal episodes and related-position retries rather than memorized moves | #7 · `transfer.scheduled` is a declared event with no producer; scheduling unit is an open owner ruling | B7, `01-training-model.md §Repetition` |
| Optional personal-history recommender | Recommend relevant packs/positions without making game import mandatory or restoring the rejected v1 identity | #7 · strictly additive; follows F3 | B7, ADR-0003 |
| Create/curate system | Pack studio, study/repertoire/game/session imports, provenance, review and regression workflow | #6 · validation/lint/digest/graduation ship; no write endpoint, no UI, no importer | `03-product-breadth.md`, `arch/product/content_pack_authoring.md` |
| Automatic candidate-pack mining | Real corpus pipeline emits unpublished candidates into the author/review workflow; never auto-publishes lessons | #6 · proof is one unpublished candidate through the real pipeline | B6, `arch/08` |
| Theory-sourcing dossier executable re-verification | 📝 owner-tier update proposed by B6a: record `[V]` for pinned `lichess-org/chess-openings` commit `4b8622759e7ae6f93f011cc6c83a3823401ab45e`, its `eco/name/pgn` TSV header, and successful offline D35 candidate emission; preserve the existing CC0 licence note | `design/research/theory-sourcing.md`; proposal text also in `planning/archive/content-sourcing-foundation/log.md` |
| Puzzle-source dossier executable re-verification | 📝 owner-tier replacement proposed by B6d: “2026-08-12 `[V]`: the live Lichess puzzle dump has eleven columns ending in `DailyDate`; `00008`, `0000D`, and `000Pw` replay legally through their complete UCI lines; and the official theme vocabulary contains `opening`, `middlegame`, and `endgame`. B6d emits the full-line aftermath without serving the solution, streams/discards the zstd body, and records CC0 source identity in a private sidecar. The real `000Pw` row has 629 plays and is a transformation fixture rather than a production candidate under the 1000-play floor.” | `design/research/theory-sourcing.md`; `planning/archive/content-sourcing-position-seeds/log.md` |
| Explorer priority evidence authenticated and executable | 📝 owner-tier replacement proposed by B6c: “2026-08-12 `[V]`: the exact canonical anonymous `explorer.lichess.org/lichess` request returned HTTP 401 from nginx; the identical request authenticated with a scope-less personal operator token returned HTTP 200 and real rating-band statistics. B6c produced an available priority artifact; Q6’s offline-explorer revival condition did not fire. Operator sourcing credentials remain separate from optional learner-facing Lichess account linking.” | `design/research/theory-sourcing.md`; `planning/exploration/log.md` |
| Lichess account linking vs sourcing credential | 💡 keep two boundaries: an operator/admin OAuth token may supply shared catalog-authoring explorer data; optional learner-facing “Link with Lichess” later serves identity/import/recommendation workflows and must never be required for shared content or lend individual credentials to the sourcing pipeline | B6c operator config; B7 optional personal history/import |
| Share/spectate/deep links | Drill/run URLs, read-only projections, export/import and spectator-safe views | #3 · the follower loop already works end to end; blocked on **D1** (a run link is currently a write credential) | `03-product-breadth.md` |
| Drill-in-a-URL | Address a FEN/objective or pack/run directly for coaches, forums, stream sessions, and handoff between contexts | #3 · **half-shipped** — grammar, validator and resolver exist and are tested in `schema/src/drill-pack/urls.ts` with zero production consumers | B8, `03-product-breadth.md` |
| Full evidence/explanation surface | Authored + Stockfish + Maia + corpus/history + Syzygy + features + evidence-bound LLM, timing controlled | #2 · blocked on **F1**; six of nine contracts pin against merged code today | `03-product-breadth.md`, `arch/09`, `planning/breadth/evidence-explanation.md` |
| Full-spectrum application shell | Play/Learn/Review/Live/Create/Library/Settings with shared board/run/branch/evidence primitives | #1 · routes ship; residuals (`/settings` has no form control, `phase` unprojected, URL grammar unused) belong to the foundation edge | `03-product-breadth.md`, `02-product-shape.md` |

## Foundation primitives (✅ ALL THREE IMPLEMENTED 2026-08-12)

**Status: complete.** F1 shipped as `rfc/archive/authored-explanation-surface.md`;
F3 then F2 shipped in that order as migrations 2 and 3. Defects D1, D2 and D3
closed with them. The table below is kept because the *finding* — that the work
missing across eight program items reduced to three primitives — is what made
the sequencing possible, and because later RFCs cite these names.

Note for anyone citing this section: `feedbackIsRevealed` no longer exists. F2
replaced it with `feedbackDisclosed` (`packages/runtime/src/feedback.ts:3`).
Coordinates below are pre-implementation and are historical.

Six parallel code-verified passes (`planning/breadth/`) found that the missing
work across all eight program items reduces to **three absent primitives**, each
blocking areas that never talk to each other, each small in isolation, and all
three mutually independent. Full evidence: `planning/breadth/synthesis.md` §2.

| Primitive | What is absent | Blocks |
|---|---|---|
| **F1 — per-scope reveal** | `feedbackIsRevealed(pack, run)` (`feedback-policy.ts:11`) is one boolean per run; reaching Pack A's first of three checkpoints would release prose for all three | B4 authored-prose delivery (the named prerequisite in the *implemented* `rfc/archive/authored-feedback-delivery.md`); the streamer overlay, which needs per-viewer withholding; coach-reveals-prose in academy sessions |
| **F2 — pack-optional run identity** | A pack is mandatory at six layers; the pack-blind branch at `service.ts:145-152` is reachable only from tests, and the browser's `CreateRunRequest` cannot express a pack-less start | Just Play and from-position starts (#3), Arena leg import (#8), the pack playtest harness (#6). Must land together with D2 |
| **F3 — a subject** | `grep -riE "learnerId\|userId\|profileId\|accountId"` over all source returns nothing; the only actor token is a browser-local UUID and there is one database table | all of B7; host/participant/spectator roles in B5; the fix for D1. One migration now versus three later |

**A companion finding worth keeping visible:** the foundation RFCs cut slots that
no surface ever filled — `outcome.reached`, `transfer.scheduled`,
`human_model_predicted`, `feedback.generated`, `CheckpointInteraction.prediction`,
the drill-address grammar, and the generic predicate evaluator are all shipped,
tested, and have **zero producers**. Breadth-first worked: the contracts exist
and what remains is mostly producers and surfaces. The mirror risk is that a
slot with no producer reads exactly like a working feature — which is how five
RFC drafts died, and why every row in this pass carries a `file.ts:line`.

## Open defects (🐞 — found by the alignment pass, not yet fixed)

| # | Defect | Status |
|---|---|---|
| D1 ✅ | **CLOSED 2026-08-12 by F3.** *A run link was a write credential.* `assertActiveWriter` is string equality (`runtime/src/errors.ts:37-44`); the server publishes `activeWriterId` in `GET /runs/:id/graph` (`service.ts:258`) and every `GET /runs` summary (`storage.ts:20`); no authentication exists anywhere in source. Any reader can send it as `x-writer-id` and take the lease | 🐞 low impact on a local box; **hard-blocks** all of B5 and any shared-link deployment |
| D2 ✅ | **CLOSED 2026-08-12 by F2.** *The withholding barrier failed open.* `publicNodes`/`publicEvents` return everything when `pack === undefined` (`feedback-policy.ts:21,48`), so F2 would violate ADR-0006 silently. `service.ts:186,205` also enqueue no evidence for a pack-less run — both halves must flip in the same slice | 🐞 latent; becomes live with F2 |
| D3 ✅ | **CLOSED 2026-08-12 by F2.** *`POST /runs` silently accepted unknown nested fields* — violates never-silent. Same shape as the old checkpoint-action problem: the author writes something, the validator blesses it, nothing happens | 🐞 (codex, session 2) |
| D4 | **Action-vocabulary drift risk.** The server allow-list (`pack-validation.ts:11`) and the client's recognized-action switch are two hand-maintained lists that currently happen to agree. A shared constant or an equality test prevents the next divergence | 🐞 latent |
| D5 | **The release compose has no light profile** — `ENGINE_MODE: maia` is hardcoded with an unconditional Maia dependency, so self-hosters following the published artefact must run Maia | 🐞 |
| D11 ✅ | **CLOSED 2026-08-12 by `terminal-outcome-events`.** *A run that ended before its checkpoint stranded its feedback forever.* `feedbackDisclosed` reveals only on `checkpoint.reached`, `segment.completed` or `feedback.revealed` (`packages/runtime/src/feedback.ts:3-12`). If a legal game reaches checkmate or stalemate first, the terminal move commits, every later move throws `RUN_TERMINATED` (`runtime.ts:275`), no checkpoint fires, the objective never achieves, and evidence is withheld permanently. **`outcome.reached` is precisely the missing producer** — declared in `types.ts:186` with a projection case at `events.ts:162` and emitted nowhere, one of the zero-producer slots the alignment pass catalogued. Hits endgame and Outcome Drill packs hardest, since those are the runs that actually end. Fixing it means producing `outcome.reached` on terminal detection and treating it as a reveal event | 🐞 found 2026-08-12 by the second sourcing review |
| D12 ✅ | **CLOSED 2026-08-12 by `outcome-drill-grading`.** *Pack C had never been playable — verified by execution.** Its `still-holding` checkpoint triggers on `materialBalance {black, atLeast, -1}`, and Black is *already* a pawn down at the root (White e4/f2/g2/h2 vs Black f7/g7/h6, balance −1), so the condition is true before anyone moves. The opponent's first move fires it, the objective grades `achieved`, `achieved` is absorbing, and `commitMove` throws `RUN_TERMINATED` before the learner ever moves. The one real `hold` pack we authored is a drill you cannot play | 🐞 found 2026-08-12 by the Outcome Drill draft |
| D12a ✅ | **CLOSED — now a build failure, not a warning.** *No lint caught a trigger already true at the root.* Pack C passes `pack-check` green. A condition satisfied by `start.fen` is always an authoring error, and nothing detects it | 🐞 |
| D12b ✅ | **CLOSED by the monotone law + `resolveAt` → `preserved`.** *`achieved` is absorbing, so grading a hold as achieved ended the drill.* A defensive objective that succeeds *by continuing* cannot use a state that stops play. The proposed law: a state that stops play may only be entered where play has actually ended | 🐞 |
| D12c ✅ | **CLOSED with D12a** — the scenario is rejected at validation. *The disclosure barrier opened at ply 1* in the same scenario — `delayed_checkpoint` discloses on the first `checkpoint.reached`, so a checkpoint that fires immediately reveals everything before the learner has made a decision. Anti-contamination defeated by an authoring slip | 🐞 |
| D13 ✅ | **CLOSED.** *Zero-length segments were emitted.* `reachCheckpoint` pairs each checkpoint with the previous one, so two checkpoints firing at the same node produce a segment spanning nothing | 🐞 |
| D14 ✅ | **CLOSED 2026-08-12 — diagnosed, not masked.** Root cause: the tests targeted Chessground's private `<cg-board>` during transient relayout; they now synchronize on the stable public `aria-label="Chessboard"` wrapper and require visible geometry. `retries` remains unset (default 0), so the fix is verified rather than hidden — ten consecutive zero-retry suites passed, five of them run independently. *The gate had failed once in five runs on an unchanged tree* `served Najdorf pack plays, rewinds, branches, compares, and exports` failed once in five consecutive runs on an unchanged tree, then passed four times. `playwright.config.ts` sets no `retries` (Playwright defaults to 0) with `workers: 1`, so a single flake fails the whole suite — and a gate that can report either result on identical code cannot be evidence for anything. Needs the flake diagnosed (the Chessground stale-bounds fix in `d1b26db` suggests timing sensitivity in that area), not `retries` raised to paper over it | 🐞 found 2026-08-12 by independent verification |
| D17 | **The lease claim has no compare-and-swap and no transaction — any write-capable learner can seize the board.** `claimLease` (`apps/server/src/storage.ts:758-777`) runs `UPDATE drill_runs SET active_writer_id = ?, active_writer_learner_id = ? WHERE id = ?` with no predicate on the current holder, and the role check is not in the same transaction. So a `participant` can take the board from a host mid-session, and two simultaneous claimants both pass the check, both update, and both believe they hold it. F3 delivered *who may write*; it never delivered **policy over possession**. Fixed in `live-session-platform.md` by a `host_directed`/`rotation`/`free_claim` board-control mode plus the CAS the claim never had | 🐞 found 2026-08-13 |
| D18 | **A withheld spectator's board freezes with no signal.** `publicEvents` truncates at the barrier and pins `nextSeq` to `sinceSeq` (`apps/server/src/feedback-policy.ts:45-50`), so a polling reader asks for the same sequence forever. A player eventually reaches a checkpoint and unblocks; a spectator cannot, and nothing distinguishes "nothing new" from "withheld" — the honest-absence problem on a surface where the viewer has no way to resolve it. Fixed additively with `withheld: true` | 🐞 found 2026-08-13 |
| D19 | **A multi-writer run cannot say who played a given ply.** `Node.actor` is `"user" \| "opponent" \| "system"` with no identity. It must **not** widen: in an Arena match both legs' reference-side moves are legitimately `"user"` though different humans played them, so putting a learner id there would break the very comparison the mode exists for. Attribution belongs beside the node, not inside it — `live-session-platform.md` derives it from a `board.granted` possession journal by interval lookup, with no run-schema change | 🐞 found 2026-08-13 |
| D16 ✅ | **CLOSED 2026-08-13 by owner ruling: branch switch worries at 100 ms and intervenes at 200 ms**, superseding the unsourced `<50 ms`. Measured 45–53 ms sits comfortably inside. The ruling also fixed the shape — a worry/intervene band instead of a single pass/fail line — and `design/02` now records that the budgets are a tripwire for when to go look, not the criterion itself. *One operation carried two different budgets, and I had escalated an imperceptible delta against the stricter one* |
| D15 ✅ | **CLOSED 2026-08-13** — `policyModeApplied` records the applied mode, and the off-spine fallback records `human_common` by construction. *The `theory_strict` fallback was invisible to the product.* When a position leaves the authored spine the selector silently falls back to `human_common` and emits only a `console.warn` (`apps/server/src/opponent-selector.ts:455-459`) — it reaches no event, no run, no response and no screen. Consistent with `DEGRADED_POLICY_MASS` (`:469-471`), but consistency with an invisible signal is not a defence for **Line Drill**, a mode whose entire subject is theory: the opponent can stop playing theory mid-drill and nothing the learner or the run can see records it. **Owner ruling 2026-08-12: closed inside the Line Drill RFC, and it blocks acceptance.** `policyModeApplied` (`human_common | strong_engine | theory_strict | unknown`) is added to `opponent.move_selected.selection`; on-spine `theory_strict` records `theory_strict`, off-spine fallback records `human_common`, historical selections migrate to `unknown` and are **never inferred**. Run schema v0.7, migration 5. This reverses `outcome-drill-grading`'s refusal of the field, deliberately: that RFC declined it to preserve no-migration scope, and the owner ruled the migration is worth paying to turn a disclaimer into traceable run evidence | 🐞 found 2026-08-12 reviewing the Line Drill draft |
| D10 | **Both shipped Stockfish specs report `version: "unknown"`.** `parseIdentity` fills `version` from the UCI `id name` line *only* when `spec.name` is unset (`apps/server/src/engine-supervisor.ts:116-126`), and both specs set it — so engine provenance recorded against any evidence is anonymous. B6b works around it by omitting `name` in its authoring spec, which is a workaround, not a fix | 🐞 found 2026-08-12 by the sourcing revision |
| D9 | **`start.side` is schema-optional, and the real consequence is an inverted grading verdict — not the crash originally ledgered.** Re-verified 2026-08-13 while drafting the defect sweep. The stated symptom (`packStartSide` throwing) is now unreachable: `apps/server/src/service.ts:183-185` 400s a side-less pack before any screen renders. Underneath it is worse. `apps/server/src/pack-validation.ts:369` silently coerces a missing side to white (`=== "black" ? "black" : "white"`), and `:346-348` then evaluates `learner === sideToMove` with `learner` **undefined** — always false — so the declared Syzygy category is passed through `opposite()` unconditionally. A `save` pack (tablebase loss) reads as a win. **It passes `pack-check` green and cannot be run**, so the wrong verdict is latent rather than observed — which is why nobody caught it. A silent inversion in the one place the product claims machine-exact grounding | 🐞 raised in severity 2026-08-13 |
| D8 | **The JSON Schema and the runtime validator disagree on two declared values.** `perfect_tablebase` (opponent mode) and `immediate_blunder_guard` (feedback policy) both validate against `schemas/drill_pack.schema.json` and are then rejected at load by `apps/server/src/pack-validation.ts:104-129`. Consequence beyond tidiness: the **on-ramp layer's declared feedback knob** (`design/00-thesis.md` §Target player specifies the 1000–1400 lane on exactly three knobs, one of them this) and the **endgame perfect-play opponent** are both currently unencodable. Same divergence class as D4 — two sources of truth for one vocabulary | 🐞 found 2026-08-12 by the sourcing draft |
| D7 ✅ | **CLOSED 2026-08-13 by `line-drill-theory-grading`.** *`deviations` were not linted at all.* The new `AUTHORED_PROSE_AFTER_LAST_CHECKPOINT` warning covers `annotations` only, so unreachable deviation prose passes silently — but Pack C found the gap is wider than reveal: **no lint checks that a deviation's `moveUci` is even legal in its anchor position, or that it belongs to the side to move.** An author can ship an illegal deviation, or one for the wrong colour, and `pack-check` passes. Both pack authors resorted to hand-auditing every deviation with chessops (10 and 12 respectively) | 🐞 found 2026-08-12 by authoring |
| D6 | **`phase` never reaches the client.** `PackSummary` omits it (`pack-registry.ts:16-24`); `grep -rn "phase" apps/web/src` finds two unrelated prose strings. The Learn IA is organized on this axis | 🐞 |

## Authoring-format friction (💡 — raised by authors while writing real packs)

The point of authoring one pack per phase is to find these. Each is a concrete
proposal from someone who hit the limitation with content in hand, which is the
evidence the withdrawn contracts RFCs never had.

| Friction | Proposal | Raised by |
|---|---|---|
| A plan drill's objective cannot be expressed. Success is relative to the **captured intent** — did you execute the plan you committed to — but `successConditions` supports only intent-blind `reach_checkpoint`, so plan packs ship with no objective at all | Intent-relative success conditions. Directly connected to `intent_capture` being inert (`checkpoints[].interaction` has no consumer) and to the objective-type widening in program #4 | Pack B (Carlsbad), 2026-08-12 |
| Deviations have no link to a plan class. 11.a3 and 11.Rab1 are literally the *same plan*, and `accepted_alternative` says nothing about that | Optional `planClassId` on a deviation | Pack B, 2026-08-12 |
| `concept_violation` does two different jobs — a **timing** error (right plan, wrong moment) and a **plan-coherence** error (a move that contradicts the committed plan). Collapsing them loses the distinction the feedback most needs to make | Split the class, or add a dimension | Pack B, 2026-08-12 |

## Open shape questions (💡 = genuinely undecided; owned by exploration)

| Idea | Take | Home |
|---|---|---|
| Source model, deployment, monetization, and content/data rights | Four independent axes; copyleft obligations constrain combinations but do not prohibit charging | exploration **Q2**, `02-product-shape.md` |
| Target learner/coach problem value | Competitive whitespace is insufficient; interview and concept-test the recurring job and preference over simpler workflows | exploration **Q1b**, `planning/exploration/plan.md` |
| Learning effect versus simpler training | Test transfer and delayed retention only after a slice exists; cannot gate construction of its own test instrument | exploration **Q1c**, H1–H4 |
| Mobile (PWA vs native vs non-goal) | Brief excludes native from v0; whether the loop is a strong mobile fit was never examined | exploration **Q3**, `02-product-shape.md` |
| Runnable human-opponent policies (Maia-3 variants, ChessMimic if reproducible, corpus baseline, future released models) | Benchmark identical positions/seeds; Chessformer is Maia-3's architecture, not a separate engine | exploration **Q5**, `arch/research/source_index.md` R41–R42 |
| Branch growth and compare comprehension | Owner n=1 walkthrough 2026-08-11: fork/rewind is quick and promising, but manual compare selection is cumbersome and the current comparison lacks enough instruction to judge learning value. Test default-all selection, 2/4/8-branch overview, grouping/cleanup, and phone separately | exploration **Q9**, `02-product-shape.md`, `planning/drill-client/log.md` |
| Branch race UX (two boards, alternating moves) | Tangible divergence but high cognitive load — experimental, optional | `arch/10 §Board swapping` |
| LLM as renderer of validated evidence (never source of truth) | Bounded by ADR-0005; wording/summarization only. Owner walkthrough confirmed its absence is conspicuous: the next feedback slice must actually render validated authored/engine/Maia/corpus evidence instead of stopping at generic evidence chips. **Correction 2026-08-12:** Maia is not an unintegrated source — policy mass is parsed (`opponent-selector.ts:225`), persisted in the v0.4 selection payload, and already reaching the browser inside `opponent.move_selected` (the barrier gates only `evidence.attached` and engine-referenced objective changes). It is a rendering gap, not an integration gap, and `docs/explanation-grounds.md` grouping it with corpus/Syzygy is a category error | Q8, `arch/09`, `arch/rfcs/RFC-0006`, `planning/drill-client/log.md` |
| LLM concept-anchoring agent (self-hosted LLM/provider + search + embeddings) | 💡 owner idea 2026-08-10: when a run derails off the trajectory spine, classify the resulting structure, retrieve the nearest curated concept/pack, switch objective, narrate the transition. Retrieval + deterministic features + engine validation pick the anchor; the LLM only words it — stays inside ADR-0005. Also candidate authoring assistant (drafts stay unpublished until review, per ADR-0001) | Q4b, Q8, `arch/07 §related-position jumps` |
| Position/structure embeddings for related-position retrieval | 💡 the retrieval layer the agent above needs; compare against pawn-structure signatures (Q4b's deterministic features) before adopting learned embeddings | Q4b, Q6 |
| Opponent-intent prompts ("what does their move want; what is the moved piece no longer doing?") | 💡 owner idea 2026-08-10: extend intent capture (currently own-move only, `arch/03 §Commit`) to reading the opponent — prophylaxis/threat-scan checkpoints. Drillable with curated claims + features; strong fit for the sub-1400 blunder-stopping leg | `01-training-model.md`, Q8 |
| Deployment packaging — root compose.yaml (server + healthchecked Maia sidecar), GHCR multi-arch images pulled by digest, compose profiles (light vs engines), devcontainer sharing the same toolchain (kills the machine-lacks-stockfish class of gate failures) | 📐 ruled 2026-08-12 alongside the docker-required ruling; scheduled for the client/vertical-slice era | rulings in `planning/archive/engine-workers/log.md` |
| Browser-run engines (stockfish WASM; small Maia via web runtime) | 💡 owner posture 2026-08-10: pushes compute to the client, hosting cost → near-static + agent inference; Chess Endgame Training [V] proves the PWA/WASM pattern. Complements ADR-0004, doesn't replace the server workers | Q2, `arch/12`, `research/competitor-value-props.md` |
| On-ramp pack lane (1000–1400) | 📐 designed 2026-08-10 at thesis level: same pack object, three knobs (2–8-ply branches, pack-declared immediate blunder-guard, principle/threat objectives). Adds a per-pack feedback-policy field to the pack format — note for the future drill-pack RFC | `00-thesis.md §Target player` |
| Content architecture — full-breadth map (both colours, chosen + faced openings, 10 structure families, 7 endgame families, trajectories, on-ramp) | 📐 designed 2026-08-11 on owner ruling: content is planned at product breadth, not one defense | `04-content-architecture.md` |
| Anti-opening packs — drill *facing* an opening you don't play | 💡 owner idea 2026-08-10, from lived pain: White vs the Caro-Kann — 3.e5 (Advance) is enticing but hard to handle (c5/f6 breaks, Bf5 placement, Tal Variation 4.h4 sharpness). You chose 1.e4; the *opponent* chose the resulting board states and threats you must know. Repertoire trainers drill your side of your openings; nothing drills the defender's/anti side. Strong candidate first pack — owner can dogfood it. Demand signals: R46–R49 | `01-training-model.md`, Q7 candidate pack, `research/source-index.md` R46–R49 |
| Authored explanation vocabulary — claim `when` triggers, timing-window semantics (planMoves/opponentArrival/luxury accounting), authored-boundary provenanceMode, feedback-packet abstraction, non-Stockfish evidence refs | 📐 **revived 2026-08-12 — the withdrawal's root-cause diagnosis was partly wrong.** Withdrawn 2026-08-11 after three failed reviews as "an authored vocabulary with no authored content to design against". The alignment pass falsified the load-bearing premise: `authoring-contracts-v03.md` §1 argued claim `when:` triggers cannot reuse the runtime's `ObjectivePredicate` "without duplicating a second vocabulary", but `pack-orchestrator.ts:39-59` **already** translates the pack's key-discriminated `SimpleTrigger` into exactly that runtime type and `evaluateObjectivePredicate` (`objective.ts:195`) evaluates it — the seam is in production use for checkpoints. Six of nine contracts pin against merged code today; only the timing move-set, non-Stockfish payload widening, and the LLM provider genuinely lack a pin. Scheduled under program #2. The reviews' salvage is preserved in the withdrawn RFCs: boundary must be "plyHorizon caps, does not grant"; `pack:` must split per id space; comparison scopes by {branchId,startSeq,endSeq}; recorded-claims requires a real recording site or it is off-cursor re-evaluation renamed | `rfc/authoring-contracts-v03.md`, `rfc/evidence-composer.md` (both withdrawn), Q7 |
| Breadth #2 split history | 📜 RFC 2026-08-11. The composer review found claim triggers, executable window semantics, a boundary combinator, and a multi-source ref grammar all assumed-but-absent; they became a prerequisite amendment RFC rather than implementer improvisation | `rfc/authoring-contracts-v03.md`, `rfc/evidence-composer.md` |
| Off-spine graceful-degradation contract — how feedback honestly thins out as a run deviates from authored content | 💡 2026-08-11 from owner's deviation question. Deviation handling itself is designed (move classification incl. "interesting deviation — continue and compare"; off-objective branches saved not marked wrong; objective transformation; opponent deviations required; agent re-anchoring). The unwritten part: the UI must visibly downgrade from authored coach-voice to instruments-only (engine/corpus/tablebase) at the authored boundary, and the LLM must not paper over the gap (ADR-0005 pressure is highest off-spine). Needs an explicit section in the future drill-pack RFC | Q4a, Q8, `arch/04 §Stage B`, `arch/09 §off-objective` |
| Forward-branching "simulate" — at a spine node with N variations, auto-fork N branches, walk each authored line to its end, render a grid of mini-boards showing the resulting structures | 📐 scheduled under program #5. Owner idea 2026-08-11. Cheap by construction: spine playout is deterministic (no engine), previews are REAL branches in the run graph (enter one and keep playing vs the opponent; compare; export all free). Pairs with the explain-sidebar: sidebar explains, simulate shows. **Two verified costs (2026-08-12), neither fatal:** authored plies must commit as `actor: "system"` — an `opponent` commit demands an engine selection and corrupts read-back replay (`runtime.ts:196-204`, `replay.ts:68-74`) — and every move currently enqueues a Stockfish job (`service.ts:405-411`), so an unguarded 4×12 simulate submits 48 unwanted jobs. Depends on N-way comparison (see the N-way row) | `01-training-model.md` Stage A, `rfc/archive/drill-client.md`-follow-up, Q9 |
| Step-indexed reasoning transcript (steal from ChessMotive) | 💡 2026-08-12. Capture the learner's reasoning as structured rows — candidates generated → shortlist → chosen move → concrete line → objective vs practical evaluation — then diff row-by-row. Localizes failure to **generation vs elimination vs selection vs calculation vs judgment** instead of one "wrong move" verdict. Composes with our intent-capture checkpoints and, unlike theirs, could diff attempt-vs-attempt rather than only vs an authority. Cheap: structured data, no engine | `01-training-model.md`, Q8, prediction-checkpoint row |
| Category-scan scaffold for the on-ramp (steal #2 from ChessMotive) | 💡 2026-08-12. Their Foundation track walks a fixed scan — **checks → captures → threats → piece improvement → pawn moves last**, auto-advancing per category. Cheap structured intent capture, no engine, and it is the natural vehicle for the owner's "what does their move want / what is the moved piece no longer doing" prompt at 1000–1400. Pairs with the reasoning-transcript row: the scan produces the transcript's first row (candidate generation) | `04-content-architecture.md` §6 on-ramp, `01-training-model.md`, Q8 |
| Feedback-timing convergence (ChessMotive) | 🔬 external validation 2026-08-12: an independent product in our band withholds authored explanation until after commit — the same rule as ADR-0006, reached independently. Not a change; recorded so the decision has outside support rather than only principle | `arch/adrs/ADR-0006`, `teardown-chessmotive-desk.md` |
| Prediction checkpoints — flip the board at pivotal moments, predict the opponent's reply, then explain why/why not | 📜 **authored half already ships** (verified 2026-08-12): `CheckpointInteraction.prediction` with `grading{source,topK,minMass}` and `flipBoard` is in the v0.2 schema, and the sparsity constraint below is *mechanically enforced* — `lint.ts:115-138`, max 2 predictions per segment. What is missing is delivery (`pack-registry.ts:66-71` projects checkpoints to `{id,label,actions}` and drops `interaction`, with a regression test asserting it), a durable interaction record, and a board-orientation parameter. `grading.source: "engine"` is not backed — the strong-engine profile ships `multiPv: 1`. Scheduled under program #5. Origin: owner idea 2026-08-10. The active form of opponent-intent prompts; solitaire-chess/"guess the move" pedigree with a twist no competitor has: grade the prediction against the **Maia distribution at the opponent's level** ("42% of 1500s play Qb6; the engine's c4 is found by 8%") plus engine validation — three-way honesty (what's likely, what's best, what you feared). Constraints: a checkpoint *interaction*, not a mode; pack-authored, sparse (pivotal moments only — interrupting mid-segment fights the uninterrupted-consequence stage of the episode, `01-training-model.md §3`); explanation obeys ADR-0005 (claims + frequencies rendered, never freestyle LLM). Board-flip optional per checkpoint — perspective-taking gain vs disorientation cost is a Q9 prototype question | `01-training-model.md`, Q8, Q9 |
| Checkpoint/compare explanation sidebar with selectable evidence layers | 💡 owner walkthrough 2026-08-11: reveal the authored theory claim and objective change, Stockfish best line/evaluation, Maia chosen move plus plausible alternatives/policy mass, and historical-game evidence after commitment; let the user control layers. An LLM may connect and phrase only this validated bundle. This is the missing theory tie-in—anti-contamination governs timing, not permanent absence | Q8, ADR-0005/0006, `research/competitor-value-props.md` §WhyThisMove, future feedback RFC |
| Compare defaults and multi-branch overview | 📐 scheduled under program #5, and the **first** thing in it. Owner walkthrough 2026-08-11: eligible branches should be checked by default; repeated manual pair selection is cumbersome. Ordering rule from `03-product-breadth.md` stands — manual N-branch inclusion is correct first, default selection and branch scoring optimize it afterwards | Q9, N-way comparison row, forward-branching simulate row |
| N-way branch comparison (the runtime change under every multi-branch surface) | 📐 scheduled under program #5, **precedes** simulate/branch-race/overview. Verified 2026-08-12: comparison is pairwise at the *runtime type* level — `BranchComparison` hard-codes `{a,b}` on every collection (`compare.ts:49-64`) and REST/transport/controller inherit it; the UI checkboxes are theater (`compareIds.slice(-1)` evicts the oldest). **It cannot be composed from N−1 pairwise calls**: each pair's `plyOffset` is relative to that pair's own fork (`compare.ts:198-211`), so branches forked at different nodes would land on different axes and align unrelated plies. PGN export is already N-way; the UI just never passes branch ids | `planning/breadth/review-branching.md`, Q9 |
| Viewport-contained desktop app shell | ✅ shipped (program #1) — `docs/app-shell.md`. Origin: owner walkthrough 2026-08-11 ("the drill scrolls like a document"). **Residuals belong to the foundation edge, not to a later item:** `/settings` contains no form control at all (only `<dl>`/`<ul>`), so the opponent/feedback/engines/accessibility IA row is nominal; `phase` is never projected to the client; the drill-address grammar has no route | Q9, `02-product-shape.md`, `planning/breadth/live-and-platform.md` |

## Untracked-until-now gaps (💡 batch, 2026-08-11 — surfaced by the "what else is open?" sweep)

| Idea | Take | Home |
|---|---|---|
| Skill/progress model + return loop | The product has no answer to "what does it believe you know?" or "why open it on Tuesday?" — per-concept mastery tracking, the varied-repetition scheduler's *trigger*, and progress display. "Voluntary return to the same concept" is a success metric with no designed mechanism behind it. For a solo OSS tool this decides shelfware vs habit | 💡 → needs a design pass; feeds Q1b |
| Time-pressure dimension | A top-3 loss cause at 1000–2000 (clock panic, time-trouble blunders) and the brief only gestures at "relevant clock or move budget." Are drills ever timed? Is there a time-scramble Outcome Drill variant? Undesigned | 💡 | `01-training-model.md` |
| Pack interop: import Lichess studies / existing repertoires as pack seeds | 📐 scheduled under program #6. **K10 framing corrected 2026-08-12:** measured authoring cost is 105 min, 45 of it tooling friction (43%, past the ~25% threshold that fires the build-tooling rule) — but all 45 minutes were *playtest/run-assembly* friction, which import does not touch. Import attacks the other 55% (research + encoding), so it is the lever for the long tail (`04-content-architecture.md` §8 order item 5), not the first one. The lever that has actually fired is a **pack playtest harness** (server-derived run id/policyConfig/seed). And `owner-review` is still 0 — the clock `content-era/plan.md` §1b calls decisive — so no K10 verdict is supportable in either direction yet. Still the community on-ramp for contributed content | Q7, `planning/breadth/create-and-return.md` |
| Open pack format as the ecosystem contribution | The value-to-chess-ecosystem may be the openly-specified drill-pack format + runtime (what PGN/EPD did for games), not our app's user count. Reframes "low expected usage" from weakness to irrelevance: others embed/extend the format. Affects Q2 content-rights axis and the novelty story | 💡 | Q2, `00-thesis.md` candidate amendment |
| Small-n evaluation methodology | The validation design (H1–H4, C2–C4) assumes user cohorts we will not have ("I don't expect much usage"). Honest alternatives: n-of-1 self-experiments with preregistered protocols, coach expert review as the primary quality gate, delayed self-testing. Without this, Q1c/C2–C4 are unfalsifiable theater | 💡 → must be settled before E-gate thresholds are preregistered | `gates.md`, Q1c |

## Depth beyond the minimal-real version (the surface itself is scheduled)

These are not postponed features. In each case the *surface* is scheduled under
a program item above, and the row records how much depth the minimal-real
version carries — the boundary `03-product-breadth.md` already fixes when it
says native matchmaking "can deepen later without erasing the surface".
Deepening one of these never re-opens whether the surface exists.

| Topic | Minimal-real scope | Home |
|---|---|---|
| Position Arena native matchmaking/clocks/moderation | Program #8 ships the external invitation + PGN-return handoff as a real workflow. Native matchmaking adds depth to a surface that already works | `arch/rfcs/RFC-0007` sketch, `arch/11`, `arch/schemas/position_arena.schema.json` |
| Corpus ingestion beyond Stage 0 (streamed months, historical slices) | Program #6 ships the explorer-API/curated-PGN path and one mined unpublished candidate through the real pipeline. Bulk volume is throughput on a working pipeline, and bulk-ingestion-*first* stays rejected | `arch/08`, `arch/13` |

## Provisional decisions (the archive ADRs — held, with revisit triggers)

Not accepted RFC-tier decisions; they predate validation. Treated as standing defaults
until their trigger fires or an owner ruling changes them.

| ADR | Decision | Revisit trigger |
|---|---|---|
| ADR-0001 | Curated-first content: reviewed packs before automatic lesson generation | C6 met and mining quality proven |
| ADR-0002 | Stockfish is judge, not default opponent | H5 fails in *both* directions (weakened SF also beats Maia/corpus on believability) |
| ADR-0003 | Personal history optional; core drills work without imports | Standing — protects against the v1 identity error |
| ADR-0004 | Local-first modular monolith + workers | 🔔 **TRIGGER FIRED 2026-08-12** — owner ruled hosted multi-user deployment. Needs an explicit re-decision; a fired trigger is not itself a reversal, and nothing yet says the monolith is the wrong architecture for a hosted deployment |
| ADR-0005 | No LLM as chess source of truth | Standing law (AGENTS.md §7); owner override only |
| ADR-0006 | Delayed feedback in Plan Drills (until checkpoint/segment end) | H3/K4 evidence shows delayed feedback hurts rather than helps |

## Rejected (⛔ — kept for the record; see also AGENTS.md §Rejected)

| Idea | Why |
|---|---|
| v1 identity: personal game-analysis AI coach (mine games → weaknesses → episodes) | `arch/CHANGE_FROM_V1.md` — legitimate adjacent product, not this one; caused the v1 brief's comparison and kill-signal errors |
| Mandatory game import as entry point | Same source; drifts back into personalized analysis |
| Bulk-ingestion-first data strategy | `arch/08` — Stage 0 (explorer API + curated PGNs) suffices; billions of games are not a prerequisite |
| LLM-generated strategic lessons as content | ADR-0005 rationale: fluent but shallow/hallucinated coaching |
| Weakened Stockfish as default opponent | `arch/08` — samples weaker engine moves; does not model human choice |
| "Paid products exist" as a kill signal | `arch/02` — v1's calibration error; real kill criteria live in `gates.md` |
